r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Chat
    RESTful API for Twilio's next-generation chat service.  Real-time delivery of new messages and channel updates is available over a separate WebSocket connection; obtain its credentials via `POST /v4/Auth`.  **Authentication:** Every request must include the `I-Twilio-Auth-Account` header containing a valid Twilio Account SID (format: `AC` + 32 hex characters). In production, an API gateway validates JWTs and injects this header.  **Conventions:** - Paths: UpperCamelCase (`/Users`, `/Channels`, `/Messages`) - Parameters: lowerCamelCase (`pageSize`, `pageToken`) - IDs: opaque resource identifiers (e.g. `chat_channel_…`, `chat_message_…`, `chat_user_…`) - Errors: RFC 9457 Problem Details - Pagination: Opaque cursor tokens (`meta.nextToken`, `meta.previousToken`) 

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination





class UserInstance(InstanceResource):



    class ParticipantType(object):
        HUMAN_AGENT = "HUMAN_AGENT"
        CUSTOMER = "CUSTOMER"
        AI_AGENT = "AI_AGENT"
        UNKNOWN = "UNKNOWN"

    """
    :ivar account_id: Twilio Account SID (pattern: `^AC[0-9a-f]{32}$`)
    :ivar created_at: ISO-8601 timestamp when the user was created
    :ivar deleted_at: ISO-8601 timestamp of soft-deletion; null if the user is active. Username becomes available for reuse 90 days after this timestamp.
    :ivar display_name: Human-readable name shown in the UI
    :ivar participant_type: 
    :ivar status: Presence status of a user. - `ONLINE`  — user is active and reachable - `AWAY`    — user is idle (no recent activity) - `OFFLINE` — user is not connected 
    :ivar status_message: Optional human-readable status note (max 280 chars)
    :ivar updated_at: ISO-8601 timestamp of most recent profile update
    :ivar user_id: User TTID (pattern: `^chat_user_[0-7][a-z0-9]{25}$`)
    :ivar user_info: 
    :ivar username: Unique, URL-safe handle used for @mentions (lowercase alphanumeric + underscore, 3–30 chars)
    :ivar type: A URI reference (RFC3986) that identifies the problem type. This URI should resolve to human-readable documentation for the problem type. When dereferenced, it SHOULD provide human-readable documentation for the problem type (e.g., using HTML). For Twilio errors, this should be a URL to https://www.twilio.com/docs/api/errors/{code} 
    :ivar title: A short, human-readable summary of the problem type. It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization. 
    :ivar detail: A human-readable explanation specific to this occurrence of the problem. This field's value can change between different occurrences of the same problem type to provide specific details. 
    :ivar instance: A URI reference that identifies the specific occurrence of the problem. It may or may not yield further information if dereferenced. This can be used for correlation and debugging. 
    :ivar code: Twilio-specific error code (extension member). This numeric code uniquely identifies the error type within Twilio's error taxonomy and can be used for programmatic handling. Registered at https://www.twilio.com/docs/api/errors 
    :ivar errors: Twilio-specific extension for validation errors (typically HTTP 400/422). Contains an array of individual field-level validation errors, each with a human-readable detail message and a JSON Pointer (RFC 6901) indicating the location of the error in the request. This is particularly useful for request validation failures where multiple fields may have errors. 
    """

    def __init__(self, version: Version, payload:ResponseResource, user_id: Optional[str] = None):
        super().__init__(version)

        
        self.account_id: Optional[str] = payload.get("accountId")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.deleted_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("deletedAt"))
        self.display_name: Optional[str] = payload.get("displayName")
        self.participant_type: Optional["UserInstance.str"] = payload.get("participantType")
        self.status: Optional["UserInstance.str"] = payload.get("status")
        self.status_message: Optional[str] = payload.get("statusMessage")
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("updatedAt"))
        self.user_id: Optional[str] = payload.get("userId")
        self.user_info: Optional[str] = payload.get("userInfo")
        self.username: Optional[str] = payload.get("username")
        self.type: Optional[str] = payload.get("type")
        self.title: Optional[str] = payload.get("title")
        self.detail: Optional[str] = payload.get("detail")
        self.instance: Optional[str] = payload.get("instance")
        self.code: Optional[int] = deserialize.integer(payload.get("code"))
        self.errors: Optional[List[TwilioServiceErrorResponseRFC9457Errors]] = payload.get("errors")

        
        
        # Only set _solution if path params are provided (not None)
        if user_id is not None:
            self._solution = { 
                "user_id": user_id,
            }
        
        
        self._context: Optional[UserContext] = None

    @property
    def _proxy(self) -> "UserContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: UserContext for this UserInstance
        """
        if self._context is None:
            self._context = UserContext(self._version, user_id=self._solution['user_id'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the UserInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the UserInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the UserInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the UserInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    
    def update(self, update_user_request: UpdateUserRequest) -> "UserInstance":
        """
        Update the UserInstance
        
        :param update_user_request: 

        :returns: The updated UserInstance
        """
        return self._proxy.update(update_user_request=update_user_request, )

    async def update_async(self, update_user_request: UpdateUserRequest) -> "UserInstance":
        """
        Asynchronous coroutine to update the UserInstance
        
        :param update_user_request: 

        :returns: The updated UserInstance
        """
        return await self._proxy.update_async(update_user_request=update_user_request, )

    def update_with_http_info(self, update_user_request: UpdateUserRequest) -> ApiResponse:
        """
        Update the UserInstance with HTTP info
        
        :param update_user_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(update_user_request=update_user_request, )

    async def update_with_http_info_async(self, update_user_request: UpdateUserRequest) -> ApiResponse:
        """
        Asynchronous coroutine to update the UserInstance with HTTP info
        
        :param update_user_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(update_user_request=update_user_request, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.UserInstance {}>'.format(context)

class UserContext(InstanceContext):


    def __init__(self, version: Version, user_id: str):
        """
        Initialize the UserContext

        :param version: Version that contains the resource
        :param user_id: User ID (TTID format)
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'user_id': user_id,
        }
        self._uri = '/Users/{user_id}'.format(**self._solution)
        
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the UserInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the UserInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the UserInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the UserInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    
    def _update(self, update_user_request: UpdateUserRequest) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_user_request.to_dict()
        
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='PATCH', uri=self._uri, data=data, headers=headers)

    def update(self, update_user_request: UpdateUserRequest) -> UserInstance:
        """
        Update the UserInstance
        
        :param update_user_request: 

        :returns: The updated UserInstance
        """
        payload, _, _ = self._update(update_user_request=update_user_request)
        return UserInstance(
            self._version,
            payload,
            user_id=self._solution['user_id']
        )

    def update_with_http_info(self, update_user_request: UpdateUserRequest) -> ApiResponse:
        """
        Update the UserInstance and return response metadata
        
        :param update_user_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(update_user_request=update_user_request)
        instance = UserInstance(
            self._version,
            payload,
            user_id=self._solution['user_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, update_user_request: UpdateUserRequest) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_user_request.to_dict()
        
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='PATCH', uri=self._uri, data=data, headers=headers)

    async def update_async(self, update_user_request: UpdateUserRequest) -> UserInstance:
        """
        Asynchronous coroutine to update the UserInstance
        
        :param update_user_request: 

        :returns: The updated UserInstance
        """
        payload, _, _ = await self._update_async(update_user_request=update_user_request)
        return UserInstance(
            self._version,
            payload,
            user_id=self._solution['user_id']
        )

    async def update_with_http_info_async(self, update_user_request: UpdateUserRequest) -> ApiResponse:
        """
        Asynchronous coroutine to update the UserInstance and return response metadata
        
        :param update_user_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(update_user_request=update_user_request)
        instance = UserInstance(
            self._version,
            payload,
            user_id=self._solution['user_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.UserContext {}>'.format(context)





class UserPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> UserInstance:
        """
        Build an instance of UserInstance

        :param payload: Payload response from the API
        """
        
        return UserInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.UserPage>"








class UserPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> UserInstance:
        """
        Build an instance of UserInstance

        :param payload: Payload response from the API
        """
        
        return UserInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.UserPage>"






class UserList(ListResource):
    
    class CommunicationRecipient(object):
        """
            :ivar address: The recipient’s communication address within a channel, such as a phone number, email address, or device token.
            :ivar communication_channel: The channel Type of the Address
            :ivar profile_id: Resolved profile ID (TTID format: `mem_profile_*`)
            :ivar memory_store_id: Memory Store ID for Profile Resolution (TTID format: `mem_store_*`)
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.address: Optional[str] = payload.get("address")
            self.communication_channel: Optional["UserInstance.str"] = payload.get("communicationChannel")
            self.profile_id: Optional[str] = payload.get("profileId")
            self.memory_store_id: Optional[str] = payload.get("memoryStoreId")

        def to_dict(self):
            return {
                
                    "address": self.address,
                    "communicationChannel": self.communication_channel,
                    "profileId": self.profile_id,
                    "memoryStoreId": self.memory_store_id,
            }

    class CreateUserRequest(object):
        """
            :ivar username: Unique @mention handle (lowercase alphanumeric + underscore, 3-30 chars).
            :ivar display_name: Human-readable display name (1-100 chars).
            :ivar participant_type: 
            :ivar user_info: Additional communication info (reference only, not used for delivery).
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.username: Optional[str] = payload.get("username")
            self.display_name: Optional[str] = payload.get("displayName")
            self.participant_type: Optional["UserInstance.ParticipantType"] = payload.get("participantType")
            self.user_info: Optional[UserList.CommunicationRecipient] = payload.get("userInfo")

        def to_dict(self):
            return {
                
                    "username": self.username,
                    "displayName": self.display_name,
                    "participantType": self.participant_type,
                    "userInfo": self.user_info.to_dict() if self.user_info is not None else None ,
            }

    class UpdateUserRequest(object):
        """
            :ivar display_name: Updated display name (1-100 chars).
            :ivar status: Presence status.
            :ivar status_message: Human-readable status note (max 280 chars).
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.display_name: Optional[str] = payload.get("displayName")
            self.status: Optional["UserInstance.str"] = payload.get("status")
            self.status_message: Optional[str] = payload.get("statusMessage")

        def to_dict(self):
            return {
                
                    "displayName": self.display_name,
                    "status": self.status,
                    "statusMessage": self.status_message,
            }


    def __init__(self, version: Version):
        """
        Initialize the UserList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/Users'
        
        
    
    
    def list(self, 
    ) -> List[UserInstance]:
        """
        Lists UserInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [UserInstance(self._version, item) for item in payload["items"]]
        

    async def list_async(self, 
    ) -> List[UserInstance]:
        """
        Asynchronously lists UserInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [UserInstance(self._version, item) for item in payload["items"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists UserInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists UserInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> UserPage:
        """
        Retrieve a single page of UserInstance records from the API.
        Request is executed immediately
        
        :returns: Page of UserInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return UserPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
    ) -> UserPage:
        """
        Asynchronously retrieve a single page of UserInstance records from the API.
        Request is executed immediately
        
        :returns: Page of UserInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return UserPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with UserPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = UserPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with UserPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = UserPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> UserPage:
        """
        Retrieve a specific page of UserInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of UserInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return UserPage(self._version, response)

    async def get_page_async(self, target_url: str) -> UserPage:
        """
        Asynchronously retrieve a specific page of UserInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of UserInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return UserPage(self._version, response)

    
    
    def _create(self, create_user_request: CreateUserRequest) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_user_request.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, create_user_request: CreateUserRequest) -> UserInstance:
        """
        Create the UserInstance

        :param create_user_request: 
        
        :returns: The created UserInstance
        """
        payload, _, _ = self._create(create_user_request=create_user_request)
        return UserInstance(self._version, payload)

    def create_with_http_info(self, create_user_request: CreateUserRequest) -> ApiResponse:
        """
        Create the UserInstance and return response metadata

        :param create_user_request: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(create_user_request=create_user_request)
        instance = UserInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, create_user_request: CreateUserRequest) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_user_request.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, create_user_request: CreateUserRequest) -> UserInstance:
        """
        Asynchronously create the UserInstance

        :param create_user_request: 
        
        :returns: The created UserInstance
        """
        payload, _, _ = await self._create_async(create_user_request=create_user_request)
        return UserInstance(self._version, payload)

    async def create_with_http_info_async(self, create_user_request: CreateUserRequest) -> ApiResponse:
        """
        Asynchronously create the UserInstance and return response metadata

        :param create_user_request: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(create_user_request=create_user_request)
        instance = UserInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        username: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[UserInstance]:
        """
        Streams UserInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            username=username,
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        username: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[UserInstance]:
        """
        Asynchronously streams UserInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            username=username,
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        username: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams UserInstance and returns headers from first page

        
        :param str username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            username=username,
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        username: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams UserInstance and returns headers from first page

        
        :param str username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            username=username,
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        username: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[UserInstance]:
        """
        Lists UserInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            username=username,
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        username: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[UserInstance]:
        """
        Asynchronously lists UserInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            username=username,
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        username: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists UserInstance and returns headers from first page

        
        :param str username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            username=username,
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        username: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists UserInstance and returns headers from first page

        
        :param str username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            username=username,
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      username: Union[str, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      previous_token: Union[str, object] = values.unset,
    ) -> UserPage:
        """
        Retrieve a single page of UserInstance records from the API.
        Request is executed immediately
        
        :param username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param page_size: Page size (max 100, default 50)
        :param page_token: Forward pagination token
        :param previous_token: Backward pagination token
        :returns: Page of UserInstance
        """
        data = values.of({ 
            'username': username,
            'pageSize': page_size,
            'pageToken': page_token,
            'previousToken': previous_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return UserPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
      username: Union[str, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      previous_token: Union[str, object] = values.unset,
    ) -> UserPage:
        """
        Asynchronously retrieve a single page of UserInstance records from the API.
        Request is executed immediately
        
        :param username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param page_size: Page size (max 100, default 50)
        :param page_token: Forward pagination token
        :param previous_token: Backward pagination token
        :returns: Page of UserInstance
        """
        data = values.of({ 
            'username': username,
            'pageSize': page_size,
            'pageToken': page_token,
            'previousToken': previous_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return UserPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        username: Union[str, object] = values.unset,
        
        
        previous_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param page_token: Forward pagination token
        :param previous_token: Backward pagination token
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with UserPage, status code, and headers
        """
        data = values.of({ 
            'username': username,
            'pageToken': page_token,
            'previousToken': previous_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = UserPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        username: Union[str, object] = values.unset,
        
        
        previous_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param username: Exact username filter. When present, returns a 0- or 1-item list matching that username (pagination params are ignored).
        :param page_token: Forward pagination token
        :param previous_token: Backward pagination token
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with UserPage, status code, and headers
        """
        data = values.of({ 
            'username': username,
            'pageToken': page_token,
            'previousToken': previous_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = UserPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> UserPage:
        """
        Retrieve a specific page of UserInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of UserInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return UserPage(self._version, response)

    async def get_page_async(self, target_url: str) -> UserPage:
        """
        Asynchronously retrieve a specific page of UserInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of UserInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return UserPage(self._version, response)



    def get(self, user_id: str) -> UserContext:
        """
        Constructs a UserContext
        
        :param user_id: User ID (TTID format)
        """
        return UserContext(self._version, user_id=user_id)

    def __call__(self, user_id: str) -> UserContext:
        """
        Constructs a UserContext
        
        :param user_id: User ID (TTID format)
        """
        return UserContext(self._version, user_id=user_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.UserList>'





