r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Chat
    RESTful API for Twilio's next-generation chat service.  Real-time delivery of new messages and channel updates is available over a separate WebSocket connection; obtain its credentials via `POST /v4/Auth`.  **Authentication:** Every request must include the `I-Twilio-Auth-Account` header containing a valid Twilio Account SID (format: `AC` + 32 hex characters). In production, an API gateway validates JWTs and injects this header.  **Conventions:** - Paths: UpperCamelCase (`/Users`, `/Channels`, `/Messages`) - Parameters: lowerCamelCase (`pageSize`, `pageToken`) - IDs: opaque resource identifiers (e.g. `chat_channel_…`, `chat_message_…`, `chat_user_…`) - Errors: RFC 9457 Problem Details - Pagination: Opaque cursor tokens (`meta.nextToken`, `meta.previousToken`) 

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination





class MessageInstance(InstanceResource):



    """
    :ivar type: A URI reference (RFC3986) that identifies the problem type. This URI should resolve to human-readable documentation for the problem type. When dereferenced, it SHOULD provide human-readable documentation for the problem type (e.g., using HTML). For Twilio errors, this should be a URL to https://www.twilio.com/docs/api/errors/{code} 
    :ivar title: A short, human-readable summary of the problem type. It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization. 
    :ivar status: The HTTP status code generated by the origin server for this occurrence of the problem. This is the same as the HTTP status code returned in the response. 
    :ivar detail: A human-readable explanation specific to this occurrence of the problem. This field's value can change between different occurrences of the same problem type to provide specific details. 
    :ivar instance: A URI reference that identifies the specific occurrence of the problem. It may or may not yield further information if dereferenced. This can be used for correlation and debugging. 
    :ivar code: Twilio-specific error code (extension member). This numeric code uniquely identifies the error type within Twilio's error taxonomy and can be used for programmatic handling. Registered at https://www.twilio.com/docs/api/errors 
    :ivar errors: Twilio-specific extension for validation errors (typically HTTP 400/422). Contains an array of individual field-level validation errors, each with a human-readable detail message and a JSON Pointer (RFC 6901) indicating the location of the error in the request. This is particularly useful for request validation failures where multiple fields may have errors. 
    :ivar attachments: Attachments (images, files) associated with this message. Null or empty if the message has no attachments.
    :ivar channel_id: Channel TTID (pattern: `^chat_channel_[0-7][a-z0-9]{25}$`)
    :ivar content: 
    :ivar created_at: ISO-8601 timestamp when the message was originally created.
    :ivar deleted_at: ISO-8601 timestamp when the message was soft-deleted. Null for non-deleted messages. When set, content text is shown as '[deleted]' in the UI.
    :ivar edited_at: Timestamp of the most recent edit. Null if the message has never been edited.
    :ivar is_edited: True if this message has been edited after original sending. The UI should show an 'edited' indicator.
    :ivar is_pinned: True if this message has been pinned by a channel admin or owner.
    :ivar mentioned_user_ids: List of user TTIDs @-mentioned in this message. Each mentioned user is notified of the message and has their `mentionCount` incremented.
    :ivar message_id: Message TTID (pattern: `^chat_message_[0-7][a-z0-9]{25}$`)
    :ivar sender_display_name: Display name of the sender at the time the message was sent. Does not update if the user later changes their display name.
    :ivar sender_username: Username of the sender at the time the message was sent. Does not update if the user later changes their username.
    :ivar updated_at: ISO-8601 timestamp when the message was last updated (edit or metadata change).
    :ivar user_id: User TTID (pattern: `^chat_user_[0-7][a-z0-9]{25}$`)
    """

    def __init__(self, version: Version, payload:ResponseResource, channel_id: str, message_id: Optional[str] = None):
        super().__init__(version)

        
        self.type: Optional[str] = payload.get("type")
        self.title: Optional[str] = payload.get("title")
        self.status: Optional[int] = deserialize.integer(payload.get("status"))
        self.detail: Optional[str] = payload.get("detail")
        self.instance: Optional[str] = payload.get("instance")
        self.code: Optional[int] = deserialize.integer(payload.get("code"))
        self.errors: Optional[List[str]] = payload.get("errors")
        self.attachments: Optional[List[AttachmentResponse]] = payload.get("attachments")
        self.channel_id: Optional[str] = payload.get("channelId")
        self.content: Optional[MessageResponseContent] = payload.get("content")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.deleted_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("deletedAt"))
        self.edited_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("editedAt"))
        self.is_edited: Optional[bool] = payload.get("isEdited")
        self.is_pinned: Optional[bool] = payload.get("isPinned")
        self.mentioned_user_ids: Optional[List[str]] = payload.get("mentionedUserIds")
        self.message_id: Optional[str] = payload.get("messageId")
        self.sender_display_name: Optional[str] = payload.get("senderDisplayName")
        self.sender_username: Optional[str] = payload.get("senderUsername")
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("updatedAt"))
        self.user_id: Optional[str] = payload.get("userId")

        
        
        # Only set _solution if path params are provided (not None)
        if channel_id is not None or message_id is not None:
            self._solution = { 
                "channel_id": channel_id,
                "message_id": message_id,
            }
        
        
        self._context: Optional[MessageContext] = None

    @property
    def _proxy(self) -> "MessageContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: MessageContext for this MessageInstance
        """
        if self._context is None:
            self._context = MessageContext(self._version, channel_id=self._solution['channel_id'], message_id=self._solution['message_id'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the MessageInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the MessageInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the MessageInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the MessageInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    
    def update(self, update_message_request: UpdateMessageRequest) -> "MessageInstance":
        """
        Update the MessageInstance
        
        :param update_message_request: 

        :returns: The updated MessageInstance
        """
        return self._proxy.update(update_message_request=update_message_request, )

    async def update_async(self, update_message_request: UpdateMessageRequest) -> "MessageInstance":
        """
        Asynchronous coroutine to update the MessageInstance
        
        :param update_message_request: 

        :returns: The updated MessageInstance
        """
        return await self._proxy.update_async(update_message_request=update_message_request, )

    def update_with_http_info(self, update_message_request: UpdateMessageRequest) -> ApiResponse:
        """
        Update the MessageInstance with HTTP info
        
        :param update_message_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(update_message_request=update_message_request, )

    async def update_with_http_info_async(self, update_message_request: UpdateMessageRequest) -> ApiResponse:
        """
        Asynchronous coroutine to update the MessageInstance with HTTP info
        
        :param update_message_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(update_message_request=update_message_request, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.MessageInstance {}>'.format(context)

class MessageContext(InstanceContext):


    def __init__(self, version: Version, channel_id: str, message_id: str):
        """
        Initialize the MessageContext

        :param version: Version that contains the resource
        :param channel_id: Channel ID (TTID format: `chat_channel_*`)
        :param message_id: Message ID (TTID format: `chat_message_*`)
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'channel_id': channel_id,
            'message_id': message_id,
        }
        self._uri = '/Channels/{channel_id}/Messages/{message_id}'.format(**self._solution)
        
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the MessageInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the MessageInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the MessageInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the MessageInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    
    def _update(self, update_message_request: UpdateMessageRequest) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_message_request.to_dict()
        
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='PATCH', uri=self._uri, data=data, headers=headers)

    def update(self, update_message_request: UpdateMessageRequest) -> MessageInstance:
        """
        Update the MessageInstance
        
        :param update_message_request: 

        :returns: The updated MessageInstance
        """
        payload, _, _ = self._update(update_message_request=update_message_request)
        return MessageInstance(
            self._version,
            payload,
            channel_id=self._solution['channel_id'],
            message_id=self._solution['message_id']
        )

    def update_with_http_info(self, update_message_request: UpdateMessageRequest) -> ApiResponse:
        """
        Update the MessageInstance and return response metadata
        
        :param update_message_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(update_message_request=update_message_request)
        instance = MessageInstance(
            self._version,
            payload,
            channel_id=self._solution['channel_id'],
            message_id=self._solution['message_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, update_message_request: UpdateMessageRequest) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_message_request.to_dict()
        
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='PATCH', uri=self._uri, data=data, headers=headers)

    async def update_async(self, update_message_request: UpdateMessageRequest) -> MessageInstance:
        """
        Asynchronous coroutine to update the MessageInstance
        
        :param update_message_request: 

        :returns: The updated MessageInstance
        """
        payload, _, _ = await self._update_async(update_message_request=update_message_request)
        return MessageInstance(
            self._version,
            payload,
            channel_id=self._solution['channel_id'],
            message_id=self._solution['message_id']
        )

    async def update_with_http_info_async(self, update_message_request: UpdateMessageRequest) -> ApiResponse:
        """
        Asynchronous coroutine to update the MessageInstance and return response metadata
        
        :param update_message_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(update_message_request=update_message_request)
        instance = MessageInstance(
            self._version,
            payload,
            channel_id=self._solution['channel_id'],
            message_id=self._solution['message_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.MessageContext {}>'.format(context)





class MessagePage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> MessageInstance:
        """
        Build an instance of MessageInstance

        :param payload: Payload response from the API
        """
        
        return MessageInstance(self._version, payload, channel_id=self._solution["channel_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.MessagePage>"






class MessagePage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> MessageInstance:
        """
        Build an instance of MessageInstance

        :param payload: Payload response from the API
        """
        
        return MessageInstance(self._version, payload, channel_id=self._solution["channel_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.MessagePage>"




class MessagePage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> MessageInstance:
        """
        Build an instance of MessageInstance

        :param payload: Payload response from the API
        """
        
        return MessageInstance(self._version, payload, channel_id=self._solution["channel_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.MessagePage>"






class MessageList(ListResource):
    
    class AttachmentResponse(object):
        """
            :ivar attachment_id: Attachment TTID (pattern: `^chat_attachment_[0-7][a-z0-9]{25}$`)
            :ivar channel_id: TTID of the channel this attachment belongs to; null before the attachment is linked to a message.
            :ivar created_at: Timestamp when the attachment record was created (ISO-8601).
            :ivar download_allowed: Convenience flag: `true` only when `scanStatus` is `clean`. Use this to gate download UI without re-checking `scanStatus` string.
            :ivar duration: Duration in seconds for audio and video attachments. Null for image and file types.
            :ivar filename: Original filename as provided by the uploader, including extension.
            :ivar height: Image height in pixels. Populated for `type: image` and `type: video` only. Null for audio and file types.
            :ivar message_id: TTID of the message this attachment is linked to; null for a freshly uploaded attachment not yet attached to a message.
            :ivar mime_type: MIME type of the file, as declared during upload.
            :ivar scan_status: Virus scan lifecycle state for an attachment. - `PENDING`      — scan queued or in progress - `CLEAN`        — no threats found; download allowed - `INFECTED`     — malware detected; download blocked - `SCAN_TIMEOUT` — scanner did not respond in time; download blocked 
            :ivar size: File size in bytes.
            :ivar thumbnail_url: CDN URL of the generated thumbnail. Null for non-image/video attachments or before thumbnail generation completes.
            :ivar type: Attachment category derived from MIME type at upload time. - `IMAGE` — image file (jpeg, png, gif, webp, etc.) - `VIDEO` — video file (mp4, webm, etc.) - `AUDIO` — audio file (mp3, ogg, wav, etc.) - `FILE`  — any other document or binary 
            :ivar updated_at: Timestamp when the attachment was last updated — e.g., when the scan status changed (ISO-8601).
            :ivar uploaded_by: User TTID (pattern: `^chat_user_[0-7][a-z0-9]{25}$`)
            :ivar width: Image width in pixels. Populated for `type: image` and `type: video` only. Null for audio and file types.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.attachment_id: Optional[str] = payload.get("attachmentId")
            self.channel_id: Optional[str] = payload.get("channelId")
            self.created_at: Optional[datetime] = payload.get("createdAt")
            self.download_allowed: Optional[bool] = payload.get("downloadAllowed")
            self.duration: Optional[int] = payload.get("duration")
            self.filename: Optional[str] = payload.get("filename")
            self.height: Optional[int] = payload.get("height")
            self.message_id: Optional[str] = payload.get("messageId")
            self.mime_type: Optional[str] = payload.get("mimeType")
            self.scan_status: Optional[str] = payload.get("scanStatus")
            self.size: Optional[int] = payload.get("size")
            self.thumbnail_url: Optional[str] = payload.get("thumbnailUrl")
            self.type: Optional[str] = payload.get("type")
            self.updated_at: Optional[datetime] = payload.get("updatedAt")
            self.uploaded_by: Optional[str] = payload.get("uploadedBy")
            self.width: Optional[int] = payload.get("width")

        def to_dict(self):
            return {
                
                    "attachmentId": self.attachment_id,
                    "channelId": self.channel_id,
                    "createdAt": self.created_at,
                    "downloadAllowed": self.download_allowed,
                    "duration": self.duration,
                    "filename": self.filename,
                    "height": self.height,
                    "messageId": self.message_id,
                    "mimeType": self.mime_type,
                    "scanStatus": self.scan_status,
                    "size": self.size,
                    "thumbnailUrl": self.thumbnail_url,
                    "type": self.type,
                    "updatedAt": self.updated_at,
                    "uploadedBy": self.uploaded_by,
                    "width": self.width,
            }

    class CreateMessageRequest(object):
        """
            :ivar user_id: TTID of the sending user.
            :ivar content: Structured message content (oneOf).
            :ivar mentioned_user_ids: User TTIDs @-mentioned in this message (max 50).
            :ivar attachment_ids: TTIDs of pre-uploaded, confirmed attachments (max 10).
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.user_id: Optional[str] = payload.get("userId")
            self.content: Optional[MessageList.MessageContent] = payload.get("content")
            self.mentioned_user_ids: Optional[List[str]] = payload.get("mentionedUserIds")
            self.attachment_ids: Optional[List[str]] = payload.get("attachmentIds")

        def to_dict(self):
            return {
                
                    "userId": self.user_id,
                    "content": self.content.to_dict() if self.content is not None else None ,
                    "mentionedUserIds": self.mentioned_user_ids,
                    "attachmentIds": self.attachment_ids,
            }

    class MessageContent(object):
        """
            :ivar text: 
            :ivar markdown: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.text: Optional[str] = payload.get("text")
            self.markdown: Optional[str] = payload.get("markdown")

        def to_dict(self):
            return {
                
                    "text": self.text,
                    "markdown": self.markdown,
            }

    class MessageResponseContent(object):
        """
            :ivar markdown: 
            :ivar text: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.markdown: Optional[str] = payload.get("markdown")
            self.text: Optional[str] = payload.get("text")

        def to_dict(self):
            return {
                
                    "markdown": self.markdown,
                    "text": self.text,
            }

    class UpdateMessageRequest(object):
        """
            :ivar content: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.content: Optional[MessageList.MessageResponseContent] = payload.get("content")

        def to_dict(self):
            return {
                
                    "content": self.content.to_dict() if self.content is not None else None ,
            }


    def __init__(self, version: Version, channel_id: str):
        """
        Initialize the MessageList

        :param version: Version that contains the resource
        :param channel_id: Channel ID (TTID format: `chat_channel_*`)
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'channel_id': channel_id,  }
        self._uri = '/Channels/{channel_id}/Messages'.format(**self._solution)
        
        
    
    
    def list(self, 
    ) -> List[MessageInstance]:
        """
        Lists MessageInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [MessageInstance(self._version, item, channel_id=self._solution['channel_id']) for item in payload["items"]]
        

    async def list_async(self, 
    ) -> List[MessageInstance]:
        """
        Asynchronously lists MessageInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [MessageInstance(self._version, item, channel_id=self._solution['channel_id']) for item in payload["items"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists MessageInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists MessageInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> MessagePage:
        """
        Retrieve a single page of MessageInstance records from the API.
        Request is executed immediately
        
        :returns: Page of MessageInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return MessagePage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
    ) -> MessagePage:
        """
        Asynchronously retrieve a single page of MessageInstance records from the API.
        Request is executed immediately
        
        :returns: Page of MessageInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return MessagePage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with MessagePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = MessagePage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with MessagePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = MessagePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> MessagePage:
        """
        Retrieve a specific page of MessageInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of MessageInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return MessagePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> MessagePage:
        """
        Asynchronously retrieve a specific page of MessageInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of MessageInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return MessagePage(self._version, response, solution=self._solution)

    
    
    def stream(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[MessageInstance]:
        """
        Streams MessageInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param str previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param str created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_token=page_token,
            previous_token=previous_token,
            created_after=created_after,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[MessageInstance]:
        """
        Asynchronously streams MessageInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param str previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param str created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_token=page_token,
            previous_token=previous_token,
            created_after=created_after,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams MessageInstance and returns headers from first page

        
        :param str page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param str previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param str created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_token=page_token,
            previous_token=previous_token,
            created_after=created_after,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams MessageInstance and returns headers from first page

        
        :param str page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param str previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param str created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_token=page_token,
            previous_token=previous_token,
            created_after=created_after,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[MessageInstance]:
        """
        Lists MessageInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param str previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param str created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            page_token=page_token,
            previous_token=previous_token,
            created_after=created_after,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[MessageInstance]:
        """
        Asynchronously lists MessageInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param str previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param str created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            page_token=page_token,
            previous_token=previous_token,
            created_after=created_after,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists MessageInstance and returns headers from first page

        
        :param str page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param str previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param str created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            previous_token=previous_token,
            created_after=created_after,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists MessageInstance and returns headers from first page

        
        :param str page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param str previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param str created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            previous_token=previous_token,
            created_after=created_after,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      previous_token: Union[str, object] = values.unset,
      created_after: Union[str, object] = values.unset,
    ) -> MessagePage:
        """
        Retrieve a single page of MessageInstance records from the API.
        Request is executed immediately
        
        :param page_size: Number of messages to return (1–100, default 50)
        :param page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :returns: Page of MessageInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'pageToken': page_token,
            'previousToken': previous_token,
            'createdAfter': created_after,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return MessagePage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      previous_token: Union[str, object] = values.unset,
      created_after: Union[str, object] = values.unset,
    ) -> MessagePage:
        """
        Asynchronously retrieve a single page of MessageInstance records from the API.
        Request is executed immediately
        
        :param page_size: Number of messages to return (1–100, default 50)
        :param page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :returns: Page of MessageInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'pageToken': page_token,
            'previousToken': previous_token,
            'createdAfter': created_after,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return MessagePage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        
        
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with MessagePage, status code, and headers
        """
        data = values.of({ 
            'pageToken': page_token,
            'previousToken': previous_token,
            'createdAfter': created_after,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = MessagePage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        
        previous_token: Union[str, object] = values.unset,
        created_after: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: Opaque forward pagination cursor from a previous response's `meta.nextToken`
        :param previous_token: Opaque backward pagination cursor from a previous response's `meta.previousToken`
        :param created_after: Return only messages created after this ISO-8601 timestamp. Used for catch-up on reconnect — supply the timestamp of the last message received to fetch only missed messages.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with MessagePage, status code, and headers
        """
        data = values.of({ 
            'pageToken': page_token,
            'previousToken': previous_token,
            'createdAfter': created_after,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = MessagePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> MessagePage:
        """
        Retrieve a specific page of MessageInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of MessageInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return MessagePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> MessagePage:
        """
        Asynchronously retrieve a specific page of MessageInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of MessageInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return MessagePage(self._version, response, solution=self._solution)

    
    def list(self, 
        create_message_request: Union[CreateMessageRequest, object] = values.unset,
    ) -> List[MessageInstance]:
        """
        Lists MessageInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'CreateMessageRequest': create_message_request,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [MessageInstance(self._version, item, channel_id=self._solution['channel_id']) for item in payload["items"]]
        

    async def list_async(self, 
        create_message_request: Union[CreateMessageRequest, object] = values.unset,
    ) -> List[MessageInstance]:
        """
        Asynchronously lists MessageInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'CreateMessageRequest': create_message_request,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [MessageInstance(self._version, item, channel_id=self._solution['channel_id']) for item in payload["items"]]
        


    def list_with_http_info(self, 
        create_message_request: Union[CreateMessageRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists MessageInstance and returns headers from first page

        
        :param CreateMessageRequest create_message_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            create_message_request=create_message_request,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        create_message_request: Union[CreateMessageRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists MessageInstance and returns headers from first page

        
        :param CreateMessageRequest create_message_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            create_message_request=create_message_request,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      create_message_request: Union[CreateMessageRequest, object] = values.unset,
    ) -> MessagePage:
        """
        Retrieve a single page of MessageInstance records from the API.
        Request is executed immediately
        
        :param create_message_request: 
        :returns: Page of MessageInstance
        """
        data = values.of({ 
            'CreateMessageRequest': create_message_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='POST', uri=self._uri, params=data, headers=headers)
        return MessagePage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      create_message_request: Union[CreateMessageRequest, object] = values.unset,
    ) -> MessagePage:
        """
        Asynchronously retrieve a single page of MessageInstance records from the API.
        Request is executed immediately
        
        :param create_message_request: 
        :returns: Page of MessageInstance
        """
        data = values.of({ 
            'CreateMessageRequest': create_message_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='POST', uri=self._uri, params=data, headers=headers)
        return MessagePage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        create_message_request: Union[CreateMessageRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param create_message_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with MessagePage, status code, and headers
        """
        data = values.of({ 
            'CreateMessageRequest': create_message_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        page = MessagePage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        create_message_request: Union[CreateMessageRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param create_message_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with MessagePage, status code, and headers
        """
        data = values.of({ 
            'CreateMessageRequest': create_message_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        page = MessagePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> MessagePage:
        """
        Retrieve a specific page of MessageInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of MessageInstance
        """
        response = self._version.domain.twilio.request(
            'POST',
            target_url
        )
        return MessagePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> MessagePage:
        """
        Asynchronously retrieve a specific page of MessageInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of MessageInstance
        """
        response = await self._version.domain.twilio.request_async(
            'POST',
            target_url
        )
        return MessagePage(self._version, response, solution=self._solution)



    def get(self, message_id: str) -> MessageContext:
        """
        Constructs a MessageContext
        
        :param message_id: Message ID (TTID format: `chat_message_*`)
        """
        return MessageContext(self._version, channel_id=self._solution['channel_id'], message_id=message_id)

    def __call__(self, message_id: str) -> MessageContext:
        """
        Constructs a MessageContext
        
        :param message_id: Message ID (TTID format: `chat_message_*`)
        """
        return MessageContext(self._version, channel_id=self._solution['channel_id'], message_id=message_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.MessageList>'





