r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Chat
    RESTful API for Twilio's next-generation chat service.  Real-time delivery of new messages and channel updates is available over a separate WebSocket connection; obtain its credentials via `POST /v4/Auth`.  **Authentication:** Every request must include the `I-Twilio-Auth-Account` header containing a valid Twilio Account SID (format: `AC` + 32 hex characters). In production, an API gateway validates JWTs and injects this header.  **Conventions:** - Paths: UpperCamelCase (`/Users`, `/Channels`, `/Messages`) - Parameters: lowerCamelCase (`pageSize`, `pageToken`) - IDs: opaque resource identifiers (e.g. `chat_channel_…`, `chat_message_…`, `chat_user_…`) - Errors: RFC 9457 Problem Details - Pagination: Opaque cursor tokens (`meta.nextToken`, `meta.previousToken`) 

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination





class ChannelInstance(InstanceResource):



    """
    :ivar type: A URI reference (RFC3986) that identifies the problem type. This URI should resolve to human-readable documentation for the problem type. When dereferenced, it SHOULD provide human-readable documentation for the problem type (e.g., using HTML). For Twilio errors, this should be a URL to https://www.twilio.com/docs/api/errors/{code} 
    :ivar title: A short, human-readable summary of the problem type. It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization. 
    :ivar status: The HTTP status code generated by the origin server for this occurrence of the problem. This is the same as the HTTP status code returned in the response. 
    :ivar detail: A human-readable explanation specific to this occurrence of the problem. This field's value can change between different occurrences of the same problem type to provide specific details. 
    :ivar instance: A URI reference that identifies the specific occurrence of the problem. It may or may not yield further information if dereferenced. This can be used for correlation and debugging. 
    :ivar code: Twilio-specific error code (extension member). This numeric code uniquely identifies the error type within Twilio's error taxonomy and can be used for programmatic handling. Registered at https://www.twilio.com/docs/api/errors 
    :ivar errors: Twilio-specific extension for validation errors (typically HTTP 400/422). Contains an array of individual field-level validation errors, each with a human-readable detail message and a JSON Pointer (RFC 6901) indicating the location of the error in the request. This is particularly useful for request validation failures where multiple fields may have errors. 
    :ivar channel_id: Channel TTID (pattern: `^chat_channel_[0-7][a-z0-9]{25}$`)
    :ivar configuration: 
    :ivar created_at: ISO-8601 UTC timestamp when the channel was created.
    :ivar deleted_at: ISO-8601 UTC soft-delete timestamp. Non-null means the channel has been archived. Hard purge occurs 90 days after this timestamp.
    :ivar description: Optional channel description or topic shown in the channel header. Null when no description has been set.
    :ivar display_name: Human-readable channel name displayed in the UI sidebar and headers. May contain spaces, mixed case, and emoji.
    :ivar last_message_at: ISO-8601 UTC timestamp of the most recent message. Null when no messages have been sent. Used for chronological sorting in the sidebar.
    :ivar last_message_preview: Truncated preview text of the most recent non-deleted message. Null when no messages have been sent. Maximum ~200 characters.
    :ivar message_count: Total number of messages ever sent to this channel (includes soft-deleted messages).
    :ivar name: URL-safe channel slug used in routing and @channel references. Lowercase letters, digits, underscores, and hyphens.
    :ivar participant_count: Current number of active (non-removed) members in the channel.
    :ivar updated_at: ISO-8601 UTC timestamp of the last modification to the channel record (name, description, metadata, or counter update).
    """

    def __init__(self, version: Version, payload:ResponseResource, channel_id: Optional[str] = None, user_id: str):
        super().__init__(version)

        
        self.type: Optional[str] = payload.get("type")
        self.title: Optional[str] = payload.get("title")
        self.status: Optional[int] = deserialize.integer(payload.get("status"))
        self.detail: Optional[str] = payload.get("detail")
        self.instance: Optional[str] = payload.get("instance")
        self.code: Optional[int] = deserialize.integer(payload.get("code"))
        self.errors: Optional[List[TwilioServiceErrorResponseRFC9457Errors]] = payload.get("errors")
        self.channel_id: Optional[str] = payload.get("channelId")
        self.configuration: Optional[str] = payload.get("configuration")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.deleted_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("deletedAt"))
        self.description: Optional[str] = payload.get("description")
        self.display_name: Optional[str] = payload.get("displayName")
        self.last_message_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("lastMessageAt"))
        self.last_message_preview: Optional[str] = payload.get("lastMessagePreview")
        self.message_count: Optional[int] = payload.get("messageCount")
        self.name: Optional[str] = payload.get("name")
        self.participant_count: Optional[int] = payload.get("participantCount")
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("updatedAt"))

        
        
        # Only set _solution if path params are provided (not None)
        if channel_id is not None or user_id is not None:
            self._solution = { 
                "channel_id": channel_id,
                "user_id": user_id,
            }
        
        
        self._context: Optional[ChannelContext] = None

    @property
    def _proxy(self) -> "ChannelContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: ChannelContext for this ChannelInstance
        """
        if self._context is None:
            self._context = ChannelContext(self._version, channel_id=self._solution['channel_id'], user_id=self._solution['user_id'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the ChannelInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the ChannelInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the ChannelInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the ChannelInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    
    def update(self, update_channel_request: UpdateChannelRequest) -> "ChannelInstance":
        """
        Update the ChannelInstance
        
        :param update_channel_request: 

        :returns: The updated ChannelInstance
        """
        return self._proxy.update(update_channel_request=update_channel_request, )

    async def update_async(self, update_channel_request: UpdateChannelRequest) -> "ChannelInstance":
        """
        Asynchronous coroutine to update the ChannelInstance
        
        :param update_channel_request: 

        :returns: The updated ChannelInstance
        """
        return await self._proxy.update_async(update_channel_request=update_channel_request, )

    def update_with_http_info(self, update_channel_request: UpdateChannelRequest) -> ApiResponse:
        """
        Update the ChannelInstance with HTTP info
        
        :param update_channel_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(update_channel_request=update_channel_request, )

    async def update_with_http_info_async(self, update_channel_request: UpdateChannelRequest) -> ApiResponse:
        """
        Asynchronous coroutine to update the ChannelInstance with HTTP info
        
        :param update_channel_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(update_channel_request=update_channel_request, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.ChannelInstance {}>'.format(context)

class ChannelContext(InstanceContext):


    def __init__(self, version: Version, channel_id: str, user_id: str):
        """
        Initialize the ChannelContext

        :param version: Version that contains the resource
        :param channel_id: Channel TTID (pattern: `chat_channel_[0-7][a-z0-9]{25}`)
        :param user_id: User ID (TTID format)
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'channel_id': channel_id,
            'user_id': user_id,
        }
        self._uri = '/Channels/{channel_id}'.format(**self._solution)
        
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the ChannelInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the ChannelInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the ChannelInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the ChannelInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    
    def _update(self, update_channel_request: UpdateChannelRequest) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_channel_request.to_dict()
        
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='PATCH', uri=self._uri, data=data, headers=headers)

    def update(self, update_channel_request: UpdateChannelRequest) -> ChannelInstance:
        """
        Update the ChannelInstance
        
        :param update_channel_request: 

        :returns: The updated ChannelInstance
        """
        payload, _, _ = self._update(update_channel_request=update_channel_request)
        return ChannelInstance(
            self._version,
            payload,
            channel_id=self._solution['channel_id'],
            user_id=self._solution['user_id']
        )

    def update_with_http_info(self, update_channel_request: UpdateChannelRequest) -> ApiResponse:
        """
        Update the ChannelInstance and return response metadata
        
        :param update_channel_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(update_channel_request=update_channel_request)
        instance = ChannelInstance(
            self._version,
            payload,
            channel_id=self._solution['channel_id'],
            user_id=self._solution['user_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, update_channel_request: UpdateChannelRequest) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_channel_request.to_dict()
        
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='PATCH', uri=self._uri, data=data, headers=headers)

    async def update_async(self, update_channel_request: UpdateChannelRequest) -> ChannelInstance:
        """
        Asynchronous coroutine to update the ChannelInstance
        
        :param update_channel_request: 

        :returns: The updated ChannelInstance
        """
        payload, _, _ = await self._update_async(update_channel_request=update_channel_request)
        return ChannelInstance(
            self._version,
            payload,
            channel_id=self._solution['channel_id'],
            user_id=self._solution['user_id']
        )

    async def update_with_http_info_async(self, update_channel_request: UpdateChannelRequest) -> ApiResponse:
        """
        Asynchronous coroutine to update the ChannelInstance and return response metadata
        
        :param update_channel_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(update_channel_request=update_channel_request)
        instance = ChannelInstance(
            self._version,
            payload,
            channel_id=self._solution['channel_id'],
            user_id=self._solution['user_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.ChannelContext {}>'.format(context)





class ChannelPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> ChannelInstance:
        """
        Build an instance of ChannelInstance

        :param payload: Payload response from the API
        """
        
        return ChannelInstance(self._version, payload, user_id=self._solution["user_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.ChannelPage>"








class ChannelPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> ChannelInstance:
        """
        Build an instance of ChannelInstance

        :param payload: Payload response from the API
        """
        
        return ChannelInstance(self._version, payload, user_id=self._solution["user_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.ChannelPage>"




class ChannelPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> ChannelInstance:
        """
        Build an instance of ChannelInstance

        :param payload: Payload response from the API
        """
        
        return ChannelInstance(self._version, payload, user_id=self._solution["user_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.ChannelPage>"






class ChannelList(ListResource):
    
    class ChannelConfigurationRequest(object):
        """
            :ivar display_name: Human-facing name of the configuration.
            :ivar description: Optional human-facing description.
            :ivar status_timeouts: Channel lifecycle timers.
            :ivar status_callbacks: Outbound HTTP status callbacks.
            :ivar memory_store_id: Memory store bound to this channel.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.display_name: Optional[str] = payload.get("displayName")
            self.description: Optional[str] = payload.get("description")
            self.status_timeouts: Optional[ChannelList.ChannelTimeouts] = payload.get("statusTimeouts")
            self.status_callbacks: Optional[List[ChannelList.StatusCallbackDto]] = payload.get("statusCallbacks")
            self.memory_store_id: Optional[str] = payload.get("memoryStoreId")

        def to_dict(self):
            return {
                
                    "displayName": self.display_name,
                    "description": self.description,
                    "statusTimeouts": self.status_timeouts.to_dict() if self.status_timeouts is not None else None ,
                    "statusCallbacks": [status_callbacks.to_dict() for status_callbacks in self.status_callbacks] if self.status_callbacks is not None else None,
                    "memoryStoreId": self.memory_store_id,
            }

    class ChannelTimeouts(object):
        """
            :ivar closed: Seconds before → CLOSED. Null = no transition.
            :ivar inactive: Seconds of inactivity before ACTIVE → INACTIVE. Null = no transition.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.closed: Optional[int] = payload.get("closed")
            self.inactive: Optional[int] = payload.get("inactive")

        def to_dict(self):
            return {
                
                    "closed": self.closed,
                    "inactive": self.inactive,
            }

    class CreateChannelRequest(object):
        """
            :ivar name: URL-safe channel slug (lowercase alphanumeric, underscores, hyphens; 3-30 chars; unique within the account).
            :ivar display_name: Human-readable channel name (1-100 chars).
            :ivar description: Optional channel description or topic (max 500 chars).
            :ivar configuration_id: Named configuration to bind to this channel. Supply exactly one of configurationId or configuration.
            :ivar configuration: Inline configuration to snapshot onto this channel. Supply exactly one of configurationId or configuration.
            :ivar creator_user_id: TTID of the user creating the channel; added as the first member with OWNER role.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.display_name: Optional[str] = payload.get("displayName")
            self.description: Optional[str] = payload.get("description")
            self.configuration_id: Optional[str] = payload.get("configurationId")
            self.configuration: Optional[ChannelList.ChannelConfigurationRequest] = payload.get("configuration")
            self.creator_user_id: Optional[str] = payload.get("creatorUserId")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "displayName": self.display_name,
                    "description": self.description,
                    "configurationId": self.configuration_id,
                    "configuration": self.configuration.to_dict() if self.configuration is not None else None ,
                    "creatorUserId": self.creator_user_id,
            }

    class StatusCallbackDto(object):
        """
            :ivar method: HTTP method used to deliver the callback
            :ivar url: Destination URL
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.method: Optional["ChannelInstance.str"] = payload.get("method")
            self.url: Optional[str] = payload.get("url")

        def to_dict(self):
            return {
                
                    "method": self.method,
                    "url": self.url,
            }

    class TwilioServiceErrorResponseRFC9457Errors(object):
        """
            :ivar detail: A human-readable explanation of the validation error for this specific field.
            :ivar pointer: A JSON Pointer (RFC 6901) to the location in the request where the error occurred.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.detail: Optional[str] = payload.get("detail")
            self.pointer: Optional[str] = payload.get("pointer")

        def to_dict(self):
            return {
                
                    "detail": self.detail,
                    "pointer": self.pointer,
            }

    class UpdateChannelRequest(object):
        """
            :ivar name: New channel slug (null = no change).
            :ivar description: New channel description (null = no change; empty string clears it).
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "description": self.description,
            }


    def __init__(self, version: Version, user_id: str):
        """
        Initialize the ChannelList

        :param version: Version that contains the resource
        :param user_id: User ID (TTID format)
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'user_id': user_id,  }
        self._uri = '/Users/{user_id}/Channels'.format(**self._solution)
        
        
    
    
    def list(self, 
    ) -> List[ChannelInstance]:
        """
        Lists ChannelInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [ChannelInstance(self._version, item, user_id=self._solution['user_id']) for item in payload["items"]]
        

    async def list_async(self, 
    ) -> List[ChannelInstance]:
        """
        Asynchronously lists ChannelInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [ChannelInstance(self._version, item, user_id=self._solution['user_id']) for item in payload["items"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> ChannelPage:
        """
        Retrieve a single page of ChannelInstance records from the API.
        Request is executed immediately
        
        :returns: Page of ChannelInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ChannelPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
    ) -> ChannelPage:
        """
        Asynchronously retrieve a single page of ChannelInstance records from the API.
        Request is executed immediately
        
        :returns: Page of ChannelInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ChannelPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChannelPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ChannelPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChannelPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ChannelPage:
        """
        Retrieve a specific page of ChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChannelInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ChannelPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ChannelPage:
        """
        Asynchronously retrieve a specific page of ChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChannelInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ChannelPage(self._version, response, solution=self._solution)

    
    
    def _create(self, create_channel_request: CreateChannelRequest) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_channel_request.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, create_channel_request: CreateChannelRequest) -> ChannelInstance:
        """
        Create the ChannelInstance

        :param create_channel_request: 
        
        :returns: The created ChannelInstance
        """
        payload, _, _ = self._create(create_channel_request=create_channel_request)
        return ChannelInstance(self._version, payload, user_id=self._solution['user_id'])

    def create_with_http_info(self, create_channel_request: CreateChannelRequest) -> ApiResponse:
        """
        Create the ChannelInstance and return response metadata

        :param create_channel_request: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(create_channel_request=create_channel_request)
        instance = ChannelInstance(self._version, payload, user_id=self._solution['user_id'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, create_channel_request: CreateChannelRequest) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_channel_request.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, create_channel_request: CreateChannelRequest) -> ChannelInstance:
        """
        Asynchronously create the ChannelInstance

        :param create_channel_request: 
        
        :returns: The created ChannelInstance
        """
        payload, _, _ = await self._create_async(create_channel_request=create_channel_request)
        return ChannelInstance(self._version, payload, user_id=self._solution['user_id'])

    async def create_with_http_info_async(self, create_channel_request: CreateChannelRequest) -> ApiResponse:
        """
        Asynchronously create the ChannelInstance and return response metadata

        :param create_channel_request: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(create_channel_request=create_channel_request)
        instance = ChannelInstance(self._version, payload, user_id=self._solution['user_id'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        user_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[ChannelInstance]:
        """
        Streams ChannelInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str user_id: Filter by user membership — returns only channels this user belongs to.
        :param str page_token: Opaque forward-pagination token from `meta.nextToken`
        :param str previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            user_id=user_id,
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        user_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[ChannelInstance]:
        """
        Asynchronously streams ChannelInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str user_id: Filter by user membership — returns only channels this user belongs to.
        :param str page_token: Opaque forward-pagination token from `meta.nextToken`
        :param str previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            user_id=user_id,
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        user_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams ChannelInstance and returns headers from first page

        
        :param str user_id: Filter by user membership — returns only channels this user belongs to.
        :param str page_token: Opaque forward-pagination token from `meta.nextToken`
        :param str previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            user_id=user_id,
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        user_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams ChannelInstance and returns headers from first page

        
        :param str user_id: Filter by user membership — returns only channels this user belongs to.
        :param str page_token: Opaque forward-pagination token from `meta.nextToken`
        :param str previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            user_id=user_id,
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        user_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ChannelInstance]:
        """
        Lists ChannelInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str user_id: Filter by user membership — returns only channels this user belongs to.
        :param str page_token: Opaque forward-pagination token from `meta.nextToken`
        :param str previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            user_id=user_id,
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        user_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ChannelInstance]:
        """
        Asynchronously lists ChannelInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str user_id: Filter by user membership — returns only channels this user belongs to.
        :param str page_token: Opaque forward-pagination token from `meta.nextToken`
        :param str previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            user_id=user_id,
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        user_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ChannelInstance and returns headers from first page

        
        :param str user_id: Filter by user membership — returns only channels this user belongs to.
        :param str page_token: Opaque forward-pagination token from `meta.nextToken`
        :param str previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            user_id=user_id,
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        user_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ChannelInstance and returns headers from first page

        
        :param str user_id: Filter by user membership — returns only channels this user belongs to.
        :param str page_token: Opaque forward-pagination token from `meta.nextToken`
        :param str previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            user_id=user_id,
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      user_id: Union[str, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      previous_token: Union[str, object] = values.unset,
    ) -> ChannelPage:
        """
        Retrieve a single page of ChannelInstance records from the API.
        Request is executed immediately
        
        :param user_id: Filter by user membership — returns only channels this user belongs to.
        :param page_size: Page size (1–100, default 50)
        :param page_token: Opaque forward-pagination token from `meta.nextToken`
        :param previous_token: Opaque backward-pagination token from `meta.previousToken`
        :returns: Page of ChannelInstance
        """
        data = values.of({ 
            'userId': user_id,
            'pageSize': page_size,
            'pageToken': page_token,
            'previousToken': previous_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ChannelPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      user_id: Union[str, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      previous_token: Union[str, object] = values.unset,
    ) -> ChannelPage:
        """
        Asynchronously retrieve a single page of ChannelInstance records from the API.
        Request is executed immediately
        
        :param user_id: Filter by user membership — returns only channels this user belongs to.
        :param page_size: Page size (1–100, default 50)
        :param page_token: Opaque forward-pagination token from `meta.nextToken`
        :param previous_token: Opaque backward-pagination token from `meta.previousToken`
        :returns: Page of ChannelInstance
        """
        data = values.of({ 
            'userId': user_id,
            'pageSize': page_size,
            'pageToken': page_token,
            'previousToken': previous_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ChannelPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        user_id: Union[str, object] = values.unset,
        
        
        previous_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param user_id: Filter by user membership — returns only channels this user belongs to.
        :param page_token: Opaque forward-pagination token from `meta.nextToken`
        :param previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChannelPage, status code, and headers
        """
        data = values.of({ 
            'userId': user_id,
            'pageToken': page_token,
            'previousToken': previous_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ChannelPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        user_id: Union[str, object] = values.unset,
        
        
        previous_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param user_id: Filter by user membership — returns only channels this user belongs to.
        :param page_token: Opaque forward-pagination token from `meta.nextToken`
        :param previous_token: Opaque backward-pagination token from `meta.previousToken`
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChannelPage, status code, and headers
        """
        data = values.of({ 
            'userId': user_id,
            'pageToken': page_token,
            'previousToken': previous_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ChannelPage:
        """
        Retrieve a specific page of ChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChannelInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ChannelPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ChannelPage:
        """
        Asynchronously retrieve a specific page of ChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChannelInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ChannelPage(self._version, response, solution=self._solution)

    
    def stream(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[ChannelInstance]:
        """
        Streams ChannelInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[ChannelInstance]:
        """
        Asynchronously streams ChannelInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams ChannelInstance and returns headers from first page

        
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams ChannelInstance and returns headers from first page

        
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_token=page_token,
            previous_token=previous_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ChannelInstance]:
        """
        Lists ChannelInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ChannelInstance]:
        """
        Asynchronously lists ChannelInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ChannelInstance and returns headers from first page

        
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        previous_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ChannelInstance and returns headers from first page

        
        :param str page_token: Forward pagination token
        :param str previous_token: Backward pagination token
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            previous_token=previous_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      previous_token: Union[str, object] = values.unset,
    ) -> ChannelPage:
        """
        Retrieve a single page of ChannelInstance records from the API.
        Request is executed immediately
        
        :param page_size: Page size (max 100, default 50)
        :param page_token: Forward pagination token
        :param previous_token: Backward pagination token
        :returns: Page of ChannelInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'pageToken': page_token,
            'previousToken': previous_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ChannelPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      previous_token: Union[str, object] = values.unset,
    ) -> ChannelPage:
        """
        Asynchronously retrieve a single page of ChannelInstance records from the API.
        Request is executed immediately
        
        :param page_size: Page size (max 100, default 50)
        :param page_token: Forward pagination token
        :param previous_token: Backward pagination token
        :returns: Page of ChannelInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'pageToken': page_token,
            'previousToken': previous_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ChannelPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        
        
        previous_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: Forward pagination token
        :param previous_token: Backward pagination token
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChannelPage, status code, and headers
        """
        data = values.of({ 
            'pageToken': page_token,
            'previousToken': previous_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ChannelPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        
        previous_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: Forward pagination token
        :param previous_token: Backward pagination token
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChannelPage, status code, and headers
        """
        data = values.of({ 
            'pageToken': page_token,
            'previousToken': previous_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ChannelPage:
        """
        Retrieve a specific page of ChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChannelInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ChannelPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ChannelPage:
        """
        Asynchronously retrieve a specific page of ChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChannelInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ChannelPage(self._version, response, solution=self._solution)



    def get(self, channel_id: str) -> ChannelContext:
        """
        Constructs a ChannelContext
        
        :param channel_id: Channel TTID (pattern: `chat_channel_[0-7][a-z0-9]{25}`)
        """
        return ChannelContext(self._version, channel_id=channel_id, user_id=self._solution['user_id'])

    def __call__(self, channel_id: str) -> ChannelContext:
        """
        Constructs a ChannelContext
        
        :param channel_id: Channel TTID (pattern: `chat_channel_[0-7][a-z0-9]{25}`)
        """
        return ChannelContext(self._version, channel_id=channel_id, user_id=self._solution['user_id'])

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Chat.Users_manisingh_codehq_open_api_spec_chat_v4_openapi_yaml.ChannelList>'





