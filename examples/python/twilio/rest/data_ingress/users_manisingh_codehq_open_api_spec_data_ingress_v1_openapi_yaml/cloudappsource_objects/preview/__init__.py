r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Data Ingress API
    APIs for managing data integration sources, datasets, and data ingestion into Twilio products.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version

from twilio.rest.data_ingress.users_manisingh_codehq_open_api_spec_data_ingress_v1_openapi_yaml.cloudappsource_objects.preview.preview import PreviewList








class PreviewInstance(InstanceResource):


    class ArrayPropertyItems(object):
        """
            :ivar type: Data type of array elements
            :ivar format: Format specification for array element type
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[str] = payload.get("type")
            self.format: Optional[str] = payload.get("format")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "format": self.format,
            }

    class CloudAppDatasetCreateSchedule(object):
        """
            :ivar type: 
            :ivar interval: Duration string in ISO 8601 duration format representing the time interval between executions.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["DatasetInstance.str"] = payload.get("type")
            self.interval: Optional[str] = payload.get("interval")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "interval": self.interval,
            }

    class CloudAppPreviewRequest(object):
        """
            :ivar properties: List of property names to preview
            :ivar limit: Maximum number of records to return in the preview
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.properties: Optional[List[str]] = payload.get("properties")
            self.limit: Optional[int] = payload.get("limit")

        def to_dict(self):
            return {
                
                    "properties": self.properties,
                    "limit": self.limit,
            }

    class OAuthJWTBearerCredentials(object):
        """
            :ivar credentials_type: Constant value of oauth_jwt_bearer for authentication method. Required for OpenAPI discriminator mapping.
            :ivar client_id: Client identifier for your OAuth application. Some platforms may refer to this as \"Client ID\", \"Consumer Key\", or \"Application ID\". This value is used as the \"iss\" (issuer) claim in the JWT assertion. 
            :ivar username: The username or email of the integration user that the service will authenticate as. This value is used as the \"sub\" (subject) claim in the JWT assertion. 
            :ivar private_key: PEM-encoded RSA private key in PKCS#8 format used to sign the JWT Bearer assertion. Key length must be at least 2048-bit. PKCS#1 format (`BEGIN RSA PRIVATE KEY`) is not supported.  When provided, this customer-supplied key is used for all signing for this source.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
            :ivar private_key_passphrase: **Optional.** Passphrase for an encrypted private key. Only required if `privateKey` is encrypted.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.credentials_type: Optional["CloudAppSourceInstance.str"] = payload.get("credentialsType")
            self.client_id: Optional[str] = payload.get("clientId")
            self.username: Optional[str] = payload.get("username")
            self.private_key: Optional[str] = payload.get("privateKey")
            self.private_key_passphrase: Optional[str] = payload.get("privateKeyPassphrase")

        def to_dict(self):
            return {
                
                    "credentialsType": self.credentials_type,
                    "clientId": self.client_id,
                    "username": self.username,
                    "privateKey": self.private_key,
                    "privateKeyPassphrase": self.private_key_passphrase,
            }

    class SalesforceConfig(object):
        """
            :ivar type: Constant value of SALESFORCE as platform type. Required for OpenAPI discriminator mapping.
            :ivar domain_url: Custom Salesforce domain URL (optional). Required only when using a custom My Domain or enhanced domain. If not provided, the system will construct the standard Salesforce URL based on the environment field. Examples:   - https://your-company.my.salesforce.com   - https://custom-domain.my.salesforce.com 
            :ivar environment: Salesforce environment type. This determines the OAuth authentication endpoint: - `PRODUCTION`: login.salesforce.com - `SANDBOX`: test.salesforce.com This field is used to construct the authentication URL when `domainUrl` is not provided. 
            :ivar credentials: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["CloudAppSourceInstance.str"] = payload.get("type")
            self.domain_url: Optional[str] = payload.get("domainUrl")
            self.environment: Optional["CloudAppSourceInstance.str"] = payload.get("environment")
            self.credentials: Optional[CloudAppSourceList.OAuthJWTBearerCredentials] = payload.get("credentials")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "domainUrl": self.domain_url,
                    "environment": self.environment,
                    "credentials": self.credentials.to_dict() if self.credentials is not None else None ,
            }

    class SchemaPropertiesValue(object):
        """
            :ivar type: Data type of the field
            :ivar format: Format specification for the data type (e.g., date-time, email, uuid)
            :ivar items: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[str] = payload.get("type")
            self.format: Optional[str] = payload.get("format")
            self.items: Optional[ArrayPropertyItems] = payload.get("items")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "format": self.format,
                    "items": self.items.to_dict() if self.items is not None else None ,
            }



    class LongRunningOperationStatus(object):
        PENDING = "PENDING"
        RUNNING = "RUNNING"
        CANCELLED = "CANCELLED"
        COMPLETED = "COMPLETED"
        FAILED = "FAILED"

    """
    :ivar status: 
    :ivar status_url: Resource URI to check operation status.
    :ivar rule_id: Unique identifier for the auto-instrumentation rule
    :ivar name: Name of the auto-instrumentation rule
    :ivar is_identify: Whether this rule is an identify event
    :ivar sigql: SigQL rule definition
    :ivar version: Current version number of the rule
    :ivar source_id: The event source ID this rule belongs to
    :ivar schema_id: The event schema ID that was auto-created for this rule
    :ivar dataset_id: The dataset ID created for this rule
    :ivar created_at: Timestamp when the rule was created
    :ivar updated_at: Timestamp when the rule was last updated
    :ivar code: The Twilio error code.
    :ivar message: A human readable message describing the error.
    :ivar more_info: A URL to a [Twilio error directory](https://www.twilio.com/docs/api/errors) page with more information about the error code.
    :ivar config: 
    :ivar object: Object (or resource) name to pull data from the CloudApp source.
    :ivar properties: List of properties or fields to pull from the object.
    :ivar schedule: 
    :ivar var_schema: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], source_id: str, name: Optional[str] = None):
        super().__init__(version)

        
        self.status: Optional["AutoInstrumentationRuleInstance.str"] = payload.get("status")
        self.status_url: Optional[str] = payload.get("statusUrl")
        self.rule_id: Optional[str] = payload.get("ruleId")
        self.name: Optional[str] = payload.get("name")
        self.is_identify: Optional[bool] = payload.get("isIdentify")
        self.sigql: Optional[str] = payload.get("sigql")
        self.version: Optional[int] = deserialize.integer(payload.get("version"))
        self.source_id: Optional[str] = payload.get("sourceId")
        self.schema_id: Optional[str] = payload.get("schemaId")
        self.dataset_id: Optional[str] = payload.get("datasetId")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("updatedAt"))
        self.code: Optional[int] = payload.get("code")
        self.message: Optional[str] = payload.get("message")
        self.more_info: Optional[str] = payload.get("more_info")
        self.config: Optional[str] = payload.get("config")
        self.object: Optional[str] = payload.get("object")
        self.properties: Optional[List[str]] = payload.get("properties")
        self.schedule: Optional[str] = payload.get("schedule")
        self.var_schema: Optional[str] = payload.get("schema")

        
        
        self._solution = { 
            "source_id": source_id,
            "name": name or self.name,
        }
        
        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.DataIngress.Users_manisingh_codehq_open_api_spec_data_ingress_v1_openapi_yaml.PreviewInstance {}>'.format(context)




class PreviewList(ListResource):
    
    class ArrayPropertyItems(object):
        """
            :ivar type: Data type of array elements
            :ivar format: Format specification for array element type
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[str] = payload.get("type")
            self.format: Optional[str] = payload.get("format")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "format": self.format,
            }

    class CloudAppDatasetCreateSchedule(object):
        """
            :ivar type: 
            :ivar interval: Duration string in ISO 8601 duration format representing the time interval between executions.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["DatasetInstance.str"] = payload.get("type")
            self.interval: Optional[str] = payload.get("interval")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "interval": self.interval,
            }

    class CloudAppPreviewRequest(object):
        """
            :ivar properties: List of property names to preview
            :ivar limit: Maximum number of records to return in the preview
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.properties: Optional[List[str]] = payload.get("properties")
            self.limit: Optional[int] = payload.get("limit")

        def to_dict(self):
            return {
                
                    "properties": self.properties,
                    "limit": self.limit,
            }

    class OAuthJWTBearerCredentials(object):
        """
            :ivar credentials_type: Constant value of oauth_jwt_bearer for authentication method. Required for OpenAPI discriminator mapping.
            :ivar client_id: Client identifier for your OAuth application. Some platforms may refer to this as \"Client ID\", \"Consumer Key\", or \"Application ID\". This value is used as the \"iss\" (issuer) claim in the JWT assertion. 
            :ivar username: The username or email of the integration user that the service will authenticate as. This value is used as the \"sub\" (subject) claim in the JWT assertion. 
            :ivar private_key: PEM-encoded RSA private key in PKCS#8 format used to sign the JWT Bearer assertion. Key length must be at least 2048-bit. PKCS#1 format (`BEGIN RSA PRIVATE KEY`) is not supported.  When provided, this customer-supplied key is used for all signing for this source.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
            :ivar private_key_passphrase: **Optional.** Passphrase for an encrypted private key. Only required if `privateKey` is encrypted.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.credentials_type: Optional["CloudAppSourceInstance.str"] = payload.get("credentialsType")
            self.client_id: Optional[str] = payload.get("clientId")
            self.username: Optional[str] = payload.get("username")
            self.private_key: Optional[str] = payload.get("privateKey")
            self.private_key_passphrase: Optional[str] = payload.get("privateKeyPassphrase")

        def to_dict(self):
            return {
                
                    "credentialsType": self.credentials_type,
                    "clientId": self.client_id,
                    "username": self.username,
                    "privateKey": self.private_key,
                    "privateKeyPassphrase": self.private_key_passphrase,
            }

    class SalesforceConfig(object):
        """
            :ivar type: Constant value of SALESFORCE as platform type. Required for OpenAPI discriminator mapping.
            :ivar domain_url: Custom Salesforce domain URL (optional). Required only when using a custom My Domain or enhanced domain. If not provided, the system will construct the standard Salesforce URL based on the environment field. Examples:   - https://your-company.my.salesforce.com   - https://custom-domain.my.salesforce.com 
            :ivar environment: Salesforce environment type. This determines the OAuth authentication endpoint: - `PRODUCTION`: login.salesforce.com - `SANDBOX`: test.salesforce.com This field is used to construct the authentication URL when `domainUrl` is not provided. 
            :ivar credentials: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["CloudAppSourceInstance.str"] = payload.get("type")
            self.domain_url: Optional[str] = payload.get("domainUrl")
            self.environment: Optional["CloudAppSourceInstance.str"] = payload.get("environment")
            self.credentials: Optional[CloudAppSourceList.OAuthJWTBearerCredentials] = payload.get("credentials")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "domainUrl": self.domain_url,
                    "environment": self.environment,
                    "credentials": self.credentials.to_dict() if self.credentials is not None else None ,
            }

    class SchemaPropertiesValue(object):
        """
            :ivar type: Data type of the field
            :ivar format: Format specification for the data type (e.g., date-time, email, uuid)
            :ivar items: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[str] = payload.get("type")
            self.format: Optional[str] = payload.get("format")
            self.items: Optional[ArrayPropertyItems] = payload.get("items")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "format": self.format,
                    "items": self.items.to_dict() if self.items is not None else None ,
            }


    def __init__(self, version: Version, source_id: str, name: str):
        """
        Initialize the PreviewList

        :param version: Version that contains the resource
        :param source_id: Unique identifier of the CloudApp source
        :param name: Name of the object to preview
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'source_id': source_id, 'name': name,  }
        self._uri = '/CloudAppSources/{source_id}/Objects/{name}/Preview'.format(**self._solution)
        
        self._preview: Optional[PreviewList] = None
        
    
    def _create(self, idempotency_key: str, cloud_app_preview_request: CloudAppPreviewRequest) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = cloud_app_preview_request.to_dict()
        
        headers = values.of({
                'Idempotency-Key': idempotency_key,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, idempotency_key: str, cloud_app_preview_request: CloudAppPreviewRequest) -> PreviewInstance:
        """
        Create the PreviewInstance

        :param idempotency_key: Client-generated key to ensure idempotent behavior for operations that modify resources. Submitting the same key for identical requests will return the same result without executing the operation twice.  Format Requirements: - MUST use UUIDv7 format for standardization and consistency across all APIs - Keys are guaranteed to be unique for the same account and region combination  Behavior Details: - `Idempotency-Key` is scoped to a Twilio Account and Region (same key can be reused in different accounts/regions) - Server will verify repeated requests with the same key contain identical payloads - Requests with the same key but different payloads are rejected with `409 Conflict` error - Uniqueness is preserved for at least 24 hours (TTL), after which the key may be reused 
        :param cloud_app_preview_request: 
        
        :returns: The created PreviewInstance
        """
        payload, _, _ = self._create(idempotency_key=idempotency_key, cloud_app_preview_request=cloud_app_preview_request)
        return PreviewInstance(self._version, payload, source_id=self._solution['source_id'], name=self._solution['name'])

    def create_with_http_info(self, idempotency_key: str, cloud_app_preview_request: CloudAppPreviewRequest) -> ApiResponse:
        """
        Create the PreviewInstance and return response metadata

        :param idempotency_key: Client-generated key to ensure idempotent behavior for operations that modify resources. Submitting the same key for identical requests will return the same result without executing the operation twice.  Format Requirements: - MUST use UUIDv7 format for standardization and consistency across all APIs - Keys are guaranteed to be unique for the same account and region combination  Behavior Details: - `Idempotency-Key` is scoped to a Twilio Account and Region (same key can be reused in different accounts/regions) - Server will verify repeated requests with the same key contain identical payloads - Requests with the same key but different payloads are rejected with `409 Conflict` error - Uniqueness is preserved for at least 24 hours (TTL), after which the key may be reused 
        :param cloud_app_preview_request: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(idempotency_key=idempotency_key, cloud_app_preview_request=cloud_app_preview_request)
        instance = PreviewInstance(self._version, payload, source_id=self._solution['source_id'], name=self._solution['name'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, idempotency_key: str, cloud_app_preview_request: CloudAppPreviewRequest) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = cloud_app_preview_request.to_dict()
        
        headers = values.of({
                'Idempotency-Key': idempotency_key,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, idempotency_key: str, cloud_app_preview_request: CloudAppPreviewRequest) -> PreviewInstance:
        """
        Asynchronously create the PreviewInstance

        :param idempotency_key: Client-generated key to ensure idempotent behavior for operations that modify resources. Submitting the same key for identical requests will return the same result without executing the operation twice.  Format Requirements: - MUST use UUIDv7 format for standardization and consistency across all APIs - Keys are guaranteed to be unique for the same account and region combination  Behavior Details: - `Idempotency-Key` is scoped to a Twilio Account and Region (same key can be reused in different accounts/regions) - Server will verify repeated requests with the same key contain identical payloads - Requests with the same key but different payloads are rejected with `409 Conflict` error - Uniqueness is preserved for at least 24 hours (TTL), after which the key may be reused 
        :param cloud_app_preview_request: 
        
        :returns: The created PreviewInstance
        """
        payload, _, _ = await self._create_async(idempotency_key=idempotency_key, cloud_app_preview_request=cloud_app_preview_request)
        return PreviewInstance(self._version, payload, source_id=self._solution['source_id'], name=self._solution['name'])

    async def create_with_http_info_async(self, idempotency_key: str, cloud_app_preview_request: CloudAppPreviewRequest) -> ApiResponse:
        """
        Asynchronously create the PreviewInstance and return response metadata

        :param idempotency_key: Client-generated key to ensure idempotent behavior for operations that modify resources. Submitting the same key for identical requests will return the same result without executing the operation twice.  Format Requirements: - MUST use UUIDv7 format for standardization and consistency across all APIs - Keys are guaranteed to be unique for the same account and region combination  Behavior Details: - `Idempotency-Key` is scoped to a Twilio Account and Region (same key can be reused in different accounts/regions) - Server will verify repeated requests with the same key contain identical payloads - Requests with the same key but different payloads are rejected with `409 Conflict` error - Uniqueness is preserved for at least 24 hours (TTL), after which the key may be reused 
        :param cloud_app_preview_request: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(idempotency_key=idempotency_key, cloud_app_preview_request=cloud_app_preview_request)
        instance = PreviewInstance(self._version, payload, source_id=self._solution['source_id'], name=self._solution['name'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    



    @property
    def preview(self) -> PreviewList:
        """
        Access the preview
        """
        if self._preview is None:
            self._preview = PreviewList(self._version, source_id=self._solution['source_id'], name=self._solution['name'])
        return self._preview



    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.DataIngress.Users_manisingh_codehq_open_api_spec_data_ingress_v1_openapi_yaml.PreviewList>'


