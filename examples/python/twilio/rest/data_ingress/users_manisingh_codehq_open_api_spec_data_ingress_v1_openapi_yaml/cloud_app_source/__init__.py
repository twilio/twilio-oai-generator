r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Data Ingress API
    APIs for managing data integration sources, datasets, and data ingestion into Twilio products.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page
from twilio.rest.data_ingress.users_manisingh_codehq_open_api_spec_data_ingress_v1_openapi_yaml.cloud_app_source.dataset import DatasetList








class CloudAppSourceInstance(InstanceResource):


    class CloudAppSourceCreate(object):
        """
            :ivar name: The friendly name for the CloudApp source
            :ivar status: Status of the source. Disabling the source will stop all data ingestion for all associated datasets.
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.status: Optional["CloudAppSourceInstance.str"] = payload.get("status")
            self.config: Optional[CloudAppSourceList.SalesforceConfig] = payload.get("config")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "status": self.status,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class CloudAppSourceUpdate(object):
        """
            :ivar name: The friendly name for the CloudApp source
            :ivar status: Status of the source. Disabling the source will stop all data ingestion for all associated datasets. Enabling the source will start data ingestion for already enabled datasets. 
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.status: Optional["CloudAppSourceInstance.str"] = payload.get("status")
            self.config: Optional[CloudAppSourceList.SalesforceConfigUpdate] = payload.get("config")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "status": self.status,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class OAuthJWTBearerCredentials(object):
        """
            :ivar credentials_type: Constant value of oauth_jwt_bearer for authentication method. Required for OpenAPI discriminator mapping.
            :ivar client_id: Client identifier for your OAuth application. Some platforms may refer to this as \"Client ID\", \"Consumer Key\", or \"Application ID\". This value is used as the \"iss\" (issuer) claim in the JWT assertion. 
            :ivar username: The username or email of the integration user that the service will authenticate as. This value is used as the \"sub\" (subject) claim in the JWT assertion. 
            :ivar private_key: PEM-encoded RSA private key in PKCS#8 format used to sign the JWT Bearer assertion. Key length must be at least 2048-bit. PKCS#1 format (`BEGIN RSA PRIVATE KEY`) is not supported.  When provided, this customer-supplied key is used for all signing for this source.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
            :ivar private_key_passphrase: **Optional.** Passphrase for an encrypted private key. Only required if `privateKey` is encrypted.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.credentials_type: Optional["CloudAppSourceInstance.str"] = payload.get("credentialsType")
            self.client_id: Optional[str] = payload.get("clientId")
            self.username: Optional[str] = payload.get("username")
            self.private_key: Optional[str] = payload.get("privateKey")
            self.private_key_passphrase: Optional[str] = payload.get("privateKeyPassphrase")

        def to_dict(self):
            return {
                
                    "credentialsType": self.credentials_type,
                    "clientId": self.client_id,
                    "username": self.username,
                    "privateKey": self.private_key,
                    "privateKeyPassphrase": self.private_key_passphrase,
            }

    class SalesforceConfig(object):
        """
            :ivar type: Constant value of SALESFORCE as platform type. Required for OpenAPI discriminator mapping.
            :ivar domain_url: Custom Salesforce domain URL (optional). Required only when using a custom My Domain or enhanced domain. If not provided, the system will construct the standard Salesforce URL based on the environment field. Examples:   - https://your-company.my.salesforce.com   - https://custom-domain.my.salesforce.com 
            :ivar environment: Salesforce environment type. This determines the OAuth authentication endpoint: - `PRODUCTION`: login.salesforce.com - `SANDBOX`: test.salesforce.com This field is used to construct the authentication URL when `domainUrl` is not provided. 
            :ivar credentials: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["CloudAppSourceInstance.str"] = payload.get("type")
            self.domain_url: Optional[str] = payload.get("domainUrl")
            self.environment: Optional["CloudAppSourceInstance.str"] = payload.get("environment")
            self.credentials: Optional[CloudAppSourceList.OAuthJWTBearerCredentials] = payload.get("credentials")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "domainUrl": self.domain_url,
                    "environment": self.environment,
                    "credentials": self.credentials.to_dict() if self.credentials is not None else None ,
            }

    class SalesforceConfigUpdate(object):
        """
            :ivar type: Constant value of SALESFORCE as platform type. Required for OpenAPI discriminator mapping.
            :ivar domain_url: Custom Salesforce domain URL (optional). Can only be set if it was never previously configured. Attempting to patch this field when it already has a value will fail the request. Required only when using a custom My Domain or enhanced domain. If not provided, the system will construct the standard Salesforce URL based on the environment field. Examples:   - https://your-company.my.salesforce.com   - https://custom-domain.my.salesforce.com 
            :ivar credentials: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["CloudAppSourceInstance.str"] = payload.get("type")
            self.domain_url: Optional[str] = payload.get("domainUrl")
            self.credentials: Optional[CloudAppSourceList.OAuthJWTBearerCredentials] = payload.get("credentials")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "domainUrl": self.domain_url,
                    "credentials": self.credentials.to_dict() if self.credentials is not None else None ,
            }



    class LongRunningOperationStatus(object):
        PENDING = "PENDING"
        RUNNING = "RUNNING"
        CANCELLED = "CANCELLED"
        COMPLETED = "COMPLETED"
        FAILED = "FAILED"

    """
    :ivar status: 
    :ivar status_url: Resource URI to check operation status.
    :ivar rule_id: Unique identifier for the auto-instrumentation rule
    :ivar name: Name of the auto-instrumentation rule
    :ivar is_identify: Whether this rule is an identify event
    :ivar sigql: SigQL rule definition
    :ivar version: Current version number of the rule
    :ivar source_id: The event source ID this rule belongs to
    :ivar schema_id: The event schema ID that was auto-created for this rule
    :ivar dataset_id: The dataset ID created for this rule
    :ivar created_at: Timestamp when the rule was created
    :ivar updated_at: Timestamp when the rule was last updated
    :ivar code: The Twilio error code.
    :ivar message: A human readable message describing the error.
    :ivar more_info: A URL to a [Twilio error directory](https://www.twilio.com/docs/api/errors) page with more information about the error code.
    :ivar config: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], source_id: str):
        super().__init__(version)

        
        self.status: Optional["AutoInstrumentationRuleInstance.str"] = payload.get("status")
        self.status_url: Optional[str] = payload.get("statusUrl")
        self.rule_id: Optional[str] = payload.get("ruleId")
        self.name: Optional[str] = payload.get("name")
        self.is_identify: Optional[bool] = payload.get("isIdentify")
        self.sigql: Optional[str] = payload.get("sigql")
        self.version: Optional[int] = deserialize.integer(payload.get("version"))
        self.source_id: Optional[str] = payload.get("sourceId")
        self.schema_id: Optional[str] = payload.get("schemaId")
        self.dataset_id: Optional[str] = payload.get("datasetId")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("updatedAt"))
        self.code: Optional[int] = payload.get("code")
        self.message: Optional[str] = payload.get("message")
        self.more_info: Optional[str] = payload.get("more_info")
        self.config: Optional[str] = payload.get("config")

        
        
        self._solution = { 
            "source_id": source_id,
        }
        
        
        self._context: Optional[CloudAppSourceContext] = None

    @property
    def _proxy(self) -> "CloudAppSourceContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: CloudAppSourceContext for this CloudAppSourceInstance
        """
        if self._context is None:
            self._context = CloudAppSourceContext(self._version, source_id=self._solution['source_id'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the CloudAppSourceInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the CloudAppSourceInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the CloudAppSourceInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the CloudAppSourceInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    def fetch(self) -> "CloudAppSourceInstance":
        """
        Fetch the CloudAppSourceInstance
        

        :returns: The fetched CloudAppSourceInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "CloudAppSourceInstance":
        """
        Asynchronous coroutine to fetch the CloudAppSourceInstance
        

        :returns: The fetched CloudAppSourceInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the CloudAppSourceInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the CloudAppSourceInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()
    
    
    def patch(self, if_match: str, cloud_app_source_update: CloudAppSourceUpdate) -> "CloudAppSourceInstance":
        """
        Patch the CloudAppSourceInstance
        
        :param if_match: Header for optimistic concurrency control. Provide the `ETag` value obtained from a previous `GET` request to ensure the resource hasn't been modified by another client. The server will process the request only if the provided `ETag` matches the current version of the resource. If there is a mismatch, the server will respond with a `412 Precondition Failed` status, indicating that the resource has been modified since the `ETag` was obtained. 
        :param cloud_app_source_update: 

        :returns: The patched CloudAppSourceInstance
        """
        return self._proxy.patch(if_match=if_match, cloud_app_source_update=cloud_app_source_update, )

    async def patch_async(self, if_match: str, cloud_app_source_update: CloudAppSourceUpdate) -> "CloudAppSourceInstance":
        """
        Asynchronous coroutine to patch the CloudAppSourceInstance
        
        :param if_match: Header for optimistic concurrency control. Provide the `ETag` value obtained from a previous `GET` request to ensure the resource hasn't been modified by another client. The server will process the request only if the provided `ETag` matches the current version of the resource. If there is a mismatch, the server will respond with a `412 Precondition Failed` status, indicating that the resource has been modified since the `ETag` was obtained. 
        :param cloud_app_source_update: 

        :returns: The patched CloudAppSourceInstance
        """
        return await self._proxy.patch_async(if_match=if_match, cloud_app_source_update=cloud_app_source_update, )
    
    @property
    def datasets(self) -> DatasetList:
        """
        Access the datasets
        """
        return self._proxy.datasets
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.DataIngress.Users_manisingh_codehq_open_api_spec_data_ingress_v1_openapi_yaml.CloudAppSourceInstance {}>'.format(context)

class CloudAppSourceContext(InstanceContext):


    class CloudAppSourceCreate(object):
        """
            :ivar name: The friendly name for the CloudApp source
            :ivar status: Status of the source. Disabling the source will stop all data ingestion for all associated datasets.
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.status: Optional["CloudAppSourceInstance.str"] = payload.get("status")
            self.config: Optional[CloudAppSourceList.SalesforceConfig] = payload.get("config")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "status": self.status,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class CloudAppSourceUpdate(object):
        """
            :ivar name: The friendly name for the CloudApp source
            :ivar status: Status of the source. Disabling the source will stop all data ingestion for all associated datasets. Enabling the source will start data ingestion for already enabled datasets. 
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.status: Optional["CloudAppSourceInstance.str"] = payload.get("status")
            self.config: Optional[CloudAppSourceList.SalesforceConfigUpdate] = payload.get("config")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "status": self.status,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class OAuthJWTBearerCredentials(object):
        """
            :ivar credentials_type: Constant value of oauth_jwt_bearer for authentication method. Required for OpenAPI discriminator mapping.
            :ivar client_id: Client identifier for your OAuth application. Some platforms may refer to this as \"Client ID\", \"Consumer Key\", or \"Application ID\". This value is used as the \"iss\" (issuer) claim in the JWT assertion. 
            :ivar username: The username or email of the integration user that the service will authenticate as. This value is used as the \"sub\" (subject) claim in the JWT assertion. 
            :ivar private_key: PEM-encoded RSA private key in PKCS#8 format used to sign the JWT Bearer assertion. Key length must be at least 2048-bit. PKCS#1 format (`BEGIN RSA PRIVATE KEY`) is not supported.  When provided, this customer-supplied key is used for all signing for this source.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
            :ivar private_key_passphrase: **Optional.** Passphrase for an encrypted private key. Only required if `privateKey` is encrypted.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.credentials_type: Optional["CloudAppSourceInstance.str"] = payload.get("credentialsType")
            self.client_id: Optional[str] = payload.get("clientId")
            self.username: Optional[str] = payload.get("username")
            self.private_key: Optional[str] = payload.get("privateKey")
            self.private_key_passphrase: Optional[str] = payload.get("privateKeyPassphrase")

        def to_dict(self):
            return {
                
                    "credentialsType": self.credentials_type,
                    "clientId": self.client_id,
                    "username": self.username,
                    "privateKey": self.private_key,
                    "privateKeyPassphrase": self.private_key_passphrase,
            }

    class SalesforceConfig(object):
        """
            :ivar type: Constant value of SALESFORCE as platform type. Required for OpenAPI discriminator mapping.
            :ivar domain_url: Custom Salesforce domain URL (optional). Required only when using a custom My Domain or enhanced domain. If not provided, the system will construct the standard Salesforce URL based on the environment field. Examples:   - https://your-company.my.salesforce.com   - https://custom-domain.my.salesforce.com 
            :ivar environment: Salesforce environment type. This determines the OAuth authentication endpoint: - `PRODUCTION`: login.salesforce.com - `SANDBOX`: test.salesforce.com This field is used to construct the authentication URL when `domainUrl` is not provided. 
            :ivar credentials: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["CloudAppSourceInstance.str"] = payload.get("type")
            self.domain_url: Optional[str] = payload.get("domainUrl")
            self.environment: Optional["CloudAppSourceInstance.str"] = payload.get("environment")
            self.credentials: Optional[CloudAppSourceList.OAuthJWTBearerCredentials] = payload.get("credentials")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "domainUrl": self.domain_url,
                    "environment": self.environment,
                    "credentials": self.credentials.to_dict() if self.credentials is not None else None ,
            }

    class SalesforceConfigUpdate(object):
        """
            :ivar type: Constant value of SALESFORCE as platform type. Required for OpenAPI discriminator mapping.
            :ivar domain_url: Custom Salesforce domain URL (optional). Can only be set if it was never previously configured. Attempting to patch this field when it already has a value will fail the request. Required only when using a custom My Domain or enhanced domain. If not provided, the system will construct the standard Salesforce URL based on the environment field. Examples:   - https://your-company.my.salesforce.com   - https://custom-domain.my.salesforce.com 
            :ivar credentials: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["CloudAppSourceInstance.str"] = payload.get("type")
            self.domain_url: Optional[str] = payload.get("domainUrl")
            self.credentials: Optional[CloudAppSourceList.OAuthJWTBearerCredentials] = payload.get("credentials")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "domainUrl": self.domain_url,
                    "credentials": self.credentials.to_dict() if self.credentials is not None else None ,
            }


    def __init__(self, version: Version, source_id: str):
        """
        Initialize the CloudAppSourceContext

        :param version: Version that contains the resource
        :param source_id: Unique identifier for the CloudApp source.
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'source_id': source_id,
        }
        self._uri = '/ControlPlane/CloudAppSources/{source_id}'.format(**self._solution)
        
        self._datasets: Optional[DatasetList] = None
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the CloudAppSourceInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the CloudAppSourceInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the CloudAppSourceInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the CloudAppSourceInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers)

    def fetch(self) -> CloudAppSourceInstance:
        """
        Fetch the CloudAppSourceInstance
        

        :returns: The fetched CloudAppSourceInstance
        """
        payload, _, _ = self._fetch()
        return CloudAppSourceInstance(
            self._version,
            payload,
            source_id=self._solution['source_id'],
            
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the CloudAppSourceInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = CloudAppSourceInstance(
            self._version,
            payload,
            source_id=self._solution['source_id'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers)

    async def fetch_async(self) -> CloudAppSourceInstance:
        """
        Asynchronous coroutine to fetch the CloudAppSourceInstance
        

        :returns: The fetched CloudAppSourceInstance
        """
        payload, _, _ = await self._fetch_async()
        return CloudAppSourceInstance(
            self._version,
            payload,
            source_id=self._solution['source_id'],
            
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the CloudAppSourceInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = CloudAppSourceInstance(
            self._version,
            payload,
            source_id=self._solution['source_id'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def _patch(self, if_match: str, cloud_app_source_update: CloudAppSourceUpdate) -> tuple:
        """
        Internal helper for patch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = cloud_app_source_update.to_dict()
        
        headers = values.of({})
        
        
        if not (if_match is values.unset or (isinstance(if_match, str) and not if_match)):
            headers['If-Match'] = if_match
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.patch_with_response_info(method='PATCH', uri=self._uri, data=data, headers=headers)

    def patch(self, if_match: str, cloud_app_source_update: CloudAppSourceUpdate) -> CloudAppSourceInstance:
        """
        Patch the CloudAppSourceInstance
        
        :param if_match: Header for optimistic concurrency control. Provide the `ETag` value obtained from a previous `GET` request to ensure the resource hasn't been modified by another client. The server will process the request only if the provided `ETag` matches the current version of the resource. If there is a mismatch, the server will respond with a `412 Precondition Failed` status, indicating that the resource has been modified since the `ETag` was obtained. 
        :param cloud_app_source_update: 

        :returns: The patched CloudAppSourceInstance
        """
        payload, _, _ = self._patch(if_match=if_match, cloud_app_source_update=cloud_app_source_update)
        return CloudAppSourceInstance(
            self._version,
            payload,
            source_id=self._solution['source_id']
        )

    def patch_with_http_info(self, if_match: str, cloud_app_source_update: CloudAppSourceUpdate) -> ApiResponse:
        """
        Patch the CloudAppSourceInstance and return response metadata
        
        :param if_match: Header for optimistic concurrency control. Provide the `ETag` value obtained from a previous `GET` request to ensure the resource hasn't been modified by another client. The server will process the request only if the provided `ETag` matches the current version of the resource. If there is a mismatch, the server will respond with a `412 Precondition Failed` status, indicating that the resource has been modified since the `ETag` was obtained. 
        :param cloud_app_source_update: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._patch(if_match=if_match, cloud_app_source_update=cloud_app_source_update)
        instance = CloudAppSourceInstance(
            self._version,
            payload,
            source_id=self._solution['source_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _patch_async(self, if_match: str, cloud_app_source_update: CloudAppSourceUpdate) -> tuple:
        """
        Internal async helper for patch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = cloud_app_source_update.to_dict()
        
        headers = values.of({})
        
        
        if not (if_match is values.unset or (isinstance(if_match, str) and not if_match)):
            headers['If-Match'] = if_match
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.patch_with_response_info_async(method='PATCH', uri=self._uri, data=data, headers=headers)

    async def patch_async(self, if_match: str, cloud_app_source_update: CloudAppSourceUpdate) -> CloudAppSourceInstance:
        """
        Asynchronous coroutine to patch the CloudAppSourceInstance
        
        :param if_match: Header for optimistic concurrency control. Provide the `ETag` value obtained from a previous `GET` request to ensure the resource hasn't been modified by another client. The server will process the request only if the provided `ETag` matches the current version of the resource. If there is a mismatch, the server will respond with a `412 Precondition Failed` status, indicating that the resource has been modified since the `ETag` was obtained. 
        :param cloud_app_source_update: 

        :returns: The patched CloudAppSourceInstance
        """
        payload, _, _ = await self._patch_async(if_match=if_match, cloud_app_source_update=cloud_app_source_update)
        return CloudAppSourceInstance(
            self._version,
            payload,
            source_id=self._solution['source_id']
        )

    async def patch_with_http_info_async(self, if_match: str, cloud_app_source_update: CloudAppSourceUpdate) -> ApiResponse:
        """
        Asynchronous coroutine to patch the CloudAppSourceInstance and return response metadata
        
        :param if_match: Header for optimistic concurrency control. Provide the `ETag` value obtained from a previous `GET` request to ensure the resource hasn't been modified by another client. The server will process the request only if the provided `ETag` matches the current version of the resource. If there is a mismatch, the server will respond with a `412 Precondition Failed` status, indicating that the resource has been modified since the `ETag` was obtained. 
        :param cloud_app_source_update: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._patch_async(if_match=if_match, cloud_app_source_update=cloud_app_source_update)
        instance = CloudAppSourceInstance(
            self._version,
            payload,
            source_id=self._solution['source_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    @property
    def datasets(self) -> DatasetList:
        """
        Access the datasets
        """
        if self._datasets is None:
            self._datasets = DatasetList(
                self._version, 
                self._solution['source_id'],
            )
        return self._datasets
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.DataIngress.Users_manisingh_codehq_open_api_spec_data_ingress_v1_openapi_yaml.CloudAppSourceContext {}>'.format(context)











class CloudAppSourcePage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> CloudAppSourceInstance:
        """
        Build an instance of CloudAppSourceInstance

        :param payload: Payload response from the API
        """
        
        return CloudAppSourceInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.DataIngress.Users_manisingh_codehq_open_api_spec_data_ingress_v1_openapi_yaml.CloudAppSourcePage>"





class CloudAppSourceList(ListResource):
    
    class CloudAppSourceCreate(object):
        """
            :ivar name: The friendly name for the CloudApp source
            :ivar status: Status of the source. Disabling the source will stop all data ingestion for all associated datasets.
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.status: Optional["CloudAppSourceInstance.str"] = payload.get("status")
            self.config: Optional[CloudAppSourceList.SalesforceConfig] = payload.get("config")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "status": self.status,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class CloudAppSourceUpdate(object):
        """
            :ivar name: The friendly name for the CloudApp source
            :ivar status: Status of the source. Disabling the source will stop all data ingestion for all associated datasets. Enabling the source will start data ingestion for already enabled datasets. 
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.status: Optional["CloudAppSourceInstance.str"] = payload.get("status")
            self.config: Optional[CloudAppSourceList.SalesforceConfigUpdate] = payload.get("config")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "status": self.status,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class OAuthJWTBearerCredentials(object):
        """
            :ivar credentials_type: Constant value of oauth_jwt_bearer for authentication method. Required for OpenAPI discriminator mapping.
            :ivar client_id: Client identifier for your OAuth application. Some platforms may refer to this as \"Client ID\", \"Consumer Key\", or \"Application ID\". This value is used as the \"iss\" (issuer) claim in the JWT assertion. 
            :ivar username: The username or email of the integration user that the service will authenticate as. This value is used as the \"sub\" (subject) claim in the JWT assertion. 
            :ivar private_key: PEM-encoded RSA private key in PKCS#8 format used to sign the JWT Bearer assertion. Key length must be at least 2048-bit. PKCS#1 format (`BEGIN RSA PRIVATE KEY`) is not supported.  When provided, this customer-supplied key is used for all signing for this source.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
            :ivar private_key_passphrase: **Optional.** Passphrase for an encrypted private key. Only required if `privateKey` is encrypted.  **Security**: Write-only. Encrypted at rest and never returned in any API response. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.credentials_type: Optional["CloudAppSourceInstance.str"] = payload.get("credentialsType")
            self.client_id: Optional[str] = payload.get("clientId")
            self.username: Optional[str] = payload.get("username")
            self.private_key: Optional[str] = payload.get("privateKey")
            self.private_key_passphrase: Optional[str] = payload.get("privateKeyPassphrase")

        def to_dict(self):
            return {
                
                    "credentialsType": self.credentials_type,
                    "clientId": self.client_id,
                    "username": self.username,
                    "privateKey": self.private_key,
                    "privateKeyPassphrase": self.private_key_passphrase,
            }

    class SalesforceConfig(object):
        """
            :ivar type: Constant value of SALESFORCE as platform type. Required for OpenAPI discriminator mapping.
            :ivar domain_url: Custom Salesforce domain URL (optional). Required only when using a custom My Domain or enhanced domain. If not provided, the system will construct the standard Salesforce URL based on the environment field. Examples:   - https://your-company.my.salesforce.com   - https://custom-domain.my.salesforce.com 
            :ivar environment: Salesforce environment type. This determines the OAuth authentication endpoint: - `PRODUCTION`: login.salesforce.com - `SANDBOX`: test.salesforce.com This field is used to construct the authentication URL when `domainUrl` is not provided. 
            :ivar credentials: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["CloudAppSourceInstance.str"] = payload.get("type")
            self.domain_url: Optional[str] = payload.get("domainUrl")
            self.environment: Optional["CloudAppSourceInstance.str"] = payload.get("environment")
            self.credentials: Optional[CloudAppSourceList.OAuthJWTBearerCredentials] = payload.get("credentials")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "domainUrl": self.domain_url,
                    "environment": self.environment,
                    "credentials": self.credentials.to_dict() if self.credentials is not None else None ,
            }

    class SalesforceConfigUpdate(object):
        """
            :ivar type: Constant value of SALESFORCE as platform type. Required for OpenAPI discriminator mapping.
            :ivar domain_url: Custom Salesforce domain URL (optional). Can only be set if it was never previously configured. Attempting to patch this field when it already has a value will fail the request. Required only when using a custom My Domain or enhanced domain. If not provided, the system will construct the standard Salesforce URL based on the environment field. Examples:   - https://your-company.my.salesforce.com   - https://custom-domain.my.salesforce.com 
            :ivar credentials: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["CloudAppSourceInstance.str"] = payload.get("type")
            self.domain_url: Optional[str] = payload.get("domainUrl")
            self.credentials: Optional[CloudAppSourceList.OAuthJWTBearerCredentials] = payload.get("credentials")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "domainUrl": self.domain_url,
                    "credentials": self.credentials.to_dict() if self.credentials is not None else None ,
            }


    def __init__(self, version: Version):
        """
        Initialize the CloudAppSourceList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/ControlPlane/CloudAppSources'
        
        
    
    
    
    
    def _create(self, idempotency_key: str, cloud_app_source_create: CloudAppSourceCreate) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = cloud_app_source_create.to_dict()
        
        headers = values.of({
                'Idempotency-Key': idempotency_key,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, idempotency_key: str, cloud_app_source_create: CloudAppSourceCreate) -> CloudAppSourceInstance:
        """
        Create the CloudAppSourceInstance

        :param idempotency_key: Client-generated key to ensure idempotent behavior for operations that modify resources. Submitting the same key for identical requests will return the same result without executing the operation twice.  Format Requirements: - MUST use UUIDv7 format for standardization and consistency across all APIs - Keys are guaranteed to be unique for the same account and region combination  Behavior Details: - `Idempotency-Key` is scoped to a Twilio Account and Region (same key can be reused in different accounts/regions) - Server will verify repeated requests with the same key contain identical payloads - Requests with the same key but different payloads are rejected with `409 Conflict` error - Uniqueness is preserved for at least 24 hours (TTL), after which the key may be reused 
        :param cloud_app_source_create: 
        
        :returns: The created CloudAppSourceInstance
        """
        payload, _, _ = self._create(idempotency_key=idempotency_key, cloud_app_source_create=cloud_app_source_create)
        return CloudAppSourceInstance(self._version, payload)

    def create_with_http_info(self, idempotency_key: str, cloud_app_source_create: CloudAppSourceCreate) -> ApiResponse:
        """
        Create the CloudAppSourceInstance and return response metadata

        :param idempotency_key: Client-generated key to ensure idempotent behavior for operations that modify resources. Submitting the same key for identical requests will return the same result without executing the operation twice.  Format Requirements: - MUST use UUIDv7 format for standardization and consistency across all APIs - Keys are guaranteed to be unique for the same account and region combination  Behavior Details: - `Idempotency-Key` is scoped to a Twilio Account and Region (same key can be reused in different accounts/regions) - Server will verify repeated requests with the same key contain identical payloads - Requests with the same key but different payloads are rejected with `409 Conflict` error - Uniqueness is preserved for at least 24 hours (TTL), after which the key may be reused 
        :param cloud_app_source_create: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(idempotency_key=idempotency_key, cloud_app_source_create=cloud_app_source_create)
        instance = CloudAppSourceInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, idempotency_key: str, cloud_app_source_create: CloudAppSourceCreate) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = cloud_app_source_create.to_dict()
        
        headers = values.of({
                'Idempotency-Key': idempotency_key,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, idempotency_key: str, cloud_app_source_create: CloudAppSourceCreate) -> CloudAppSourceInstance:
        """
        Asynchronously create the CloudAppSourceInstance

        :param idempotency_key: Client-generated key to ensure idempotent behavior for operations that modify resources. Submitting the same key for identical requests will return the same result without executing the operation twice.  Format Requirements: - MUST use UUIDv7 format for standardization and consistency across all APIs - Keys are guaranteed to be unique for the same account and region combination  Behavior Details: - `Idempotency-Key` is scoped to a Twilio Account and Region (same key can be reused in different accounts/regions) - Server will verify repeated requests with the same key contain identical payloads - Requests with the same key but different payloads are rejected with `409 Conflict` error - Uniqueness is preserved for at least 24 hours (TTL), after which the key may be reused 
        :param cloud_app_source_create: 
        
        :returns: The created CloudAppSourceInstance
        """
        payload, _, _ = await self._create_async(idempotency_key=idempotency_key, cloud_app_source_create=cloud_app_source_create)
        return CloudAppSourceInstance(self._version, payload)

    async def create_with_http_info_async(self, idempotency_key: str, cloud_app_source_create: CloudAppSourceCreate) -> ApiResponse:
        """
        Asynchronously create the CloudAppSourceInstance and return response metadata

        :param idempotency_key: Client-generated key to ensure idempotent behavior for operations that modify resources. Submitting the same key for identical requests will return the same result without executing the operation twice.  Format Requirements: - MUST use UUIDv7 format for standardization and consistency across all APIs - Keys are guaranteed to be unique for the same account and region combination  Behavior Details: - `Idempotency-Key` is scoped to a Twilio Account and Region (same key can be reused in different accounts/regions) - Server will verify repeated requests with the same key contain identical payloads - Requests with the same key but different payloads are rejected with `409 Conflict` error - Uniqueness is preserved for at least 24 hours (TTL), after which the key may be reused 
        :param cloud_app_source_create: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(idempotency_key=idempotency_key, cloud_app_source_create=cloud_app_source_create)
        instance = CloudAppSourceInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[CloudAppSourceInstance]:
        """
        Streams CloudAppSourceInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param List[str] source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            order_by=order_by,
            source_ids=source_ids,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[CloudAppSourceInstance]:
        """
        Asynchronously streams CloudAppSourceInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param List[str] source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            order_by=order_by,
            source_ids=source_ids,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams CloudAppSourceInstance and returns headers from first page

        
        :param str order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param List[str] source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            order_by=order_by,
            source_ids=source_ids,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams CloudAppSourceInstance and returns headers from first page

        
        :param str order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param List[str] source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            order_by=order_by,
            source_ids=source_ids,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[CloudAppSourceInstance]:
        """
        Lists CloudAppSourceInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param List[str] source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            order_by=order_by,
            source_ids=source_ids,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[CloudAppSourceInstance]:
        """
        Asynchronously lists CloudAppSourceInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param List[str] source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            order_by=order_by,
            source_ids=source_ids,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists CloudAppSourceInstance and returns headers from first page

        
        :param str order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param List[str] source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            order_by=order_by,
            source_ids=source_ids,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists CloudAppSourceInstance and returns headers from first page

        
        :param str order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param List[str] source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            order_by=order_by,
            source_ids=source_ids,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      
      order_by: Union[str, object] = values.unset,
      source_ids: Union[List[str], object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> CloudAppSourcePage:
        """
        Retrieve a single page of CloudAppSourceInstance records from the API.
        Request is executed immediately
        
        :param order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of CloudAppSourceInstance
        """
        data = values.of({ 
            'orderBy': order_by,
            'sourceIds': serialize.map(source_ids, lambda e: e),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return CloudAppSourcePage(self._version, response)

    async def page_async(self, 
      
      order_by: Union[str, object] = values.unset,
      source_ids: Union[List[str], object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> CloudAppSourcePage:
        """
        Asynchronously retrieve a single page of CloudAppSourceInstance records from the API.
        Request is executed immediately
        
        :param order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of CloudAppSourceInstance
        """
        data = values.of({ 
            'orderBy': order_by,
            'sourceIds': serialize.map(source_ids, lambda e: e),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return CloudAppSourcePage(self._version, response)

    def page_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with CloudAppSourcePage, status code, and headers
        """
        data = values.of({ 
            'orderBy': order_by,
            'sourceIds': serialize.map(source_ids, lambda e: e),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = CloudAppSourcePage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        source_ids: Union[List[str], object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param order_by: Sort order for results. DESC returns items in descending order (default), ASC returns items in ascending order.
        :param source_ids: Filter by specific source IDs (repeated query params, maximum 25 IDs). When provided, pagination is disabled and results are returned in the order of IDs provided. If pageSize is specified, it must be at least equal to the number of IDs. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with CloudAppSourcePage, status code, and headers
        """
        data = values.of({ 
            'orderBy': order_by,
            'sourceIds': serialize.map(source_ids, lambda e: e),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = CloudAppSourcePage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> CloudAppSourcePage:
        """
        Retrieve a specific page of CloudAppSourceInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of CloudAppSourceInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return CloudAppSourcePage(self._version, response)

    async def get_page_async(self, target_url: str) -> CloudAppSourcePage:
        """
        Asynchronously retrieve a specific page of CloudAppSourceInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of CloudAppSourceInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return CloudAppSourcePage(self._version, response)





    def get(self) -> CloudAppSourceContext:
        """
        Constructs a CloudAppSourceContext
        
        """
        return CloudAppSourceContext(self._version, source_id=self._solution['source_id'])

    def __call__(self) -> CloudAppSourceContext:
        """
        Constructs a CloudAppSourceContext
        
        """
        return CloudAppSourceContext(self._version, source_id=self._solution['source_id'])

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.DataIngress.Users_manisingh_codehq_open_api_spec_data_ingress_v1_openapi_yaml.CloudAppSourceList>'


