r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Audiences API
    APIs for audience management, segmentation, and targeting.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination





class ProfileInstance(InstanceResource):



    """
    :ivar profile_id: The profile ID
    :ivar variables: Results of each CEL variable expression, keyed by the `displayName`. If a `displayName` was provided for the variable, it is used as the key. Otherwise, a key is auto-generated from the expression (e.g., `profile.address.email` becomes `email`, `profile.trait.Contact.age` becomes `Contact_age`). Values depend on the expression type (string, number, boolean, etc.). 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], snapshot_id: str):
        super().__init__(version)

        
        self.profile_id: Optional[str] = payload.get("profileId")
        self.variables: Optional[Dict[str, object]] = payload.get("variables")

        
        
        # Only set _solution if path params are provided (not None)
        if snapshot_id is not None:
            self._solution = { 
                "snapshot_id": snapshot_id,
            }
        
        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.ProfileInstance {}>'.format(context)




class ProfilePage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> ProfileInstance:
        """
        Build an instance of ProfileInstance

        :param payload: Payload response from the API
        """
        
        return ProfileInstance(self._version, payload, snapshot_id=self._solution["snapshot_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.ProfilePage>"






class ProfileList(ListResource):
    
    def __init__(self, version: Version, snapshot_id: str):
        """
        Initialize the ProfileList

        :param version: Version that contains the resource
        :param snapshot_id: The snapshotId identifier
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'snapshot_id': snapshot_id,  }
        self._uri = '/Snapshots/{snapshot_id}/Profiles'.format(**self._solution)
        
        
    
    def stream(self, 
        partition: Union[int, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[ProfileInstance]:
        """
        Streams ProfileInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param int partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            partition=partition,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        partition: Union[int, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[ProfileInstance]:
        """
        Asynchronously streams ProfileInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param int partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            partition=partition,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        partition: Union[int, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams ProfileInstance and returns headers from first page

        
        :param int partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            partition=partition,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        partition: Union[int, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams ProfileInstance and returns headers from first page

        
        :param int partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            partition=partition,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        partition: Union[int, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ProfileInstance]:
        """
        Lists ProfileInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param int partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            partition=partition,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        partition: Union[int, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ProfileInstance]:
        """
        Asynchronously lists ProfileInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param int partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            partition=partition,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        partition: Union[int, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ProfileInstance and returns headers from first page

        
        :param int partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            partition=partition,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        partition: Union[int, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ProfileInstance and returns headers from first page

        
        :param int partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            partition=partition,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      partition: Union[int, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> ProfilePage:
        """
        Retrieve a single page of ProfileInstance records from the API.
        Request is executed immediately
        
        :param partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param page_size: Maximum number of items to return in a single response. If not specified, the API will use a default page size. 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :returns: Page of ProfileInstance
        """
        data = values.of({ 
            'partition': partition,
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ProfilePage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      partition: Union[int, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> ProfilePage:
        """
        Asynchronously retrieve a single page of ProfileInstance records from the API.
        Request is executed immediately
        
        :param partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param page_size: Maximum number of items to return in a single response. If not specified, the API will use a default page size. 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :returns: Page of ProfileInstance
        """
        data = values.of({ 
            'partition': partition,
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ProfilePage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        partition: Union[int, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ProfilePage, status code, and headers
        """
        data = values.of({ 
            'partition': partition,
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ProfilePage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        partition: Union[int, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param partition: Optional zero-based partition number to retrieve profiles from. When omitted, returns profiles from all partitions aggregated. When specified, returns only profiles from that partition (better performance for large snapshots). Must be less than the snapshot's partitionCount. 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ProfilePage, status code, and headers
        """
        data = values.of({ 
            'partition': partition,
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ProfilePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ProfilePage:
        """
        Retrieve a specific page of ProfileInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ProfileInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ProfilePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ProfilePage:
        """
        Asynchronously retrieve a specific page of ProfileInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ProfileInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ProfilePage(self._version, response, solution=self._solution)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.ProfileList>'





