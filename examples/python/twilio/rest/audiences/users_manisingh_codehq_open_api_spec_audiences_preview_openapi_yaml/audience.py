r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Audiences API
    APIs for audience management, segmentation, and targeting.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination





class AudienceInstance(InstanceResource):



    class OperationStatus(object):
        PENDING = "PENDING"
        RUNNING = "RUNNING"
        COMPLETED = "COMPLETED"
        FAILED = "FAILED"
        CANCELLED = "CANCELLED"

    class StoreType(object):
        TWILIO = "TWILIO"

    """
    :ivar id: Unique identifier for the audience following TTID format. Format: aud_audience_{uuidv7-encoded} 
    :ivar display_name: Human-readable name for the audience
    :ivar description: Optional description of the audience
    :ivar store_id: Unique identifier for the profile store
    :ivar store_type: 
    :ivar criteria: Boolean filter expression in the Audiences CEL Specification that determines which profiles belong to this audience. Profiles matching this criteria will be included in the audience.  See the **Audiences CEL Specification** section for full syntax reference, supported operators, functions, and examples. 
    :ivar labels: Labels for organizing and categorizing audiences as key-value pairs. - Maximum size of a label key is 128 characters - Maximum size of a label value is 256 characters - Maximum of 10 key-value pairs - Tag values cannot contain #, :, or ; characters 
    :ivar created_at: Timestamp when the audience was created
    :ivar updated_at: Timestamp when the audience was last updated
    :ivar operation_id: Unique identifier for the operation following TTID format. Format: aud_operation_{uuidv7-encoded} 
    :ivar status: 
    :ivar status_url: URL to poll for operation status
    :ivar completed_at: Timestamp when the operation completed (present for terminal states)
    :ivar payload: 
    :ivar result_url: URL of the resulting resource. Present when status is COMPLETED and the operation produced or modified a resource.  Not present for deletion operations (DELETE, SNAPSHOT_DELETE) since the resource no longer exists after completion. 
    :ivar result: 
    :ivar error: 
    """

    def __init__(self, version: Version, payload:ResponseResource, audience_id: Optional[str] = None):
        super().__init__(version)

        
        self.id: Optional[str] = payload.get("id")
        self.display_name: Optional[str] = payload.get("displayName")
        self.description: Optional[str] = payload.get("description")
        self.store_id: Optional[str] = payload.get("storeId")
        self.store_type: Optional["AudienceInstance.str"] = payload.get("storeType")
        self.criteria: Optional[str] = payload.get("criteria")
        self.labels: Optional[Dict[str, str]] = payload.get("labels")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("updatedAt"))
        self.operation_id: Optional[str] = payload.get("operationId")
        self.status: Optional["AudienceInstance.OperationStatus"] = payload.get("status")
        self.status_url: Optional[str] = payload.get("statusUrl")
        self.completed_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("completedAt"))
        self.payload: Optional[OperationPayload] = payload.get("payload")
        self.result_url: Optional[str] = payload.get("resultUrl")
        self.result: Optional[OperationResult] = payload.get("result")
        self.error: Optional[OperationError] = payload.get("error")

        
        
        # Only set _solution if path params are provided (not None)
        if audience_id is not None:
            self._solution = { 
                "audience_id": audience_id,
            }
        
        
        self._context: Optional[AudienceContext] = None

    @property
    def _proxy(self) -> "AudienceContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: AudienceContext for this AudienceInstance
        """
        if self._context is None:
            self._context = AudienceContext(self._version, audience_id=self._solution['audience_id'],)
        return self._context
    
    
    def delete(self, if_match: Union[str, object]=values.unset) -> bool:
        """
        Deletes the AudienceInstance
        
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete(if_match=if_match, )
    async def delete_async(self, if_match: Union[str, object]=values.unset) -> bool:
        """
        Asynchronous coroutine that deletes the AudienceInstance
        
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async(if_match=if_match, )

    def delete_with_http_info(self, if_match: Union[str, object]=values.unset) -> ApiResponse:
        """
        Deletes the AudienceInstance with HTTP info
        
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info(if_match=if_match, )

    async def delete_with_http_info_async(self, if_match: Union[str, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the AudienceInstance with HTTP info
        
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async(if_match=if_match, )
    
    
    
    def update(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> "AudienceInstance":
        """
        Update the AudienceInstance
        
        :param update_audience_request: 
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: The updated AudienceInstance
        """
        return self._proxy.update(update_audience_request=update_audience_request, if_match=if_match, )

    async def update_async(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> "AudienceInstance":
        """
        Asynchronous coroutine to update the AudienceInstance
        
        :param update_audience_request: 
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: The updated AudienceInstance
        """
        return await self._proxy.update_async(update_audience_request=update_audience_request, if_match=if_match, )

    def update_with_http_info(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> ApiResponse:
        """
        Update the AudienceInstance with HTTP info
        
        :param update_audience_request: 
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(update_audience_request=update_audience_request, if_match=if_match, )

    async def update_with_http_info_async(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the AudienceInstance with HTTP info
        
        :param update_audience_request: 
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(update_audience_request=update_audience_request, if_match=if_match, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.AudienceInstance {}>'.format(context)

class AudienceContext(InstanceContext):


    def __init__(self, version: Version, audience_id: str):
        """
        Initialize the AudienceContext

        :param version: Version that contains the resource
        :param audience_id: The audienceId identifier
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'audience_id': audience_id,
        }
        self._uri = '/Audiences/{audience_id}'.format(**self._solution)
        
    
    
    def _delete(self, if_match: Union[str, object]=values.unset) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        headers = values.of({'If-Match': if_match, })
        

        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self, if_match: Union[str, object]=values.unset) -> bool:
        """
        Deletes the AudienceInstance

        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 
        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete(if_match=if_match)
        return success

    def delete_with_http_info(self, if_match: Union[str, object]=values.unset) -> ApiResponse:
        """
        Deletes the AudienceInstance and return response metadata

        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 
        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete(if_match=if_match)
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self, if_match: Union[str, object]=values.unset) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        headers = values.of({'If-Match': if_match, })
        
        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self, if_match: Union[str, object]=values.unset) -> bool:
        """
        Asynchronous coroutine that deletes the AudienceInstance

        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 
        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async(if_match=if_match)
        return success

    async def delete_with_http_info_async(self, if_match: Union[str, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the AudienceInstance and return response metadata

        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 
        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async(if_match=if_match)
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    
    def _update(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_audience_request.to_dict()
        
        headers = values.of({})
        
        
        if not (if_match is values.unset or (isinstance(if_match, str) and not if_match)):
            headers['If-Match'] = if_match
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='PATCH', uri=self._uri, data=data, headers=headers)

    def update(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> AudienceInstance:
        """
        Update the AudienceInstance
        
        :param update_audience_request: 
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: The updated AudienceInstance
        """
        payload, _, _ = self._update(update_audience_request=update_audience_request, if_match=if_match)
        return AudienceInstance(
            self._version,
            payload,
            audience_id=self._solution['audience_id']
        )

    def update_with_http_info(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> ApiResponse:
        """
        Update the AudienceInstance and return response metadata
        
        :param update_audience_request: 
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(update_audience_request=update_audience_request, if_match=if_match)
        instance = AudienceInstance(
            self._version,
            payload,
            audience_id=self._solution['audience_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_audience_request.to_dict()
        
        headers = values.of({})
        
        
        if not (if_match is values.unset or (isinstance(if_match, str) and not if_match)):
            headers['If-Match'] = if_match
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='PATCH', uri=self._uri, data=data, headers=headers)

    async def update_async(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> AudienceInstance:
        """
        Asynchronous coroutine to update the AudienceInstance
        
        :param update_audience_request: 
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: The updated AudienceInstance
        """
        payload, _, _ = await self._update_async(update_audience_request=update_audience_request, if_match=if_match)
        return AudienceInstance(
            self._version,
            payload,
            audience_id=self._solution['audience_id']
        )

    async def update_with_http_info_async(self, update_audience_request: UpdateAudienceRequest, if_match: Union[str, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the AudienceInstance and return response metadata
        
        :param update_audience_request: 
        :param if_match: Used for optimistic concurrency control on mutating requests (PATCH, PUT, DELETE). The value should be the ETag returned from a previous GET request for this resource.  If the current ETag does not match, the server returns 412 Precondition Failed, indicating the resource has been modified since it was last retrieved.  When omitted, the write proceeds without ETag validation. 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(update_audience_request=update_audience_request, if_match=if_match)
        instance = AudienceInstance(
            self._version,
            payload,
            audience_id=self._solution['audience_id']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.AudienceContext {}>'.format(context)





class AudiencePage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> AudienceInstance:
        """
        Build an instance of AudienceInstance

        :param payload: Payload response from the API
        """
        
        return AudienceInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.AudiencePage>"








class AudiencePage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> AudienceInstance:
        """
        Build an instance of AudienceInstance

        :param payload: Payload response from the API
        """
        
        return AudienceInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.AudiencePage>"






class AudienceList(ListResource):
    
    class CreateAudienceRequest(object):
        """
            :ivar display_name: Human-readable name for the audience
            :ivar description: Optional description of the audience
            :ivar store_type: 
            :ivar store_id: Unique identifier for the profile store
            :ivar criteria: Boolean filter expression in the Audiences CEL Specification that determines which profiles belong to this audience. Profiles matching this criteria will be included in the audience.  See the **Audiences CEL Specification** section for full syntax reference, supported operators, functions, and examples. 
            :ivar labels: Labels for organizing and categorizing audiences as key-value pairs. - Maximum size of a label key is 128 characters - Maximum size of a label value is 256 characters - Maximum of 10 key-value pairs - Tag values cannot contain #, :, or ; characters 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.display_name: Optional[str] = payload.get("displayName")
            self.description: Optional[str] = payload.get("description")
            self.store_type: Optional["AudienceInstance.StoreType"] = payload.get("storeType")
            self.store_id: Optional[str] = payload.get("storeId")
            self.criteria: Optional[str] = payload.get("criteria")
            self.labels: Optional[Dict[str, str]] = payload.get("labels")

        def to_dict(self):
            return {
                
                    "displayName": self.display_name,
                    "description": self.description,
                    "storeType": self.store_type,
                    "storeId": self.store_id,
                    "criteria": self.criteria,
                    "labels": self.labels,
            }

    class OperationError(object):
        """
            :ivar type: A URI reference that identifies the problem type
            :ivar title: A short, human-readable summary of the problem type
            :ivar status: The HTTP status code for this error
            :ivar detail: A human-readable explanation specific to this occurrence
            :ivar instance: A URI reference identifying the specific occurrence
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[str] = payload.get("type")
            self.title: Optional[str] = payload.get("title")
            self.status: Optional[int] = payload.get("status")
            self.detail: Optional[str] = payload.get("detail")
            self.instance: Optional[str] = payload.get("instance")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "title": self.title,
                    "status": self.status,
                    "detail": self.detail,
                    "instance": self.instance,
            }

    class OperationPayload(object):
        """
            :ivar operation_type: The type of operation
            :ivar audience_id: The audience this operation is associated with. Present for AUDIENCE-sourced snapshots and audience operations.
            :ivar store_id: The store this operation is associated with. Present for STORE-sourced snapshot operations.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.operation_type: Optional[str] = payload.get("operationType")
            self.audience_id: Optional[str] = payload.get("audienceId")
            self.store_id: Optional[str] = payload.get("storeId")

        def to_dict(self):
            return {
                
                    "operationType": self.operation_type,
                    "audienceId": self.audience_id,
                    "storeId": self.store_id,
            }

    class OperationResult(object):
        """
            :ivar snapshot_id: The snapshot ID. Present for SNAPSHOT and SNAPSHOT_DELETE operations.
            :ivar profile_count: Total number of profiles in the snapshot. Present for SNAPSHOT operations.
            :ivar partition_count: Number of partitions in the snapshot, if partitioning was requested. Present for SNAPSHOT operations.
            :ivar audience_id: The audience that was updated or deleted. Present for UPDATE and DELETE operations.
            :ivar updated_at: Timestamp of the update. Present for UPDATE operations.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.snapshot_id: Optional[str] = payload.get("snapshotId")
            self.profile_count: Optional[int] = payload.get("profileCount")
            self.partition_count: Optional[int] = payload.get("partitionCount")
            self.audience_id: Optional[str] = payload.get("audienceId")
            self.updated_at: Optional[datetime] = payload.get("updatedAt")

        def to_dict(self):
            return {
                
                    "snapshotId": self.snapshot_id,
                    "profileCount": self.profile_count,
                    "partitionCount": self.partition_count,
                    "audienceId": self.audience_id,
                    "updatedAt": self.updated_at,
            }

    class UpdateAudienceRequest(object):
        """
            :ivar display_name: Human-readable name for the audience
            :ivar description: Optional description of the audience
            :ivar criteria: Boolean filter expression in CEL that determines which profiles belong to this audience. Updating criteria will affect which profiles match the audience.  See the **Audiences CEL Specification** section for full syntax reference. 
            :ivar labels: Labels to add, update, or remove using merge patch semantics. - To add or update a label: include the key with a string value - To remove a label: include the key with a null value - Labels not included in the request are left unchanged - Label values cannot contain #, :, or ; characters 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.display_name: Optional[str] = payload.get("displayName")
            self.description: Optional[str] = payload.get("description")
            self.criteria: Optional[str] = payload.get("criteria")
            self.labels: Optional[Dict[str, Optional[str]]] = payload.get("labels")

        def to_dict(self):
            return {
                
                    "displayName": self.display_name,
                    "description": self.description,
                    "criteria": self.criteria,
                    "labels": self.labels,
            }


    def __init__(self, version: Version):
        """
        Initialize the AudienceList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/Audiences'
        
        
    
    
    def list(self, 
        if_none_match: Union[str, object] = values.unset,
    ) -> List[AudienceInstance]:
        """
        Lists AudienceInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'If-None-Match': if_none_match,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [AudienceInstance(self._version, item) for item in payload["audiences"]]
        

    async def list_async(self, 
        if_none_match: Union[str, object] = values.unset,
    ) -> List[AudienceInstance]:
        """
        Asynchronously lists AudienceInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'If-None-Match': if_none_match,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [AudienceInstance(self._version, item) for item in payload["audiences"]]
        


    def list_with_http_info(self, 
        if_none_match: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AudienceInstance and returns headers from first page

        
        :param str if_none_match: Used for conditional requests. If the ETag matches, the server returns 304 Not Modified, reducing bandwidth and improving performance. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            if_none_match=if_none_match,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        if_none_match: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AudienceInstance and returns headers from first page

        
        :param str if_none_match: Used for conditional requests. If the ETag matches, the server returns 304 Not Modified, reducing bandwidth and improving performance. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            if_none_match=if_none_match,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      if_none_match: Union[str, object] = values.unset,
    ) -> AudiencePage:
        """
        Retrieve a single page of AudienceInstance records from the API.
        Request is executed immediately
        
        :param if_none_match: Used for conditional requests. If the ETag matches, the server returns 304 Not Modified, reducing bandwidth and improving performance. 
        :returns: Page of AudienceInstance
        """
        data = values.of({ 
            'If-None-Match': if_none_match,
        })

        headers = values.of({
        'If-None-Match': if_none_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return AudiencePage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
      if_none_match: Union[str, object] = values.unset,
    ) -> AudiencePage:
        """
        Asynchronously retrieve a single page of AudienceInstance records from the API.
        Request is executed immediately
        
        :param if_none_match: Used for conditional requests. If the ETag matches, the server returns 304 Not Modified, reducing bandwidth and improving performance. 
        :returns: Page of AudienceInstance
        """
        data = values.of({ 
            'If-None-Match': if_none_match,
        })

        headers = values.of({
        'If-None-Match': if_none_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return AudiencePage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        if_none_match: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param if_none_match: Used for conditional requests. If the ETag matches, the server returns 304 Not Modified, reducing bandwidth and improving performance. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AudiencePage, status code, and headers
        """
        data = values.of({ 
            'If-None-Match': if_none_match,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'If-None-Match': if_none_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = AudiencePage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        if_none_match: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param if_none_match: Used for conditional requests. If the ETag matches, the server returns 304 Not Modified, reducing bandwidth and improving performance. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AudiencePage, status code, and headers
        """
        data = values.of({ 
            'If-None-Match': if_none_match,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'If-None-Match': if_none_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = AudiencePage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AudiencePage:
        """
        Retrieve a specific page of AudienceInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AudienceInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return AudiencePage(self._version, response)

    async def get_page_async(self, target_url: str) -> AudiencePage:
        """
        Asynchronously retrieve a specific page of AudienceInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AudienceInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return AudiencePage(self._version, response)

    
    
    def _create(self, create_audience_request: CreateAudienceRequest, idempotency_key: Union[str, object]=values.unset) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_audience_request.to_dict()
        
        headers = values.of({
                'Idempotency-Key': idempotency_key,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, create_audience_request: CreateAudienceRequest, idempotency_key: Union[str, object]=values.unset) -> AudienceInstance:
        """
        Create the AudienceInstance

        :param create_audience_request: 
        :param idempotency_key: Client-provided idempotency key for safe request retries.  When provided, the server ensures that duplicate requests with the same key produce the same result without executing the operation multiple times.  Requirements: - Must be a non-empty string (max 128 characters) - Keys are retained for at least 24 hours - Scope: Account + Region combination - If a different payload is sent with the same key, returns 422 Unprocessable Content  If a request is replayed with the same idempotency key, the response will include the header `Idempotent-Replayed: true`. 
        
        :returns: The created AudienceInstance
        """
        payload, _, _ = self._create(create_audience_request=create_audience_request, idempotency_key=idempotency_key)
        return AudienceInstance(self._version, payload)

    def create_with_http_info(self, create_audience_request: CreateAudienceRequest, idempotency_key: Union[str, object]=values.unset) -> ApiResponse:
        """
        Create the AudienceInstance and return response metadata

        :param create_audience_request: 
        :param idempotency_key: Client-provided idempotency key for safe request retries.  When provided, the server ensures that duplicate requests with the same key produce the same result without executing the operation multiple times.  Requirements: - Must be a non-empty string (max 128 characters) - Keys are retained for at least 24 hours - Scope: Account + Region combination - If a different payload is sent with the same key, returns 422 Unprocessable Content  If a request is replayed with the same idempotency key, the response will include the header `Idempotent-Replayed: true`. 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(create_audience_request=create_audience_request, idempotency_key=idempotency_key)
        instance = AudienceInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, create_audience_request: CreateAudienceRequest, idempotency_key: Union[str, object]=values.unset) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_audience_request.to_dict()
        
        headers = values.of({
                'Idempotency-Key': idempotency_key,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, create_audience_request: CreateAudienceRequest, idempotency_key: Union[str, object]=values.unset) -> AudienceInstance:
        """
        Asynchronously create the AudienceInstance

        :param create_audience_request: 
        :param idempotency_key: Client-provided idempotency key for safe request retries.  When provided, the server ensures that duplicate requests with the same key produce the same result without executing the operation multiple times.  Requirements: - Must be a non-empty string (max 128 characters) - Keys are retained for at least 24 hours - Scope: Account + Region combination - If a different payload is sent with the same key, returns 422 Unprocessable Content  If a request is replayed with the same idempotency key, the response will include the header `Idempotent-Replayed: true`. 
        
        :returns: The created AudienceInstance
        """
        payload, _, _ = await self._create_async(create_audience_request=create_audience_request, idempotency_key=idempotency_key)
        return AudienceInstance(self._version, payload)

    async def create_with_http_info_async(self, create_audience_request: CreateAudienceRequest, idempotency_key: Union[str, object]=values.unset) -> ApiResponse:
        """
        Asynchronously create the AudienceInstance and return response metadata

        :param create_audience_request: 
        :param idempotency_key: Client-provided idempotency key for safe request retries.  When provided, the server ensures that duplicate requests with the same key produce the same result without executing the operation multiple times.  Requirements: - Must be a non-empty string (max 128 characters) - Keys are retained for at least 24 hours - Scope: Account + Region combination - If a different payload is sent with the same key, returns 422 Unprocessable Content  If a request is replayed with the same idempotency key, the response will include the header `Idempotent-Replayed: true`. 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(create_audience_request=create_audience_request, idempotency_key=idempotency_key)
        instance = AudienceInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[AudienceInstance]:
        """
        Streams AudienceInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param StoreType store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param str store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            labels=labels,
            store_type=store_type,
            store_id=store_id,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[AudienceInstance]:
        """
        Asynchronously streams AudienceInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param StoreType store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param str store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            labels=labels,
            store_type=store_type,
            store_id=store_id,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams AudienceInstance and returns headers from first page

        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param StoreType store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param str store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            labels=labels,
            store_type=store_type,
            store_id=store_id,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams AudienceInstance and returns headers from first page

        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param StoreType store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param str store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            labels=labels,
            store_type=store_type,
            store_id=store_id,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AudienceInstance]:
        """
        Lists AudienceInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param StoreType store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param str store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            labels=labels,
            store_type=store_type,
            store_id=store_id,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AudienceInstance]:
        """
        Asynchronously lists AudienceInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param StoreType store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param str store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            labels=labels,
            store_type=store_type,
            store_id=store_id,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AudienceInstance and returns headers from first page

        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param StoreType store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param str store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            labels=labels,
            store_type=store_type,
            store_id=store_id,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AudienceInstance and returns headers from first page

        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param StoreType store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param str store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            labels=labels,
            store_type=store_type,
            store_id=store_id,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      labels: Union[str, object] = values.unset,
      store_type: Union[StoreType, object] = values.unset,
      store_id: Union[str, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> AudiencePage:
        """
        Retrieve a single page of AudienceInstance records from the API.
        Request is executed immediately
        
        :param labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param page_size: Maximum number of items to return in a single response. If not specified, the API will use a default page size. 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :returns: Page of AudienceInstance
        """
        data = values.of({ 
            'labels': labels,
            'storeType': store_type,
            'storeId': store_id,
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return AudiencePage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
      labels: Union[str, object] = values.unset,
      store_type: Union[StoreType, object] = values.unset,
      store_id: Union[str, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> AudiencePage:
        """
        Asynchronously retrieve a single page of AudienceInstance records from the API.
        Request is executed immediately
        
        :param labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param page_size: Maximum number of items to return in a single response. If not specified, the API will use a default page size. 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :returns: Page of AudienceInstance
        """
        data = values.of({ 
            'labels': labels,
            'storeType': store_type,
            'storeId': store_id,
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return AudiencePage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AudiencePage, status code, and headers
        """
        data = values.of({ 
            'labels': labels,
            'storeType': store_type,
            'storeId': store_id,
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = AudiencePage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        labels: Union[str, object] = values.unset,
        store_type: Union[StoreType, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param store_type: Filter by profile store type. **Must be used together with `storeId`.** 
        :param store_id: Filter by profile store ID. **Must be used together with `storeType`.** 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AudiencePage, status code, and headers
        """
        data = values.of({ 
            'labels': labels,
            'storeType': store_type,
            'storeId': store_id,
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = AudiencePage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AudiencePage:
        """
        Retrieve a specific page of AudienceInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AudienceInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return AudiencePage(self._version, response)

    async def get_page_async(self, target_url: str) -> AudiencePage:
        """
        Asynchronously retrieve a specific page of AudienceInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AudienceInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return AudiencePage(self._version, response)



    def get(self, audience_id: str) -> AudienceContext:
        """
        Constructs a AudienceContext
        
        :param audience_id: The audienceId identifier
        """
        return AudienceContext(self._version, audience_id=audience_id)

    def __call__(self, audience_id: str) -> AudienceContext:
        """
        Constructs a AudienceContext
        
        :param audience_id: The audienceId identifier
        """
        return AudienceContext(self._version, audience_id=audience_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.AudienceList>'





