r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Audiences API
    APIs for audience management, segmentation, and targeting.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination





class SnapshotInstance(InstanceResource):



    class OperationStatus(object):
        PENDING = "PENDING"
        RUNNING = "RUNNING"
        COMPLETED = "COMPLETED"
        FAILED = "FAILED"
        CANCELLED = "CANCELLED"

    class StoreType(object):
        TWILIO = "TWILIO"

    """
    :ivar snapshot: 
    :ivar operation_id: Unique identifier for the operation following TTID format. Format: aud_operation_{uuidv7-encoded} 
    :ivar status: 
    :ivar status_url: URL to poll for operation status
    :ivar created_at: Timestamp when the operation was created
    :ivar completed_at: Timestamp when the operation completed (present for terminal states)
    :ivar payload: 
    :ivar result_url: URL of the resulting resource. Present when status is COMPLETED and the operation produced or modified a resource.  Not present for deletion operations (DELETE, SNAPSHOT_DELETE) since the resource no longer exists after completion. 
    :ivar result: 
    :ivar error: 
    :ivar snapshot_id: Unique identifier for the snapshot following TTID format. Format: aud_snapshot_{uuidv7-encoded} 
    :ivar source: 
    :ivar profile_count: Total number of profiles included in the snapshot
    :ivar partition_count: Number of partitions the snapshot is split into. Defaults to 1 (single partition). Use the `partition` query parameter on ListSnapshotProfiles to retrieve profiles from a specific partition. 
    :ivar variables: Variable expressions included for each profile in the snapshot.
    :ivar labels: Labels for organizing and categorizing snapshots as key-value pairs. - Maximum size of a label key is 128 characters - Maximum size of a label value is 256 characters - Maximum of 10 key-value pairs 
    :ivar profiles_expires_at: When the snapshot's profile data expires. After this time, profile data is permanently deleted and ListSnapshotProfiles will return a 404.  The snapshot itself remains valid — metadata such as IDs, counts, variables, and timestamps can still be retrieved via GetSnapshot.  Maximum 30 days from creation. Defaults to 7 days. 
    """

    def __init__(self, version: Version, payload:ResponseResource, snapshot_id: Optional[str] = None):
        super().__init__(version)

        
        self.snapshot: Optional[Snapshot] = payload.get("snapshot")
        self.operation_id: Optional[str] = payload.get("operationId")
        self.status: Optional["AudienceInstance.str"] = payload.get("status")
        self.status_url: Optional[str] = payload.get("statusUrl")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.completed_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("completedAt"))
        self.payload: Optional[str] = payload.get("payload")
        self.result_url: Optional[str] = payload.get("resultUrl")
        self.result: Optional[str] = payload.get("result")
        self.error: Optional[str] = payload.get("error")
        self.snapshot_id: Optional[str] = payload.get("snapshotId")
        self.source: Optional[SnapshotSource] = payload.get("source")
        self.profile_count: Optional[int] = deserialize.integer(payload.get("profileCount"))
        self.partition_count: Optional[int] = deserialize.integer(payload.get("partitionCount"))
        self.variables: Optional[List[SnapshotVariable]] = payload.get("variables")
        self.labels: Optional[Dict[str, str]] = payload.get("labels")
        self.profiles_expires_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("profilesExpiresAt"))

        
        
        # Only set _solution if path params are provided (not None)
        if snapshot_id is not None:
            self._solution = { 
                "snapshot_id": snapshot_id,
            }
        
        
        self._context: Optional[SnapshotContext] = None

    @property
    def _proxy(self) -> "SnapshotContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: SnapshotContext for this SnapshotInstance
        """
        if self._context is None:
            self._context = SnapshotContext(self._version, snapshot_id=self._solution['snapshot_id'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the SnapshotInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the SnapshotInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the SnapshotInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the SnapshotInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.SnapshotInstance {}>'.format(context)

class SnapshotContext(InstanceContext):


    def __init__(self, version: Version, snapshot_id: str):
        """
        Initialize the SnapshotContext

        :param version: Version that contains the resource
        :param snapshot_id: The snapshotId identifier
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'snapshot_id': snapshot_id,
        }
        self._uri = '/Snapshots/{snapshot_id}'.format(**self._solution)
        
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the SnapshotInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the SnapshotInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the SnapshotInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the SnapshotInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.SnapshotContext {}>'.format(context)





class SnapshotPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> SnapshotInstance:
        """
        Build an instance of SnapshotInstance

        :param payload: Payload response from the API
        """
        
        return SnapshotInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.SnapshotPage>"






class SnapshotPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> SnapshotInstance:
        """
        Build an instance of SnapshotInstance

        :param payload: Payload response from the API
        """
        
        return SnapshotInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.SnapshotPage>"






class SnapshotList(ListResource):
    
    class CreateSnapshotRequest(object):
        """
            :ivar source: 
            :ivar variables: CEL projection expressions specifying what data to include for each profile in the snapshot. Each variable has a required `expression` (an CEL expression) and an optional `displayName` for human-readable labeling.  Results are included per-profile in the snapshot response keyed by the `displayName` or an auto-generated key.  See the **Audiences CEL Specification** section for full syntax reference and variable examples. 
            :ivar partition_count_override: Optional override that can be used for concurrent processing when the number of workers is fixed and known at Snapshot creation time. If provided, the snapshot will be split into exactly this many partitions (up to 500).  If not provided, the system automatically determines the optimal number of partitions based on the snapshot size.  Partitions are roughly equal in size but may contain different numbers of profiles depending on individual profile size. 
            :ivar profiles_expires_at: When the snapshot's profile data expires. After this time, profile data is permanently deleted and ListSnapshotProfiles will return a 404.  The snapshot itself remains valid — metadata such as IDs, counts, variables, and timestamps can still be retrieved via GetSnapshot.  Must be within 30 days of creation. Defaults to 7 days if not specified. 
            :ivar profile_limit: Maximum number of profiles to include in the snapshot.  If the source has more matching profiles than this limit, an arbitrary subset of matching profiles will be included. The selection is not guaranteed to be deterministic or ordered. 
            :ivar labels: Labels for organizing and categorizing snapshots as key-value pairs. - Maximum size of a label key is 128 characters - Maximum size of a label value is 256 characters - Maximum of 10 key-value pairs 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.source: Optional[SnapshotList.CreateSnapshotRequestSource] = payload.get("source")
            self.variables: Optional[List[SnapshotList.SnapshotVariable]] = payload.get("variables")
            self.partition_count_override: Optional[int] = payload.get("partitionCountOverride")
            self.profiles_expires_at: Optional[datetime] = payload.get("profilesExpiresAt")
            self.profile_limit: Optional[int] = payload.get("profileLimit")
            self.labels: Optional[Dict[str, str]] = payload.get("labels")

        def to_dict(self):
            return {
                
                    "source": self.source.to_dict() if self.source is not None else None ,
                    "variables": [variables.to_dict() for variables in self.variables] if self.variables is not None else None,
                    "partitionCountOverride": self.partition_count_override,
                    "profilesExpiresAt": self.profiles_expires_at,
                    "profileLimit": self.profile_limit,
                    "labels": self.labels,
            }

    class CreateSnapshotRequestSource(object):
        """
            :ivar audience_id: Unique identifier for the audience following TTID format. Format: aud_audience_{uuidv7-encoded} 
            :ivar store_type: 
            :ivar store_id: Unique identifier for the profile store
            :ivar criteria: Boolean filter expression in the Audiences CEL Specification that determines which profiles belong to this audience. Profiles matching this criteria will be included in the audience.  See the **Audiences CEL Specification** section for full syntax reference, supported operators, functions, and examples. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.audience_id: Optional[str] = payload.get("audienceId")
            self.store_type: Optional["SnapshotInstance.StoreType"] = payload.get("storeType")
            self.store_id: Optional[str] = payload.get("storeId")
            self.criteria: Optional[str] = payload.get("criteria")

        def to_dict(self):
            return {
                
                    "audienceId": self.audience_id,
                    "storeType": self.store_type,
                    "storeId": self.store_id,
                    "criteria": self.criteria,
            }

    class Snapshot(object):
        """
            :ivar snapshot_id: Unique identifier for the snapshot following TTID format. Format: aud_snapshot_{uuidv7-encoded} 
            :ivar source: 
            :ivar profile_count: Total number of profiles included in the snapshot
            :ivar partition_count: Number of partitions the snapshot is split into. Defaults to 1 (single partition). Use the `partition` query parameter on ListSnapshotProfiles to retrieve profiles from a specific partition. 
            :ivar variables: Variable expressions included for each profile in the snapshot.
            :ivar labels: Labels for organizing and categorizing snapshots as key-value pairs. - Maximum size of a label key is 128 characters - Maximum size of a label value is 256 characters - Maximum of 10 key-value pairs 
            :ivar created_at: Timestamp when the snapshot was created
            :ivar completed_at: Timestamp when the snapshot completed
            :ivar profiles_expires_at: When the snapshot's profile data expires. After this time, profile data is permanently deleted and ListSnapshotProfiles will return a 404.  The snapshot itself remains valid — metadata such as IDs, counts, variables, and timestamps can still be retrieved via GetSnapshot.  Maximum 30 days from creation. Defaults to 7 days. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.snapshot_id: Optional[str] = payload.get("snapshotId")
            self.source: Optional[SnapshotSource] = payload.get("source")
            self.profile_count: Optional[int] = deserialize.integer(payload.get("profileCount"))
            self.partition_count: Optional[int] = deserialize.integer(payload.get("partitionCount"))
            self.variables: Optional[List[SnapshotVariable]] = payload.get("variables")
            self.labels: Optional[Dict[str, str]] = payload.get("labels")
            self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
            self.completed_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("completedAt"))
            self.profiles_expires_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("profilesExpiresAt"))

        def to_dict(self):
            return {
                
                    "snapshotId": self.snapshot_id,
                    "source": self.source.to_dict() if self.source is not None else None ,
                    "profileCount": self.profile_count,
                    "partitionCount": self.partition_count,
                    "variables": [variables.to_dict() for variables in self.variables] if self.variables is not None else None,
                    "labels": self.labels,
                    "createdAt": self.created_at,
                    "completedAt": self.completed_at,
                    "profilesExpiresAt": self.profiles_expires_at,
            }

    class SnapshotSource(object):
        """
            :ivar audience_id: Unique identifier for the audience following TTID format. Format: aud_audience_{uuidv7-encoded} 
            :ivar store_type: 
            :ivar store_id: Unique identifier for the profile store
            :ivar criteria: Boolean filter expression in the Audiences CEL Specification that determines which profiles belong to this audience. Profiles matching this criteria will be included in the audience.  See the **Audiences CEL Specification** section for full syntax reference, supported operators, functions, and examples. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.audience_id: Optional[str] = payload.get("audienceId")
            self.store_type: Optional[StoreType] = payload.get("storeType")
            self.store_id: Optional[str] = payload.get("storeId")
            self.criteria: Optional[str] = payload.get("criteria")

        def to_dict(self):
            return {
                
                    "audienceId": self.audience_id,
                    "storeType": self.store_type.to_dict() if self.store_type is not None else None ,
                    "storeId": self.store_id,
                    "criteria": self.criteria,
            }

    class SnapshotVariable(object):
        """
            :ivar expression: The CEL expression.
            :ivar display_name: Human-readable display name for this variable. Must be a valid identifier: starts with a letter or underscore, followed by letters, digits, or underscores. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.expression: Optional[str] = payload.get("expression")
            self.display_name: Optional[str] = payload.get("displayName")

        def to_dict(self):
            return {
                
                    "expression": self.expression,
                    "displayName": self.display_name,
            }


    def __init__(self, version: Version):
        """
        Initialize the SnapshotList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/Snapshots'
        
        
    
    
    def list(self, 
    ) -> List[SnapshotInstance]:
        """
        Lists SnapshotInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [SnapshotInstance(self._version, item) for item in payload["snapshots"]]
        

    async def list_async(self, 
    ) -> List[SnapshotInstance]:
        """
        Asynchronously lists SnapshotInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [SnapshotInstance(self._version, item) for item in payload["snapshots"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists SnapshotInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists SnapshotInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> SnapshotPage:
        """
        Retrieve a single page of SnapshotInstance records from the API.
        Request is executed immediately
        
        :returns: Page of SnapshotInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return SnapshotPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
    ) -> SnapshotPage:
        """
        Asynchronously retrieve a single page of SnapshotInstance records from the API.
        Request is executed immediately
        
        :returns: Page of SnapshotInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return SnapshotPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with SnapshotPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = SnapshotPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with SnapshotPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = SnapshotPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> SnapshotPage:
        """
        Retrieve a specific page of SnapshotInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of SnapshotInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return SnapshotPage(self._version, response)

    async def get_page_async(self, target_url: str) -> SnapshotPage:
        """
        Asynchronously retrieve a specific page of SnapshotInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of SnapshotInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return SnapshotPage(self._version, response)

    
    def _create(self, create_snapshot_request: CreateSnapshotRequest, idempotency_key: Union[str, object]=values.unset) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_snapshot_request.to_dict()
        
        headers = values.of({
                'Idempotency-Key': idempotency_key,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, create_snapshot_request: CreateSnapshotRequest, idempotency_key: Union[str, object]=values.unset) -> SnapshotInstance:
        """
        Create the SnapshotInstance

        :param create_snapshot_request: 
        :param idempotency_key: Client-provided idempotency key for safe request retries.  When provided, the server ensures that duplicate requests with the same key produce the same result without executing the operation multiple times.  Requirements: - Must be a non-empty string (max 128 characters) - Keys are retained for at least 24 hours - Scope: Account + Region combination - If a different payload is sent with the same key, returns 422 Unprocessable Content  If a request is replayed with the same idempotency key, the response will include the header `Idempotent-Replayed: true`. 
        
        :returns: The created SnapshotInstance
        """
        payload, _, _ = self._create(create_snapshot_request=create_snapshot_request, idempotency_key=idempotency_key)
        return SnapshotInstance(self._version, payload)

    def create_with_http_info(self, create_snapshot_request: CreateSnapshotRequest, idempotency_key: Union[str, object]=values.unset) -> ApiResponse:
        """
        Create the SnapshotInstance and return response metadata

        :param create_snapshot_request: 
        :param idempotency_key: Client-provided idempotency key for safe request retries.  When provided, the server ensures that duplicate requests with the same key produce the same result without executing the operation multiple times.  Requirements: - Must be a non-empty string (max 128 characters) - Keys are retained for at least 24 hours - Scope: Account + Region combination - If a different payload is sent with the same key, returns 422 Unprocessable Content  If a request is replayed with the same idempotency key, the response will include the header `Idempotent-Replayed: true`. 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(create_snapshot_request=create_snapshot_request, idempotency_key=idempotency_key)
        instance = SnapshotInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, create_snapshot_request: CreateSnapshotRequest, idempotency_key: Union[str, object]=values.unset) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_snapshot_request.to_dict()
        
        headers = values.of({
                'Idempotency-Key': idempotency_key,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, create_snapshot_request: CreateSnapshotRequest, idempotency_key: Union[str, object]=values.unset) -> SnapshotInstance:
        """
        Asynchronously create the SnapshotInstance

        :param create_snapshot_request: 
        :param idempotency_key: Client-provided idempotency key for safe request retries.  When provided, the server ensures that duplicate requests with the same key produce the same result without executing the operation multiple times.  Requirements: - Must be a non-empty string (max 128 characters) - Keys are retained for at least 24 hours - Scope: Account + Region combination - If a different payload is sent with the same key, returns 422 Unprocessable Content  If a request is replayed with the same idempotency key, the response will include the header `Idempotent-Replayed: true`. 
        
        :returns: The created SnapshotInstance
        """
        payload, _, _ = await self._create_async(create_snapshot_request=create_snapshot_request, idempotency_key=idempotency_key)
        return SnapshotInstance(self._version, payload)

    async def create_with_http_info_async(self, create_snapshot_request: CreateSnapshotRequest, idempotency_key: Union[str, object]=values.unset) -> ApiResponse:
        """
        Asynchronously create the SnapshotInstance and return response metadata

        :param create_snapshot_request: 
        :param idempotency_key: Client-provided idempotency key for safe request retries.  When provided, the server ensures that duplicate requests with the same key produce the same result without executing the operation multiple times.  Requirements: - Must be a non-empty string (max 128 characters) - Keys are retained for at least 24 hours - Scope: Account + Region combination - If a different payload is sent with the same key, returns 422 Unprocessable Content  If a request is replayed with the same idempotency key, the response will include the header `Idempotent-Replayed: true`. 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(create_snapshot_request=create_snapshot_request, idempotency_key=idempotency_key)
        instance = SnapshotInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[SnapshotInstance]:
        """
        Streams SnapshotInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param str store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param str audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param str order_by: Field to order results by
        :param str order_direction: Sort direction for results
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            labels=labels,
            store_id=store_id,
            audience_id=audience_id,
            order_by=order_by,
            order_direction=order_direction,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[SnapshotInstance]:
        """
        Asynchronously streams SnapshotInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param str store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param str audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param str order_by: Field to order results by
        :param str order_direction: Sort direction for results
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            labels=labels,
            store_id=store_id,
            audience_id=audience_id,
            order_by=order_by,
            order_direction=order_direction,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams SnapshotInstance and returns headers from first page

        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param str store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param str audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param str order_by: Field to order results by
        :param str order_direction: Sort direction for results
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            labels=labels,
            store_id=store_id,
            audience_id=audience_id,
            order_by=order_by,
            order_direction=order_direction,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams SnapshotInstance and returns headers from first page

        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param str store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param str audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param str order_by: Field to order results by
        :param str order_direction: Sort direction for results
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            labels=labels,
            store_id=store_id,
            audience_id=audience_id,
            order_by=order_by,
            order_direction=order_direction,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[SnapshotInstance]:
        """
        Lists SnapshotInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param str store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param str audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param str order_by: Field to order results by
        :param str order_direction: Sort direction for results
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            labels=labels,
            store_id=store_id,
            audience_id=audience_id,
            order_by=order_by,
            order_direction=order_direction,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[SnapshotInstance]:
        """
        Asynchronously lists SnapshotInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param str store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param str audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param str order_by: Field to order results by
        :param str order_direction: Sort direction for results
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            labels=labels,
            store_id=store_id,
            audience_id=audience_id,
            order_by=order_by,
            order_direction=order_direction,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists SnapshotInstance and returns headers from first page

        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param str store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param str audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param str order_by: Field to order results by
        :param str order_direction: Sort direction for results
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            labels=labels,
            store_id=store_id,
            audience_id=audience_id,
            order_by=order_by,
            order_direction=order_direction,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists SnapshotInstance and returns headers from first page

        
        :param str labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param str store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param str audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param str order_by: Field to order results by
        :param str order_direction: Sort direction for results
        :param str page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            labels=labels,
            store_id=store_id,
            audience_id=audience_id,
            order_by=order_by,
            order_direction=order_direction,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      labels: Union[str, object] = values.unset,
      store_id: Union[str, object] = values.unset,
      audience_id: Union[str, object] = values.unset,
      order_by: Union[str, object] = values.unset,
      order_direction: Union[str, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> SnapshotPage:
        """
        Retrieve a single page of SnapshotInstance records from the API.
        Request is executed immediately
        
        :param labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param order_by: Field to order results by
        :param order_direction: Sort direction for results
        :param page_size: Maximum number of items to return in a single response. If not specified, the API will use a default page size. 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :returns: Page of SnapshotInstance
        """
        data = values.of({ 
            'labels': labels,
            'storeId': store_id,
            'audienceId': audience_id,
            'orderBy': order_by,
            'orderDirection': order_direction,
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return SnapshotPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
      labels: Union[str, object] = values.unset,
      store_id: Union[str, object] = values.unset,
      audience_id: Union[str, object] = values.unset,
      order_by: Union[str, object] = values.unset,
      order_direction: Union[str, object] = values.unset,
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> SnapshotPage:
        """
        Asynchronously retrieve a single page of SnapshotInstance records from the API.
        Request is executed immediately
        
        :param labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param order_by: Field to order results by
        :param order_direction: Sort direction for results
        :param page_size: Maximum number of items to return in a single response. If not specified, the API will use a default page size. 
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :returns: Page of SnapshotInstance
        """
        data = values.of({ 
            'labels': labels,
            'storeId': store_id,
            'audienceId': audience_id,
            'orderBy': order_by,
            'orderDirection': order_direction,
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return SnapshotPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param order_by: Field to order results by
        :param order_direction: Sort direction for results
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with SnapshotPage, status code, and headers
        """
        data = values.of({ 
            'labels': labels,
            'storeId': store_id,
            'audienceId': audience_id,
            'orderBy': order_by,
            'orderDirection': order_direction,
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = SnapshotPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        labels: Union[str, object] = values.unset,
        store_id: Union[str, object] = values.unset,
        audience_id: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        order_direction: Union[str, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param labels: Match audience definitions by one or many labels. If more than one label is specified in the query, the search will return audience definitions that have all the labels.  Format: `key1:value1,key2:value2`  Example: `labels=ageGroup:20s,industry:engineering` 
        :param store_id: Filter to snapshots for a specific store (only applicable for STORE-sourced snapshots)
        :param audience_id: Filter to snapshots for a specific audience (only applicable for AUDIENCE-sourced snapshots)
        :param order_by: Field to order results by
        :param order_direction: Sort direction for results
        :param page_token: A URL-safe, base64-encoded token representing the page of results to return. This token contains the position information needed to fetch the next set of results. If omitted, the API returns the first page of results. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with SnapshotPage, status code, and headers
        """
        data = values.of({ 
            'labels': labels,
            'storeId': store_id,
            'audienceId': audience_id,
            'orderBy': order_by,
            'orderDirection': order_direction,
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = SnapshotPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> SnapshotPage:
        """
        Retrieve a specific page of SnapshotInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of SnapshotInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return SnapshotPage(self._version, response)

    async def get_page_async(self, target_url: str) -> SnapshotPage:
        """
        Asynchronously retrieve a specific page of SnapshotInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of SnapshotInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return SnapshotPage(self._version, response)



    def get(self, snapshot_id: str) -> SnapshotContext:
        """
        Constructs a SnapshotContext
        
        :param snapshot_id: The snapshotId identifier
        """
        return SnapshotContext(self._version, snapshot_id=snapshot_id)

    def __call__(self, snapshot_id: str) -> SnapshotContext:
        """
        Constructs a SnapshotContext
        
        :param snapshot_id: The snapshotId identifier
        """
        return SnapshotContext(self._version, snapshot_id=snapshot_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Audiences.Users_manisingh_codehq_open_api_spec_audiences_preview_openapi_yaml.SnapshotList>'





