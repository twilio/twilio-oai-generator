r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Stytch Domain Unified API
    Customer Identity and Access Management API

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class AdvanceInstance(InstanceResource):


    class LoginAdvanceRequest(object):
        """
            :ivar acr_values: Optional space-delimited ACR values selecting the target ACR needed by the resource server. 
            :ivar chosen_method: The method the user selected in response to a CLIENT_CHOICE step. 
            :ivar login_hint: Optional identifier (email or phone) used to look up or enroll factors in the flow. 
            :ivar otp_code: One-time passcode supplied in response to an SMS_OTP step.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.acr_values: Optional[str] = payload.get("acrValues")
            self.chosen_method: Optional[str] = payload.get("chosenMethod")
            self.login_hint: Optional[str] = payload.get("loginHint")
            self.otp_code: Optional[str] = payload.get("otpCode")

        def to_dict(self):
            return {
                
                    "acrValues": self.acr_values,
                    "chosenMethod": self.chosen_method,
                    "loginHint": self.login_hint,
                    "otpCode": self.otp_code,
            }



    class LoginAdvanceErrorHint(object):
        INVALID_CREDENTIAL = "INVALID_CREDENTIAL"
        MAX_ATTEMPTS_EXCEEDED = "MAX_ATTEMPTS_EXCEEDED"
        CONFIG_FLOW_CHANGED = "CONFIG_FLOW_CHANGED"
        EDGE_NO_MATCH = "EDGE_NO_MATCH"
        ACR_UNSATISFIED = "ACR_UNSATISFIED"
        CONFIG_MISSING_FLOW = "CONFIG_MISSING_FLOW"
        NO_VIABLE_NEXT_STEP = "NO_VIABLE_NEXT_STEP"
        ACR_UNREACHABLE = "ACR_UNREACHABLE"
        COLLECT_INVALID_FIELDS = "COLLECT_INVALID_FIELDS"
        ENROLLMENT_NO_PHONE = "ENROLLMENT_NO_PHONE"
        CREDENTIAL_EXHAUSTED = "CREDENTIAL_EXHAUSTED"

    class LoginAdvanceStatus(object):
        CHALLENGE = "CHALLENGE"
        SUCCESS = "SUCCESS"
        FAILURE = "FAILURE"

    class NextStepType(object):
        SMS_OTP = "SMS_OTP"
        SMS_OTP_ENROLL = "SMS_OTP_ENROLL"
        CLIENT_CHOICE = "CLIENT_CHOICE"
        GOOGLE_OAUTH = "GOOGLE_OAUTH"
        COLLECT_USER_INFO = "COLLECT_USER_INFO"
        USER_INPUT = "USER_INPUT"

    """
    :ivar error_hint: Failure classification. May be present when status=CHALLENGE (retryable) or status=FAILURE (terminal). 
    :ivar next_steps: Credential collections to perform next. Present when status=CHALLENGE. Always a list; one entry for single-path flows, multiple for client_choice fan-out. 
    :ivar redirect_uri: URL to navigate the user to when a flow terminates. On SUCCESS this continues the OAuth flow (the session cookie authenticates the user); on FAILURE this is the web fallback. 
    :ivar status: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any]):
        super().__init__(version)

        
        self.error_hint: Optional["AdvanceInstance.str"] = payload.get("errorHint")
        self.next_steps: Optional[List[str]] = payload.get("nextSteps")
        self.redirect_uri: Optional[str] = payload.get("redirectUri")
        self.status: Optional["AdvanceInstance.str"] = payload.get("status")

        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        
        return '<Twilio.Ciam.Users_manisingh_codehq_open_api_spec_ciam_tenanted_openapi_yaml.AdvanceInstance>'




class AdvanceList(ListResource):
    
    class LoginAdvanceRequest(object):
        """
            :ivar acr_values: Optional space-delimited ACR values selecting the target ACR needed by the resource server. 
            :ivar chosen_method: The method the user selected in response to a CLIENT_CHOICE step. 
            :ivar login_hint: Optional identifier (email or phone) used to look up or enroll factors in the flow. 
            :ivar otp_code: One-time passcode supplied in response to an SMS_OTP step.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.acr_values: Optional[str] = payload.get("acrValues")
            self.chosen_method: Optional[str] = payload.get("chosenMethod")
            self.login_hint: Optional[str] = payload.get("loginHint")
            self.otp_code: Optional[str] = payload.get("otpCode")

        def to_dict(self):
            return {
                
                    "acrValues": self.acr_values,
                    "chosenMethod": self.chosen_method,
                    "loginHint": self.login_hint,
                    "otpCode": self.otp_code,
            }


    def __init__(self, version: Version):
        """
        Initialize the AdvanceList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/advance'
        
        
    
    def list(self, 
        login_advance_request: Union[LoginAdvanceRequest, object] = values.unset,
        auth_session: Union[str, object] = values.unset,
        redirect_to: Union[str, object] = values.unset,
    ) -> List[AdvanceInstance]:
        """
        Lists AdvanceInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'LoginAdvanceRequest': login_advance_request,
            'auth_session': auth_session,
            'redirect_to': redirect_to,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [AdvanceInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        login_advance_request: Union[LoginAdvanceRequest, object] = values.unset,
        auth_session: Union[str, object] = values.unset,
        redirect_to: Union[str, object] = values.unset,
    ) -> List[AdvanceInstance]:
        """
        Asynchronously lists AdvanceInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'LoginAdvanceRequest': login_advance_request,
            'auth_session': auth_session,
            'redirect_to': redirect_to,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [AdvanceInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        login_advance_request: Union[LoginAdvanceRequest, object] = values.unset,
        auth_session: Union[str, object] = values.unset,
        redirect_to: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AdvanceInstance and returns headers from first page

        
        :param LoginAdvanceRequest login_advance_request: 
        :param str auth_session: Opaque challenge-session token threading state across challenge rounds. Absent on the first call (the server mints it); present thereafter. 
        :param str redirect_to: URL-escaped original /oauth2/authorize request, set by the authorize redirect. Parsed on the first call to recover OAuth params (scope, PKCE, redirect_uri, state, client_id). 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            login_advance_request=login_advance_request,
            auth_session=auth_session,
            redirect_to=redirect_to,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        login_advance_request: Union[LoginAdvanceRequest, object] = values.unset,
        auth_session: Union[str, object] = values.unset,
        redirect_to: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AdvanceInstance and returns headers from first page

        
        :param LoginAdvanceRequest login_advance_request: 
        :param str auth_session: Opaque challenge-session token threading state across challenge rounds. Absent on the first call (the server mints it); present thereafter. 
        :param str redirect_to: URL-escaped original /oauth2/authorize request, set by the authorize redirect. Parsed on the first call to recover OAuth params (scope, PKCE, redirect_uri, state, client_id). 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            login_advance_request=login_advance_request,
            auth_session=auth_session,
            redirect_to=redirect_to,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      login_advance_request: Union[LoginAdvanceRequest, object] = values.unset,
      auth_session: Union[str, object] = values.unset,
      redirect_to: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AdvancePage:
        """
        Retrieve a single page of AdvanceInstance records from the API.
        Request is executed immediately
        
        :param login_advance_request: 
        :param auth_session: Opaque challenge-session token threading state across challenge rounds. Absent on the first call (the server mints it); present thereafter. 
        :param redirect_to: URL-escaped original /oauth2/authorize request, set by the authorize redirect. Parsed on the first call to recover OAuth params (scope, PKCE, redirect_uri, state, client_id). 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AdvanceInstance
        """
        data = values.of({ 
            'LoginAdvanceRequest': login_advance_request,
            'auth_session': auth_session,
            'redirect_to': redirect_to,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='POST', uri=self._uri, params=data, headers=headers)
        return AdvancePage(self._version, response)

    async def page_async(self, 
      login_advance_request: Union[LoginAdvanceRequest, object] = values.unset,
      auth_session: Union[str, object] = values.unset,
      redirect_to: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AdvancePage:
        """
        Asynchronously retrieve a single page of AdvanceInstance records from the API.
        Request is executed immediately
        
        :param login_advance_request: 
        :param auth_session: Opaque challenge-session token threading state across challenge rounds. Absent on the first call (the server mints it); present thereafter. 
        :param redirect_to: URL-escaped original /oauth2/authorize request, set by the authorize redirect. Parsed on the first call to recover OAuth params (scope, PKCE, redirect_uri, state, client_id). 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AdvanceInstance
        """
        data = values.of({ 
            'LoginAdvanceRequest': login_advance_request,
            'auth_session': auth_session,
            'redirect_to': redirect_to,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='POST', uri=self._uri, params=data, headers=headers)
        return AdvancePage(self._version, response)

    def page_with_http_info(self, 
        login_advance_request: Union[LoginAdvanceRequest, object] = values.unset,
        auth_session: Union[str, object] = values.unset,
        redirect_to: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param login_advance_request: 
        :param auth_session: Opaque challenge-session token threading state across challenge rounds. Absent on the first call (the server mints it); present thereafter. 
        :param redirect_to: URL-escaped original /oauth2/authorize request, set by the authorize redirect. Parsed on the first call to recover OAuth params (scope, PKCE, redirect_uri, state, client_id). 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AdvancePage, status code, and headers
        """
        data = values.of({ 
            'LoginAdvanceRequest': login_advance_request,
            'auth_session': auth_session,
            'redirect_to': redirect_to,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        page = AdvancePage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        login_advance_request: Union[LoginAdvanceRequest, object] = values.unset,
        auth_session: Union[str, object] = values.unset,
        redirect_to: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param login_advance_request: 
        :param auth_session: Opaque challenge-session token threading state across challenge rounds. Absent on the first call (the server mints it); present thereafter. 
        :param redirect_to: URL-escaped original /oauth2/authorize request, set by the authorize redirect. Parsed on the first call to recover OAuth params (scope, PKCE, redirect_uri, state, client_id). 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AdvancePage, status code, and headers
        """
        data = values.of({ 
            'LoginAdvanceRequest': login_advance_request,
            'auth_session': auth_session,
            'redirect_to': redirect_to,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        page = AdvancePage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AdvancePage:
        """
        Retrieve a specific page of AdvanceInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AdvanceInstance
        """
        response = self._version.domain.twilio.request(
            'POST',
            target_url
        )
        return AdvancePage(self._version, response)

    async def get_page_async(self, target_url: str) -> AdvancePage:
        """
        Asynchronously retrieve a specific page of AdvanceInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AdvanceInstance
        """
        response = await self._version.domain.twilio.request_async(
            'POST',
            target_url
        )
        return AdvancePage(self._version, response)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Ciam.Users_manisingh_codehq_open_api_spec_ciam_tenanted_openapi_yaml.AdvanceList>'


