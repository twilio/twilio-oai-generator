r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Stytch Domain Unified API
    Customer Identity and Access Management API

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version






class ClientInstance(InstanceResource):



    class IdpClientType(object):
        THIRD_PARTY_PUBLIC = "THIRD_PARTY_PUBLIC"
        THIRD_PARTY_CONFIDENTIAL = "THIRD_PARTY_CONFIDENTIAL"
        FIRST_PARTY_PUBLIC = "FIRST_PARTY_PUBLIC"
        FIRST_PARTY_CONFIDENTIAL = "FIRST_PARTY_CONFIDENTIAL"

    """
    :ivar clients: List of clients.
    :ivar pagination: 
    :ivar code: Twilio-specific error code. This numeric code uniquely identifies the error type within Twilio's error taxonomy and can be used for programmatic handling. 
    :ivar detail: A human-readable explanation specific to this occurrence of the problem. This field's value can change between different occurrences of the same problem type. 
    :ivar errors: Array of validation errors for HTTP 400/422 responses. Each error contains a human-readable detail message and a JSON Pointer indicating the location of the error in the request. 
    :ivar instance: A URI reference that identifies the specific occurrence of the problem. It may or may not yield further information if dereferenced. 
    :ivar status: The HTTP status code generated by the origin server for this occurrence of the problem. This MUST match the HTTP status code returned in the response. 
    :ivar title: A short, human-readable summary of the problem type. It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization. 
    :ivar type: A URI reference that identifies the problem type. This URI should resolve to human-readable documentation for the problem type. 
    :ivar client: The resulting client object that was created. Contains all details about the client, including the clientId and clientSecret if applicable. 
    :ivar client_secret: The client secret for the newly created client. 
    """

    def __init__(self, version: Version, payload:ResponseResource, client_id: Optional[str] = None):
        super().__init__(version)

        
        self.clients: Optional[List[IdpClient]] = payload.get("clients")
        self.pagination: Optional[Pagination] = payload.get("pagination")
        self.code: Optional[int] = deserialize.integer(payload.get("code"))
        self.detail: Optional[str] = payload.get("detail")
        self.errors: Optional[List[ErrorDetail]] = payload.get("errors")
        self.instance: Optional[str] = payload.get("instance")
        self.status: Optional[int] = deserialize.integer(payload.get("status"))
        self.title: Optional[str] = payload.get("title")
        self.type: Optional[str] = payload.get("type")
        self.client: Optional[str] = payload.get("client")
        self.client_secret: Optional[str] = payload.get("clientSecret")

        
        
        # Only set _solution if path params are provided (not None)
        if client_id is not None:
            self._solution = { 
                "client_id": client_id,
            }
        
        
        self._context: Optional[ClientContext] = None

    @property
    def _proxy(self) -> "ClientContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: ClientContext for this ClientInstance
        """
        if self._context is None:
            self._context = ClientContext(self._version, client_id=self._solution['client_id'],)
        return self._context
    
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Ciam.Users_manisingh_codehq_open_api_spec_ciam_v1_openapi_yaml.ClientInstance {}>'.format(context)

class ClientContext(InstanceContext):


    def __init__(self, version: Version, client_id: str):
        """
        Initialize the ClientContext

        :param version: Version that contains the resource
        :param client_id: Account-unique identifier for the client. 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'client_id': client_id,
        }
        self._uri = '/Auth/Clients/{client_id}'.format(**self._solution)
        
    
    
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Ciam.Users_manisingh_codehq_open_api_spec_ciam_v1_openapi_yaml.ClientContext {}>'.format(context)




class ClientList(ListResource):
    
    class ClientsCreateRequest(object):
        """
            :ivar allowed_authorization_details_types: List of allowed Rich Authorization Request types for the client. This indicates which Rich Authorization Request types the client is permitted to use during authorization. 
            :ivar allowed_scopes: List of allowed scopes for the client. This indicates which scopes the client is permitted to request during authorization. 
            :ivar client_description: A description of the client application. This is an optional field that provides additional context about the client. 
            :ivar client_name: The name of the client application. This is a human-readable name for the client. 
            :ivar client_type: 
            :ivar grant_types: List of allowed grant types for the client. This indicates which OAuth2 flows the client is permitted to use. 
            :ivar logo_url: URL for the client's logo. This is used for display purposes in user interfaces. 
            :ivar redirect_urls: List of allowed redirect URLs for the client. These are the URLs to which the authorization server will redirect after authorization. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.allowed_authorization_details_types: Optional[List[str]] = payload.get("allowedAuthorizationDetailsTypes")
            self.allowed_scopes: Optional[List[str]] = payload.get("allowedScopes")
            self.client_description: Optional[str] = payload.get("clientDescription")
            self.client_name: Optional[str] = payload.get("clientName")
            self.client_type: Optional["ClientInstance.IdpClientType"] = payload.get("clientType")
            self.grant_types: Optional[List[str]] = payload.get("grantTypes")
            self.logo_url: Optional[str] = payload.get("logoUrl")
            self.redirect_urls: Optional[List[str]] = payload.get("redirectUrls")

        def to_dict(self):
            return {
                
                    "allowedAuthorizationDetailsTypes": self.allowed_authorization_details_types,
                    "allowedScopes": self.allowed_scopes,
                    "clientDescription": self.client_description,
                    "clientName": self.client_name,
                    "clientType": self.client_type,
                    "grantTypes": self.grant_types,
                    "logoUrl": self.logo_url,
                    "redirectUrls": self.redirect_urls,
            }

    class ClientsUpdateRequest(object):
        """
            :ivar allowed_authorization_details_types: List of allowed Rich Authorization Request types for the client. This indicates which Rich Authorization Request types the client is permitted to use during authorization. 
            :ivar allowed_scopes: List of allowed scopes for the client. This indicates which scopes the client is permitted to request during authorization. 
            :ivar client_description: A description of the client application. This is an optional field that provides additional context about the client. 
            :ivar client_name: The name of the client application. This is a human-readable name for the client. 
            :ivar grant_types: List of allowed grant types for the client. This indicates which OAuth2 flows the client is permitted to use. 
            :ivar logo_url: URL for the client's logo. This is used for display purposes in user interfaces. 
            :ivar redirect_urls: List of allowed redirect URLs for the client. These are the URLs to which the authorization server will redirect after authorization. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.allowed_authorization_details_types: Optional[List[str]] = payload.get("allowedAuthorizationDetailsTypes")
            self.allowed_scopes: Optional[List[str]] = payload.get("allowedScopes")
            self.client_description: Optional[str] = payload.get("clientDescription")
            self.client_name: Optional[str] = payload.get("clientName")
            self.grant_types: Optional[List[str]] = payload.get("grantTypes")
            self.logo_url: Optional[str] = payload.get("logoUrl")
            self.redirect_urls: Optional[List[str]] = payload.get("redirectUrls")

        def to_dict(self):
            return {
                
                    "allowedAuthorizationDetailsTypes": self.allowed_authorization_details_types,
                    "allowedScopes": self.allowed_scopes,
                    "clientDescription": self.client_description,
                    "clientName": self.client_name,
                    "grantTypes": self.grant_types,
                    "logoUrl": self.logo_url,
                    "redirectUrls": self.redirect_urls,
            }

    class ErrorDetail(object):
        """
            :ivar detail: A human-readable explanation of the validation error for this specific field.
            :ivar pointer: A JSON Pointer (RFC 6901) to the location in the request where the error occurred.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.detail: Optional[str] = payload.get("detail")
            self.pointer: Optional[str] = payload.get("pointer")

        def to_dict(self):
            return {
                
                    "detail": self.detail,
                    "pointer": self.pointer,
            }

    class Pagination(object):
        """
            :ivar key: Identifier for the paginated resource type.
            :ivar next_token: Opaque cursor to retrieve the next page of results. Absent when there are no more results.
            :ivar previous_token: Opaque cursor to retrieve the previous page of results. Absent when on the first page.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.key: Optional[str] = payload.get("key")
            self.next_token: Optional[str] = payload.get("nextToken")
            self.previous_token: Optional[str] = payload.get("previousToken")

        def to_dict(self):
            return {
                
                    "key": self.key,
                    "nextToken": self.next_token,
                    "previousToken": self.previous_token,
            }


    def __init__(self, version: Version):
        """
        Initialize the ClientList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/Auth/Clients'
        
        
    
    def list(self, 
    ) -> List[ClientInstance]:
        """
        Lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/problem+json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[ClientInstance]:
        """
        Asynchronously lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/problem+json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ClientInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ClientInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> ClientPage:
        """
        Retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :returns: Page of ClientInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response = self._version.page(method='DELETE', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
    ) -> ClientPage:
        """
        Asynchronously retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :returns: Page of ClientInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ClientPage:
        """
        Retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = self._version.domain.twilio.request(
            'DELETE',
            target_url
        )
        return ClientPage(self._version, response)

    async def get_page_async(self, target_url: str) -> ClientPage:
        """
        Asynchronously retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = await self._version.domain.twilio.request_async(
            'DELETE',
            target_url
        )
        return ClientPage(self._version, response)

    
    def list(self, 
    ) -> List[ClientInstance]:
        """
        Lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[ClientInstance]:
        """
        Asynchronously lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ClientInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ClientInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> ClientPage:
        """
        Retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :returns: Page of ClientInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
    ) -> ClientPage:
        """
        Asynchronously retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :returns: Page of ClientInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ClientPage:
        """
        Retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ClientPage(self._version, response)

    async def get_page_async(self, target_url: str) -> ClientPage:
        """
        Asynchronously retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ClientPage(self._version, response)

    
    def list(self, 
        clients_update_request: Union[ClientsUpdateRequest, object] = values.unset,
    ) -> List[ClientInstance]:
        """
        Lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'ClientsUpdateRequest': clients_update_request,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='PATCH', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        clients_update_request: Union[ClientsUpdateRequest, object] = values.unset,
    ) -> List[ClientInstance]:
        """
        Asynchronously lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'ClientsUpdateRequest': clients_update_request,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='PATCH', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        clients_update_request: Union[ClientsUpdateRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ClientInstance and returns headers from first page

        
        :param ClientsUpdateRequest clients_update_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            clients_update_request=clients_update_request,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        clients_update_request: Union[ClientsUpdateRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ClientInstance and returns headers from first page

        
        :param ClientsUpdateRequest clients_update_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            clients_update_request=clients_update_request,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      clients_update_request: Union[ClientsUpdateRequest, object] = values.unset,
    ) -> ClientPage:
        """
        Retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :param clients_update_request: 
        :returns: Page of ClientInstance
        """
        data = values.of({ 
            'ClientsUpdateRequest': clients_update_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='PATCH', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
      clients_update_request: Union[ClientsUpdateRequest, object] = values.unset,
    ) -> ClientPage:
        """
        Asynchronously retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :param clients_update_request: 
        :returns: Page of ClientInstance
        """
        data = values.of({ 
            'ClientsUpdateRequest': clients_update_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='PATCH', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        clients_update_request: Union[ClientsUpdateRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param clients_update_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'ClientsUpdateRequest': clients_update_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='PATCH', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        clients_update_request: Union[ClientsUpdateRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param clients_update_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'ClientsUpdateRequest': clients_update_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='PATCH', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ClientPage:
        """
        Retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = self._version.domain.twilio.request(
            'PATCH',
            target_url
        )
        return ClientPage(self._version, response)

    async def get_page_async(self, target_url: str) -> ClientPage:
        """
        Asynchronously retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = await self._version.domain.twilio.request_async(
            'PATCH',
            target_url
        )
        return ClientPage(self._version, response)

    
    def list(self, 
        clients_create_request: Union[ClientsCreateRequest, object] = values.unset,
    ) -> List[ClientInstance]:
        """
        Lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'ClientsCreateRequest': clients_create_request,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        clients_create_request: Union[ClientsCreateRequest, object] = values.unset,
    ) -> List[ClientInstance]:
        """
        Asynchronously lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'ClientsCreateRequest': clients_create_request,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        clients_create_request: Union[ClientsCreateRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ClientInstance and returns headers from first page

        
        :param ClientsCreateRequest clients_create_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            clients_create_request=clients_create_request,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        clients_create_request: Union[ClientsCreateRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ClientInstance and returns headers from first page

        
        :param ClientsCreateRequest clients_create_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            clients_create_request=clients_create_request,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      clients_create_request: Union[ClientsCreateRequest, object] = values.unset,
    ) -> ClientPage:
        """
        Retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :param clients_create_request: 
        :returns: Page of ClientInstance
        """
        data = values.of({ 
            'ClientsCreateRequest': clients_create_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='POST', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
      clients_create_request: Union[ClientsCreateRequest, object] = values.unset,
    ) -> ClientPage:
        """
        Asynchronously retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :param clients_create_request: 
        :returns: Page of ClientInstance
        """
        data = values.of({ 
            'ClientsCreateRequest': clients_create_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='POST', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        clients_create_request: Union[ClientsCreateRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param clients_create_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'ClientsCreateRequest': clients_create_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        clients_create_request: Union[ClientsCreateRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param clients_create_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'ClientsCreateRequest': clients_create_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ClientPage:
        """
        Retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = self._version.domain.twilio.request(
            'POST',
            target_url
        )
        return ClientPage(self._version, response)

    async def get_page_async(self, target_url: str) -> ClientPage:
        """
        Asynchronously retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = await self._version.domain.twilio.request_async(
            'POST',
            target_url
        )
        return ClientPage(self._version, response)

    
    def list(self, 
        
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
    ) -> List[ClientInstance]:
        """
        Lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': created_after,
            'createdBefore': created_before,
            'updatedAfter': updated_after,
            'updatedBefore': updated_before,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
    ) -> List[ClientInstance]:
        """
        Asynchronously lists ClientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': created_after,
            'createdBefore': created_before,
            'updatedAfter': updated_after,
            'updatedBefore': updated_before,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [ClientInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ClientInstance and returns headers from first page

        
        :param str order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param str page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param datetime created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param datetime created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param datetime updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param datetime updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            order_by=order_by,
            page_token=page_token,
            created_after=created_after,
            created_before=created_before,
            updated_after=updated_after,
            updated_before=updated_before,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ClientInstance and returns headers from first page

        
        :param str order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param str page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param datetime created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param datetime created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param datetime updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param datetime updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            order_by=order_by,
            page_token=page_token,
            created_after=created_after,
            created_before=created_before,
            updated_after=updated_after,
            updated_before=updated_before,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      page_size: Union[int, object] = values.unset,
      order_by: Union[str, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      created_after: Union[datetime, object] = values.unset,
      created_before: Union[datetime, object] = values.unset,
      updated_after: Union[datetime, object] = values.unset,
      updated_before: Union[datetime, object] = values.unset,
    ) -> ClientPage:
        """
        Retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :param page_size: Maximum number of results to return per page.
        :param order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :returns: Page of ClientInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': serialize.iso8601_datetime(created_after),
            'createdBefore': serialize.iso8601_datetime(created_before),
            'updatedAfter': serialize.iso8601_datetime(updated_after),
            'updatedBefore': serialize.iso8601_datetime(updated_before),
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
      page_size: Union[int, object] = values.unset,
      order_by: Union[str, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      created_after: Union[datetime, object] = values.unset,
      created_before: Union[datetime, object] = values.unset,
      updated_after: Union[datetime, object] = values.unset,
      updated_before: Union[datetime, object] = values.unset,
    ) -> ClientPage:
        """
        Asynchronously retrieve a single page of ClientInstance records from the API.
        Request is executed immediately
        
        :param page_size: Maximum number of results to return per page.
        :param order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :returns: Page of ClientInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': serialize.iso8601_datetime(created_after),
            'createdBefore': serialize.iso8601_datetime(created_before),
            'updatedAfter': serialize.iso8601_datetime(updated_after),
            'updatedBefore': serialize.iso8601_datetime(updated_before),
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ClientPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': serialize.iso8601_datetime(created_after),
            'createdBefore': serialize.iso8601_datetime(created_before),
            'updatedAfter': serialize.iso8601_datetime(updated_after),
            'updatedBefore': serialize.iso8601_datetime(updated_before),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ClientPage, status code, and headers
        """
        data = values.of({ 
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': serialize.iso8601_datetime(created_after),
            'createdBefore': serialize.iso8601_datetime(created_before),
            'updatedAfter': serialize.iso8601_datetime(updated_after),
            'updatedBefore': serialize.iso8601_datetime(updated_before),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ClientPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ClientPage:
        """
        Retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ClientPage(self._version, response)

    async def get_page_async(self, target_url: str) -> ClientPage:
        """
        Asynchronously retrieve a specific page of ClientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ClientInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ClientPage(self._version, response)



    def get(self, client_id: str) -> ClientContext:
        """
        Constructs a ClientContext
        
        :param client_id: Account-unique identifier for the client. 
        """
        return ClientContext(self._version, client_id=client_id)

    def __call__(self, client_id: str) -> ClientContext:
        """
        Constructs a ClientContext
        
        :param client_id: Account-unique identifier for the client. 
        """
        return ClientContext(self._version, client_id=client_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Ciam.Users_manisingh_codehq_open_api_spec_ciam_v1_openapi_yaml.ClientList>'





