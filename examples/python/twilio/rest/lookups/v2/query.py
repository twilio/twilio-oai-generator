r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Lookup
    Lookup APIs for individual, bulk and job based requests  Discussion topics: - API version to use - Using or not lookup in the path or just as lookups subdomain

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class QueryInstance(InstanceResource):


    class CallForwarding(object):
        """
            :ivar call_forwarding_enabled: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.call_forwarding_enabled: Optional[bool] = payload.get("call_forwarding_enabled")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "call_forwarding_enabled": self.call_forwarding_enabled,
                    "error_code": self.error_code,
            }

    class CallerName(object):
        """
            :ivar caller_name: 
            :ivar caller_type: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.caller_name: Optional[str] = payload.get("caller_name")
            self.caller_type: Optional[str] = payload.get("caller_type")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "caller_name": self.caller_name,
                    "caller_type": self.caller_type,
                    "error_code": self.error_code,
            }

    class IdentityMatch(object):
        """
            :ivar first_name_match: 
            :ivar last_name_match: 
            :ivar address_line_match: 
            :ivar city_match: 
            :ivar state_match: 
            :ivar postal_code_match: 
            :ivar country_code_match: 
            :ivar national_id_match: 
            :ivar date_of_birth_match: 
            :ivar summary_score: 
            :ivar error_code: 
            :ivar error_message: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.first_name_match: Optional[str] = payload.get("first_name_match")
            self.last_name_match: Optional[str] = payload.get("last_name_match")
            self.address_line_match: Optional[str] = payload.get("address_line_match")
            self.city_match: Optional[str] = payload.get("city_match")
            self.state_match: Optional[str] = payload.get("state_match")
            self.postal_code_match: Optional[str] = payload.get("postal_code_match")
            self.country_code_match: Optional[str] = payload.get("country_code_match")
            self.national_id_match: Optional[str] = payload.get("national_id_match")
            self.date_of_birth_match: Optional[str] = payload.get("date_of_birth_match")
            self.summary_score: Optional[int] = payload.get("summary_score")
            self.error_code: Optional[int] = payload.get("error_code")
            self.error_message: Optional[str] = payload.get("error_message")

        def to_dict(self):
            return {
                
                    "first_name_match": self.first_name_match,
                    "last_name_match": self.last_name_match,
                    "address_line_match": self.address_line_match,
                    "city_match": self.city_match,
                    "state_match": self.state_match,
                    "postal_code_match": self.postal_code_match,
                    "country_code_match": self.country_code_match,
                    "national_id_match": self.national_id_match,
                    "date_of_birth_match": self.date_of_birth_match,
                    "summary_score": self.summary_score,
                    "error_code": self.error_code,
                    "error_message": self.error_message,
            }

    class IdentityMatchParameters(object):
        """
            :ivar first_name: 
            :ivar last_name: 
            :ivar address_line1: 
            :ivar address_line2: 
            :ivar city: 
            :ivar state: 
            :ivar postal_code: 
            :ivar address_country_code: 
            :ivar national_id: 
            :ivar date_of_birth: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.first_name: Optional[str] = payload.get("first_name")
            self.last_name: Optional[str] = payload.get("last_name")
            self.address_line1: Optional[str] = payload.get("address_line1")
            self.address_line2: Optional[str] = payload.get("address_line2")
            self.city: Optional[str] = payload.get("city")
            self.state: Optional[str] = payload.get("state")
            self.postal_code: Optional[str] = payload.get("postal_code")
            self.address_country_code: Optional[str] = payload.get("address_country_code")
            self.national_id: Optional[str] = payload.get("national_id")
            self.date_of_birth: Optional[str] = payload.get("date_of_birth")

        def to_dict(self):
            return {
                
                    "first_name": self.first_name,
                    "last_name": self.last_name,
                    "address_line1": self.address_line1,
                    "address_line2": self.address_line2,
                    "city": self.city,
                    "state": self.state,
                    "postal_code": self.postal_code,
                    "address_country_code": self.address_country_code,
                    "national_id": self.national_id,
                    "date_of_birth": self.date_of_birth,
            }

    class LastSimSwap(object):
        """
            :ivar last_sim_swap_date: 
            :ivar swapped_period: 
            :ivar swapped_in_period: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.last_sim_swap_date: Optional[datetime] = payload.get("last_sim_swap_date")
            self.swapped_period: Optional[str] = payload.get("swapped_period")
            self.swapped_in_period: Optional[bool] = payload.get("swapped_in_period")

        def to_dict(self):
            return {
                
                    "last_sim_swap_date": self.last_sim_swap_date,
                    "swapped_period": self.swapped_period,
                    "swapped_in_period": self.swapped_in_period,
            }

    class LineStatus(object):
        """
            :ivar status: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.status: Optional[str] = payload.get("status")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "status": self.status,
                    "error_code": self.error_code,
            }

    class LineTypeIntelligence(object):
        """
            :ivar type: 
            :ivar carrier_name: 
            :ivar mobile_country_code: 
            :ivar mobile_network_code: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[str] = payload.get("type")
            self.carrier_name: Optional[str] = payload.get("carrier_name")
            self.mobile_country_code: Optional[str] = payload.get("mobile_country_code")
            self.mobile_network_code: Optional[str] = payload.get("mobile_network_code")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "carrier_name": self.carrier_name,
                    "mobile_country_code": self.mobile_country_code,
                    "mobile_network_code": self.mobile_network_code,
                    "error_code": self.error_code,
            }

    class LookupRequest1(object):
        """
            :ivar phone_numbers: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.phone_numbers: Optional[List[QueryList.LookupRequestWithCorId]] = payload.get("phone_numbers")

        def to_dict(self):
            return {
                
                    "phone_numbers": [phone_numbers.to_dict() for phone_numbers in self.phone_numbers] if self.phone_numbers is not None else None,
            }

    class LookupRequestWithCorId(object):
        """
            :ivar correlation_id: Unique identifier used to match request with response
            :ivar phone_number: 
            :ivar fields: 
            :ivar country_code: 
            :ivar identity_match: 
            :ivar reassigned_number: 
            :ivar sms_pumping_risk: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.correlation_id: Optional[str] = payload.get("correlation_id")
            self.phone_number: Optional[str] = payload.get("phone_number")
            self.fields: Optional[List[Enumstr]] = payload.get("fields")
            self.country_code: Optional[str] = payload.get("country_code")
            self.identity_match: Optional[QueryList.IdentityMatchParameters] = payload.get("identity_match")
            self.reassigned_number: Optional[QueryList.ReassignedNumberRequest] = payload.get("reassigned_number")
            self.sms_pumping_risk: Optional[QueryList.SmsPumpingRiskParameters] = payload.get("sms_pumping_risk")

        def to_dict(self):
            return {
                
                    "correlation_id": self.correlation_id,
                    "phone_number": self.phone_number,
                    "fields": self.fields,
                    "country_code": self.country_code,
                    "identity_match": self.identity_match.to_dict() if self.identity_match is not None else None ,
                    "reassigned_number": self.reassigned_number.to_dict() if self.reassigned_number is not None else None ,
                    "sms_pumping_risk": self.sms_pumping_risk.to_dict() if self.sms_pumping_risk is not None else None ,
            }

    class ReassignedNumberRequest(object):
        """
            :ivar last_verified_date: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.last_verified_date: Optional[str] = payload.get("last_verified_date")

        def to_dict(self):
            return {
                
                    "last_verified_date": self.last_verified_date,
            }

    class ReassignedNumberResponse(object):
        """
            :ivar last_verified_date: 
            :ivar is_number_reassigned: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.last_verified_date: Optional[str] = payload.get("last_verified_date")
            self.is_number_reassigned: Optional[str] = payload.get("is_number_reassigned")
            self.error_code: Optional[str] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "last_verified_date": self.last_verified_date,
                    "is_number_reassigned": self.is_number_reassigned,
                    "error_code": self.error_code,
            }

    class SimSwap(object):
        """
            :ivar last_sim_swap: 
            :ivar carrier_name: 
            :ivar mobile_country_code: 
            :ivar mobile_network_code: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.last_sim_swap: Optional[LastSimSwap] = payload.get("last_sim_swap")
            self.carrier_name: Optional[str] = payload.get("carrier_name")
            self.mobile_country_code: Optional[str] = payload.get("mobile_country_code")
            self.mobile_network_code: Optional[str] = payload.get("mobile_network_code")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "last_sim_swap": self.last_sim_swap.to_dict() if self.last_sim_swap is not None else None ,
                    "carrier_name": self.carrier_name,
                    "mobile_country_code": self.mobile_country_code,
                    "mobile_network_code": self.mobile_network_code,
                    "error_code": self.error_code,
            }

    class SmsPumpingRisk(object):
        """
            :ivar carrier_risk_category: 
            :ivar number_blocked: 
            :ivar number_blocked_date: 
            :ivar number_blocked_last_3_months: 
            :ivar sms_pumping_risk_score: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.carrier_risk_category: Optional[str] = payload.get("carrier_risk_category")
            self.number_blocked: Optional[bool] = payload.get("number_blocked")
            self.number_blocked_date: Optional[datetime] = payload.get("number_blocked_date")
            self.number_blocked_last_3_months: Optional[bool] = payload.get("number_blocked_last_3_months")
            self.sms_pumping_risk_score: Optional[int] = payload.get("sms_pumping_risk_score")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "carrier_risk_category": self.carrier_risk_category,
                    "number_blocked": self.number_blocked,
                    "number_blocked_date": self.number_blocked_date,
                    "number_blocked_last_3_months": self.number_blocked_last_3_months,
                    "sms_pumping_risk_score": self.sms_pumping_risk_score,
                    "error_code": self.error_code,
            }

    class SmsPumpingRiskParameters(object):
        """
            :ivar partner_sub_id: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.partner_sub_id: Optional[str] = payload.get("partner_sub_id")

        def to_dict(self):
            return {
                
                    "partner_sub_id": self.partner_sub_id,
            }



    """
    :ivar phone_numbers: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any]):
        super().__init__(version)

        
        self.phone_numbers: Optional[List[str]] = payload.get("phone_numbers")

        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        
        return '<Twilio.Lookups.V2.QueryInstance>'




class QueryList(ListResource):
    
    class CallForwarding(object):
        """
            :ivar call_forwarding_enabled: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.call_forwarding_enabled: Optional[bool] = payload.get("call_forwarding_enabled")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "call_forwarding_enabled": self.call_forwarding_enabled,
                    "error_code": self.error_code,
            }

    class CallerName(object):
        """
            :ivar caller_name: 
            :ivar caller_type: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.caller_name: Optional[str] = payload.get("caller_name")
            self.caller_type: Optional[str] = payload.get("caller_type")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "caller_name": self.caller_name,
                    "caller_type": self.caller_type,
                    "error_code": self.error_code,
            }

    class IdentityMatch(object):
        """
            :ivar first_name_match: 
            :ivar last_name_match: 
            :ivar address_line_match: 
            :ivar city_match: 
            :ivar state_match: 
            :ivar postal_code_match: 
            :ivar country_code_match: 
            :ivar national_id_match: 
            :ivar date_of_birth_match: 
            :ivar summary_score: 
            :ivar error_code: 
            :ivar error_message: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.first_name_match: Optional[str] = payload.get("first_name_match")
            self.last_name_match: Optional[str] = payload.get("last_name_match")
            self.address_line_match: Optional[str] = payload.get("address_line_match")
            self.city_match: Optional[str] = payload.get("city_match")
            self.state_match: Optional[str] = payload.get("state_match")
            self.postal_code_match: Optional[str] = payload.get("postal_code_match")
            self.country_code_match: Optional[str] = payload.get("country_code_match")
            self.national_id_match: Optional[str] = payload.get("national_id_match")
            self.date_of_birth_match: Optional[str] = payload.get("date_of_birth_match")
            self.summary_score: Optional[int] = payload.get("summary_score")
            self.error_code: Optional[int] = payload.get("error_code")
            self.error_message: Optional[str] = payload.get("error_message")

        def to_dict(self):
            return {
                
                    "first_name_match": self.first_name_match,
                    "last_name_match": self.last_name_match,
                    "address_line_match": self.address_line_match,
                    "city_match": self.city_match,
                    "state_match": self.state_match,
                    "postal_code_match": self.postal_code_match,
                    "country_code_match": self.country_code_match,
                    "national_id_match": self.national_id_match,
                    "date_of_birth_match": self.date_of_birth_match,
                    "summary_score": self.summary_score,
                    "error_code": self.error_code,
                    "error_message": self.error_message,
            }

    class IdentityMatchParameters(object):
        """
            :ivar first_name: 
            :ivar last_name: 
            :ivar address_line1: 
            :ivar address_line2: 
            :ivar city: 
            :ivar state: 
            :ivar postal_code: 
            :ivar address_country_code: 
            :ivar national_id: 
            :ivar date_of_birth: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.first_name: Optional[str] = payload.get("first_name")
            self.last_name: Optional[str] = payload.get("last_name")
            self.address_line1: Optional[str] = payload.get("address_line1")
            self.address_line2: Optional[str] = payload.get("address_line2")
            self.city: Optional[str] = payload.get("city")
            self.state: Optional[str] = payload.get("state")
            self.postal_code: Optional[str] = payload.get("postal_code")
            self.address_country_code: Optional[str] = payload.get("address_country_code")
            self.national_id: Optional[str] = payload.get("national_id")
            self.date_of_birth: Optional[str] = payload.get("date_of_birth")

        def to_dict(self):
            return {
                
                    "first_name": self.first_name,
                    "last_name": self.last_name,
                    "address_line1": self.address_line1,
                    "address_line2": self.address_line2,
                    "city": self.city,
                    "state": self.state,
                    "postal_code": self.postal_code,
                    "address_country_code": self.address_country_code,
                    "national_id": self.national_id,
                    "date_of_birth": self.date_of_birth,
            }

    class LastSimSwap(object):
        """
            :ivar last_sim_swap_date: 
            :ivar swapped_period: 
            :ivar swapped_in_period: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.last_sim_swap_date: Optional[datetime] = payload.get("last_sim_swap_date")
            self.swapped_period: Optional[str] = payload.get("swapped_period")
            self.swapped_in_period: Optional[bool] = payload.get("swapped_in_period")

        def to_dict(self):
            return {
                
                    "last_sim_swap_date": self.last_sim_swap_date,
                    "swapped_period": self.swapped_period,
                    "swapped_in_period": self.swapped_in_period,
            }

    class LineStatus(object):
        """
            :ivar status: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.status: Optional[str] = payload.get("status")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "status": self.status,
                    "error_code": self.error_code,
            }

    class LineTypeIntelligence(object):
        """
            :ivar type: 
            :ivar carrier_name: 
            :ivar mobile_country_code: 
            :ivar mobile_network_code: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[str] = payload.get("type")
            self.carrier_name: Optional[str] = payload.get("carrier_name")
            self.mobile_country_code: Optional[str] = payload.get("mobile_country_code")
            self.mobile_network_code: Optional[str] = payload.get("mobile_network_code")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "carrier_name": self.carrier_name,
                    "mobile_country_code": self.mobile_country_code,
                    "mobile_network_code": self.mobile_network_code,
                    "error_code": self.error_code,
            }

    class LookupRequest1(object):
        """
            :ivar phone_numbers: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.phone_numbers: Optional[List[QueryList.LookupRequestWithCorId]] = payload.get("phone_numbers")

        def to_dict(self):
            return {
                
                    "phone_numbers": [phone_numbers.to_dict() for phone_numbers in self.phone_numbers] if self.phone_numbers is not None else None,
            }

    class LookupRequestWithCorId(object):
        """
            :ivar correlation_id: Unique identifier used to match request with response
            :ivar phone_number: 
            :ivar fields: 
            :ivar country_code: 
            :ivar identity_match: 
            :ivar reassigned_number: 
            :ivar sms_pumping_risk: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.correlation_id: Optional[str] = payload.get("correlation_id")
            self.phone_number: Optional[str] = payload.get("phone_number")
            self.fields: Optional[List[Enumstr]] = payload.get("fields")
            self.country_code: Optional[str] = payload.get("country_code")
            self.identity_match: Optional[QueryList.IdentityMatchParameters] = payload.get("identity_match")
            self.reassigned_number: Optional[QueryList.ReassignedNumberRequest] = payload.get("reassigned_number")
            self.sms_pumping_risk: Optional[QueryList.SmsPumpingRiskParameters] = payload.get("sms_pumping_risk")

        def to_dict(self):
            return {
                
                    "correlation_id": self.correlation_id,
                    "phone_number": self.phone_number,
                    "fields": self.fields,
                    "country_code": self.country_code,
                    "identity_match": self.identity_match.to_dict() if self.identity_match is not None else None ,
                    "reassigned_number": self.reassigned_number.to_dict() if self.reassigned_number is not None else None ,
                    "sms_pumping_risk": self.sms_pumping_risk.to_dict() if self.sms_pumping_risk is not None else None ,
            }

    class ReassignedNumberRequest(object):
        """
            :ivar last_verified_date: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.last_verified_date: Optional[str] = payload.get("last_verified_date")

        def to_dict(self):
            return {
                
                    "last_verified_date": self.last_verified_date,
            }

    class ReassignedNumberResponse(object):
        """
            :ivar last_verified_date: 
            :ivar is_number_reassigned: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.last_verified_date: Optional[str] = payload.get("last_verified_date")
            self.is_number_reassigned: Optional[str] = payload.get("is_number_reassigned")
            self.error_code: Optional[str] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "last_verified_date": self.last_verified_date,
                    "is_number_reassigned": self.is_number_reassigned,
                    "error_code": self.error_code,
            }

    class SimSwap(object):
        """
            :ivar last_sim_swap: 
            :ivar carrier_name: 
            :ivar mobile_country_code: 
            :ivar mobile_network_code: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.last_sim_swap: Optional[LastSimSwap] = payload.get("last_sim_swap")
            self.carrier_name: Optional[str] = payload.get("carrier_name")
            self.mobile_country_code: Optional[str] = payload.get("mobile_country_code")
            self.mobile_network_code: Optional[str] = payload.get("mobile_network_code")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "last_sim_swap": self.last_sim_swap.to_dict() if self.last_sim_swap is not None else None ,
                    "carrier_name": self.carrier_name,
                    "mobile_country_code": self.mobile_country_code,
                    "mobile_network_code": self.mobile_network_code,
                    "error_code": self.error_code,
            }

    class SmsPumpingRisk(object):
        """
            :ivar carrier_risk_category: 
            :ivar number_blocked: 
            :ivar number_blocked_date: 
            :ivar number_blocked_last_3_months: 
            :ivar sms_pumping_risk_score: 
            :ivar error_code: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.carrier_risk_category: Optional[str] = payload.get("carrier_risk_category")
            self.number_blocked: Optional[bool] = payload.get("number_blocked")
            self.number_blocked_date: Optional[datetime] = payload.get("number_blocked_date")
            self.number_blocked_last_3_months: Optional[bool] = payload.get("number_blocked_last_3_months")
            self.sms_pumping_risk_score: Optional[int] = payload.get("sms_pumping_risk_score")
            self.error_code: Optional[int] = payload.get("error_code")

        def to_dict(self):
            return {
                
                    "carrier_risk_category": self.carrier_risk_category,
                    "number_blocked": self.number_blocked,
                    "number_blocked_date": self.number_blocked_date,
                    "number_blocked_last_3_months": self.number_blocked_last_3_months,
                    "sms_pumping_risk_score": self.sms_pumping_risk_score,
                    "error_code": self.error_code,
            }

    class SmsPumpingRiskParameters(object):
        """
            :ivar partner_sub_id: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.partner_sub_id: Optional[str] = payload.get("partner_sub_id")

        def to_dict(self):
            return {
                
                    "partner_sub_id": self.partner_sub_id,
            }


    def __init__(self, version: Version):
        """
        Initialize the QueryList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/batch/query'
        
        
    
    def _create(self, lookup_request1: Union[LookupRequest1, object]=values.unset) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = lookup_request1.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, lookup_request1: Union[LookupRequest1, object]=values.unset) -> QueryInstance:
        """
        Create the QueryInstance

        :param lookup_request1: 
        
        :returns: The created QueryInstance
        """
        payload, _, _ = self._create(lookup_request1=lookup_request1)
        return QueryInstance(self._version, payload)

    def create_with_http_info(self, lookup_request1: Union[LookupRequest1, object]=values.unset) -> ApiResponse:
        """
        Create the QueryInstance and return response metadata

        :param lookup_request1: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(lookup_request1=lookup_request1)
        instance = QueryInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, lookup_request1: Union[LookupRequest1, object]=values.unset) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = lookup_request1.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, lookup_request1: Union[LookupRequest1, object]=values.unset) -> QueryInstance:
        """
        Asynchronously create the QueryInstance

        :param lookup_request1: 
        
        :returns: The created QueryInstance
        """
        payload, _, _ = await self._create_async(lookup_request1=lookup_request1)
        return QueryInstance(self._version, payload)

    async def create_with_http_info_async(self, lookup_request1: Union[LookupRequest1, object]=values.unset) -> ApiResponse:
        """
        Asynchronously create the QueryInstance and return response metadata

        :param lookup_request1: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(lookup_request1=lookup_request1)
        instance = QueryInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def list(self, 
    ) -> List[QueryInstance]:
        """
        Lists QueryInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [QueryInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[QueryInstance]:
        """
        Asynchronously lists QueryInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [QueryInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists QueryInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists QueryInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> QueryPage:
        """
        Retrieve a single page of QueryInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of QueryInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return QueryPage(self._version, response)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> QueryPage:
        """
        Asynchronously retrieve a single page of QueryInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of QueryInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return QueryPage(self._version, response)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with QueryPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = QueryPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with QueryPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = QueryPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> QueryPage:
        """
        Retrieve a specific page of QueryInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of QueryInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return QueryPage(self._version, response)

    async def get_page_async(self, target_url: str) -> QueryPage:
        """
        Asynchronously retrieve a specific page of QueryInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of QueryInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return QueryPage(self._version, response)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Lookups.V2.QueryList>'


