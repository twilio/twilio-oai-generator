r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Iam
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class ExternalUserInstance(InstanceResource):



    """
    :ivar user_sid: This field represents the unique identifier (SID) of the user
    :ivar email: Email of this user
    :ivar name: Name of this user
    :ivar status: User status
    :ivar sso_enabled: A boolean flag showing whether the user is \"SSO enabled\".
    :ivar role_assignments: An array of role assignments associated with the user
    """

    def __init__(self, version: Version, payload:Dict[str, Any]):
        super().__init__(version)

        
        self.user_sid: Optional[str] = payload.get("userSid")
        self.email: Optional[str] = payload.get("email")
        self.name: Optional[str] = payload.get("name")
        self.status: Optional[str] = payload.get("status")
        self.sso_enabled: Optional[bool] = payload.get("ssoEnabled")
        self.role_assignments: Optional[List[str]] = payload.get("roleAssignments")

        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        
        return '<Twilio.Iam.Users_manisingh_codehq_open_api_spec_iam_v2_openapi_yaml.ExternalUserInstance>'




class ExternalUserPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> ExternalUserInstance:
        """
        Build an instance of ExternalUserInstance

        :param payload: Payload response from the API
        """
        
        return ExternalUserInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Iam.Users_manisingh_codehq_open_api_spec_iam_v2_openapi_yaml.ExternalUserPage>"





class ExternalUserList(ListResource):
    
    def __init__(self, version: Version):
        """
        Initialize the ExternalUserList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/Organizations/ExternalUsers'
        
        
    
    def stream(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[ExternalUserInstance]:
        """
        Streams ExternalUserInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param str status: A string to filter user by the \"status\" attribute
        :param bool sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param List[str] role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            search_string=search_string,
            status=status,
            sso_enabled=sso_enabled,
            role_sid=role_sid,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[ExternalUserInstance]:
        """
        Asynchronously streams ExternalUserInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param str status: A string to filter user by the \"status\" attribute
        :param bool sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param List[str] role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            search_string=search_string,
            status=status,
            sso_enabled=sso_enabled,
            role_sid=role_sid,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams ExternalUserInstance and returns headers from first page

        
        :param str search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param str status: A string to filter user by the \"status\" attribute
        :param bool sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param List[str] role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            search_string=search_string,
            status=status,
            sso_enabled=sso_enabled,
            role_sid=role_sid,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams ExternalUserInstance and returns headers from first page

        
        :param str search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param str status: A string to filter user by the \"status\" attribute
        :param bool sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param List[str] role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            search_string=search_string,
            status=status,
            sso_enabled=sso_enabled,
            role_sid=role_sid,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ExternalUserInstance]:
        """
        Lists ExternalUserInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param str status: A string to filter user by the \"status\" attribute
        :param bool sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param List[str] role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            search_string=search_string,
            status=status,
            sso_enabled=sso_enabled,
            role_sid=role_sid,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ExternalUserInstance]:
        """
        Asynchronously lists ExternalUserInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param str status: A string to filter user by the \"status\" attribute
        :param bool sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param List[str] role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            search_string=search_string,
            status=status,
            sso_enabled=sso_enabled,
            role_sid=role_sid,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ExternalUserInstance and returns headers from first page

        
        :param str search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param str status: A string to filter user by the \"status\" attribute
        :param bool sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param List[str] role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            search_string=search_string,
            status=status,
            sso_enabled=sso_enabled,
            role_sid=role_sid,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ExternalUserInstance and returns headers from first page

        
        :param str search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param str status: A string to filter user by the \"status\" attribute
        :param bool sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param List[str] role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            search_string=search_string,
            status=status,
            sso_enabled=sso_enabled,
            role_sid=role_sid,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      search_string: Union[str, object] = values.unset,
      status: Union[str, object] = values.unset,
      sso_enabled: Union[bool, object] = values.unset,
      role_sid: Union[List[str], object] = values.unset,
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ExternalUserPage:
        """
        Retrieve a single page of ExternalUserInstance records from the API.
        Request is executed immediately
        
        :param search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param status: A string to filter user by the \"status\" attribute
        :param sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ExternalUserInstance
        """
        data = values.of({ 
            'searchString': search_string,
            'status': status,
            'ssoEnabled': serialize.boolean_to_string(sso_enabled),
            'roleSid': serialize.map(role_sid, lambda e: e),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ExternalUserPage(self._version, response)

    async def page_async(self, 
      search_string: Union[str, object] = values.unset,
      status: Union[str, object] = values.unset,
      sso_enabled: Union[bool, object] = values.unset,
      role_sid: Union[List[str], object] = values.unset,
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ExternalUserPage:
        """
        Asynchronously retrieve a single page of ExternalUserInstance records from the API.
        Request is executed immediately
        
        :param search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param status: A string to filter user by the \"status\" attribute
        :param sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ExternalUserInstance
        """
        data = values.of({ 
            'searchString': search_string,
            'status': status,
            'ssoEnabled': serialize.boolean_to_string(sso_enabled),
            'roleSid': serialize.map(role_sid, lambda e: e),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ExternalUserPage(self._version, response)

    def page_with_http_info(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param status: A string to filter user by the \"status\" attribute
        :param sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ExternalUserPage, status code, and headers
        """
        data = values.of({ 
            'searchString': search_string,
            'status': status,
            'ssoEnabled': serialize.boolean_to_string(sso_enabled),
            'roleSid': serialize.map(role_sid, lambda e: e),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ExternalUserPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        search_string: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
        sso_enabled: Union[bool, object] = values.unset,
        role_sid: Union[List[str], object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param search_string: A string to search for in user name, email, user SID, assigned scope SID, or assigned scope friendly name such as an account or organization name
        :param status: A string to filter user by the \"status\" attribute
        :param sso_enabled: a boolean flag to filter accounts by the \"sso enabled\" attribute
        :param role_sid: a string representing the role ID to filter by, i.e. searching for users with associated role
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ExternalUserPage, status code, and headers
        """
        data = values.of({ 
            'searchString': search_string,
            'status': status,
            'ssoEnabled': serialize.boolean_to_string(sso_enabled),
            'roleSid': serialize.map(role_sid, lambda e: e),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ExternalUserPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ExternalUserPage:
        """
        Retrieve a specific page of ExternalUserInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ExternalUserInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ExternalUserPage(self._version, response)

    async def get_page_async(self, target_url: str) -> ExternalUserPage:
        """
        Asynchronously retrieve a specific page of ExternalUserInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ExternalUserInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ExternalUserPage(self._version, response)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Iam.Users_manisingh_codehq_open_api_spec_iam_v2_openapi_yaml.ExternalUserList>'


