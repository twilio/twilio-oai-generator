r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Customer Communication Admin API
    This is the API spec for Communications Center on OneAdmin.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class RecipientInstance(InstanceResource):



    """
    :ivar id: 
    :ivar advisory_id: Advisory Id
    :ivar user_sid: User Id of the advisory recipient
    :ivar email: Email of the advisory recipient
    :ivar first_name: First name of the advisory recipient
    :ivar last_name: Last name of the advisory recipient
    :ivar advisory_accounts: Accounts in the advisory
    :ivar modified_date: Modified timestamp
    """

    def __init__(self, version: Version, payload:Dict[str, Any], advisory_id: Optional[float] = None):
        super().__init__(version)

        
        self.id: Optional[float] = deserialize.decimal(payload.get("id"))
        self.advisory_id: Optional[float] = deserialize.decimal(payload.get("advisory_id"))
        self.user_sid: Optional[str] = payload.get("user_sid")
        self.email: Optional[str] = payload.get("email")
        self.first_name: Optional[str] = payload.get("first_name")
        self.last_name: Optional[str] = payload.get("last_name")
        self.advisory_accounts: Optional[List[str]] = payload.get("advisory_accounts")
        self.modified_date: Optional[datetime] = deserialize.iso8601_datetime(payload.get("modified_date"))

        
        
        self._solution = { 
            "advisory_id": advisory_id or self.advisory_id,
        }
        
        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.CommsCenterAdminApiUs1.Admin_v1_openapi.RecipientInstance {}>'.format(context)




class RecipientPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> RecipientInstance:
        """
        Build an instance of RecipientInstance

        :param payload: Payload response from the API
        """
        
        return RecipientInstance(self._version, payload, advisory_id=self._solution["advisory_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.CommsCenterAdminApiUs1.Admin_v1_openapi.RecipientPage>"




class RecipientPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> RecipientInstance:
        """
        Build an instance of RecipientInstance

        :param payload: Payload response from the API
        """
        
        return RecipientInstance(self._version, payload, advisory_id=self._solution["advisory_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.CommsCenterAdminApiUs1.Admin_v1_openapi.RecipientPage>"





class RecipientList(ListResource):
    
    def __init__(self, version: Version, advisory_id: float):
        """
        Initialize the RecipientList

        :param version: Version that contains the resource
        :param advisory_id: Advisory Id
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'advisory_id': advisory_id,  }
        self._uri = '/advisory/{advisory_id}/recipient'.format(**self._solution)
        
        
    
    def stream(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[RecipientInstance]:
        """
        Streams RecipientInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            limit=limit,
            include_accounts=include_accounts,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[RecipientInstance]:
        """
        Asynchronously streams RecipientInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            limit=limit,
            include_accounts=include_accounts,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams RecipientInstance and returns headers from first page

        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            limit=limit,
            include_accounts=include_accounts,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams RecipientInstance and returns headers from first page

        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            limit=limit,
            include_accounts=include_accounts,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[RecipientInstance]:
        """
        Lists RecipientInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            limit=limit,
            include_accounts=include_accounts,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[RecipientInstance]:
        """
        Asynchronously lists RecipientInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            limit=limit,
            include_accounts=include_accounts,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists RecipientInstance and returns headers from first page

        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            include_accounts=include_accounts,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists RecipientInstance and returns headers from first page

        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            include_accounts=include_accounts,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      limit: Union[float, object] = values.unset,
      include_accounts: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> RecipientPage:
        """
        Retrieve a single page of RecipientInstance records from the API.
        Request is executed immediately
        
        :param limit: Number of items per page
        :param include_accounts: If true, response would include advisory account data associated to each recpient
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of RecipientInstance
        """
        data = values.of({ 
            'limit': limit,
            'includeAccounts': serialize.boolean_to_string(include_accounts),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return RecipientPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      limit: Union[float, object] = values.unset,
      include_accounts: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> RecipientPage:
        """
        Asynchronously retrieve a single page of RecipientInstance records from the API.
        Request is executed immediately
        
        :param limit: Number of items per page
        :param include_accounts: If true, response would include advisory account data associated to each recpient
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of RecipientInstance
        """
        data = values.of({ 
            'limit': limit,
            'includeAccounts': serialize.boolean_to_string(include_accounts),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return RecipientPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param limit: Number of items per page
        :param include_accounts: If true, response would include advisory account data associated to each recpient
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with RecipientPage, status code, and headers
        """
        data = values.of({ 
            'limit': limit,
            'includeAccounts': serialize.boolean_to_string(include_accounts),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = RecipientPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param limit: Number of items per page
        :param include_accounts: If true, response would include advisory account data associated to each recpient
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with RecipientPage, status code, and headers
        """
        data = values.of({ 
            'limit': limit,
            'includeAccounts': serialize.boolean_to_string(include_accounts),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = RecipientPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> RecipientPage:
        """
        Retrieve a specific page of RecipientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of RecipientInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return RecipientPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> RecipientPage:
        """
        Asynchronously retrieve a specific page of RecipientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of RecipientInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return RecipientPage(self._version, response, solution=self._solution)

    
    def list(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
    ) -> List[RecipientInstance]:
        """
        Lists RecipientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
            'limit': limit,
            'includeAccounts': include_accounts,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [RecipientInstance(self._version, item, advisory_id=self._solution['advisory_id']) for item in payload["data"]]
        

    async def list_async(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
    ) -> List[RecipientInstance]:
        """
        Asynchronously lists RecipientInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
            'limit': limit,
            'includeAccounts': include_accounts,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [RecipientInstance(self._version, item, advisory_id=self._solution['advisory_id']) for item in payload["data"]]
        


    def list_with_http_info(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists RecipientInstance and returns headers from first page

        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            include_accounts=include_accounts,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists RecipientInstance and returns headers from first page

        
        :param float limit: Number of items per page
        :param bool include_accounts: If true, response would include advisory account data associated to each recpient
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            include_accounts=include_accounts,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      limit: Union[float, object] = values.unset,
      include_accounts: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> RecipientPage:
        """
        Retrieve a single page of RecipientInstance records from the API.
        Request is executed immediately
        
        :param limit: Number of items per page
        :param include_accounts: If true, response would include advisory account data associated to each recpient
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of RecipientInstance
        """
        data = values.of({ 
            'limit': limit,
            'includeAccounts': serialize.boolean_to_string(include_accounts),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return RecipientPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      limit: Union[float, object] = values.unset,
      include_accounts: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> RecipientPage:
        """
        Asynchronously retrieve a single page of RecipientInstance records from the API.
        Request is executed immediately
        
        :param limit: Number of items per page
        :param include_accounts: If true, response would include advisory account data associated to each recpient
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of RecipientInstance
        """
        data = values.of({ 
            'limit': limit,
            'includeAccounts': serialize.boolean_to_string(include_accounts),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return RecipientPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param limit: Number of items per page
        :param include_accounts: If true, response would include advisory account data associated to each recpient
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with RecipientPage, status code, and headers
        """
        data = values.of({ 
            'limit': limit,
            'includeAccounts': serialize.boolean_to_string(include_accounts),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = RecipientPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        limit: Union[float, object] = values.unset,
        include_accounts: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param limit: Number of items per page
        :param include_accounts: If true, response would include advisory account data associated to each recpient
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with RecipientPage, status code, and headers
        """
        data = values.of({ 
            'limit': limit,
            'includeAccounts': serialize.boolean_to_string(include_accounts),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = RecipientPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> RecipientPage:
        """
        Retrieve a specific page of RecipientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of RecipientInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return RecipientPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> RecipientPage:
        """
        Asynchronously retrieve a specific page of RecipientInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of RecipientInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return RecipientPage(self._version, response, solution=self._solution)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.CommsCenterAdminApiUs1.Admin_v1_openapi.RecipientList>'


