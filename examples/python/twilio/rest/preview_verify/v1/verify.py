r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Verify Passkeys API
    An API facade for the creation, storage and verification of FIDO2 (passkey) credentials. This API contains many objects referenced directly from the [Level 2 Webauthn API specification](https://www.w3.org/TR/webauthn-2/).

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class VerifyInstance(InstanceResource):


    class AllowCredentials1(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar type: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "transports": self.transports,
            }

    class AuthenticatorAssertionResponse(object):
        """
            :ivar authenticator_data: A CBOR-encoded string, in  [CTAP2 canonical CBOR encoding form](https://fidoalliance.org/specs/fido-v2.0-ps-20190130/fido-client-to-authenticator-protocol-v2.0-ps-20190130.html#ctap2-canonical-cbor-encoding-form).
            :ivar client_data_json: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar signature: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar user_handle: The user handle stored in the authenticator, specified as `user.id` in the options passed to the originating `navigator.credentials.create()` call. This property should contain a base64url-encoded entity SID.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_data: Optional[str] = payload.get("authenticatorData")
            self.client_data_json: Optional[str] = payload.get("clientDataJSON")
            self.signature: Optional[str] = payload.get("signature")
            self.user_handle: Optional[str] = payload.get("userHandle")

        def to_dict(self):
            return {
                
                    "authenticatorData": self.authenticator_data,
                    "clientDataJSON": self.client_data_json,
                    "signature": self.signature,
                    "userHandle": self.user_handle,
            }

    class AuthenticatorAttestationResponse(object):
        """
            :ivar attestation_object: A CBOR-encoded string, in  [CTAP2 canonical CBOR encoding form](https://fidoalliance.org/specs/fido-v2.0-ps-20190130/fido-client-to-authenticator-protocol-v2.0-ps-20190130.html#ctap2-canonical-cbor-encoding-form).
            :ivar client_data_json: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.attestation_object: Optional[str] = payload.get("attestationObject")
            self.client_data_json: Optional[str] = payload.get("clientDataJSON")
            self.transports: Optional[List[Enumstr]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "attestationObject": self.attestation_object,
                    "clientDataJSON": self.client_data_json,
                    "transports": self.transports,
            }

    class ChallengePublicKeyCredential(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar raw_id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar authenticator_attachment: 
            :ivar type: 
            :ivar response: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.raw_id: Optional[str] = payload.get("rawId")
            self.authenticator_attachment: Optional["VerifyInstance.AuthenticatorAttachment"] = payload.get("authenticatorAttachment")
            self.type: Optional["VerifyInstance.PublicKeyType"] = payload.get("type")
            self.response: Optional[VerifyList.AuthenticatorAssertionResponse] = payload.get("response")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "rawId": self.raw_id,
                    "authenticatorAttachment": self.authenticator_attachment,
                    "type": self.type,
                    "response": self.response.to_dict() if self.response is not None else None ,
            }

    class PasskeyFactorBindingAuthenticatorMetadata(object):
        """
            :ivar aaguid: The authenticator attestation GUID.
            :ivar sign_count: The signature counter value of the authenticator.
            :ivar clone_warning: A flag indicating that the authenticator may be cloned.
            :ivar authenticator_attachment: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.aaguid: Optional[str] = payload.get("AAGUID")
            self.sign_count: Optional[int] = payload.get("sign_count")
            self.clone_warning: Optional[bool] = payload.get("clone_warning")
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticator_attachment")

        def to_dict(self):
            return {
                
                    "AAGUID": self.aaguid,
                    "sign_count": self.sign_count,
                    "clone_warning": self.clone_warning,
                    "authenticator_attachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
            }

    class PasskeyFactorConfigRequestAuthenticatorCriteria(object):
        """
            :ivar authenticator_attachment: 
            :ivar discoverable_credentials: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional["FactorInstance.ExpandedAuthenticatorAttachment"] = payload.get("authenticator_attachment")
            self.discoverable_credentials: Optional["FactorInstance.ResidentKey"] = payload.get("discoverable_credentials")
            self.user_verification: Optional["FactorInstance.UserVerification"] = payload.get("user_verification")

        def to_dict(self):
            return {
                
                    "authenticator_attachment": self.authenticator_attachment,
                    "discoverable_credentials": self.discoverable_credentials,
                    "user_verification": self.user_verification,
            }

    class PasskeyFactorConfigRequestRelyingParty(object):
        """
            :ivar id: The relying party identifier. This should generally be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
            :ivar origins: List of Relying Party Server Origins or App IDs that are permitted.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.origins: Optional[List[str]] = payload.get("origins")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "origins": self.origins,
            }

    class PasskeyFactorPublicKeyCredential(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar raw_id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar authenticator_attachment: 
            :ivar type: 
            :ivar response: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.raw_id: Optional[str] = payload.get("rawId")
            self.authenticator_attachment: Optional["VerifyInstance.AuthenticatorAttachment"] = payload.get("authenticatorAttachment")
            self.type: Optional["VerifyInstance.PublicKeyType"] = payload.get("type")
            self.response: Optional[VerifyList.AuthenticatorAttestationResponse] = payload.get("response")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "rawId": self.raw_id,
                    "authenticatorAttachment": self.authenticator_attachment,
                    "type": self.type,
                    "response": self.response.to_dict() if self.response is not None else None ,
            }

    class PublicKeyChallengePublicKey(object):
        """
            :ivar challenge: A cryptographic challenge created by the replying party server. This value must be signed by the authenticator as part of the challenge verification flow.
            :ivar rp_id: The relying party identifier. If omitted, it will default to the current origin's domain.
            :ivar allow_credentials: An array of objects defining a restricted list of the acceptable credentials for retrieval.  If `allowCredentials` is omitted, it will default to an empty array, meaning that any credential is potentially acceptable for retrieval without the relying party first providing an ID. An empty array enables the use of discoverable credentials on the client.
            :ivar timeout: The time in milliseconds that the challenge is valid for.
            :ivar user_verification: 
            :ivar extensions: An object containing properties representing the input values for any requested [Webauthn extensions](https://developer.mozilla.org/en-US/docs/Web/API/Web_Authentication_API/WebAuthn_extensions).  These extensions are used to specific additional processing by the client or authenticator during the authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.challenge: Optional[str] = payload.get("challenge")
            self.rp_id: Optional[str] = payload.get("rpId")
            self.allow_credentials: Optional[List[AllowCredentials1]] = payload.get("allowCredentials")
            self.timeout: Optional[int] = payload.get("timeout")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")
            self.extensions: Optional[Dict[str, object]] = payload.get("extensions")

        def to_dict(self):
            return {
                
                    "challenge": self.challenge,
                    "rpId": self.rp_id,
                    "allowCredentials": [allow_credentials.to_dict() for allow_credentials in self.allow_credentials] if self.allow_credentials is not None else None,
                    "timeout": self.timeout,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
                    "extensions": self.extensions,
            }

    class PublicKeyCredentialCreationOptions(object):
        """
            :ivar rp: 
            :ivar user: 
            :ivar challenge: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar pub_key_cred_params: An array of objects which specify the key types and signature algorithms the relying party supports, ordered from most preferred to least preferred.
            :ivar timeout: A numerical hint, in milliseconds, which indicates the time the calling app is willing to wait for the creation operation to complete.
            :ivar exclude_credentials: An array of objects describing existing credentials that are already mapped to this user account (as identified by `user.id`).
            :ivar authenticator_selection: 
            :ivar attestation: A string specifying the relying party's preference for how the attestation statement (i.e., provision of verifiable evidence of the authenticity of the authenticator and its data) is conveyed during credential creation.  Attestation is not supported in the current version of the API.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rp: Optional[PublicKeyCredentialCreationOptionsRp] = payload.get("rp")
            self.user: Optional[PublicKeyCredentialCreationOptionsUser] = payload.get("user")
            self.challenge: Optional[str] = payload.get("challenge")
            self.pub_key_cred_params: Optional[List[PublicKeyCredentialCreationOptionsPubKeyCredParams]] = payload.get("pubKeyCredParams")
            self.timeout: Optional[int] = payload.get("timeout")
            self.exclude_credentials: Optional[List[PublicKeyCredentialCreationOptionsExcludeCredentials]] = payload.get("excludeCredentials")
            self.authenticator_selection: Optional[PublicKeyCredentialCreationOptionsAuthenticatorSelection] = payload.get("authenticatorSelection")
            self.attestation: Optional[str] = payload.get("attestation")

        def to_dict(self):
            return {
                
                    "rp": self.rp.to_dict() if self.rp is not None else None ,
                    "user": self.user.to_dict() if self.user is not None else None ,
                    "challenge": self.challenge,
                    "pubKeyCredParams": [pub_key_cred_params.to_dict() for pub_key_cred_params in self.pub_key_cred_params] if self.pub_key_cred_params is not None else None,
                    "timeout": self.timeout,
                    "excludeCredentials": [exclude_credentials.to_dict() for exclude_credentials in self.exclude_credentials] if self.exclude_credentials is not None else None,
                    "authenticatorSelection": self.authenticator_selection.to_dict() if self.authenticator_selection is not None else None ,
                    "attestation": self.attestation,
            }

    class PublicKeyCredentialCreationOptionsAuthenticatorSelection(object):
        """
            :ivar authenticator_attachment: 
            :ivar require_resident_key: A flag indicating that the authenticator must support the resident key requirement.
            :ivar resident_key: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticatorAttachment")
            self.require_resident_key: Optional[bool] = payload.get("requireResidentKey")
            self.resident_key: Optional[ResidentKey] = payload.get("residentKey")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")

        def to_dict(self):
            return {
                
                    "authenticatorAttachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
                    "requireResidentKey": self.require_resident_key,
                    "residentKey": self.resident_key.to_dict() if self.resident_key is not None else None ,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsExcludeCredentials(object):
        """
            :ivar type: 
            :ivar id: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.id: Optional[str] = payload.get("id")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "id": self.id,
                    "transports": self.transports,
            }

    class PublicKeyCredentialCreationOptionsPubKeyCredParams(object):
        """
            :ivar type: 
            :ivar alg: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.alg: Optional[PublicKeyAlgorithm] = payload.get("alg")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "alg": self.alg.to_dict() if self.alg is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsRp(object):
        """
            :ivar id: The relying party identifier. This should be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
            }

    class PublicKeyCredentialCreationOptionsUser(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar name: A string providing a human-friendly identifier for the user's account, to help distinguish between different accounts with similar `displayName`s.
            :ivar display_name: A string providing a human-friendly user display name.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.display_name: Optional[str] = payload.get("displayName")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "displayName": self.display_name,
            }



    class AuthenticatorAttachment(object):
        PLATFORM = "platform"
        CROSS_PLATFORM = "cross-platform"

    class ExpandedAuthenticatorAttachment(object):
        PLATFORM = "platform"
        CROSS_PLATFORM = "cross-platform"
        ANY = "any"

    class FactorType(object):
        PASSKEYS = "passkeys"

    class PublicKeyAlgorithm(object):
        _8 = "-8"
        _7 = "-7"
        _257 = "-257"

    class PublicKeyType(object):
        PUBLIC_KEY = "public-key"

    class ResidentKey(object):
        REQUIRED = "required"
        PREFERRED = "preferred"
        DISCOURAGED = "discouraged"

    class UserVerification(object):
        REQUIRED = "required"
        PREFERRED = "preferred"
        DISCOURAGED = "discouraged"

    """
    :ivar sid: 
    :ivar account_sid: 
    :ivar service_sid: 
    :ivar factor_sid: 
    :ivar factor_type: 
    :ivar entity_sid: 
    :ivar entity_identity: 
    :ivar status: 
    :ivar details: 
    :ivar date_created: 
    :ivar date_updated: 
    :ivar friendly_name: 
    :ivar binding: 
    :ivar config: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], service_sid: Optional[str] = None):
        super().__init__(version)

        
        self.sid: Optional[str] = payload.get("sid")
        self.account_sid: Optional[str] = payload.get("account_sid")
        self.service_sid: Optional[str] = payload.get("service_sid")
        self.factor_sid: Optional[str] = payload.get("factor_sid")
        self.factor_type: Optional["ChallengeInstance.str"] = payload.get("factor_type")
        self.entity_sid: Optional[str] = payload.get("entity_sid")
        self.entity_identity: Optional[str] = payload.get("entity_identity")
        self.status: Optional["ChallengeInstance.str"] = payload.get("status")
        self.details: Optional[str] = payload.get("details")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_created"))
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_updated"))
        self.friendly_name: Optional[str] = payload.get("friendly_name")
        self.binding: Optional[str] = payload.get("binding")
        self.config: Optional[str] = payload.get("config")

        
        
        self._solution = { 
            "service_sid": service_sid or self.service_sid,
        }
        
        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.PreviewVerify.V1.VerifyInstance {}>'.format(context)




class VerifyList(ListResource):
    
    class AllowCredentials1(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar type: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "transports": self.transports,
            }

    class AuthenticatorAssertionResponse(object):
        """
            :ivar authenticator_data: A CBOR-encoded string, in  [CTAP2 canonical CBOR encoding form](https://fidoalliance.org/specs/fido-v2.0-ps-20190130/fido-client-to-authenticator-protocol-v2.0-ps-20190130.html#ctap2-canonical-cbor-encoding-form).
            :ivar client_data_json: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar signature: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar user_handle: The user handle stored in the authenticator, specified as `user.id` in the options passed to the originating `navigator.credentials.create()` call. This property should contain a base64url-encoded entity SID.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_data: Optional[str] = payload.get("authenticatorData")
            self.client_data_json: Optional[str] = payload.get("clientDataJSON")
            self.signature: Optional[str] = payload.get("signature")
            self.user_handle: Optional[str] = payload.get("userHandle")

        def to_dict(self):
            return {
                
                    "authenticatorData": self.authenticator_data,
                    "clientDataJSON": self.client_data_json,
                    "signature": self.signature,
                    "userHandle": self.user_handle,
            }

    class AuthenticatorAttestationResponse(object):
        """
            :ivar attestation_object: A CBOR-encoded string, in  [CTAP2 canonical CBOR encoding form](https://fidoalliance.org/specs/fido-v2.0-ps-20190130/fido-client-to-authenticator-protocol-v2.0-ps-20190130.html#ctap2-canonical-cbor-encoding-form).
            :ivar client_data_json: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.attestation_object: Optional[str] = payload.get("attestationObject")
            self.client_data_json: Optional[str] = payload.get("clientDataJSON")
            self.transports: Optional[List[Enumstr]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "attestationObject": self.attestation_object,
                    "clientDataJSON": self.client_data_json,
                    "transports": self.transports,
            }

    class ChallengePublicKeyCredential(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar raw_id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar authenticator_attachment: 
            :ivar type: 
            :ivar response: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.raw_id: Optional[str] = payload.get("rawId")
            self.authenticator_attachment: Optional["VerifyInstance.AuthenticatorAttachment"] = payload.get("authenticatorAttachment")
            self.type: Optional["VerifyInstance.PublicKeyType"] = payload.get("type")
            self.response: Optional[VerifyList.AuthenticatorAssertionResponse] = payload.get("response")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "rawId": self.raw_id,
                    "authenticatorAttachment": self.authenticator_attachment,
                    "type": self.type,
                    "response": self.response.to_dict() if self.response is not None else None ,
            }

    class PasskeyFactorBindingAuthenticatorMetadata(object):
        """
            :ivar aaguid: The authenticator attestation GUID.
            :ivar sign_count: The signature counter value of the authenticator.
            :ivar clone_warning: A flag indicating that the authenticator may be cloned.
            :ivar authenticator_attachment: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.aaguid: Optional[str] = payload.get("AAGUID")
            self.sign_count: Optional[int] = payload.get("sign_count")
            self.clone_warning: Optional[bool] = payload.get("clone_warning")
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticator_attachment")

        def to_dict(self):
            return {
                
                    "AAGUID": self.aaguid,
                    "sign_count": self.sign_count,
                    "clone_warning": self.clone_warning,
                    "authenticator_attachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
            }

    class PasskeyFactorConfigRequestAuthenticatorCriteria(object):
        """
            :ivar authenticator_attachment: 
            :ivar discoverable_credentials: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional["FactorInstance.ExpandedAuthenticatorAttachment"] = payload.get("authenticator_attachment")
            self.discoverable_credentials: Optional["FactorInstance.ResidentKey"] = payload.get("discoverable_credentials")
            self.user_verification: Optional["FactorInstance.UserVerification"] = payload.get("user_verification")

        def to_dict(self):
            return {
                
                    "authenticator_attachment": self.authenticator_attachment,
                    "discoverable_credentials": self.discoverable_credentials,
                    "user_verification": self.user_verification,
            }

    class PasskeyFactorConfigRequestRelyingParty(object):
        """
            :ivar id: The relying party identifier. This should generally be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
            :ivar origins: List of Relying Party Server Origins or App IDs that are permitted.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.origins: Optional[List[str]] = payload.get("origins")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "origins": self.origins,
            }

    class PasskeyFactorPublicKeyCredential(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar raw_id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar authenticator_attachment: 
            :ivar type: 
            :ivar response: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.raw_id: Optional[str] = payload.get("rawId")
            self.authenticator_attachment: Optional["VerifyInstance.AuthenticatorAttachment"] = payload.get("authenticatorAttachment")
            self.type: Optional["VerifyInstance.PublicKeyType"] = payload.get("type")
            self.response: Optional[VerifyList.AuthenticatorAttestationResponse] = payload.get("response")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "rawId": self.raw_id,
                    "authenticatorAttachment": self.authenticator_attachment,
                    "type": self.type,
                    "response": self.response.to_dict() if self.response is not None else None ,
            }

    class PublicKeyChallengePublicKey(object):
        """
            :ivar challenge: A cryptographic challenge created by the replying party server. This value must be signed by the authenticator as part of the challenge verification flow.
            :ivar rp_id: The relying party identifier. If omitted, it will default to the current origin's domain.
            :ivar allow_credentials: An array of objects defining a restricted list of the acceptable credentials for retrieval.  If `allowCredentials` is omitted, it will default to an empty array, meaning that any credential is potentially acceptable for retrieval without the relying party first providing an ID. An empty array enables the use of discoverable credentials on the client.
            :ivar timeout: The time in milliseconds that the challenge is valid for.
            :ivar user_verification: 
            :ivar extensions: An object containing properties representing the input values for any requested [Webauthn extensions](https://developer.mozilla.org/en-US/docs/Web/API/Web_Authentication_API/WebAuthn_extensions).  These extensions are used to specific additional processing by the client or authenticator during the authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.challenge: Optional[str] = payload.get("challenge")
            self.rp_id: Optional[str] = payload.get("rpId")
            self.allow_credentials: Optional[List[AllowCredentials1]] = payload.get("allowCredentials")
            self.timeout: Optional[int] = payload.get("timeout")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")
            self.extensions: Optional[Dict[str, object]] = payload.get("extensions")

        def to_dict(self):
            return {
                
                    "challenge": self.challenge,
                    "rpId": self.rp_id,
                    "allowCredentials": [allow_credentials.to_dict() for allow_credentials in self.allow_credentials] if self.allow_credentials is not None else None,
                    "timeout": self.timeout,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
                    "extensions": self.extensions,
            }

    class PublicKeyCredentialCreationOptions(object):
        """
            :ivar rp: 
            :ivar user: 
            :ivar challenge: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar pub_key_cred_params: An array of objects which specify the key types and signature algorithms the relying party supports, ordered from most preferred to least preferred.
            :ivar timeout: A numerical hint, in milliseconds, which indicates the time the calling app is willing to wait for the creation operation to complete.
            :ivar exclude_credentials: An array of objects describing existing credentials that are already mapped to this user account (as identified by `user.id`).
            :ivar authenticator_selection: 
            :ivar attestation: A string specifying the relying party's preference for how the attestation statement (i.e., provision of verifiable evidence of the authenticity of the authenticator and its data) is conveyed during credential creation.  Attestation is not supported in the current version of the API.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rp: Optional[PublicKeyCredentialCreationOptionsRp] = payload.get("rp")
            self.user: Optional[PublicKeyCredentialCreationOptionsUser] = payload.get("user")
            self.challenge: Optional[str] = payload.get("challenge")
            self.pub_key_cred_params: Optional[List[PublicKeyCredentialCreationOptionsPubKeyCredParams]] = payload.get("pubKeyCredParams")
            self.timeout: Optional[int] = payload.get("timeout")
            self.exclude_credentials: Optional[List[PublicKeyCredentialCreationOptionsExcludeCredentials]] = payload.get("excludeCredentials")
            self.authenticator_selection: Optional[PublicKeyCredentialCreationOptionsAuthenticatorSelection] = payload.get("authenticatorSelection")
            self.attestation: Optional[str] = payload.get("attestation")

        def to_dict(self):
            return {
                
                    "rp": self.rp.to_dict() if self.rp is not None else None ,
                    "user": self.user.to_dict() if self.user is not None else None ,
                    "challenge": self.challenge,
                    "pubKeyCredParams": [pub_key_cred_params.to_dict() for pub_key_cred_params in self.pub_key_cred_params] if self.pub_key_cred_params is not None else None,
                    "timeout": self.timeout,
                    "excludeCredentials": [exclude_credentials.to_dict() for exclude_credentials in self.exclude_credentials] if self.exclude_credentials is not None else None,
                    "authenticatorSelection": self.authenticator_selection.to_dict() if self.authenticator_selection is not None else None ,
                    "attestation": self.attestation,
            }

    class PublicKeyCredentialCreationOptionsAuthenticatorSelection(object):
        """
            :ivar authenticator_attachment: 
            :ivar require_resident_key: A flag indicating that the authenticator must support the resident key requirement.
            :ivar resident_key: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticatorAttachment")
            self.require_resident_key: Optional[bool] = payload.get("requireResidentKey")
            self.resident_key: Optional[ResidentKey] = payload.get("residentKey")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")

        def to_dict(self):
            return {
                
                    "authenticatorAttachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
                    "requireResidentKey": self.require_resident_key,
                    "residentKey": self.resident_key.to_dict() if self.resident_key is not None else None ,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsExcludeCredentials(object):
        """
            :ivar type: 
            :ivar id: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.id: Optional[str] = payload.get("id")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "id": self.id,
                    "transports": self.transports,
            }

    class PublicKeyCredentialCreationOptionsPubKeyCredParams(object):
        """
            :ivar type: 
            :ivar alg: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.alg: Optional[PublicKeyAlgorithm] = payload.get("alg")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "alg": self.alg.to_dict() if self.alg is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsRp(object):
        """
            :ivar id: The relying party identifier. This should be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
            }

    class PublicKeyCredentialCreationOptionsUser(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar name: A string providing a human-friendly identifier for the user's account, to help distinguish between different accounts with similar `displayName`s.
            :ivar display_name: A string providing a human-friendly user display name.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.display_name: Optional[str] = payload.get("displayName")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "displayName": self.display_name,
            }


    def __init__(self, version: Version, service_sid: str):
        """
        Initialize the VerifyList

        :param version: Version that contains the resource
        :param service_sid: 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'service_sid': service_sid,  }
        self._uri = '/Services/{service_sid}/Factors/Verify'.format(**self._solution)
        
        
    
    def _create(self, challenge_public_key_credential: Union[ChallengePublicKeyCredential, object]=values.unset) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = challenge_public_key_credential.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, challenge_public_key_credential: Union[ChallengePublicKeyCredential, object]=values.unset) -> VerifyInstance:
        """
        Create the VerifyInstance

        :param challenge_public_key_credential: 
        
        :returns: The created VerifyInstance
        """
        payload, _, _ = self._create(challenge_public_key_credential=challenge_public_key_credential)
        return VerifyInstance(self._version, payload, service_sid=self._solution['service_sid'])

    def create_with_http_info(self, challenge_public_key_credential: Union[ChallengePublicKeyCredential, object]=values.unset) -> ApiResponse:
        """
        Create the VerifyInstance and return response metadata

        :param challenge_public_key_credential: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(challenge_public_key_credential=challenge_public_key_credential)
        instance = VerifyInstance(self._version, payload, service_sid=self._solution['service_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, challenge_public_key_credential: Union[ChallengePublicKeyCredential, object]=values.unset) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = challenge_public_key_credential.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, challenge_public_key_credential: Union[ChallengePublicKeyCredential, object]=values.unset) -> VerifyInstance:
        """
        Asynchronously create the VerifyInstance

        :param challenge_public_key_credential: 
        
        :returns: The created VerifyInstance
        """
        payload, _, _ = await self._create_async(challenge_public_key_credential=challenge_public_key_credential)
        return VerifyInstance(self._version, payload, service_sid=self._solution['service_sid'])

    async def create_with_http_info_async(self, challenge_public_key_credential: Union[ChallengePublicKeyCredential, object]=values.unset) -> ApiResponse:
        """
        Asynchronously create the VerifyInstance and return response metadata

        :param challenge_public_key_credential: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(challenge_public_key_credential=challenge_public_key_credential)
        instance = VerifyInstance(self._version, payload, service_sid=self._solution['service_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    

    def _update(self, passkey_factor_public_key_credential: Union[PasskeyFactorPublicKeyCredential, object]=values.unset) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = passkey_factor_public_key_credential.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.update_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def update(self, passkey_factor_public_key_credential: Union[PasskeyFactorPublicKeyCredential, object]=values.unset) -> VerifyInstance:
        """
        Update the VerifyInstance

        :param passkey_factor_public_key_credential: 
        
        :returns: The updated VerifyInstance
        """
        payload, _, _ = self._update(passkey_factor_public_key_credential=passkey_factor_public_key_credential)
        return VerifyInstance(self._version, payload, service_sid=self._solution['service_sid'])

    def update_with_http_info(self, passkey_factor_public_key_credential: Union[PasskeyFactorPublicKeyCredential, object]=values.unset) -> ApiResponse:
        """
        Update the VerifyInstance and return response metadata

        :param passkey_factor_public_key_credential: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(passkey_factor_public_key_credential=passkey_factor_public_key_credential)
        instance = VerifyInstance(self._version, payload, service_sid=self._solution['service_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, passkey_factor_public_key_credential: Union[PasskeyFactorPublicKeyCredential, object]=values.unset) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = passkey_factor_public_key_credential.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.update_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def update_async(self, passkey_factor_public_key_credential: Union[PasskeyFactorPublicKeyCredential, object]=values.unset) -> VerifyInstance:
        """
        Asynchronously update the VerifyInstance

        :param passkey_factor_public_key_credential: 
        
        :returns: The updated VerifyInstance
        """
        payload, _, _ = await self._update_async(passkey_factor_public_key_credential=passkey_factor_public_key_credential)
        return VerifyInstance(self._version, payload, service_sid=self._solution['service_sid'])

    async def update_with_http_info_async(self, passkey_factor_public_key_credential: Union[PasskeyFactorPublicKeyCredential, object]=values.unset) -> ApiResponse:
        """
        Asynchronously update the VerifyInstance and return response metadata

        :param passkey_factor_public_key_credential: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(passkey_factor_public_key_credential=passkey_factor_public_key_credential)
        instance = VerifyInstance(self._version, payload, service_sid=self._solution['service_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    def list(self, 
    ) -> List[VerifyInstance]:
        """
        Lists VerifyInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VerifyInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[VerifyInstance]:
        """
        Asynchronously lists VerifyInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VerifyInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VerifyInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VerifyInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VerifyPage:
        """
        Retrieve a single page of VerifyInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VerifyInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VerifyPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VerifyPage:
        """
        Asynchronously retrieve a single page of VerifyInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VerifyInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VerifyPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VerifyPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VerifyPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VerifyPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VerifyPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VerifyPage:
        """
        Retrieve a specific page of VerifyInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VerifyInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return VerifyPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VerifyPage:
        """
        Asynchronously retrieve a specific page of VerifyInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VerifyInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return VerifyPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[VerifyInstance]:
        """
        Lists VerifyInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VerifyInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[VerifyInstance]:
        """
        Asynchronously lists VerifyInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VerifyInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VerifyInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VerifyInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VerifyPage:
        """
        Retrieve a single page of VerifyInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VerifyInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VerifyPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VerifyPage:
        """
        Asynchronously retrieve a single page of VerifyInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VerifyInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VerifyPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VerifyPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VerifyPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VerifyPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VerifyPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VerifyPage:
        """
        Retrieve a specific page of VerifyInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VerifyInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return VerifyPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VerifyPage:
        """
        Asynchronously retrieve a specific page of VerifyInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VerifyInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return VerifyPage(self._version, response, solution=self._solution)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.PreviewVerify.V1.VerifyList>'


