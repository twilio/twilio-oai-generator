r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Verify Passkeys API
    An API facade for the creation, storage and verification of FIDO2 (passkey) credentials. This API contains many objects referenced directly from the [Level 2 Webauthn API specification](https://www.w3.org/TR/webauthn-2/).

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class ChallengeInstance(InstanceResource):


    class AllowCredentials1(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar type: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "transports": self.transports,
            }

    class CreateChallenge(object):
        """
            :ivar factor_sid: 
            :ivar entity: 
            :ivar details: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.factor_sid: Optional[str] = payload.get("factor_sid")
            self.entity: Optional[ChallengeList.EntityIdentifier] = payload.get("entity")
            self.details: Optional[ChallengeList.CreateChallengeDetails] = payload.get("details")

        def to_dict(self):
            return {
                
                    "factor_sid": self.factor_sid,
                    "entity": self.entity.to_dict() if self.entity is not None else None ,
                    "details": self.details.to_dict() if self.details is not None else None ,
            }

    class CreateChallengeDetails(object):
        """
            :ivar rp_id: The relying party identifier.
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rp_id: Optional[str] = payload.get("rpId")
            self.user_verification: Optional["ChallengeInstance.UserVerification"] = payload.get("userVerification")

        def to_dict(self):
            return {
                
                    "rpId": self.rp_id,
                    "userVerification": self.user_verification,
            }

    class EntityIdentifier(object):
        """
            :ivar sid: 
            :ivar identity: 
            :ivar display_name: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.sid: Optional[str] = payload.get("sid")
            self.identity: Optional[str] = payload.get("identity")
            self.display_name: Optional[str] = payload.get("display_name")

        def to_dict(self):
            return {
                
                    "sid": self.sid,
                    "identity": self.identity,
                    "display_name": self.display_name,
            }

    class PublicKeyChallengePublicKey(object):
        """
            :ivar challenge: A cryptographic challenge created by the replying party server. This value must be signed by the authenticator as part of the challenge verification flow.
            :ivar rp_id: The relying party identifier. If omitted, it will default to the current origin's domain.
            :ivar allow_credentials: An array of objects defining a restricted list of the acceptable credentials for retrieval.  If `allowCredentials` is omitted, it will default to an empty array, meaning that any credential is potentially acceptable for retrieval without the relying party first providing an ID. An empty array enables the use of discoverable credentials on the client.
            :ivar timeout: The time in milliseconds that the challenge is valid for.
            :ivar user_verification: 
            :ivar extensions: An object containing properties representing the input values for any requested [Webauthn extensions](https://developer.mozilla.org/en-US/docs/Web/API/Web_Authentication_API/WebAuthn_extensions).  These extensions are used to specific additional processing by the client or authenticator during the authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.challenge: Optional[str] = payload.get("challenge")
            self.rp_id: Optional[str] = payload.get("rpId")
            self.allow_credentials: Optional[List[AllowCredentials1]] = payload.get("allowCredentials")
            self.timeout: Optional[int] = payload.get("timeout")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")
            self.extensions: Optional[Dict[str, object]] = payload.get("extensions")

        def to_dict(self):
            return {
                
                    "challenge": self.challenge,
                    "rpId": self.rp_id,
                    "allowCredentials": [allow_credentials.to_dict() for allow_credentials in self.allow_credentials] if self.allow_credentials is not None else None,
                    "timeout": self.timeout,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
                    "extensions": self.extensions,
            }



    class FactorType(object):
        PASSKEYS = "passkeys"

    class PublicKeyType(object):
        PUBLIC_KEY = "public-key"

    class UserVerification(object):
        REQUIRED = "required"
        PREFERRED = "preferred"
        DISCOURAGED = "discouraged"

    """
    :ivar sid: 
    :ivar account_sid: 
    :ivar service_sid: 
    :ivar factor_sid: 
    :ivar factor_type: 
    :ivar entity_sid: 
    :ivar entity_identity: 
    :ivar status: 
    :ivar details: 
    :ivar date_created: 
    :ivar date_updated: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], service_sid: Optional[str] = None, challenge_sid: Optional[str] = None):
        super().__init__(version)

        
        self.sid: Optional[str] = payload.get("sid")
        self.account_sid: Optional[str] = payload.get("account_sid")
        self.service_sid: Optional[str] = payload.get("service_sid")
        self.factor_sid: Optional[str] = payload.get("factor_sid")
        self.factor_type: Optional["ChallengeInstance.str"] = payload.get("factor_type")
        self.entity_sid: Optional[str] = payload.get("entity_sid")
        self.entity_identity: Optional[str] = payload.get("entity_identity")
        self.status: Optional["ChallengeInstance.str"] = payload.get("status")
        self.details: Optional[str] = payload.get("details")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_created"))
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_updated"))

        
        
        self._solution = { 
            "service_sid": service_sid or self.service_sid,
            "challenge_sid": challenge_sid or self.challenge_sid,
        }
        
        
        self._context: Optional[ChallengeContext] = None

    @property
    def _proxy(self) -> "ChallengeContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: ChallengeContext for this ChallengeInstance
        """
        if self._context is None:
            self._context = ChallengeContext(self._version, service_sid=self._solution['service_sid'], challenge_sid=self._solution['challenge_sid'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the ChallengeInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the ChallengeInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the ChallengeInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the ChallengeInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    def fetch(self) -> "ChallengeInstance":
        """
        Fetch the ChallengeInstance
        

        :returns: The fetched ChallengeInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "ChallengeInstance":
        """
        Asynchronous coroutine to fetch the ChallengeInstance
        

        :returns: The fetched ChallengeInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the ChallengeInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the ChallengeInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.PreviewVerify.V1.ChallengeInstance {}>'.format(context)

class ChallengeContext(InstanceContext):


    class AllowCredentials1(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar type: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "transports": self.transports,
            }

    class CreateChallenge(object):
        """
            :ivar factor_sid: 
            :ivar entity: 
            :ivar details: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.factor_sid: Optional[str] = payload.get("factor_sid")
            self.entity: Optional[ChallengeList.EntityIdentifier] = payload.get("entity")
            self.details: Optional[ChallengeList.CreateChallengeDetails] = payload.get("details")

        def to_dict(self):
            return {
                
                    "factor_sid": self.factor_sid,
                    "entity": self.entity.to_dict() if self.entity is not None else None ,
                    "details": self.details.to_dict() if self.details is not None else None ,
            }

    class CreateChallengeDetails(object):
        """
            :ivar rp_id: The relying party identifier.
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rp_id: Optional[str] = payload.get("rpId")
            self.user_verification: Optional["ChallengeInstance.UserVerification"] = payload.get("userVerification")

        def to_dict(self):
            return {
                
                    "rpId": self.rp_id,
                    "userVerification": self.user_verification,
            }

    class EntityIdentifier(object):
        """
            :ivar sid: 
            :ivar identity: 
            :ivar display_name: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.sid: Optional[str] = payload.get("sid")
            self.identity: Optional[str] = payload.get("identity")
            self.display_name: Optional[str] = payload.get("display_name")

        def to_dict(self):
            return {
                
                    "sid": self.sid,
                    "identity": self.identity,
                    "display_name": self.display_name,
            }

    class PublicKeyChallengePublicKey(object):
        """
            :ivar challenge: A cryptographic challenge created by the replying party server. This value must be signed by the authenticator as part of the challenge verification flow.
            :ivar rp_id: The relying party identifier. If omitted, it will default to the current origin's domain.
            :ivar allow_credentials: An array of objects defining a restricted list of the acceptable credentials for retrieval.  If `allowCredentials` is omitted, it will default to an empty array, meaning that any credential is potentially acceptable for retrieval without the relying party first providing an ID. An empty array enables the use of discoverable credentials on the client.
            :ivar timeout: The time in milliseconds that the challenge is valid for.
            :ivar user_verification: 
            :ivar extensions: An object containing properties representing the input values for any requested [Webauthn extensions](https://developer.mozilla.org/en-US/docs/Web/API/Web_Authentication_API/WebAuthn_extensions).  These extensions are used to specific additional processing by the client or authenticator during the authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.challenge: Optional[str] = payload.get("challenge")
            self.rp_id: Optional[str] = payload.get("rpId")
            self.allow_credentials: Optional[List[AllowCredentials1]] = payload.get("allowCredentials")
            self.timeout: Optional[int] = payload.get("timeout")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")
            self.extensions: Optional[Dict[str, object]] = payload.get("extensions")

        def to_dict(self):
            return {
                
                    "challenge": self.challenge,
                    "rpId": self.rp_id,
                    "allowCredentials": [allow_credentials.to_dict() for allow_credentials in self.allow_credentials] if self.allow_credentials is not None else None,
                    "timeout": self.timeout,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
                    "extensions": self.extensions,
            }


    def __init__(self, version: Version, service_sid: str, challenge_sid: str):
        """
        Initialize the ChallengeContext

        :param version: Version that contains the resource
        :param service_sid: 
        :param challenge_sid: 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'service_sid': service_sid,
            'challenge_sid': challenge_sid,
        }
        self._uri = '/Services/{service_sid}/Challenges/{challenge_sid}'.format(**self._solution)
        
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the ChallengeInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the ChallengeInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the ChallengeInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the ChallengeInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers)

    def fetch(self) -> ChallengeInstance:
        """
        Fetch the ChallengeInstance
        

        :returns: The fetched ChallengeInstance
        """
        payload, _, _ = self._fetch()
        return ChallengeInstance(
            self._version,
            payload,
            service_sid=self._solution['service_sid'],
            challenge_sid=self._solution['challenge_sid'],
            
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the ChallengeInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = ChallengeInstance(
            self._version,
            payload,
            service_sid=self._solution['service_sid'],
            challenge_sid=self._solution['challenge_sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers)

    async def fetch_async(self) -> ChallengeInstance:
        """
        Asynchronous coroutine to fetch the ChallengeInstance
        

        :returns: The fetched ChallengeInstance
        """
        payload, _, _ = await self._fetch_async()
        return ChallengeInstance(
            self._version,
            payload,
            service_sid=self._solution['service_sid'],
            challenge_sid=self._solution['challenge_sid'],
            
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the ChallengeInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = ChallengeInstance(
            self._version,
            payload,
            service_sid=self._solution['service_sid'],
            challenge_sid=self._solution['challenge_sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.PreviewVerify.V1.ChallengeContext {}>'.format(context)







class ChallengePage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> ChallengeInstance:
        """
        Build an instance of ChallengeInstance

        :param payload: Payload response from the API
        """
        
        return ChallengeInstance(self._version, payload, service_sid=self._solution["service_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.PreviewVerify.V1.ChallengePage>"






class ChallengePage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> ChallengeInstance:
        """
        Build an instance of ChallengeInstance

        :param payload: Payload response from the API
        """
        
        return ChallengeInstance(self._version, payload, service_sid=self._solution["service_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.PreviewVerify.V1.ChallengePage>"




class ChallengePage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> ChallengeInstance:
        """
        Build an instance of ChallengeInstance

        :param payload: Payload response from the API
        """
        
        return ChallengeInstance(self._version, payload, service_sid=self._solution["service_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.PreviewVerify.V1.ChallengePage>"





class ChallengeList(ListResource):
    
    class AllowCredentials1(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar type: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "transports": self.transports,
            }

    class CreateChallenge(object):
        """
            :ivar factor_sid: 
            :ivar entity: 
            :ivar details: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.factor_sid: Optional[str] = payload.get("factor_sid")
            self.entity: Optional[ChallengeList.EntityIdentifier] = payload.get("entity")
            self.details: Optional[ChallengeList.CreateChallengeDetails] = payload.get("details")

        def to_dict(self):
            return {
                
                    "factor_sid": self.factor_sid,
                    "entity": self.entity.to_dict() if self.entity is not None else None ,
                    "details": self.details.to_dict() if self.details is not None else None ,
            }

    class CreateChallengeDetails(object):
        """
            :ivar rp_id: The relying party identifier.
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rp_id: Optional[str] = payload.get("rpId")
            self.user_verification: Optional["ChallengeInstance.UserVerification"] = payload.get("userVerification")

        def to_dict(self):
            return {
                
                    "rpId": self.rp_id,
                    "userVerification": self.user_verification,
            }

    class EntityIdentifier(object):
        """
            :ivar sid: 
            :ivar identity: 
            :ivar display_name: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.sid: Optional[str] = payload.get("sid")
            self.identity: Optional[str] = payload.get("identity")
            self.display_name: Optional[str] = payload.get("display_name")

        def to_dict(self):
            return {
                
                    "sid": self.sid,
                    "identity": self.identity,
                    "display_name": self.display_name,
            }

    class PublicKeyChallengePublicKey(object):
        """
            :ivar challenge: A cryptographic challenge created by the replying party server. This value must be signed by the authenticator as part of the challenge verification flow.
            :ivar rp_id: The relying party identifier. If omitted, it will default to the current origin's domain.
            :ivar allow_credentials: An array of objects defining a restricted list of the acceptable credentials for retrieval.  If `allowCredentials` is omitted, it will default to an empty array, meaning that any credential is potentially acceptable for retrieval without the relying party first providing an ID. An empty array enables the use of discoverable credentials on the client.
            :ivar timeout: The time in milliseconds that the challenge is valid for.
            :ivar user_verification: 
            :ivar extensions: An object containing properties representing the input values for any requested [Webauthn extensions](https://developer.mozilla.org/en-US/docs/Web/API/Web_Authentication_API/WebAuthn_extensions).  These extensions are used to specific additional processing by the client or authenticator during the authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.challenge: Optional[str] = payload.get("challenge")
            self.rp_id: Optional[str] = payload.get("rpId")
            self.allow_credentials: Optional[List[AllowCredentials1]] = payload.get("allowCredentials")
            self.timeout: Optional[int] = payload.get("timeout")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")
            self.extensions: Optional[Dict[str, object]] = payload.get("extensions")

        def to_dict(self):
            return {
                
                    "challenge": self.challenge,
                    "rpId": self.rp_id,
                    "allowCredentials": [allow_credentials.to_dict() for allow_credentials in self.allow_credentials] if self.allow_credentials is not None else None,
                    "timeout": self.timeout,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
                    "extensions": self.extensions,
            }


    def __init__(self, version: Version, service_sid: str):
        """
        Initialize the ChallengeList

        :param version: Version that contains the resource
        :param service_sid: 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'service_sid': service_sid,  }
        self._uri = '/Services/{service_sid}/Challenges'.format(**self._solution)
        
        
    
    
    
    def list(self, 
    ) -> List[ChallengeInstance]:
        """
        Lists ChallengeInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [ChallengeInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload["challenges"]]
        

    async def list_async(self, 
    ) -> List[ChallengeInstance]:
        """
        Asynchronously lists ChallengeInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [ChallengeInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload["challenges"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ChallengeInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ChallengeInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ChallengePage:
        """
        Retrieve a single page of ChallengeInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ChallengeInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return ChallengePage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ChallengePage:
        """
        Asynchronously retrieve a single page of ChallengeInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ChallengeInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return ChallengePage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChallengePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = ChallengePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChallengePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = ChallengePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ChallengePage:
        """
        Retrieve a specific page of ChallengeInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChallengeInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return ChallengePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ChallengePage:
        """
        Asynchronously retrieve a specific page of ChallengeInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChallengeInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return ChallengePage(self._version, response, solution=self._solution)

    
    def _create(self, create_challenge: Union[CreateChallenge, object]=values.unset) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_challenge.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, create_challenge: Union[CreateChallenge, object]=values.unset) -> ChallengeInstance:
        """
        Create the ChallengeInstance

        :param create_challenge: 
        
        :returns: The created ChallengeInstance
        """
        payload, _, _ = self._create(create_challenge=create_challenge)
        return ChallengeInstance(self._version, payload, service_sid=self._solution['service_sid'])

    def create_with_http_info(self, create_challenge: Union[CreateChallenge, object]=values.unset) -> ApiResponse:
        """
        Create the ChallengeInstance and return response metadata

        :param create_challenge: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(create_challenge=create_challenge)
        instance = ChallengeInstance(self._version, payload, service_sid=self._solution['service_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, create_challenge: Union[CreateChallenge, object]=values.unset) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_challenge.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, create_challenge: Union[CreateChallenge, object]=values.unset) -> ChallengeInstance:
        """
        Asynchronously create the ChallengeInstance

        :param create_challenge: 
        
        :returns: The created ChallengeInstance
        """
        payload, _, _ = await self._create_async(create_challenge=create_challenge)
        return ChallengeInstance(self._version, payload, service_sid=self._solution['service_sid'])

    async def create_with_http_info_async(self, create_challenge: Union[CreateChallenge, object]=values.unset) -> ApiResponse:
        """
        Asynchronously create the ChallengeInstance and return response metadata

        :param create_challenge: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(create_challenge=create_challenge)
        instance = ChallengeInstance(self._version, payload, service_sid=self._solution['service_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[ChallengeInstance]:
        """
        Streams ChallengeInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[ChallengeInstance]:
        """
        Asynchronously streams ChallengeInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams ChallengeInstance and returns headers from first page

        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams ChallengeInstance and returns headers from first page

        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ChallengeInstance]:
        """
        Lists ChallengeInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ChallengeInstance]:
        """
        Asynchronously lists ChallengeInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ChallengeInstance and returns headers from first page

        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ChallengeInstance and returns headers from first page

        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      entity_sid: Union[str, object] = values.unset,
      entity_identity: Union[str, object] = values.unset,
      
      page_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ChallengePage:
        """
        Retrieve a single page of ChallengeInstance records from the API.
        Request is executed immediately
        
        :param entity_sid: 
        :param entity_identity: 
        :param page_token: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ChallengeInstance
        """
        data = values.of({ 
            'entity_sid': entity_sid,
            'entity_identity': entity_identity,
            'page_token': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ChallengePage(self._version, response, solution=self._solution)

    async def page_async(self, 
      entity_sid: Union[str, object] = values.unset,
      entity_identity: Union[str, object] = values.unset,
      
      page_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ChallengePage:
        """
        Asynchronously retrieve a single page of ChallengeInstance records from the API.
        Request is executed immediately
        
        :param entity_sid: 
        :param entity_identity: 
        :param page_token: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ChallengeInstance
        """
        data = values.of({ 
            'entity_sid': entity_sid,
            'entity_identity': entity_identity,
            'page_token': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ChallengePage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param entity_sid: 
        :param entity_identity: 
        :param page_token: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChallengePage, status code, and headers
        """
        data = values.of({ 
            'entity_sid': entity_sid,
            'entity_identity': entity_identity,
            'page_token': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ChallengePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param entity_sid: 
        :param entity_identity: 
        :param page_token: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChallengePage, status code, and headers
        """
        data = values.of({ 
            'entity_sid': entity_sid,
            'entity_identity': entity_identity,
            'page_token': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ChallengePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ChallengePage:
        """
        Retrieve a specific page of ChallengeInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChallengeInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ChallengePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ChallengePage:
        """
        Asynchronously retrieve a specific page of ChallengeInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChallengeInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ChallengePage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[ChallengeInstance]:
        """
        Lists ChallengeInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [ChallengeInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload["challenges"]]
        

    async def list_async(self, 
    ) -> List[ChallengeInstance]:
        """
        Asynchronously lists ChallengeInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [ChallengeInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload["challenges"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ChallengeInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ChallengeInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ChallengePage:
        """
        Retrieve a single page of ChallengeInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ChallengeInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return ChallengePage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ChallengePage:
        """
        Asynchronously retrieve a single page of ChallengeInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ChallengeInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return ChallengePage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChallengePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = ChallengePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChallengePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = ChallengePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ChallengePage:
        """
        Retrieve a specific page of ChallengeInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChallengeInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return ChallengePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ChallengePage:
        """
        Asynchronously retrieve a specific page of ChallengeInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChallengeInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return ChallengePage(self._version, response, solution=self._solution)



    def get(self, service_sid: str, challenge_sid: str) -> ChallengeContext:
        """
        Constructs a ChallengeContext
        
        :param service_sid: 
        :param challenge_sid: 
        """
        return ChallengeContext(self._version, service_sid=service_sid, challenge_sid=challenge_sid)

    def __call__(self, service_sid: str, challenge_sid: str) -> ChallengeContext:
        """
        Constructs a ChallengeContext
        
        :param service_sid: 
        :param challenge_sid: 
        """
        return ChallengeContext(self._version, service_sid=service_sid, challenge_sid=challenge_sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.PreviewVerify.V1.ChallengeList>'


