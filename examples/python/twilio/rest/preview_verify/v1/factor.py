r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Verify Passkeys API
    An API facade for the creation, storage and verification of FIDO2 (passkey) credentials. This API contains many objects referenced directly from the [Level 2 Webauthn API specification](https://www.w3.org/TR/webauthn-2/).

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class FactorInstance(InstanceResource):


    class CreateFactor(object):
        """
            :ivar friendly_name: 
            :ivar factor_type: 
            :ivar entity: 
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.friendly_name: Optional[str] = payload.get("friendly_name")
            self.factor_type: Optional["FactorInstance.FactorType"] = payload.get("factor_type")
            self.entity: Optional[FactorList.EntityIdentifier] = payload.get("entity")
            self.config: Optional[FactorList.PasskeyFactorConfigRequest] = payload.get("config")

        def to_dict(self):
            return {
                
                    "friendly_name": self.friendly_name,
                    "factor_type": self.factor_type,
                    "entity": self.entity.to_dict() if self.entity is not None else None ,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class EntityIdentifier(object):
        """
            :ivar sid: 
            :ivar identity: 
            :ivar display_name: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.sid: Optional[str] = payload.get("sid")
            self.identity: Optional[str] = payload.get("identity")
            self.display_name: Optional[str] = payload.get("display_name")

        def to_dict(self):
            return {
                
                    "sid": self.sid,
                    "identity": self.identity,
                    "display_name": self.display_name,
            }

    class PasskeyFactorBindingAuthenticatorMetadata(object):
        """
            :ivar aaguid: The authenticator attestation GUID.
            :ivar sign_count: The signature counter value of the authenticator.
            :ivar clone_warning: A flag indicating that the authenticator may be cloned.
            :ivar authenticator_attachment: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.aaguid: Optional[str] = payload.get("AAGUID")
            self.sign_count: Optional[int] = payload.get("sign_count")
            self.clone_warning: Optional[bool] = payload.get("clone_warning")
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticator_attachment")

        def to_dict(self):
            return {
                
                    "AAGUID": self.aaguid,
                    "sign_count": self.sign_count,
                    "clone_warning": self.clone_warning,
                    "authenticator_attachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
            }

    class PasskeyFactorConfigRequest(object):
        """
            :ivar relying_party: 
            :ivar authenticator_criteria: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.relying_party: Optional[FactorList.PasskeyFactorConfigRequestRelyingParty] = payload.get("relying_party")
            self.authenticator_criteria: Optional[FactorList.PasskeyFactorConfigRequestAuthenticatorCriteria] = payload.get("authenticator_criteria")

        def to_dict(self):
            return {
                
                    "relying_party": self.relying_party.to_dict() if self.relying_party is not None else None ,
                    "authenticator_criteria": self.authenticator_criteria.to_dict() if self.authenticator_criteria is not None else None ,
            }

    class PasskeyFactorConfigRequestAuthenticatorCriteria(object):
        """
            :ivar authenticator_attachment: 
            :ivar discoverable_credentials: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional["FactorInstance.ExpandedAuthenticatorAttachment"] = payload.get("authenticator_attachment")
            self.discoverable_credentials: Optional["FactorInstance.ResidentKey"] = payload.get("discoverable_credentials")
            self.user_verification: Optional["FactorInstance.UserVerification"] = payload.get("user_verification")

        def to_dict(self):
            return {
                
                    "authenticator_attachment": self.authenticator_attachment,
                    "discoverable_credentials": self.discoverable_credentials,
                    "user_verification": self.user_verification,
            }

    class PasskeyFactorConfigRequestRelyingParty(object):
        """
            :ivar id: The relying party identifier. This should generally be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
            :ivar origins: List of Relying Party Server Origins or App IDs that are permitted.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.origins: Optional[List[str]] = payload.get("origins")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "origins": self.origins,
            }

    class PublicKeyCredentialCreationOptions(object):
        """
            :ivar rp: 
            :ivar user: 
            :ivar challenge: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar pub_key_cred_params: An array of objects which specify the key types and signature algorithms the relying party supports, ordered from most preferred to least preferred.
            :ivar timeout: A numerical hint, in milliseconds, which indicates the time the calling app is willing to wait for the creation operation to complete.
            :ivar exclude_credentials: An array of objects describing existing credentials that are already mapped to this user account (as identified by `user.id`).
            :ivar authenticator_selection: 
            :ivar attestation: A string specifying the relying party's preference for how the attestation statement (i.e., provision of verifiable evidence of the authenticity of the authenticator and its data) is conveyed during credential creation.  Attestation is not supported in the current version of the API.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rp: Optional[PublicKeyCredentialCreationOptionsRp] = payload.get("rp")
            self.user: Optional[PublicKeyCredentialCreationOptionsUser] = payload.get("user")
            self.challenge: Optional[str] = payload.get("challenge")
            self.pub_key_cred_params: Optional[List[PublicKeyCredentialCreationOptionsPubKeyCredParams]] = payload.get("pubKeyCredParams")
            self.timeout: Optional[int] = payload.get("timeout")
            self.exclude_credentials: Optional[List[PublicKeyCredentialCreationOptionsExcludeCredentials]] = payload.get("excludeCredentials")
            self.authenticator_selection: Optional[PublicKeyCredentialCreationOptionsAuthenticatorSelection] = payload.get("authenticatorSelection")
            self.attestation: Optional[str] = payload.get("attestation")

        def to_dict(self):
            return {
                
                    "rp": self.rp.to_dict() if self.rp is not None else None ,
                    "user": self.user.to_dict() if self.user is not None else None ,
                    "challenge": self.challenge,
                    "pubKeyCredParams": [pub_key_cred_params.to_dict() for pub_key_cred_params in self.pub_key_cred_params] if self.pub_key_cred_params is not None else None,
                    "timeout": self.timeout,
                    "excludeCredentials": [exclude_credentials.to_dict() for exclude_credentials in self.exclude_credentials] if self.exclude_credentials is not None else None,
                    "authenticatorSelection": self.authenticator_selection.to_dict() if self.authenticator_selection is not None else None ,
                    "attestation": self.attestation,
            }

    class PublicKeyCredentialCreationOptionsAuthenticatorSelection(object):
        """
            :ivar authenticator_attachment: 
            :ivar require_resident_key: A flag indicating that the authenticator must support the resident key requirement.
            :ivar resident_key: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticatorAttachment")
            self.require_resident_key: Optional[bool] = payload.get("requireResidentKey")
            self.resident_key: Optional[ResidentKey] = payload.get("residentKey")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")

        def to_dict(self):
            return {
                
                    "authenticatorAttachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
                    "requireResidentKey": self.require_resident_key,
                    "residentKey": self.resident_key.to_dict() if self.resident_key is not None else None ,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsExcludeCredentials(object):
        """
            :ivar type: 
            :ivar id: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.id: Optional[str] = payload.get("id")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "id": self.id,
                    "transports": self.transports,
            }

    class PublicKeyCredentialCreationOptionsPubKeyCredParams(object):
        """
            :ivar type: 
            :ivar alg: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.alg: Optional[PublicKeyAlgorithm] = payload.get("alg")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "alg": self.alg.to_dict() if self.alg is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsRp(object):
        """
            :ivar id: The relying party identifier. This should be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
            }

    class PublicKeyCredentialCreationOptionsUser(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar name: A string providing a human-friendly identifier for the user's account, to help distinguish between different accounts with similar `displayName`s.
            :ivar display_name: A string providing a human-friendly user display name.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.display_name: Optional[str] = payload.get("displayName")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "displayName": self.display_name,
            }



    class AuthenticatorAttachment(object):
        PLATFORM = "platform"
        CROSS_PLATFORM = "cross-platform"

    class ExpandedAuthenticatorAttachment(object):
        PLATFORM = "platform"
        CROSS_PLATFORM = "cross-platform"
        ANY = "any"

    class FactorType(object):
        PASSKEYS = "passkeys"

    class PublicKeyAlgorithm(object):
        _8 = "-8"
        _7 = "-7"
        _257 = "-257"

    class PublicKeyType(object):
        PUBLIC_KEY = "public-key"

    class ResidentKey(object):
        REQUIRED = "required"
        PREFERRED = "preferred"
        DISCOURAGED = "discouraged"

    class UserVerification(object):
        REQUIRED = "required"
        PREFERRED = "preferred"
        DISCOURAGED = "discouraged"

    """
    :ivar sid: 
    :ivar friendly_name: 
    :ivar account_sid: 
    :ivar service_sid: 
    :ivar entity_sid: 
    :ivar entity_identity: 
    :ivar factor_type: 
    :ivar status: The verification status of the factor.
    :ivar binding: 
    :ivar config: 
    :ivar date_created: 
    :ivar date_updated: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], service_sid: Optional[str] = None, factor_sid: Optional[str] = None):
        super().__init__(version)

        
        self.sid: Optional[str] = payload.get("sid")
        self.friendly_name: Optional[str] = payload.get("friendly_name")
        self.account_sid: Optional[str] = payload.get("account_sid")
        self.service_sid: Optional[str] = payload.get("service_sid")
        self.entity_sid: Optional[str] = payload.get("entity_sid")
        self.entity_identity: Optional[str] = payload.get("entity_identity")
        self.factor_type: Optional["FactorInstance.str"] = payload.get("factor_type")
        self.status: Optional["FactorInstance.str"] = payload.get("status")
        self.binding: Optional[str] = payload.get("binding")
        self.config: Optional[str] = payload.get("config")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_created"))
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_updated"))

        
        
        self._solution = { 
            "service_sid": service_sid or self.service_sid,
            "factor_sid": factor_sid or self.factor_sid,
        }
        
        
        self._context: Optional[FactorContext] = None

    @property
    def _proxy(self) -> "FactorContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: FactorContext for this FactorInstance
        """
        if self._context is None:
            self._context = FactorContext(self._version, service_sid=self._solution['service_sid'], factor_sid=self._solution['factor_sid'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the FactorInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the FactorInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the FactorInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the FactorInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    def fetch(self) -> "FactorInstance":
        """
        Fetch the FactorInstance
        

        :returns: The fetched FactorInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "FactorInstance":
        """
        Asynchronous coroutine to fetch the FactorInstance
        

        :returns: The fetched FactorInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the FactorInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the FactorInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.PreviewVerify.V1.FactorInstance {}>'.format(context)

class FactorContext(InstanceContext):


    class CreateFactor(object):
        """
            :ivar friendly_name: 
            :ivar factor_type: 
            :ivar entity: 
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.friendly_name: Optional[str] = payload.get("friendly_name")
            self.factor_type: Optional["FactorInstance.FactorType"] = payload.get("factor_type")
            self.entity: Optional[FactorList.EntityIdentifier] = payload.get("entity")
            self.config: Optional[FactorList.PasskeyFactorConfigRequest] = payload.get("config")

        def to_dict(self):
            return {
                
                    "friendly_name": self.friendly_name,
                    "factor_type": self.factor_type,
                    "entity": self.entity.to_dict() if self.entity is not None else None ,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class EntityIdentifier(object):
        """
            :ivar sid: 
            :ivar identity: 
            :ivar display_name: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.sid: Optional[str] = payload.get("sid")
            self.identity: Optional[str] = payload.get("identity")
            self.display_name: Optional[str] = payload.get("display_name")

        def to_dict(self):
            return {
                
                    "sid": self.sid,
                    "identity": self.identity,
                    "display_name": self.display_name,
            }

    class PasskeyFactorBindingAuthenticatorMetadata(object):
        """
            :ivar aaguid: The authenticator attestation GUID.
            :ivar sign_count: The signature counter value of the authenticator.
            :ivar clone_warning: A flag indicating that the authenticator may be cloned.
            :ivar authenticator_attachment: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.aaguid: Optional[str] = payload.get("AAGUID")
            self.sign_count: Optional[int] = payload.get("sign_count")
            self.clone_warning: Optional[bool] = payload.get("clone_warning")
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticator_attachment")

        def to_dict(self):
            return {
                
                    "AAGUID": self.aaguid,
                    "sign_count": self.sign_count,
                    "clone_warning": self.clone_warning,
                    "authenticator_attachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
            }

    class PasskeyFactorConfigRequest(object):
        """
            :ivar relying_party: 
            :ivar authenticator_criteria: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.relying_party: Optional[FactorList.PasskeyFactorConfigRequestRelyingParty] = payload.get("relying_party")
            self.authenticator_criteria: Optional[FactorList.PasskeyFactorConfigRequestAuthenticatorCriteria] = payload.get("authenticator_criteria")

        def to_dict(self):
            return {
                
                    "relying_party": self.relying_party.to_dict() if self.relying_party is not None else None ,
                    "authenticator_criteria": self.authenticator_criteria.to_dict() if self.authenticator_criteria is not None else None ,
            }

    class PasskeyFactorConfigRequestAuthenticatorCriteria(object):
        """
            :ivar authenticator_attachment: 
            :ivar discoverable_credentials: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional["FactorInstance.ExpandedAuthenticatorAttachment"] = payload.get("authenticator_attachment")
            self.discoverable_credentials: Optional["FactorInstance.ResidentKey"] = payload.get("discoverable_credentials")
            self.user_verification: Optional["FactorInstance.UserVerification"] = payload.get("user_verification")

        def to_dict(self):
            return {
                
                    "authenticator_attachment": self.authenticator_attachment,
                    "discoverable_credentials": self.discoverable_credentials,
                    "user_verification": self.user_verification,
            }

    class PasskeyFactorConfigRequestRelyingParty(object):
        """
            :ivar id: The relying party identifier. This should generally be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
            :ivar origins: List of Relying Party Server Origins or App IDs that are permitted.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.origins: Optional[List[str]] = payload.get("origins")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "origins": self.origins,
            }

    class PublicKeyCredentialCreationOptions(object):
        """
            :ivar rp: 
            :ivar user: 
            :ivar challenge: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar pub_key_cred_params: An array of objects which specify the key types and signature algorithms the relying party supports, ordered from most preferred to least preferred.
            :ivar timeout: A numerical hint, in milliseconds, which indicates the time the calling app is willing to wait for the creation operation to complete.
            :ivar exclude_credentials: An array of objects describing existing credentials that are already mapped to this user account (as identified by `user.id`).
            :ivar authenticator_selection: 
            :ivar attestation: A string specifying the relying party's preference for how the attestation statement (i.e., provision of verifiable evidence of the authenticity of the authenticator and its data) is conveyed during credential creation.  Attestation is not supported in the current version of the API.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rp: Optional[PublicKeyCredentialCreationOptionsRp] = payload.get("rp")
            self.user: Optional[PublicKeyCredentialCreationOptionsUser] = payload.get("user")
            self.challenge: Optional[str] = payload.get("challenge")
            self.pub_key_cred_params: Optional[List[PublicKeyCredentialCreationOptionsPubKeyCredParams]] = payload.get("pubKeyCredParams")
            self.timeout: Optional[int] = payload.get("timeout")
            self.exclude_credentials: Optional[List[PublicKeyCredentialCreationOptionsExcludeCredentials]] = payload.get("excludeCredentials")
            self.authenticator_selection: Optional[PublicKeyCredentialCreationOptionsAuthenticatorSelection] = payload.get("authenticatorSelection")
            self.attestation: Optional[str] = payload.get("attestation")

        def to_dict(self):
            return {
                
                    "rp": self.rp.to_dict() if self.rp is not None else None ,
                    "user": self.user.to_dict() if self.user is not None else None ,
                    "challenge": self.challenge,
                    "pubKeyCredParams": [pub_key_cred_params.to_dict() for pub_key_cred_params in self.pub_key_cred_params] if self.pub_key_cred_params is not None else None,
                    "timeout": self.timeout,
                    "excludeCredentials": [exclude_credentials.to_dict() for exclude_credentials in self.exclude_credentials] if self.exclude_credentials is not None else None,
                    "authenticatorSelection": self.authenticator_selection.to_dict() if self.authenticator_selection is not None else None ,
                    "attestation": self.attestation,
            }

    class PublicKeyCredentialCreationOptionsAuthenticatorSelection(object):
        """
            :ivar authenticator_attachment: 
            :ivar require_resident_key: A flag indicating that the authenticator must support the resident key requirement.
            :ivar resident_key: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticatorAttachment")
            self.require_resident_key: Optional[bool] = payload.get("requireResidentKey")
            self.resident_key: Optional[ResidentKey] = payload.get("residentKey")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")

        def to_dict(self):
            return {
                
                    "authenticatorAttachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
                    "requireResidentKey": self.require_resident_key,
                    "residentKey": self.resident_key.to_dict() if self.resident_key is not None else None ,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsExcludeCredentials(object):
        """
            :ivar type: 
            :ivar id: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.id: Optional[str] = payload.get("id")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "id": self.id,
                    "transports": self.transports,
            }

    class PublicKeyCredentialCreationOptionsPubKeyCredParams(object):
        """
            :ivar type: 
            :ivar alg: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.alg: Optional[PublicKeyAlgorithm] = payload.get("alg")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "alg": self.alg.to_dict() if self.alg is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsRp(object):
        """
            :ivar id: The relying party identifier. This should be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
            }

    class PublicKeyCredentialCreationOptionsUser(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar name: A string providing a human-friendly identifier for the user's account, to help distinguish between different accounts with similar `displayName`s.
            :ivar display_name: A string providing a human-friendly user display name.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.display_name: Optional[str] = payload.get("displayName")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "displayName": self.display_name,
            }


    def __init__(self, version: Version, service_sid: str, factor_sid: str):
        """
        Initialize the FactorContext

        :param version: Version that contains the resource
        :param service_sid: 
        :param factor_sid: 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'service_sid': service_sid,
            'factor_sid': factor_sid,
        }
        self._uri = '/Services/{service_sid}/Factors/{factor_sid}'.format(**self._solution)
        
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the FactorInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the FactorInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the FactorInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the FactorInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers)

    def fetch(self) -> FactorInstance:
        """
        Fetch the FactorInstance
        

        :returns: The fetched FactorInstance
        """
        payload, _, _ = self._fetch()
        return FactorInstance(
            self._version,
            payload,
            service_sid=self._solution['service_sid'],
            factor_sid=self._solution['factor_sid'],
            
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the FactorInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = FactorInstance(
            self._version,
            payload,
            service_sid=self._solution['service_sid'],
            factor_sid=self._solution['factor_sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers)

    async def fetch_async(self) -> FactorInstance:
        """
        Asynchronous coroutine to fetch the FactorInstance
        

        :returns: The fetched FactorInstance
        """
        payload, _, _ = await self._fetch_async()
        return FactorInstance(
            self._version,
            payload,
            service_sid=self._solution['service_sid'],
            factor_sid=self._solution['factor_sid'],
            
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the FactorInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = FactorInstance(
            self._version,
            payload,
            service_sid=self._solution['service_sid'],
            factor_sid=self._solution['factor_sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.PreviewVerify.V1.FactorContext {}>'.format(context)







class FactorPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> FactorInstance:
        """
        Build an instance of FactorInstance

        :param payload: Payload response from the API
        """
        
        return FactorInstance(self._version, payload, service_sid=self._solution["service_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.PreviewVerify.V1.FactorPage>"






class FactorPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> FactorInstance:
        """
        Build an instance of FactorInstance

        :param payload: Payload response from the API
        """
        
        return FactorInstance(self._version, payload, service_sid=self._solution["service_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.PreviewVerify.V1.FactorPage>"




class FactorPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> FactorInstance:
        """
        Build an instance of FactorInstance

        :param payload: Payload response from the API
        """
        
        return FactorInstance(self._version, payload, service_sid=self._solution["service_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.PreviewVerify.V1.FactorPage>"





class FactorList(ListResource):
    
    class CreateFactor(object):
        """
            :ivar friendly_name: 
            :ivar factor_type: 
            :ivar entity: 
            :ivar config: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.friendly_name: Optional[str] = payload.get("friendly_name")
            self.factor_type: Optional["FactorInstance.FactorType"] = payload.get("factor_type")
            self.entity: Optional[FactorList.EntityIdentifier] = payload.get("entity")
            self.config: Optional[FactorList.PasskeyFactorConfigRequest] = payload.get("config")

        def to_dict(self):
            return {
                
                    "friendly_name": self.friendly_name,
                    "factor_type": self.factor_type,
                    "entity": self.entity.to_dict() if self.entity is not None else None ,
                    "config": self.config.to_dict() if self.config is not None else None ,
            }

    class EntityIdentifier(object):
        """
            :ivar sid: 
            :ivar identity: 
            :ivar display_name: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.sid: Optional[str] = payload.get("sid")
            self.identity: Optional[str] = payload.get("identity")
            self.display_name: Optional[str] = payload.get("display_name")

        def to_dict(self):
            return {
                
                    "sid": self.sid,
                    "identity": self.identity,
                    "display_name": self.display_name,
            }

    class PasskeyFactorBindingAuthenticatorMetadata(object):
        """
            :ivar aaguid: The authenticator attestation GUID.
            :ivar sign_count: The signature counter value of the authenticator.
            :ivar clone_warning: A flag indicating that the authenticator may be cloned.
            :ivar authenticator_attachment: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.aaguid: Optional[str] = payload.get("AAGUID")
            self.sign_count: Optional[int] = payload.get("sign_count")
            self.clone_warning: Optional[bool] = payload.get("clone_warning")
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticator_attachment")

        def to_dict(self):
            return {
                
                    "AAGUID": self.aaguid,
                    "sign_count": self.sign_count,
                    "clone_warning": self.clone_warning,
                    "authenticator_attachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
            }

    class PasskeyFactorConfigRequest(object):
        """
            :ivar relying_party: 
            :ivar authenticator_criteria: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.relying_party: Optional[FactorList.PasskeyFactorConfigRequestRelyingParty] = payload.get("relying_party")
            self.authenticator_criteria: Optional[FactorList.PasskeyFactorConfigRequestAuthenticatorCriteria] = payload.get("authenticator_criteria")

        def to_dict(self):
            return {
                
                    "relying_party": self.relying_party.to_dict() if self.relying_party is not None else None ,
                    "authenticator_criteria": self.authenticator_criteria.to_dict() if self.authenticator_criteria is not None else None ,
            }

    class PasskeyFactorConfigRequestAuthenticatorCriteria(object):
        """
            :ivar authenticator_attachment: 
            :ivar discoverable_credentials: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional["FactorInstance.ExpandedAuthenticatorAttachment"] = payload.get("authenticator_attachment")
            self.discoverable_credentials: Optional["FactorInstance.ResidentKey"] = payload.get("discoverable_credentials")
            self.user_verification: Optional["FactorInstance.UserVerification"] = payload.get("user_verification")

        def to_dict(self):
            return {
                
                    "authenticator_attachment": self.authenticator_attachment,
                    "discoverable_credentials": self.discoverable_credentials,
                    "user_verification": self.user_verification,
            }

    class PasskeyFactorConfigRequestRelyingParty(object):
        """
            :ivar id: The relying party identifier. This should generally be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
            :ivar origins: List of Relying Party Server Origins or App IDs that are permitted.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.origins: Optional[List[str]] = payload.get("origins")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "origins": self.origins,
            }

    class PublicKeyCredentialCreationOptions(object):
        """
            :ivar rp: 
            :ivar user: 
            :ivar challenge: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar pub_key_cred_params: An array of objects which specify the key types and signature algorithms the relying party supports, ordered from most preferred to least preferred.
            :ivar timeout: A numerical hint, in milliseconds, which indicates the time the calling app is willing to wait for the creation operation to complete.
            :ivar exclude_credentials: An array of objects describing existing credentials that are already mapped to this user account (as identified by `user.id`).
            :ivar authenticator_selection: 
            :ivar attestation: A string specifying the relying party's preference for how the attestation statement (i.e., provision of verifiable evidence of the authenticity of the authenticator and its data) is conveyed during credential creation.  Attestation is not supported in the current version of the API.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rp: Optional[PublicKeyCredentialCreationOptionsRp] = payload.get("rp")
            self.user: Optional[PublicKeyCredentialCreationOptionsUser] = payload.get("user")
            self.challenge: Optional[str] = payload.get("challenge")
            self.pub_key_cred_params: Optional[List[PublicKeyCredentialCreationOptionsPubKeyCredParams]] = payload.get("pubKeyCredParams")
            self.timeout: Optional[int] = payload.get("timeout")
            self.exclude_credentials: Optional[List[PublicKeyCredentialCreationOptionsExcludeCredentials]] = payload.get("excludeCredentials")
            self.authenticator_selection: Optional[PublicKeyCredentialCreationOptionsAuthenticatorSelection] = payload.get("authenticatorSelection")
            self.attestation: Optional[str] = payload.get("attestation")

        def to_dict(self):
            return {
                
                    "rp": self.rp.to_dict() if self.rp is not None else None ,
                    "user": self.user.to_dict() if self.user is not None else None ,
                    "challenge": self.challenge,
                    "pubKeyCredParams": [pub_key_cred_params.to_dict() for pub_key_cred_params in self.pub_key_cred_params] if self.pub_key_cred_params is not None else None,
                    "timeout": self.timeout,
                    "excludeCredentials": [exclude_credentials.to_dict() for exclude_credentials in self.exclude_credentials] if self.exclude_credentials is not None else None,
                    "authenticatorSelection": self.authenticator_selection.to_dict() if self.authenticator_selection is not None else None ,
                    "attestation": self.attestation,
            }

    class PublicKeyCredentialCreationOptionsAuthenticatorSelection(object):
        """
            :ivar authenticator_attachment: 
            :ivar require_resident_key: A flag indicating that the authenticator must support the resident key requirement.
            :ivar resident_key: 
            :ivar user_verification: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.authenticator_attachment: Optional[AuthenticatorAttachment] = payload.get("authenticatorAttachment")
            self.require_resident_key: Optional[bool] = payload.get("requireResidentKey")
            self.resident_key: Optional[ResidentKey] = payload.get("residentKey")
            self.user_verification: Optional[UserVerification] = payload.get("userVerification")

        def to_dict(self):
            return {
                
                    "authenticatorAttachment": self.authenticator_attachment.to_dict() if self.authenticator_attachment is not None else None ,
                    "requireResidentKey": self.require_resident_key,
                    "residentKey": self.resident_key.to_dict() if self.resident_key is not None else None ,
                    "userVerification": self.user_verification.to_dict() if self.user_verification is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsExcludeCredentials(object):
        """
            :ivar type: 
            :ivar id: 
            :ivar transports: An array of strings providing hints as to the methods the client could use to communicate with the relevant  authenticator of the public key credential to retrieve.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.id: Optional[str] = payload.get("id")
            self.transports: Optional[List[str]] = payload.get("transports")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "id": self.id,
                    "transports": self.transports,
            }

    class PublicKeyCredentialCreationOptionsPubKeyCredParams(object):
        """
            :ivar type: 
            :ivar alg: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional[PublicKeyType] = payload.get("type")
            self.alg: Optional[PublicKeyAlgorithm] = payload.get("alg")

        def to_dict(self):
            return {
                
                    "type": self.type.to_dict() if self.type is not None else None ,
                    "alg": self.alg.to_dict() if self.alg is not None else None ,
            }

    class PublicKeyCredentialCreationOptionsRp(object):
        """
            :ivar id: The relying party identifier. This should be the origin without a scheme and port.
            :ivar name: The relying party name that the authenticator will show during the registration/authentication process.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
            }

    class PublicKeyCredentialCreationOptionsUser(object):
        """
            :ivar id: A [base64url](https://base64.guru/standards/base64url)-encoded string.
            :ivar name: A string providing a human-friendly identifier for the user's account, to help distinguish between different accounts with similar `displayName`s.
            :ivar display_name: A string providing a human-friendly user display name.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.display_name: Optional[str] = payload.get("displayName")

        def to_dict(self):
            return {
                
                    "id": self.id,
                    "name": self.name,
                    "displayName": self.display_name,
            }


    def __init__(self, version: Version, service_sid: str):
        """
        Initialize the FactorList

        :param version: Version that contains the resource
        :param service_sid: 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'service_sid': service_sid,  }
        self._uri = '/Services/{service_sid}/Factors'.format(**self._solution)
        
        
    
    
    
    def list(self, 
    ) -> List[FactorInstance]:
        """
        Lists FactorInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [FactorInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload["factors"]]
        

    async def list_async(self, 
    ) -> List[FactorInstance]:
        """
        Asynchronously lists FactorInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [FactorInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload["factors"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists FactorInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists FactorInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> FactorPage:
        """
        Retrieve a single page of FactorInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of FactorInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return FactorPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> FactorPage:
        """
        Asynchronously retrieve a single page of FactorInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of FactorInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return FactorPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with FactorPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = FactorPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with FactorPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = FactorPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> FactorPage:
        """
        Retrieve a specific page of FactorInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of FactorInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return FactorPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> FactorPage:
        """
        Asynchronously retrieve a specific page of FactorInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of FactorInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return FactorPage(self._version, response, solution=self._solution)

    
    def _create(self, create_factor: Union[CreateFactor, object]=values.unset) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_factor.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, create_factor: Union[CreateFactor, object]=values.unset) -> FactorInstance:
        """
        Create the FactorInstance

        :param create_factor: 
        
        :returns: The created FactorInstance
        """
        payload, _, _ = self._create(create_factor=create_factor)
        return FactorInstance(self._version, payload, service_sid=self._solution['service_sid'])

    def create_with_http_info(self, create_factor: Union[CreateFactor, object]=values.unset) -> ApiResponse:
        """
        Create the FactorInstance and return response metadata

        :param create_factor: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(create_factor=create_factor)
        instance = FactorInstance(self._version, payload, service_sid=self._solution['service_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, create_factor: Union[CreateFactor, object]=values.unset) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_factor.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, create_factor: Union[CreateFactor, object]=values.unset) -> FactorInstance:
        """
        Asynchronously create the FactorInstance

        :param create_factor: 
        
        :returns: The created FactorInstance
        """
        payload, _, _ = await self._create_async(create_factor=create_factor)
        return FactorInstance(self._version, payload, service_sid=self._solution['service_sid'])

    async def create_with_http_info_async(self, create_factor: Union[CreateFactor, object]=values.unset) -> ApiResponse:
        """
        Asynchronously create the FactorInstance and return response metadata

        :param create_factor: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(create_factor=create_factor)
        instance = FactorInstance(self._version, payload, service_sid=self._solution['service_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[FactorInstance]:
        """
        Streams FactorInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[FactorInstance]:
        """
        Asynchronously streams FactorInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams FactorInstance and returns headers from first page

        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams FactorInstance and returns headers from first page

        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[FactorInstance]:
        """
        Lists FactorInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[FactorInstance]:
        """
        Asynchronously lists FactorInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists FactorInstance and returns headers from first page

        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists FactorInstance and returns headers from first page

        
        :param str entity_sid: 
        :param str entity_identity: 
        :param str page_token: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            entity_sid=entity_sid,
            entity_identity=entity_identity,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      entity_sid: Union[str, object] = values.unset,
      entity_identity: Union[str, object] = values.unset,
      
      page_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> FactorPage:
        """
        Retrieve a single page of FactorInstance records from the API.
        Request is executed immediately
        
        :param entity_sid: 
        :param entity_identity: 
        :param page_token: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of FactorInstance
        """
        data = values.of({ 
            'entity_sid': entity_sid,
            'entity_identity': entity_identity,
            'page_token': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return FactorPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      entity_sid: Union[str, object] = values.unset,
      entity_identity: Union[str, object] = values.unset,
      
      page_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> FactorPage:
        """
        Asynchronously retrieve a single page of FactorInstance records from the API.
        Request is executed immediately
        
        :param entity_sid: 
        :param entity_identity: 
        :param page_token: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of FactorInstance
        """
        data = values.of({ 
            'entity_sid': entity_sid,
            'entity_identity': entity_identity,
            'page_token': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return FactorPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param entity_sid: 
        :param entity_identity: 
        :param page_token: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with FactorPage, status code, and headers
        """
        data = values.of({ 
            'entity_sid': entity_sid,
            'entity_identity': entity_identity,
            'page_token': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = FactorPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        entity_sid: Union[str, object] = values.unset,
        entity_identity: Union[str, object] = values.unset,
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param entity_sid: 
        :param entity_identity: 
        :param page_token: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with FactorPage, status code, and headers
        """
        data = values.of({ 
            'entity_sid': entity_sid,
            'entity_identity': entity_identity,
            'page_token': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = FactorPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> FactorPage:
        """
        Retrieve a specific page of FactorInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of FactorInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return FactorPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> FactorPage:
        """
        Asynchronously retrieve a specific page of FactorInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of FactorInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return FactorPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[FactorInstance]:
        """
        Lists FactorInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [FactorInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload["factors"]]
        

    async def list_async(self, 
    ) -> List[FactorInstance]:
        """
        Asynchronously lists FactorInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [FactorInstance(self._version, item, service_sid=self._solution['service_sid']) for item in payload["factors"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists FactorInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists FactorInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> FactorPage:
        """
        Retrieve a single page of FactorInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of FactorInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return FactorPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> FactorPage:
        """
        Asynchronously retrieve a single page of FactorInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of FactorInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return FactorPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with FactorPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = FactorPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with FactorPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = FactorPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> FactorPage:
        """
        Retrieve a specific page of FactorInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of FactorInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return FactorPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> FactorPage:
        """
        Asynchronously retrieve a specific page of FactorInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of FactorInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return FactorPage(self._version, response, solution=self._solution)



    def get(self, service_sid: str, factor_sid: str) -> FactorContext:
        """
        Constructs a FactorContext
        
        :param service_sid: 
        :param factor_sid: 
        """
        return FactorContext(self._version, service_sid=service_sid, factor_sid=factor_sid)

    def __call__(self, service_sid: str, factor_sid: str) -> FactorContext:
        """
        Constructs a FactorContext
        
        :param service_sid: 
        :param factor_sid: 
        """
        return FactorContext(self._version, service_sid=service_sid, factor_sid=factor_sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.PreviewVerify.V1.FactorList>'


