r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    User OAuth API
    User OAuth API

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class HeartbeatInstance(InstanceResource):



    """
    :ivar is_valid: True if session is valid, false if expired or invalid
    :ivar expires_at: datetime string when the session expires, only present if isValid is true
    :ivar last_active_at: datetime string when the session was refreshed, only present if isValid is true
    """

    def __init__(self, version: Version, payload:Dict[str, Any]):
        super().__init__(version)

        
        self.is_valid: Optional[bool] = payload.get("isValid")
        self.expires_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("expiresAt"))
        self.last_active_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("lastActiveAt"))

        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        
        return '<Twilio.Www.Users_manisingh_codehq_open_api_spec_www_v2_openapi_yaml.HeartbeatInstance>'




class HeartbeatList(ListResource):
    
    def __init__(self, version: Version):
        """
        Initialize the HeartbeatList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/user/heartbeat'
        
        
    
    def list(self, 
        force_refresh: Union[bool, object] = values.unset,
        origin: Union[str, object] = values.unset,
        tw_auth0_combined: Union[str, object] = values.unset,
        server_identity: Union[str, object] = values.unset,
    ) -> List[HeartbeatInstance]:
        """
        Lists HeartbeatInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'forceRefresh': force_refresh,
            'Origin': origin,
            'tw-auth0-combined': tw_auth0_combined,
            'server-identity': server_identity,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [HeartbeatInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        force_refresh: Union[bool, object] = values.unset,
        origin: Union[str, object] = values.unset,
        tw_auth0_combined: Union[str, object] = values.unset,
        server_identity: Union[str, object] = values.unset,
    ) -> List[HeartbeatInstance]:
        """
        Asynchronously lists HeartbeatInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'forceRefresh': force_refresh,
            'Origin': origin,
            'tw-auth0-combined': tw_auth0_combined,
            'server-identity': server_identity,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [HeartbeatInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        force_refresh: Union[bool, object] = values.unset,
        origin: Union[str, object] = values.unset,
        tw_auth0_combined: Union[str, object] = values.unset,
        server_identity: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists HeartbeatInstance and returns headers from first page

        
        :param bool force_refresh: Optional param to check if refreshToken is valid by exchanging it with Auth0 instead of just checking the cookie
        :param str origin: 
        :param str tw_auth0_combined: Encrypted cookie containing the refresh token
        :param str server_identity: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            force_refresh=force_refresh,
            origin=origin,
            tw_auth0_combined=tw_auth0_combined,
            server_identity=server_identity,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        force_refresh: Union[bool, object] = values.unset,
        origin: Union[str, object] = values.unset,
        tw_auth0_combined: Union[str, object] = values.unset,
        server_identity: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists HeartbeatInstance and returns headers from first page

        
        :param bool force_refresh: Optional param to check if refreshToken is valid by exchanging it with Auth0 instead of just checking the cookie
        :param str origin: 
        :param str tw_auth0_combined: Encrypted cookie containing the refresh token
        :param str server_identity: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            force_refresh=force_refresh,
            origin=origin,
            tw_auth0_combined=tw_auth0_combined,
            server_identity=server_identity,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      force_refresh: Union[bool, object] = values.unset,
      origin: Union[str, object] = values.unset,
      tw_auth0_combined: Union[str, object] = values.unset,
      server_identity: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> HeartbeatPage:
        """
        Retrieve a single page of HeartbeatInstance records from the API.
        Request is executed immediately
        
        :param force_refresh: Optional param to check if refreshToken is valid by exchanging it with Auth0 instead of just checking the cookie
        :param origin: 
        :param tw_auth0_combined: Encrypted cookie containing the refresh token
        :param server_identity: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of HeartbeatInstance
        """
        data = values.of({ 
            'forceRefresh': serialize.boolean_to_string(force_refresh),
            'Origin': origin,
            'tw-auth0-combined': tw_auth0_combined,
            'server-identity': server_identity,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Origin': origin,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return HeartbeatPage(self._version, response)

    async def page_async(self, 
      force_refresh: Union[bool, object] = values.unset,
      origin: Union[str, object] = values.unset,
      tw_auth0_combined: Union[str, object] = values.unset,
      server_identity: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> HeartbeatPage:
        """
        Asynchronously retrieve a single page of HeartbeatInstance records from the API.
        Request is executed immediately
        
        :param force_refresh: Optional param to check if refreshToken is valid by exchanging it with Auth0 instead of just checking the cookie
        :param origin: 
        :param tw_auth0_combined: Encrypted cookie containing the refresh token
        :param server_identity: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of HeartbeatInstance
        """
        data = values.of({ 
            'forceRefresh': serialize.boolean_to_string(force_refresh),
            'Origin': origin,
            'tw-auth0-combined': tw_auth0_combined,
            'server-identity': server_identity,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Origin': origin,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return HeartbeatPage(self._version, response)

    def page_with_http_info(self, 
        force_refresh: Union[bool, object] = values.unset,
        origin: Union[str, object] = values.unset,
        tw_auth0_combined: Union[str, object] = values.unset,
        server_identity: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param force_refresh: Optional param to check if refreshToken is valid by exchanging it with Auth0 instead of just checking the cookie
        :param origin: 
        :param tw_auth0_combined: Encrypted cookie containing the refresh token
        :param server_identity: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with HeartbeatPage, status code, and headers
        """
        data = values.of({ 
            'forceRefresh': serialize.boolean_to_string(force_refresh),
            'Origin': origin,
            'tw-auth0-combined': tw_auth0_combined,
            'server-identity': server_identity,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Origin': origin,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = HeartbeatPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        force_refresh: Union[bool, object] = values.unset,
        origin: Union[str, object] = values.unset,
        tw_auth0_combined: Union[str, object] = values.unset,
        server_identity: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param force_refresh: Optional param to check if refreshToken is valid by exchanging it with Auth0 instead of just checking the cookie
        :param origin: 
        :param tw_auth0_combined: Encrypted cookie containing the refresh token
        :param server_identity: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with HeartbeatPage, status code, and headers
        """
        data = values.of({ 
            'forceRefresh': serialize.boolean_to_string(force_refresh),
            'Origin': origin,
            'tw-auth0-combined': tw_auth0_combined,
            'server-identity': server_identity,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Origin': origin,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = HeartbeatPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> HeartbeatPage:
        """
        Retrieve a specific page of HeartbeatInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of HeartbeatInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return HeartbeatPage(self._version, response)

    async def get_page_async(self, target_url: str) -> HeartbeatPage:
        """
        Asynchronously retrieve a specific page of HeartbeatInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of HeartbeatInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return HeartbeatPage(self._version, response)

    
    def list(self, 
        origin: Union[str, object] = values.unset,
    ) -> List[HeartbeatInstance]:
        """
        Lists HeartbeatInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
            'Origin': origin,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [HeartbeatInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        origin: Union[str, object] = values.unset,
    ) -> List[HeartbeatInstance]:
        """
        Asynchronously lists HeartbeatInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
            'Origin': origin,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [HeartbeatInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        origin: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists HeartbeatInstance and returns headers from first page

        
        :param str origin: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            origin=origin,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        origin: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists HeartbeatInstance and returns headers from first page

        
        :param str origin: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            origin=origin,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      origin: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> HeartbeatPage:
        """
        Retrieve a single page of HeartbeatInstance records from the API.
        Request is executed immediately
        
        :param origin: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of HeartbeatInstance
        """
        data = values.of({ 
            'Origin': origin,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Origin': origin,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return HeartbeatPage(self._version, response)

    async def page_async(self, 
      origin: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> HeartbeatPage:
        """
        Asynchronously retrieve a single page of HeartbeatInstance records from the API.
        Request is executed immediately
        
        :param origin: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of HeartbeatInstance
        """
        data = values.of({ 
            'Origin': origin,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Origin': origin,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return HeartbeatPage(self._version, response)

    def page_with_http_info(self, 
        origin: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param origin: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with HeartbeatPage, status code, and headers
        """
        data = values.of({ 
            'Origin': origin,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Origin': origin,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = HeartbeatPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        origin: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param origin: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with HeartbeatPage, status code, and headers
        """
        data = values.of({ 
            'Origin': origin,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Origin': origin,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = HeartbeatPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> HeartbeatPage:
        """
        Retrieve a specific page of HeartbeatInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of HeartbeatInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return HeartbeatPage(self._version, response)

    async def get_page_async(self, target_url: str) -> HeartbeatPage:
        """
        Asynchronously retrieve a specific page of HeartbeatInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of HeartbeatInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return HeartbeatPage(self._version, response)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Www.Users_manisingh_codehq_open_api_spec_www_v2_openapi_yaml.HeartbeatList>'


