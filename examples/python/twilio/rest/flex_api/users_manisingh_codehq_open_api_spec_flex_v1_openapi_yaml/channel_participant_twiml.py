r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Flex
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class ChannelParticipantTwimlInstance(InstanceResource):



    """
    :ivar channel_sid: 
    :ivar participant_sid: 
    :ivar sid: 
    :ivar url: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], channel_sid: Optional[str] = None, participant_sid: Optional[str] = None):
        super().__init__(version)

        
        self.channel_sid: Optional[str] = payload.get("channel_sid")
        self.participant_sid: Optional[str] = payload.get("participant_sid")
        self.sid: Optional[str] = payload.get("sid")
        self.url: Optional[str] = payload.get("url")

        
        
        self._solution = { 
            "channel_sid": channel_sid or self.channel_sid,
            "participant_sid": participant_sid or self.participant_sid,
        }
        
        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.FlexApi.Users_manisingh_codehq_open_api_spec_flex_v1_openapi_yaml.ChannelParticipantTwimlInstance {}>'.format(context)




class ChannelParticipantTwimlList(ListResource):
    
    def __init__(self, version: Version, channel_sid: str, participant_sid: str):
        """
        Initialize the ChannelParticipantTwimlList

        :param version: Version that contains the resource
        :param channel_sid: 
        :param participant_sid: 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'channel_sid': channel_sid, 'participant_sid': participant_sid,  }
        self._uri = '/Channels/{channel_sid}/Participants/{participant_sid}/TwiML'.format(**self._solution)
        
        
    
    def list(self, 
    ) -> List[ChannelParticipantTwimlInstance]:
        """
        Lists ChannelParticipantTwimlInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [ChannelParticipantTwimlInstance(self._version, item, channel_sid=self._solution['channel_sid'], participant_sid=self._solution['participant_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[ChannelParticipantTwimlInstance]:
        """
        Asynchronously lists ChannelParticipantTwimlInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [ChannelParticipantTwimlInstance(self._version, item, channel_sid=self._solution['channel_sid'], participant_sid=self._solution['participant_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ChannelParticipantTwimlInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ChannelParticipantTwimlInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ChannelParticipantTwimlPage:
        """
        Retrieve a single page of ChannelParticipantTwimlInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ChannelParticipantTwimlInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return ChannelParticipantTwimlPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ChannelParticipantTwimlPage:
        """
        Asynchronously retrieve a single page of ChannelParticipantTwimlInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of ChannelParticipantTwimlInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return ChannelParticipantTwimlPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChannelParticipantTwimlPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = ChannelParticipantTwimlPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ChannelParticipantTwimlPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = ChannelParticipantTwimlPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ChannelParticipantTwimlPage:
        """
        Retrieve a specific page of ChannelParticipantTwimlInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChannelParticipantTwimlInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return ChannelParticipantTwimlPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ChannelParticipantTwimlPage:
        """
        Asynchronously retrieve a specific page of ChannelParticipantTwimlInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ChannelParticipantTwimlInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return ChannelParticipantTwimlPage(self._version, response, solution=self._solution)

    

    def _update(self, sid: str, parameters: Union[object, object]=values.unset) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'Sid': sid,
            'Parameters': serialize.object(parameters),
        })
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.update_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def update(self, sid: str, parameters: Union[object, object]=values.unset) -> ChannelParticipantTwimlInstance:
        """
        Update the ChannelParticipantTwimlInstance

        :param sid: 
        :param parameters: 
        
        :returns: The updated ChannelParticipantTwimlInstance
        """
        payload, _, _ = self._update(sid=sid, parameters=parameters)
        return ChannelParticipantTwimlInstance(self._version, payload, channel_sid=self._solution['channel_sid'], participant_sid=self._solution['participant_sid'])

    def update_with_http_info(self, sid: str, parameters: Union[object, object]=values.unset) -> ApiResponse:
        """
        Update the ChannelParticipantTwimlInstance and return response metadata

        :param sid: 
        :param parameters: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(sid=sid, parameters=parameters)
        instance = ChannelParticipantTwimlInstance(self._version, payload, channel_sid=self._solution['channel_sid'], participant_sid=self._solution['participant_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, sid: str, parameters: Union[object, object]=values.unset) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'Sid': sid,
            'Parameters': serialize.object(parameters),
        })
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.update_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def update_async(self, sid: str, parameters: Union[object, object]=values.unset) -> ChannelParticipantTwimlInstance:
        """
        Asynchronously update the ChannelParticipantTwimlInstance

        :param sid: 
        :param parameters: 
        
        :returns: The updated ChannelParticipantTwimlInstance
        """
        payload, _, _ = await self._update_async(sid=sid, parameters=parameters)
        return ChannelParticipantTwimlInstance(self._version, payload, channel_sid=self._solution['channel_sid'], participant_sid=self._solution['participant_sid'])

    async def update_with_http_info_async(self, sid: str, parameters: Union[object, object]=values.unset) -> ApiResponse:
        """
        Asynchronously update the ChannelParticipantTwimlInstance and return response metadata

        :param sid: 
        :param parameters: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(sid=sid, parameters=parameters)
        instance = ChannelParticipantTwimlInstance(self._version, payload, channel_sid=self._solution['channel_sid'], participant_sid=self._solution['participant_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)



    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.FlexApi.Users_manisingh_codehq_open_api_spec_flex_v1_openapi_yaml.ChannelParticipantTwimlList>'


