r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class SuggestionInstance(InstanceResource):


    class CreateDataMappingSuggestionInput(object):
        """
            :ivar mapping_from: 
            :ivar mapping_to: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mapping_from: Optional[SuggestionList.DataMappingFromTypes] = payload.get("mappingFrom")
            self.mapping_to: Optional[SuggestionList.DataMappingSuggestionToTraits] = payload.get("mappingTo")

        def to_dict(self):
            return {
                
                    "mappingFrom": self.mapping_from.to_dict() if self.mapping_from is not None else None ,
                    "mappingTo": self.mapping_to.to_dict() if self.mapping_to is not None else None ,
            }

    class DataMappingFromTypes(object):
        """
            :ivar type: The source data type, which determines the source of the data and the required configuration parameters.
            :ivar columns: The list of CSV column names that serve as the source fields.
            :ivar dataset_id: The unique identifier of the TDI dataset to connect.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["DataMappingInstance.str"] = payload.get("type")
            self.columns: Optional[List[str]] = payload.get("columns")
            self.dataset_id: Optional[str] = payload.get("datasetId")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "columns": self.columns,
                    "datasetId": self.dataset_id,
            }

    class DataMappingSuggestionToTraits(object):
        """
            :ivar type: The destination type.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["SuggestionInstance.str"] = payload.get("type")

        def to_dict(self):
            return {
                
                    "type": self.type,
            }

    class DataMappingSuggestionTraitItem(object):
        """
            :ivar mapping: 
            :ivar confidence: Confidence score for this suggestion. Higher values indicate a stronger match.
            :ivar rationale: A concise explanation of why the source field was matched to this trait.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mapping: Optional[MappingTraitItem] = payload.get("mapping")
            self.confidence: Optional[float] = payload.get("confidence")
            self.rationale: Optional[str] = payload.get("rationale")

        def to_dict(self):
            return {
                
                    "mapping": self.mapping.to_dict() if self.mapping is not None else None ,
                    "confidence": self.confidence,
                    "rationale": self.rationale,
            }

    class MappingTraitItem(object):
        """
            :ivar expression: The expression identifying the field/column in the source.
            :ivar trait_group: The name of the Trait Group to map to.
            :ivar trait_name: The name of the trait within the Trait Group to map to.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.expression: Optional[str] = payload.get("expression")
            self.trait_group: Optional[str] = payload.get("traitGroup")
            self.trait_name: Optional[str] = payload.get("traitName")

        def to_dict(self):
            return {
                
                    "expression": self.expression,
                    "traitGroup": self.trait_group,
                    "traitName": self.trait_name,
            }



    class DataMappingType(object):
        CSV = "CSV"
        DATASET = "DATASET"

    """
    :ivar message: 
    :ivar status_url: URI to check operation status.
    :ivar id: The unique identifier for this suggestion.
    :ivar mapping_from: 
    :ivar mapping_to: 
    :ivar created_at: The ISO 8601 timestamp when the suggestion was created.
    :ivar expires_at: The ISO 8601 timestamp when the suggestion expires and is no longer retrievable.
    """

    def __init__(self, version: Version, payload:Dict[str, Any], store_id: Optional[str] = None, suggestion_id: Optional[str] = None):
        super().__init__(version)

        
        self.message: Optional[str] = payload.get("message")
        self.status_url: Optional[str] = payload.get("statusUrl")
        self.id: Optional[str] = payload.get("id")
        self.mapping_from: Optional[str] = payload.get("mappingFrom")
        self.mapping_to: Optional[str] = payload.get("mappingTo")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.expires_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("expiresAt"))

        
        
        self._solution = { 
            "store_id": store_id or self.store_id,
            "suggestion_id": suggestion_id or self.suggestion_id,
        }
        
        
        self._context: Optional[SuggestionContext] = None

    @property
    def _proxy(self) -> "SuggestionContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: SuggestionContext for this SuggestionInstance
        """
        if self._context is None:
            self._context = SuggestionContext(self._version, store_id=self._solution['store_id'], suggestion_id=self._solution['suggestion_id'],)
        return self._context
    
    
    def fetch(self) -> "SuggestionInstance":
        """
        Fetch the SuggestionInstance
        

        :returns: The fetched SuggestionInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "SuggestionInstance":
        """
        Asynchronous coroutine to fetch the SuggestionInstance
        

        :returns: The fetched SuggestionInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the SuggestionInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the SuggestionInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Memory.Users_manisingh_codehq_open_api_spec_memory_v1_openapi_yaml.SuggestionInstance {}>'.format(context)

class SuggestionContext(InstanceContext):


    class CreateDataMappingSuggestionInput(object):
        """
            :ivar mapping_from: 
            :ivar mapping_to: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mapping_from: Optional[SuggestionList.DataMappingFromTypes] = payload.get("mappingFrom")
            self.mapping_to: Optional[SuggestionList.DataMappingSuggestionToTraits] = payload.get("mappingTo")

        def to_dict(self):
            return {
                
                    "mappingFrom": self.mapping_from.to_dict() if self.mapping_from is not None else None ,
                    "mappingTo": self.mapping_to.to_dict() if self.mapping_to is not None else None ,
            }

    class DataMappingFromTypes(object):
        """
            :ivar type: The source data type, which determines the source of the data and the required configuration parameters.
            :ivar columns: The list of CSV column names that serve as the source fields.
            :ivar dataset_id: The unique identifier of the TDI dataset to connect.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["DataMappingInstance.str"] = payload.get("type")
            self.columns: Optional[List[str]] = payload.get("columns")
            self.dataset_id: Optional[str] = payload.get("datasetId")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "columns": self.columns,
                    "datasetId": self.dataset_id,
            }

    class DataMappingSuggestionToTraits(object):
        """
            :ivar type: The destination type.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["SuggestionInstance.str"] = payload.get("type")

        def to_dict(self):
            return {
                
                    "type": self.type,
            }

    class DataMappingSuggestionTraitItem(object):
        """
            :ivar mapping: 
            :ivar confidence: Confidence score for this suggestion. Higher values indicate a stronger match.
            :ivar rationale: A concise explanation of why the source field was matched to this trait.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mapping: Optional[MappingTraitItem] = payload.get("mapping")
            self.confidence: Optional[float] = payload.get("confidence")
            self.rationale: Optional[str] = payload.get("rationale")

        def to_dict(self):
            return {
                
                    "mapping": self.mapping.to_dict() if self.mapping is not None else None ,
                    "confidence": self.confidence,
                    "rationale": self.rationale,
            }

    class MappingTraitItem(object):
        """
            :ivar expression: The expression identifying the field/column in the source.
            :ivar trait_group: The name of the Trait Group to map to.
            :ivar trait_name: The name of the trait within the Trait Group to map to.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.expression: Optional[str] = payload.get("expression")
            self.trait_group: Optional[str] = payload.get("traitGroup")
            self.trait_name: Optional[str] = payload.get("traitName")

        def to_dict(self):
            return {
                
                    "expression": self.expression,
                    "traitGroup": self.trait_group,
                    "traitName": self.trait_name,
            }


    def __init__(self, version: Version, store_id: str, suggestion_id: str):
        """
        Initialize the SuggestionContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param suggestion_id: A unique Data Mapping suggestion ID using Twilio Type ID (TTID) format
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'store_id': store_id,
            'suggestion_id': suggestion_id,
        }
        self._uri = '/ControlPlane/Stores/{store_id}/DataMappings/Suggestions/{suggestion_id}'.format(**self._solution)
        
    
    
    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers)

    def fetch(self) -> SuggestionInstance:
        """
        Fetch the SuggestionInstance
        

        :returns: The fetched SuggestionInstance
        """
        payload, _, _ = self._fetch()
        return SuggestionInstance(
            self._version,
            payload,
            store_id=self._solution['store_id'],
            suggestion_id=self._solution['suggestion_id'],
            
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the SuggestionInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = SuggestionInstance(
            self._version,
            payload,
            store_id=self._solution['store_id'],
            suggestion_id=self._solution['suggestion_id'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers)

    async def fetch_async(self) -> SuggestionInstance:
        """
        Asynchronous coroutine to fetch the SuggestionInstance
        

        :returns: The fetched SuggestionInstance
        """
        payload, _, _ = await self._fetch_async()
        return SuggestionInstance(
            self._version,
            payload,
            store_id=self._solution['store_id'],
            suggestion_id=self._solution['suggestion_id'],
            
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the SuggestionInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = SuggestionInstance(
            self._version,
            payload,
            store_id=self._solution['store_id'],
            suggestion_id=self._solution['suggestion_id'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Memory.Users_manisingh_codehq_open_api_spec_memory_v1_openapi_yaml.SuggestionContext {}>'.format(context)







class SuggestionPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> SuggestionInstance:
        """
        Build an instance of SuggestionInstance

        :param payload: Payload response from the API
        """
        
        return SuggestionInstance(self._version, payload, store_id=self._solution["store_id"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.Users_manisingh_codehq_open_api_spec_memory_v1_openapi_yaml.SuggestionPage>"





class SuggestionList(ListResource):
    
    class CreateDataMappingSuggestionInput(object):
        """
            :ivar mapping_from: 
            :ivar mapping_to: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mapping_from: Optional[SuggestionList.DataMappingFromTypes] = payload.get("mappingFrom")
            self.mapping_to: Optional[SuggestionList.DataMappingSuggestionToTraits] = payload.get("mappingTo")

        def to_dict(self):
            return {
                
                    "mappingFrom": self.mapping_from.to_dict() if self.mapping_from is not None else None ,
                    "mappingTo": self.mapping_to.to_dict() if self.mapping_to is not None else None ,
            }

    class DataMappingFromTypes(object):
        """
            :ivar type: The source data type, which determines the source of the data and the required configuration parameters.
            :ivar columns: The list of CSV column names that serve as the source fields.
            :ivar dataset_id: The unique identifier of the TDI dataset to connect.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["DataMappingInstance.str"] = payload.get("type")
            self.columns: Optional[List[str]] = payload.get("columns")
            self.dataset_id: Optional[str] = payload.get("datasetId")

        def to_dict(self):
            return {
                
                    "type": self.type,
                    "columns": self.columns,
                    "datasetId": self.dataset_id,
            }

    class DataMappingSuggestionToTraits(object):
        """
            :ivar type: The destination type.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.type: Optional["SuggestionInstance.str"] = payload.get("type")

        def to_dict(self):
            return {
                
                    "type": self.type,
            }

    class DataMappingSuggestionTraitItem(object):
        """
            :ivar mapping: 
            :ivar confidence: Confidence score for this suggestion. Higher values indicate a stronger match.
            :ivar rationale: A concise explanation of why the source field was matched to this trait.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mapping: Optional[MappingTraitItem] = payload.get("mapping")
            self.confidence: Optional[float] = payload.get("confidence")
            self.rationale: Optional[str] = payload.get("rationale")

        def to_dict(self):
            return {
                
                    "mapping": self.mapping.to_dict() if self.mapping is not None else None ,
                    "confidence": self.confidence,
                    "rationale": self.rationale,
            }

    class MappingTraitItem(object):
        """
            :ivar expression: The expression identifying the field/column in the source.
            :ivar trait_group: The name of the Trait Group to map to.
            :ivar trait_name: The name of the trait within the Trait Group to map to.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.expression: Optional[str] = payload.get("expression")
            self.trait_group: Optional[str] = payload.get("traitGroup")
            self.trait_name: Optional[str] = payload.get("traitName")

        def to_dict(self):
            return {
                
                    "expression": self.expression,
                    "traitGroup": self.trait_group,
                    "traitName": self.trait_name,
            }


    def __init__(self, version: Version, store_id: str):
        """
        Initialize the SuggestionList

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'store_id': store_id,  }
        self._uri = '/ControlPlane/Stores/{store_id}/DataMappings/Suggestions'.format(**self._solution)
        
        
    
    
    def _create(self, create_data_mapping_suggestion_input: CreateDataMappingSuggestionInput) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_data_mapping_suggestion_input.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, create_data_mapping_suggestion_input: CreateDataMappingSuggestionInput) -> SuggestionInstance:
        """
        Create the SuggestionInstance

        :param create_data_mapping_suggestion_input: 
        
        :returns: The created SuggestionInstance
        """
        payload, _, _ = self._create(create_data_mapping_suggestion_input=create_data_mapping_suggestion_input)
        return SuggestionInstance(self._version, payload, store_id=self._solution['store_id'])

    def create_with_http_info(self, create_data_mapping_suggestion_input: CreateDataMappingSuggestionInput) -> ApiResponse:
        """
        Create the SuggestionInstance and return response metadata

        :param create_data_mapping_suggestion_input: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(create_data_mapping_suggestion_input=create_data_mapping_suggestion_input)
        instance = SuggestionInstance(self._version, payload, store_id=self._solution['store_id'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, create_data_mapping_suggestion_input: CreateDataMappingSuggestionInput) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_data_mapping_suggestion_input.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, create_data_mapping_suggestion_input: CreateDataMappingSuggestionInput) -> SuggestionInstance:
        """
        Asynchronously create the SuggestionInstance

        :param create_data_mapping_suggestion_input: 
        
        :returns: The created SuggestionInstance
        """
        payload, _, _ = await self._create_async(create_data_mapping_suggestion_input=create_data_mapping_suggestion_input)
        return SuggestionInstance(self._version, payload, store_id=self._solution['store_id'])

    async def create_with_http_info_async(self, create_data_mapping_suggestion_input: CreateDataMappingSuggestionInput) -> ApiResponse:
        """
        Asynchronously create the SuggestionInstance and return response metadata

        :param create_data_mapping_suggestion_input: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(create_data_mapping_suggestion_input=create_data_mapping_suggestion_input)
        instance = SuggestionInstance(self._version, payload, store_id=self._solution['store_id'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[SuggestionInstance]:
        """
        Streams SuggestionInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param DataMappingType type: Filter suggestions by their source data type.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            order_by=order_by,
            type=type,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[SuggestionInstance]:
        """
        Asynchronously streams SuggestionInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param DataMappingType type: Filter suggestions by their source data type.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            order_by=order_by,
            type=type,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams SuggestionInstance and returns headers from first page

        
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param DataMappingType type: Filter suggestions by their source data type.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            order_by=order_by,
            type=type,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams SuggestionInstance and returns headers from first page

        
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param DataMappingType type: Filter suggestions by their source data type.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            order_by=order_by,
            type=type,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[SuggestionInstance]:
        """
        Lists SuggestionInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param DataMappingType type: Filter suggestions by their source data type.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            order_by=order_by,
            type=type,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[SuggestionInstance]:
        """
        Asynchronously lists SuggestionInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param DataMappingType type: Filter suggestions by their source data type.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            order_by=order_by,
            type=type,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists SuggestionInstance and returns headers from first page

        
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param DataMappingType type: Filter suggestions by their source data type.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            order_by=order_by,
            type=type,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists SuggestionInstance and returns headers from first page

        
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param DataMappingType type: Filter suggestions by their source data type.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            order_by=order_by,
            type=type,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      
      order_by: Union[str, object] = values.unset,
      type: Union[DataMappingType, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> SuggestionPage:
        """
        Retrieve a single page of SuggestionInstance records from the API.
        Request is executed immediately
        
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param type: Filter suggestions by their source data type.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of SuggestionInstance
        """
        data = values.of({ 
            'orderBy': order_by,
            'type': type,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return SuggestionPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      
      order_by: Union[str, object] = values.unset,
      type: Union[DataMappingType, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> SuggestionPage:
        """
        Asynchronously retrieve a single page of SuggestionInstance records from the API.
        Request is executed immediately
        
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param type: Filter suggestions by their source data type.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of SuggestionInstance
        """
        data = values.of({ 
            'orderBy': order_by,
            'type': type,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return SuggestionPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param type: Filter suggestions by their source data type.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with SuggestionPage, status code, and headers
        """
        data = values.of({ 
            'orderBy': order_by,
            'type': type,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = SuggestionPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        type: Union[DataMappingType, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param type: Filter suggestions by their source data type.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with SuggestionPage, status code, and headers
        """
        data = values.of({ 
            'orderBy': order_by,
            'type': type,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = SuggestionPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> SuggestionPage:
        """
        Retrieve a specific page of SuggestionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of SuggestionInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return SuggestionPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> SuggestionPage:
        """
        Asynchronously retrieve a specific page of SuggestionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of SuggestionInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return SuggestionPage(self._version, response, solution=self._solution)



    def get(self, store_id: str, suggestion_id: str) -> SuggestionContext:
        """
        Constructs a SuggestionContext
        
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param suggestion_id: A unique Data Mapping suggestion ID using Twilio Type ID (TTID) format
        """
        return SuggestionContext(self._version, store_id=store_id, suggestion_id=suggestion_id)

    def __call__(self, store_id: str, suggestion_id: str) -> SuggestionContext:
        """
        Constructs a SuggestionContext
        
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param suggestion_id: A unique Data Mapping suggestion ID using Twilio Type ID (TTID) format
        """
        return SuggestionContext(self._version, store_id=store_id, suggestion_id=suggestion_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Memory.Users_manisingh_codehq_open_api_spec_memory_v1_openapi_yaml.SuggestionList>'


