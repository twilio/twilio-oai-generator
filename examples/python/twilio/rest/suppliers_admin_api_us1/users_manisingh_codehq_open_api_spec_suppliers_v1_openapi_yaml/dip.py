r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Suppliers
    Powers Twilio Suppliers Management

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class DipInstance(InstanceResource):



    """
    :ivar mcc: 
    :ivar mnc: 
    :ivar type: 
    :ivar carrier: 
    :ivar provider: 
    :ivar source: 
    :ivar dip_status: 
    :ivar native_id: 
    :ivar iso_cc: 
    :ivar supplementary_data: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], phone_number: Optional[str] = None):
        super().__init__(version)

        
        self.mcc: Optional[str] = payload.get("mcc")
        self.mnc: Optional[str] = payload.get("mnc")
        self.type: Optional[str] = payload.get("type")
        self.carrier: Optional[str] = payload.get("carrier")
        self.provider: Optional[str] = payload.get("provider")
        self.source: Optional[str] = payload.get("source")
        self.dip_status: Optional[str] = payload.get("dipStatus")
        self.native_id: Optional[str] = payload.get("native_id")
        self.iso_cc: Optional[str] = payload.get("iso_cc")
        self.supplementary_data: Optional[str] = payload.get("supplementary_data")

        
        
        self._solution = { 
            "phone_number": phone_number or self.phone_number,
        }
        
        
        self._context: Optional[DipContext] = None

    @property
    def _proxy(self) -> "DipContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: DipContext for this DipInstance
        """
        if self._context is None:
            self._context = DipContext(self._version, phone_number=self._solution['phone_number'],)
        return self._context
    
    
    def fetch(self, t_actor: str) -> "DipInstance":
        """
        Fetch the DipInstance
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: The fetched DipInstance
        """
        return self._proxy.fetch(t_actor=t_actor, )

    async def fetch_async(self, t_actor: str) -> "DipInstance":
        """
        Asynchronous coroutine to fetch the DipInstance
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: The fetched DipInstance
        """
        return await self._proxy.fetch_async(t_actor=t_actor, )

    def fetch_with_http_info(self, t_actor: str) -> ApiResponse:
        """
        Fetch the DipInstance with HTTP info
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info(t_actor=t_actor, )

    async def fetch_with_http_info_async(self, t_actor: str) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the DipInstance with HTTP info
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async(t_actor=t_actor, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.DipInstance {}>'.format(context)

class DipContext(InstanceContext):


    def __init__(self, version: Version, phone_number: str):
        """
        Initialize the DipContext

        :param version: Version that contains the resource
        :param phone_number: Phone Number
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'phone_number': phone_number,
        }
        self._uri = '/cops-proxy/dip/{phone_number}'.format(**self._solution)
        
    
    
    def _fetch(self, t_actor: str) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        if not (t_actor is values.unset or (isinstance(t_actor, str) and not t_actor)):
            headers['T-Actor'] = t_actor
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers)

    def fetch(self, t_actor: str) -> DipInstance:
        """
        Fetch the DipInstance
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: The fetched DipInstance
        """
        payload, _, _ = self._fetch(t_actor=t_actor)
        return DipInstance(
            self._version,
            payload,
            phone_number=self._solution['phone_number'],
            
        )

    def fetch_with_http_info(self, t_actor: str) -> ApiResponse:
        """
        Fetch the DipInstance and return response metadata
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch(t_actor=t_actor)
        instance = DipInstance(
            self._version,
            payload,
            phone_number=self._solution['phone_number'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self, t_actor: str) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        if not (t_actor is values.unset or (isinstance(t_actor, str) and not t_actor)):
            headers['T-Actor'] = t_actor
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers)

    async def fetch_async(self, t_actor: str) -> DipInstance:
        """
        Asynchronous coroutine to fetch the DipInstance
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: The fetched DipInstance
        """
        payload, _, _ = await self._fetch_async(t_actor=t_actor)
        return DipInstance(
            self._version,
            payload,
            phone_number=self._solution['phone_number'],
            
        )

    async def fetch_with_http_info_async(self, t_actor: str) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the DipInstance and return response metadata
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async(t_actor=t_actor)
        instance = DipInstance(
            self._version,
            payload,
            phone_number=self._solution['phone_number'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.DipContext {}>'.format(context)



class DipList(ListResource):
    
    def __init__(self, version: Version):
        """
        Initialize the DipList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        
        
        

    def get(self, phone_number: str) -> DipContext:
        """
        Constructs a DipContext
        
        :param phone_number: Phone Number
        """
        return DipContext(self._version, phone_number=phone_number)

    def __call__(self, phone_number: str) -> DipContext:
        """
        Constructs a DipContext
        
        :param phone_number: Phone Number
        """
        return DipContext(self._version, phone_number=phone_number)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.DipList>'


