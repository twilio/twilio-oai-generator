r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Suppliers
    Powers Twilio Suppliers Management

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class NackMappingInstance(InstanceResource):


    class NackMappingUpsert(object):
        """
            :ivar mcc: MCC ID or *
            :ivar mnc: MNC ID or *
            :ivar provider_sid: Provider Sid or *
            :ivar twilio_error_code: Twilio error code corresponding to provider error code
            :ivar type: 
            :ivar value: Nack error code
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mcc: Optional[str] = payload.get("mcc")
            self.mnc: Optional[str] = payload.get("mnc")
            self.provider_sid: Optional[str] = payload.get("providerSid")
            self.twilio_error_code: Optional[int] = payload.get("twilioErrorCode")
            self.type: Optional["NackMappingInstance.NackMappingType"] = payload.get("type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                
                    "mcc": self.mcc,
                    "mnc": self.mnc,
                    "providerSid": self.provider_sid,
                    "twilioErrorCode": self.twilio_error_code,
                    "type": self.type,
                    "value": self.value,
            }



    class NackMappingType(object):
        UNKNOWN = "UNKNOWN"
        SMS_COMMAND_STATUS = "SMS_COMMAND_STATUS"
        MMS_ERROR_CODE = "MMS_ERROR_CODE"
        MMS_STATUS_TEXT = "MMS_STATUS_TEXT"

    """
    :ivar auto_id: 
    :ivar mcc: MCC ID or *
    :ivar mnc: MNC ID or *
    :ivar modified: 
    :ivar modified_by: User identifier (US* or TA* sid)
    :ivar provider_sid: Provider Sid or *
    :ivar twilio_error_code: Twilio error code corresponding to provider error code
    :ivar type: 
    :ivar value: Nack error code
    :ivar code: 
    :ivar message: 
    :ivar more_info: Link to Error Code References
    :ivar status: HTTP response status code
    """

    def __init__(self, version: Version, payload:Dict[str, Any], auto_id: Optional[int] = None, provider_sid: Optional[str] = None):
        super().__init__(version)

        
        self.auto_id: Optional[int] = payload.get("autoId")
        self.mcc: Optional[str] = payload.get("mcc")
        self.mnc: Optional[str] = payload.get("mnc")
        self.modified: Optional[datetime] = deserialize.iso8601_datetime(payload.get("modified"))
        self.modified_by: Optional[str] = payload.get("modifiedBy")
        self.provider_sid: Optional[str] = payload.get("providerSid")
        self.twilio_error_code: Optional[int] = payload.get("twilioErrorCode")
        self.type: Optional["NackMappingInstance.str"] = payload.get("type")
        self.value: Optional[str] = payload.get("value")
        self.code: Optional[int] = payload.get("code")
        self.message: Optional[str] = payload.get("message")
        self.more_info: Optional[str] = payload.get("more_info")
        self.status: Optional[int] = payload.get("status")

        
        
        self._solution = { 
            "auto_id": auto_id or self.auto_id,
            "provider_sid": provider_sid or self.provider_sid,
        }
        
        
        self._context: Optional[NackMappingContext] = None

    @property
    def _proxy(self) -> "NackMappingContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: NackMappingContext for this NackMappingInstance
        """
        if self._context is None:
            self._context = NackMappingContext(self._version, auto_id=self._solution['auto_id'], provider_sid=self._solution['provider_sid'],)
        return self._context
    
    
    def delete(self, t_actor: str) -> bool:
        """
        Deletes the NackMappingInstance
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete(t_actor=t_actor, )
    async def delete_async(self, t_actor: str) -> bool:
        """
        Asynchronous coroutine that deletes the NackMappingInstance
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async(t_actor=t_actor, )

    def delete_with_http_info(self, t_actor: str) -> ApiResponse:
        """
        Deletes the NackMappingInstance with HTTP info
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info(t_actor=t_actor, )

    async def delete_with_http_info_async(self, t_actor: str) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the NackMappingInstance with HTTP info
        
        :param t_actor: User identifier (US* or TA* sid)

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async(t_actor=t_actor, )
    
    
    def update(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> "NackMappingInstance":
        """
        Update the NackMappingInstance
        
        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 

        :returns: The updated NackMappingInstance
        """
        return self._proxy.update(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert, )

    async def update_async(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> "NackMappingInstance":
        """
        Asynchronous coroutine to update the NackMappingInstance
        
        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 

        :returns: The updated NackMappingInstance
        """
        return await self._proxy.update_async(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert, )

    def update_with_http_info(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> ApiResponse:
        """
        Update the NackMappingInstance with HTTP info
        
        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert, )

    async def update_with_http_info_async(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> ApiResponse:
        """
        Asynchronous coroutine to update the NackMappingInstance with HTTP info
        
        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert, )
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.NackMappingInstance {}>'.format(context)

class NackMappingContext(InstanceContext):


    class NackMappingUpsert(object):
        """
            :ivar mcc: MCC ID or *
            :ivar mnc: MNC ID or *
            :ivar provider_sid: Provider Sid or *
            :ivar twilio_error_code: Twilio error code corresponding to provider error code
            :ivar type: 
            :ivar value: Nack error code
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mcc: Optional[str] = payload.get("mcc")
            self.mnc: Optional[str] = payload.get("mnc")
            self.provider_sid: Optional[str] = payload.get("providerSid")
            self.twilio_error_code: Optional[int] = payload.get("twilioErrorCode")
            self.type: Optional["NackMappingInstance.NackMappingType"] = payload.get("type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                
                    "mcc": self.mcc,
                    "mnc": self.mnc,
                    "providerSid": self.provider_sid,
                    "twilioErrorCode": self.twilio_error_code,
                    "type": self.type,
                    "value": self.value,
            }


    def __init__(self, version: Version, auto_id: int, provider_sid: str):
        """
        Initialize the NackMappingContext

        :param version: Version that contains the resource
        :param auto_id: Auto ID
        :param provider_sid: 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'auto_id': auto_id,
            'provider_sid': provider_sid,
        }
        self._uri = '/nack-mappings/{auto_id}'.format(**self._solution)
        
    
    
    def _delete(self, t_actor: str) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        headers = values.of({'T-Actor': t_actor, })
        

        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self, t_actor: str) -> bool:
        """
        Deletes the NackMappingInstance

        :param t_actor: User identifier (US* or TA* sid)
        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete(t_actor=t_actor)
        return success

    def delete_with_http_info(self, t_actor: str) -> ApiResponse:
        """
        Deletes the NackMappingInstance and return response metadata

        :param t_actor: User identifier (US* or TA* sid)
        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete(t_actor=t_actor)
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self, t_actor: str) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        headers = values.of({'T-Actor': t_actor, })
        
        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self, t_actor: str) -> bool:
        """
        Asynchronous coroutine that deletes the NackMappingInstance

        :param t_actor: User identifier (US* or TA* sid)
        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async(t_actor=t_actor)
        return success

    async def delete_with_http_info_async(self, t_actor: str) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the NackMappingInstance and return response metadata

        :param t_actor: User identifier (US* or TA* sid)
        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async(t_actor=t_actor)
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    def _update(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = nack_mapping_upsert.to_dict()
        
        headers = values.of({})
        
        
        if not (t_actor is values.unset or (isinstance(t_actor, str) and not t_actor)):
            headers['T-Actor'] = t_actor
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='PUT', uri=self._uri, data=data, headers=headers)

    def update(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> NackMappingInstance:
        """
        Update the NackMappingInstance
        
        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 

        :returns: The updated NackMappingInstance
        """
        payload, _, _ = self._update(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert)
        return NackMappingInstance(
            self._version,
            payload,
            auto_id=self._solution['auto_id'],
            provider_sid=self._solution['provider_sid']
        )

    def update_with_http_info(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> ApiResponse:
        """
        Update the NackMappingInstance and return response metadata
        
        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert)
        instance = NackMappingInstance(
            self._version,
            payload,
            auto_id=self._solution['auto_id'],
            provider_sid=self._solution['provider_sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = nack_mapping_upsert.to_dict()
        
        headers = values.of({})
        
        
        if not (t_actor is values.unset or (isinstance(t_actor, str) and not t_actor)):
            headers['T-Actor'] = t_actor
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='PUT', uri=self._uri, data=data, headers=headers)

    async def update_async(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> NackMappingInstance:
        """
        Asynchronous coroutine to update the NackMappingInstance
        
        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 

        :returns: The updated NackMappingInstance
        """
        payload, _, _ = await self._update_async(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert)
        return NackMappingInstance(
            self._version,
            payload,
            auto_id=self._solution['auto_id'],
            provider_sid=self._solution['provider_sid']
        )

    async def update_with_http_info_async(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> ApiResponse:
        """
        Asynchronous coroutine to update the NackMappingInstance and return response metadata
        
        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert)
        instance = NackMappingInstance(
            self._version,
            payload,
            auto_id=self._solution['auto_id'],
            provider_sid=self._solution['provider_sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.NackMappingContext {}>'.format(context)







class NackMappingPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> NackMappingInstance:
        """
        Build an instance of NackMappingInstance

        :param payload: Payload response from the API
        """
        
        return NackMappingInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.NackMappingPage>"






class NackMappingPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> NackMappingInstance:
        """
        Build an instance of NackMappingInstance

        :param payload: Payload response from the API
        """
        
        return NackMappingInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.NackMappingPage>"




class NackMappingPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> NackMappingInstance:
        """
        Build an instance of NackMappingInstance

        :param payload: Payload response from the API
        """
        
        return NackMappingInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.NackMappingPage>"




class NackMappingPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> NackMappingInstance:
        """
        Build an instance of NackMappingInstance

        :param payload: Payload response from the API
        """
        
        return NackMappingInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.NackMappingPage>"




class NackMappingPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> NackMappingInstance:
        """
        Build an instance of NackMappingInstance

        :param payload: Payload response from the API
        """
        
        return NackMappingInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.NackMappingPage>"





class NackMappingList(ListResource):
    
    class NackMappingUpsert(object):
        """
            :ivar mcc: MCC ID or *
            :ivar mnc: MNC ID or *
            :ivar provider_sid: Provider Sid or *
            :ivar twilio_error_code: Twilio error code corresponding to provider error code
            :ivar type: 
            :ivar value: Nack error code
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mcc: Optional[str] = payload.get("mcc")
            self.mnc: Optional[str] = payload.get("mnc")
            self.provider_sid: Optional[str] = payload.get("providerSid")
            self.twilio_error_code: Optional[int] = payload.get("twilioErrorCode")
            self.type: Optional["NackMappingInstance.NackMappingType"] = payload.get("type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                
                    "mcc": self.mcc,
                    "mnc": self.mnc,
                    "providerSid": self.provider_sid,
                    "twilioErrorCode": self.twilio_error_code,
                    "type": self.type,
                    "value": self.value,
            }


    def __init__(self, version: Version, provider_sid: str):
        """
        Initialize the NackMappingList

        :param version: Version that contains the resource
        :param provider_sid: 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'provider_sid': provider_sid,  }
        self._uri = '/Providers/{provider_sid}/nack-mappings'.format(**self._solution)
        
        
    
    
    
    def list(self, 
    ) -> List[NackMappingInstance]:
        """
        Lists NackMappingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [NackMappingInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[NackMappingInstance]:
        """
        Asynchronously lists NackMappingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [NackMappingInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Asynchronously retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> NackMappingPage:
        """
        Retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> NackMappingPage:
        """
        Asynchronously retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    
    def _create(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = nack_mapping_upsert.to_dict()
        
        headers = values.of({
                'T-Actor': t_actor,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> NackMappingInstance:
        """
        Create the NackMappingInstance

        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 
        
        :returns: The created NackMappingInstance
        """
        payload, _, _ = self._create(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert)
        return NackMappingInstance(self._version, payload, provider_sid=self._solution['provider_sid'])

    def create_with_http_info(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> ApiResponse:
        """
        Create the NackMappingInstance and return response metadata

        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert)
        instance = NackMappingInstance(self._version, payload, provider_sid=self._solution['provider_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = nack_mapping_upsert.to_dict()
        
        headers = values.of({
                'T-Actor': t_actor,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> NackMappingInstance:
        """
        Asynchronously create the NackMappingInstance

        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 
        
        :returns: The created NackMappingInstance
        """
        payload, _, _ = await self._create_async(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert)
        return NackMappingInstance(self._version, payload, provider_sid=self._solution['provider_sid'])

    async def create_with_http_info_async(self, t_actor: str, nack_mapping_upsert: NackMappingUpsert) -> ApiResponse:
        """
        Asynchronously create the NackMappingInstance and return response metadata

        :param t_actor: User identifier (US* or TA* sid)
        :param nack_mapping_upsert: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(t_actor=t_actor, nack_mapping_upsert=nack_mapping_upsert)
        instance = NackMappingInstance(self._version, payload, provider_sid=self._solution['provider_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        global_only: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[NackMappingInstance]:
        """
        Streams NackMappingInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param bool global_only: List only Global NACK Mappings
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            global_only=global_only,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        global_only: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[NackMappingInstance]:
        """
        Asynchronously streams NackMappingInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param bool global_only: List only Global NACK Mappings
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            global_only=global_only,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        global_only: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams NackMappingInstance and returns headers from first page

        
        :param bool global_only: List only Global NACK Mappings
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            global_only=global_only,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        global_only: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams NackMappingInstance and returns headers from first page

        
        :param bool global_only: List only Global NACK Mappings
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            global_only=global_only,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        global_only: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[NackMappingInstance]:
        """
        Lists NackMappingInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param bool global_only: List only Global NACK Mappings
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            global_only=global_only,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        global_only: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[NackMappingInstance]:
        """
        Asynchronously lists NackMappingInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param bool global_only: List only Global NACK Mappings
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            global_only=global_only,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        global_only: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists NackMappingInstance and returns headers from first page

        
        :param bool global_only: List only Global NACK Mappings
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            global_only=global_only,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        global_only: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists NackMappingInstance and returns headers from first page

        
        :param bool global_only: List only Global NACK Mappings
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            global_only=global_only,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      global_only: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param global_only: List only Global NACK Mappings
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'globalOnly': serialize.boolean_to_string(global_only),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      global_only: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Asynchronously retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param global_only: List only Global NACK Mappings
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'globalOnly': serialize.boolean_to_string(global_only),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        global_only: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param global_only: List only Global NACK Mappings
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'globalOnly': serialize.boolean_to_string(global_only),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        global_only: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param global_only: List only Global NACK Mappings
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'globalOnly': serialize.boolean_to_string(global_only),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> NackMappingPage:
        """
        Retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> NackMappingPage:
        """
        Asynchronously retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    
    def stream(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[NackMappingInstance]:
        """
        Streams NackMappingInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[NackMappingInstance]:
        """
        Asynchronously streams NackMappingInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[NackMappingInstance]:
        """
        Lists NackMappingInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[NackMappingInstance]:
        """
        Asynchronously lists NackMappingInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Asynchronously retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> NackMappingPage:
        """
        Retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> NackMappingPage:
        """
        Asynchronously retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[NackMappingInstance]:
        """
        Lists NackMappingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [NackMappingInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[NackMappingInstance]:
        """
        Asynchronously lists NackMappingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [NackMappingInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Asynchronously retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> NackMappingPage:
        """
        Retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> NackMappingPage:
        """
        Asynchronously retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[NackMappingInstance]:
        """
        Lists NackMappingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [NackMappingInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[NackMappingInstance]:
        """
        Asynchronously lists NackMappingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [NackMappingInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists NackMappingInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> NackMappingPage:
        """
        Asynchronously retrieve a single page of NackMappingInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of NackMappingInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return NackMappingPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with NackMappingPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = NackMappingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> NackMappingPage:
        """
        Retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> NackMappingPage:
        """
        Asynchronously retrieve a specific page of NackMappingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of NackMappingInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return NackMappingPage(self._version, response, solution=self._solution)



    def get(self, auto_id: int, provider_sid: str) -> NackMappingContext:
        """
        Constructs a NackMappingContext
        
        :param auto_id: Auto ID
        :param provider_sid: 
        """
        return NackMappingContext(self._version, auto_id=auto_id, provider_sid=provider_sid)

    def __call__(self, auto_id: int, provider_sid: str) -> NackMappingContext:
        """
        Constructs a NackMappingContext
        
        :param auto_id: Auto ID
        :param provider_sid: 
        """
        return NackMappingContext(self._version, auto_id=auto_id, provider_sid=provider_sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.NackMappingList>'


