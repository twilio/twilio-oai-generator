r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Suppliers
    Powers Twilio Suppliers Management

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class TieredPlanInstance(InstanceResource):


    class TieredPlansInput(object):
        """
            :ivar comments: 
            :ivar country: 
            :ivar created_by: 
            :ivar currency: 
            :ivar effective_from: 
            :ivar effective_to: 
            :ivar plan_type: 
            :ivar providers: 
            :ivar route_type: 
            :ivar tiers: 
            :ivar volumes: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.comments: Optional[str] = payload.get("comments")
            self.country: Optional[str] = payload.get("country")
            self.created_by: Optional[str] = payload.get("createdBy")
            self.currency: Optional[str] = payload.get("currency")
            self.effective_from: Optional[datetime] = payload.get("effectiveFrom")
            self.effective_to: Optional[datetime] = payload.get("effectiveTo")
            self.plan_type: Optional[str] = payload.get("planType")
            self.providers: Optional[List[str]] = payload.get("providers")
            self.route_type: Optional[int] = payload.get("routeType")
            self.tiers: Optional[List[TieredPlanList.TieredPlansTier]] = payload.get("tiers")
            self.volumes: Optional[List[float]] = payload.get("volumes")

        def to_dict(self):
            return {
                
                    "comments": self.comments,
                    "country": self.country,
                    "createdBy": self.created_by,
                    "currency": self.currency,
                    "effectiveFrom": self.effective_from,
                    "effectiveTo": self.effective_to,
                    "planType": self.plan_type,
                    "providers": self.providers,
                    "routeType": self.route_type,
                    "tiers": [tiers.to_dict() for tiers in self.tiers] if self.tiers is not None else None,
                    "volumes": self.volumes,
            }

    class TieredPlansTier(object):
        """
            :ivar mcc: 
            :ivar mnc: 
            :ivar rates: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mcc: Optional[str] = payload.get("mcc")
            self.mnc: Optional[str] = payload.get("mnc")
            self.rates: Optional[List[float]] = payload.get("rates")

        def to_dict(self):
            return {
                
                    "mcc": self.mcc,
                    "mnc": self.mnc,
                    "rates": self.rates,
            }



    """
    :ivar comments: 
    :ivar country: 
    :ivar created_by: 
    :ivar currency: 
    :ivar date_created: 
    :ivar date_updated: 
    :ivar effective_from: 
    :ivar effective_to: 
    :ivar id: 
    :ivar plan_type: 
    :ivar providers: 
    :ivar route_type: 
    :ivar tiers: 
    :ivar volumes: 
    :ivar code: 
    :ivar message: 
    :ivar more_info: Link to Error Code References
    :ivar status: HTTP response status code
    :ivar failures: 
    :ivar billing_type: 
    :ivar mcc: 
    :ivar mnc: 
    :ivar provider_sid: 
    :ivar inbound_minute_cost: 
    :ivar inbound_minute_cost_fixed: 
    :ivar inbound_minute_cost_mobile: 
    :ivar inbound_minute_cost_payphone: 
    :ivar inbound_msg_cost: 
    :ivar monthly_recurring_cost: 
    :ivar non_recurring_cost: 
    :ivar number_type: 
    :ivar port_in_cost: 
    :ivar port_out_cost: 
    :ivar updated_by: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], id: Optional[str] = None):
        super().__init__(version)

        
        self.comments: Optional[str] = payload.get("comments")
        self.country: Optional[str] = payload.get("country")
        self.created_by: Optional[str] = payload.get("createdBy")
        self.currency: Optional[str] = payload.get("currency")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(payload.get("dateCreated"))
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(payload.get("dateUpdated"))
        self.effective_from: Optional[datetime] = deserialize.iso8601_datetime(payload.get("effectiveFrom"))
        self.effective_to: Optional[datetime] = deserialize.iso8601_datetime(payload.get("effectiveTo"))
        self.id: Optional[int] = payload.get("id")
        self.plan_type: Optional[str] = payload.get("planType")
        self.providers: Optional[List[str]] = payload.get("providers")
        self.route_type: Optional[int] = deserialize.integer(payload.get("routeType"))
        self.tiers: Optional[List[str]] = payload.get("tiers")
        self.volumes: Optional[List[float]] = payload.get("volumes")
        self.code: Optional[int] = payload.get("code")
        self.message: Optional[str] = payload.get("message")
        self.more_info: Optional[str] = payload.get("more_info")
        self.status: Optional[int] = payload.get("status")
        self.failures: Optional[List[str]] = payload.get("failures")
        self.billing_type: Optional[str] = payload.get("billingType")
        self.mcc: Optional[str] = payload.get("mcc")
        self.mnc: Optional[str] = payload.get("mnc")
        self.provider_sid: Optional[str] = payload.get("providerSid")
        self.inbound_minute_cost: Optional[float] = deserialize.decimal(payload.get("inboundMinuteCost"))
        self.inbound_minute_cost_fixed: Optional[float] = deserialize.decimal(payload.get("inboundMinuteCostFixed"))
        self.inbound_minute_cost_mobile: Optional[float] = deserialize.decimal(payload.get("inboundMinuteCostMobile"))
        self.inbound_minute_cost_payphone: Optional[float] = deserialize.decimal(payload.get("inboundMinuteCostPayphone"))
        self.inbound_msg_cost: Optional[float] = deserialize.decimal(payload.get("inboundMsgCost"))
        self.monthly_recurring_cost: Optional[float] = deserialize.decimal(payload.get("monthlyRecurringCost"))
        self.non_recurring_cost: Optional[float] = deserialize.decimal(payload.get("nonRecurringCost"))
        self.number_type: Optional[str] = payload.get("numberType")
        self.port_in_cost: Optional[float] = deserialize.decimal(payload.get("portInCost"))
        self.port_out_cost: Optional[float] = deserialize.decimal(payload.get("portOutCost"))
        self.updated_by: Optional[str] = payload.get("updatedBy")

        
        
        self._solution = { 
            "id": id or self.id,
        }
        
        
        self._context: Optional[TieredPlanContext] = None

    @property
    def _proxy(self) -> "TieredPlanContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: TieredPlanContext for this TieredPlanInstance
        """
        if self._context is None:
            self._context = TieredPlanContext(self._version, id=self._solution['id'],)
        return self._context
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.TieredPlanInstance {}>'.format(context)

class TieredPlanContext(InstanceContext):


    class TieredPlansInput(object):
        """
            :ivar comments: 
            :ivar country: 
            :ivar created_by: 
            :ivar currency: 
            :ivar effective_from: 
            :ivar effective_to: 
            :ivar plan_type: 
            :ivar providers: 
            :ivar route_type: 
            :ivar tiers: 
            :ivar volumes: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.comments: Optional[str] = payload.get("comments")
            self.country: Optional[str] = payload.get("country")
            self.created_by: Optional[str] = payload.get("createdBy")
            self.currency: Optional[str] = payload.get("currency")
            self.effective_from: Optional[datetime] = payload.get("effectiveFrom")
            self.effective_to: Optional[datetime] = payload.get("effectiveTo")
            self.plan_type: Optional[str] = payload.get("planType")
            self.providers: Optional[List[str]] = payload.get("providers")
            self.route_type: Optional[int] = payload.get("routeType")
            self.tiers: Optional[List[TieredPlanList.TieredPlansTier]] = payload.get("tiers")
            self.volumes: Optional[List[float]] = payload.get("volumes")

        def to_dict(self):
            return {
                
                    "comments": self.comments,
                    "country": self.country,
                    "createdBy": self.created_by,
                    "currency": self.currency,
                    "effectiveFrom": self.effective_from,
                    "effectiveTo": self.effective_to,
                    "planType": self.plan_type,
                    "providers": self.providers,
                    "routeType": self.route_type,
                    "tiers": [tiers.to_dict() for tiers in self.tiers] if self.tiers is not None else None,
                    "volumes": self.volumes,
            }

    class TieredPlansTier(object):
        """
            :ivar mcc: 
            :ivar mnc: 
            :ivar rates: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mcc: Optional[str] = payload.get("mcc")
            self.mnc: Optional[str] = payload.get("mnc")
            self.rates: Optional[List[float]] = payload.get("rates")

        def to_dict(self):
            return {
                
                    "mcc": self.mcc,
                    "mnc": self.mnc,
                    "rates": self.rates,
            }


    def __init__(self, version: Version, id: str):
        """
        Initialize the TieredPlanContext

        :param version: Version that contains the resource
        :param id: 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'id': id,
        }
        self._uri = '/rate/sms/tieredPlans/{id}'.format(**self._solution)
        
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.TieredPlanContext {}>'.format(context)



class TieredPlanList(ListResource):
    
    class TieredPlansInput(object):
        """
            :ivar comments: 
            :ivar country: 
            :ivar created_by: 
            :ivar currency: 
            :ivar effective_from: 
            :ivar effective_to: 
            :ivar plan_type: 
            :ivar providers: 
            :ivar route_type: 
            :ivar tiers: 
            :ivar volumes: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.comments: Optional[str] = payload.get("comments")
            self.country: Optional[str] = payload.get("country")
            self.created_by: Optional[str] = payload.get("createdBy")
            self.currency: Optional[str] = payload.get("currency")
            self.effective_from: Optional[datetime] = payload.get("effectiveFrom")
            self.effective_to: Optional[datetime] = payload.get("effectiveTo")
            self.plan_type: Optional[str] = payload.get("planType")
            self.providers: Optional[List[str]] = payload.get("providers")
            self.route_type: Optional[int] = payload.get("routeType")
            self.tiers: Optional[List[TieredPlanList.TieredPlansTier]] = payload.get("tiers")
            self.volumes: Optional[List[float]] = payload.get("volumes")

        def to_dict(self):
            return {
                
                    "comments": self.comments,
                    "country": self.country,
                    "createdBy": self.created_by,
                    "currency": self.currency,
                    "effectiveFrom": self.effective_from,
                    "effectiveTo": self.effective_to,
                    "planType": self.plan_type,
                    "providers": self.providers,
                    "routeType": self.route_type,
                    "tiers": [tiers.to_dict() for tiers in self.tiers] if self.tiers is not None else None,
                    "volumes": self.volumes,
            }

    class TieredPlansTier(object):
        """
            :ivar mcc: 
            :ivar mnc: 
            :ivar rates: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.mcc: Optional[str] = payload.get("mcc")
            self.mnc: Optional[str] = payload.get("mnc")
            self.rates: Optional[List[float]] = payload.get("rates")

        def to_dict(self):
            return {
                
                    "mcc": self.mcc,
                    "mnc": self.mnc,
                    "rates": self.rates,
            }


    def __init__(self, version: Version):
        """
        Initialize the TieredPlanList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/rate/sms/tieredPlans'
        
        
    
    def list(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
    ) -> List[TieredPlanInstance]:
        """
        Lists TieredPlanInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='PUT', uri=self._uri, params=data, headers=headers)
        
        return [TieredPlanInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
    ) -> List[TieredPlanInstance]:
        """
        Asynchronously lists TieredPlanInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='PUT', uri=self._uri, params=data, headers=headers)
        
        return [TieredPlanInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists TieredPlanInstance and returns headers from first page

        
        :param TieredPlansInput tiered_plans_input: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            tiered_plans_input=tiered_plans_input,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists TieredPlanInstance and returns headers from first page

        
        :param TieredPlansInput tiered_plans_input: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            tiered_plans_input=tiered_plans_input,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TieredPlanPage:
        """
        Retrieve a single page of TieredPlanInstance records from the API.
        Request is executed immediately
        
        :param tiered_plans_input: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TieredPlanInstance
        """
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='PUT', uri=self._uri, params=data, headers=headers)
        return TieredPlanPage(self._version, response)

    async def page_async(self, 
      tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TieredPlanPage:
        """
        Asynchronously retrieve a single page of TieredPlanInstance records from the API.
        Request is executed immediately
        
        :param tiered_plans_input: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TieredPlanInstance
        """
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='PUT', uri=self._uri, params=data, headers=headers)
        return TieredPlanPage(self._version, response)

    def page_with_http_info(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param tiered_plans_input: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TieredPlanPage, status code, and headers
        """
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='PUT', uri=self._uri, params=data, headers=headers)
        page = TieredPlanPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param tiered_plans_input: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TieredPlanPage, status code, and headers
        """
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='PUT', uri=self._uri, params=data, headers=headers)
        page = TieredPlanPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> TieredPlanPage:
        """
        Retrieve a specific page of TieredPlanInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TieredPlanInstance
        """
        response = self._version.domain.twilio.request(
            'PUT',
            target_url
        )
        return TieredPlanPage(self._version, response)

    async def get_page_async(self, target_url: str) -> TieredPlanPage:
        """
        Asynchronously retrieve a specific page of TieredPlanInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TieredPlanInstance
        """
        response = await self._version.domain.twilio.request_async(
            'PUT',
            target_url
        )
        return TieredPlanPage(self._version, response)

    
    def list(self, 
    ) -> List[TieredPlanInstance]:
        """
        Lists TieredPlanInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [TieredPlanInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[TieredPlanInstance]:
        """
        Asynchronously lists TieredPlanInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [TieredPlanInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists TieredPlanInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists TieredPlanInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TieredPlanPage:
        """
        Retrieve a single page of TieredPlanInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TieredPlanInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return TieredPlanPage(self._version, response)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TieredPlanPage:
        """
        Asynchronously retrieve a single page of TieredPlanInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TieredPlanInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return TieredPlanPage(self._version, response)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TieredPlanPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = TieredPlanPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TieredPlanPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = TieredPlanPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> TieredPlanPage:
        """
        Retrieve a specific page of TieredPlanInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TieredPlanInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return TieredPlanPage(self._version, response)

    async def get_page_async(self, target_url: str) -> TieredPlanPage:
        """
        Asynchronously retrieve a specific page of TieredPlanInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TieredPlanInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return TieredPlanPage(self._version, response)

    
    def list(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
    ) -> List[TieredPlanInstance]:
        """
        Lists TieredPlanInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [TieredPlanInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
    ) -> List[TieredPlanInstance]:
        """
        Asynchronously lists TieredPlanInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [TieredPlanInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists TieredPlanInstance and returns headers from first page

        
        :param TieredPlansInput tiered_plans_input: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            tiered_plans_input=tiered_plans_input,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists TieredPlanInstance and returns headers from first page

        
        :param TieredPlansInput tiered_plans_input: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            tiered_plans_input=tiered_plans_input,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TieredPlanPage:
        """
        Retrieve a single page of TieredPlanInstance records from the API.
        Request is executed immediately
        
        :param tiered_plans_input: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TieredPlanInstance
        """
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='POST', uri=self._uri, params=data, headers=headers)
        return TieredPlanPage(self._version, response)

    async def page_async(self, 
      tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TieredPlanPage:
        """
        Asynchronously retrieve a single page of TieredPlanInstance records from the API.
        Request is executed immediately
        
        :param tiered_plans_input: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TieredPlanInstance
        """
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='POST', uri=self._uri, params=data, headers=headers)
        return TieredPlanPage(self._version, response)

    def page_with_http_info(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param tiered_plans_input: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TieredPlanPage, status code, and headers
        """
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        page = TieredPlanPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        tiered_plans_input: Union[TieredPlansInput, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param tiered_plans_input: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TieredPlanPage, status code, and headers
        """
        data = values.of({ 
            'TieredPlansInput': tiered_plans_input,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        page = TieredPlanPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> TieredPlanPage:
        """
        Retrieve a specific page of TieredPlanInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TieredPlanInstance
        """
        response = self._version.domain.twilio.request(
            'POST',
            target_url
        )
        return TieredPlanPage(self._version, response)

    async def get_page_async(self, target_url: str) -> TieredPlanPage:
        """
        Asynchronously retrieve a specific page of TieredPlanInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TieredPlanInstance
        """
        response = await self._version.domain.twilio.request_async(
            'POST',
            target_url
        )
        return TieredPlanPage(self._version, response)



    def get(self, id: str) -> TieredPlanContext:
        """
        Constructs a TieredPlanContext
        
        :param id: 
        """
        return TieredPlanContext(self._version, id=id)

    def __call__(self, id: str) -> TieredPlanContext:
        """
        Constructs a TieredPlanContext
        
        :param id: 
        """
        return TieredPlanContext(self._version, id=id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.SuppliersAdminApiUs1.Users_manisingh_codehq_open_api_spec_suppliers_v1_openapi_yaml.TieredPlanList>'


