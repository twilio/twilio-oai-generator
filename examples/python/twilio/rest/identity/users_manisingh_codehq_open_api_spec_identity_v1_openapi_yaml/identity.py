r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Stytch Domain Unified API
    Customer Identity and Access Management API

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version






class IdentityInstance(InstanceResource):



    """
    :ivar identity: The retrieved identity object.
    :ivar code: Twilio-specific error code. This numeric code uniquely identifies the error type within Twilio's error taxonomy and can be used for programmatic handling. 
    :ivar detail: A human-readable explanation specific to this occurrence of the problem. This field's value can change between different occurrences of the same problem type. 
    :ivar errors: Array of validation errors for HTTP 400/422 responses. Each error contains a human-readable detail message and a JSON Pointer indicating the location of the error in the request. 
    :ivar instance: A URI reference that identifies the specific occurrence of the problem. It may or may not yield further information if dereferenced. 
    :ivar status: The HTTP status code generated by the origin server for this occurrence of the problem. This MUST match the HTTP status code returned in the response. 
    :ivar title: A short, human-readable summary of the problem type. It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization. 
    :ivar type: A URI reference that identifies the problem type. This URI should resolve to human-readable documentation for the problem type. 
    :ivar identities: List of identities.
    :ivar pagination: 
    """

    def __init__(self, version: Version, payload:ResponseResource, identity_id: Optional[str] = None):
        super().__init__(version)

        
        self.identity: Optional[Identity] = payload.get("identity")
        self.code: Optional[int] = deserialize.integer(payload.get("code"))
        self.detail: Optional[str] = payload.get("detail")
        self.errors: Optional[List[str]] = payload.get("errors")
        self.instance: Optional[str] = payload.get("instance")
        self.status: Optional[int] = deserialize.integer(payload.get("status"))
        self.title: Optional[str] = payload.get("title")
        self.type: Optional[str] = payload.get("type")
        self.identities: Optional[List[Identity]] = payload.get("identities")
        self.pagination: Optional[Pagination] = payload.get("pagination")

        
        
        # Only set _solution if path params are provided (not None)
        if identity_id is not None:
            self._solution = { 
                "identity_id": identity_id,
            }
        
        
        self._context: Optional[IdentityContext] = None

    @property
    def _proxy(self) -> "IdentityContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: IdentityContext for this IdentityInstance
        """
        if self._context is None:
            self._context = IdentityContext(self._version, identity_id=self._solution['identity_id'],)
        return self._context
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.IdentityInstance {}>'.format(context)

class IdentityContext(InstanceContext):


    def __init__(self, version: Version, identity_id: str):
        """
        Initialize the IdentityContext

        :param version: Version that contains the resource
        :param identity_id: The external ID of the identity, as provided by the Twilio developer's existing user management system. 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'identity_id': identity_id,
        }
        self._uri = '/Identities/{identity_id}'.format(**self._solution)
        
    
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.IdentityContext {}>'.format(context)




class IdentityList(ListResource):
    
    def __init__(self, version: Version):
        """
        Initialize the IdentityList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/Identities'
        
        
    
    def list(self, 
    ) -> List[IdentityInstance]:
        """
        Lists IdentityInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/problem+json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [IdentityInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[IdentityInstance]:
        """
        Asynchronously lists IdentityInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/problem+json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [IdentityInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists IdentityInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists IdentityInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> IdentityPage:
        """
        Retrieve a single page of IdentityInstance records from the API.
        Request is executed immediately
        
        :returns: Page of IdentityInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response = self._version.page(method='DELETE', uri=self._uri, params=data, headers=headers)
        return IdentityPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
    ) -> IdentityPage:
        """
        Asynchronously retrieve a single page of IdentityInstance records from the API.
        Request is executed immediately
        
        :returns: Page of IdentityInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        return IdentityPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = IdentityPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = IdentityPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> IdentityPage:
        """
        Retrieve a specific page of IdentityInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityInstance
        """
        response = self._version.domain.twilio.request(
            'DELETE',
            target_url
        )
        return IdentityPage(self._version, response)

    async def get_page_async(self, target_url: str) -> IdentityPage:
        """
        Asynchronously retrieve a specific page of IdentityInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityInstance
        """
        response = await self._version.domain.twilio.request_async(
            'DELETE',
            target_url
        )
        return IdentityPage(self._version, response)

    
    def list(self, 
    ) -> List[IdentityInstance]:
        """
        Lists IdentityInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [IdentityInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[IdentityInstance]:
        """
        Asynchronously lists IdentityInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [IdentityInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists IdentityInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists IdentityInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> IdentityPage:
        """
        Retrieve a single page of IdentityInstance records from the API.
        Request is executed immediately
        
        :returns: Page of IdentityInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return IdentityPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
    ) -> IdentityPage:
        """
        Asynchronously retrieve a single page of IdentityInstance records from the API.
        Request is executed immediately
        
        :returns: Page of IdentityInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return IdentityPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = IdentityPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = IdentityPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> IdentityPage:
        """
        Retrieve a specific page of IdentityInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return IdentityPage(self._version, response)

    async def get_page_async(self, target_url: str) -> IdentityPage:
        """
        Asynchronously retrieve a specific page of IdentityInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return IdentityPage(self._version, response)

    
    def list(self, 
        
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
    ) -> List[IdentityInstance]:
        """
        Lists IdentityInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': created_after,
            'createdBefore': created_before,
            'updatedAfter': updated_after,
            'updatedBefore': updated_before,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [IdentityInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
    ) -> List[IdentityInstance]:
        """
        Asynchronously lists IdentityInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': created_after,
            'createdBefore': created_before,
            'updatedAfter': updated_after,
            'updatedBefore': updated_before,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [IdentityInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists IdentityInstance and returns headers from first page

        
        :param str order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param str page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param datetime created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param datetime created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param datetime updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param datetime updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            order_by=order_by,
            page_token=page_token,
            created_after=created_after,
            created_before=created_before,
            updated_after=updated_after,
            updated_before=updated_before,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists IdentityInstance and returns headers from first page

        
        :param str order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param str page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param datetime created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param datetime created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param datetime updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param datetime updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            order_by=order_by,
            page_token=page_token,
            created_after=created_after,
            created_before=created_before,
            updated_after=updated_after,
            updated_before=updated_before,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      page_size: Union[int, object] = values.unset,
      order_by: Union[str, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      created_after: Union[datetime, object] = values.unset,
      created_before: Union[datetime, object] = values.unset,
      updated_after: Union[datetime, object] = values.unset,
      updated_before: Union[datetime, object] = values.unset,
    ) -> IdentityPage:
        """
        Retrieve a single page of IdentityInstance records from the API.
        Request is executed immediately
        
        :param page_size: Maximum number of results to return per page.
        :param order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :returns: Page of IdentityInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': serialize.iso8601_datetime(created_after),
            'createdBefore': serialize.iso8601_datetime(created_before),
            'updatedAfter': serialize.iso8601_datetime(updated_after),
            'updatedBefore': serialize.iso8601_datetime(updated_before),
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return IdentityPage(self._version, response, uri=self._uri, params=data)

    async def page_async(self, 
      page_size: Union[int, object] = values.unset,
      order_by: Union[str, object] = values.unset,
      page_token: Union[str, object] = values.unset,
      created_after: Union[datetime, object] = values.unset,
      created_before: Union[datetime, object] = values.unset,
      updated_after: Union[datetime, object] = values.unset,
      updated_before: Union[datetime, object] = values.unset,
    ) -> IdentityPage:
        """
        Asynchronously retrieve a single page of IdentityInstance records from the API.
        Request is executed immediately
        
        :param page_size: Maximum number of results to return per page.
        :param order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :returns: Page of IdentityInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': serialize.iso8601_datetime(created_after),
            'createdBefore': serialize.iso8601_datetime(created_before),
            'updatedAfter': serialize.iso8601_datetime(updated_after),
            'updatedBefore': serialize.iso8601_datetime(updated_before),
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return IdentityPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(self, 
        
        order_by: Union[str, object] = values.unset,
        
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityPage, status code, and headers
        """
        data = values.of({ 
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': serialize.iso8601_datetime(created_after),
            'createdBefore': serialize.iso8601_datetime(created_before),
            'updatedAfter': serialize.iso8601_datetime(updated_after),
            'updatedBefore': serialize.iso8601_datetime(updated_before),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = IdentityPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        order_by: Union[str, object] = values.unset,
        
        created_after: Union[datetime, object] = values.unset,
        created_before: Union[datetime, object] = values.unset,
        updated_after: Union[datetime, object] = values.unset,
        updated_before: Union[datetime, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param order_by: Sort direction for results. Must remain consistent across paginated requests.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param created_after: Filter results to records with `createdAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param created_before: Filter results to records with `createdAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `created_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param updated_after: Filter results to records with `updatedAt` greater than or equal to this instant. Must be RFC 3339 UTC (e.g. `2026-01-01T00:00:00Z`). 
        :param updated_before: Filter results to records with `updatedAt` strictly less than this instant. Must be RFC 3339 UTC (e.g. `2026-02-01T00:00:00Z`). Combine with `updated_after` to express a BETWEEN-style range (inclusive lower, exclusive upper). 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityPage, status code, and headers
        """
        data = values.of({ 
            'orderBy': order_by,
            'pageToken': page_token,
            'createdAfter': serialize.iso8601_datetime(created_after),
            'createdBefore': serialize.iso8601_datetime(created_before),
            'updatedAfter': serialize.iso8601_datetime(updated_after),
            'updatedBefore': serialize.iso8601_datetime(updated_before),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = IdentityPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> IdentityPage:
        """
        Retrieve a specific page of IdentityInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return IdentityPage(self._version, response)

    async def get_page_async(self, target_url: str) -> IdentityPage:
        """
        Asynchronously retrieve a specific page of IdentityInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return IdentityPage(self._version, response)



    def get(self, identity_id: str) -> IdentityContext:
        """
        Constructs a IdentityContext
        
        :param identity_id: The external ID of the identity, as provided by the Twilio developer's existing user management system. 
        """
        return IdentityContext(self._version, identity_id=identity_id)

    def __call__(self, identity_id: str) -> IdentityContext:
        """
        Constructs a IdentityContext
        
        :param identity_id: The external ID of the identity, as provided by the Twilio developer's existing user management system. 
        """
        return IdentityContext(self._version, identity_id=identity_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.IdentityList>'





