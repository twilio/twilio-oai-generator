r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Stytch Domain Unified API
    Customer Identity and Access Management API

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version






class AuthorizationInstance(InstanceResource):



    class IdpClientType(object):
        THIRD_PARTY_PUBLIC = "THIRD_PARTY_PUBLIC"
        THIRD_PARTY_CONFIDENTIAL = "THIRD_PARTY_CONFIDENTIAL"
        FIRST_PARTY_PUBLIC = "FIRST_PARTY_PUBLIC"
        FIRST_PARTY_CONFIDENTIAL = "FIRST_PARTY_CONFIDENTIAL"

    """
    :ivar authorizations: List of the identity's active authorizations.
    :ivar meta: 
    :ivar code: Twilio-specific error code. This numeric code uniquely identifies the error type within Twilio's error taxonomy and can be used for programmatic handling. 
    :ivar detail: A human-readable explanation specific to this occurrence of the problem. This field's value can change between different occurrences of the same problem type. 
    :ivar errors: Array of validation errors for HTTP 400/422 responses. Each error contains a human-readable detail message and a JSON Pointer indicating the location of the error in the request. 
    :ivar instance: A URI reference that identifies the specific occurrence of the problem. It may or may not yield further information if dereferenced. 
    :ivar status: The HTTP status code generated by the origin server for this occurrence of the problem. This MUST match the HTTP status code returned in the response. 
    :ivar title: A short, human-readable summary of the problem type. It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization. 
    :ivar type: A URI reference that identifies the problem type. This URI should resolve to human-readable documentation for the problem type. 
    :ivar rich_authorization_resources: RFC-9396 rich authorization resource type definitions stored for this account.
    :ivar scopes: Custom OAuth2 scope definitions stored for this account.
    """

    def __init__(self, version: Version, payload:ResponseResource, identity_id: str, client_id: Optional[str] = None):
        super().__init__(version)

        
        self.authorizations: Optional[List[IdentityAuthorization]] = payload.get("authorizations")
        self.meta: Optional[Pagination] = payload.get("meta")
        self.code: Optional[int] = deserialize.integer(payload.get("code"))
        self.detail: Optional[str] = payload.get("detail")
        self.errors: Optional[List[ErrorDetail]] = payload.get("errors")
        self.instance: Optional[str] = payload.get("instance")
        self.status: Optional[int] = deserialize.integer(payload.get("status"))
        self.title: Optional[str] = payload.get("title")
        self.type: Optional[str] = payload.get("type")
        self.rich_authorization_resources: Optional[List[RichAuthorizationResourceDefinition]] = payload.get("richAuthorizationResources")
        self.scopes: Optional[List[ScopeDefinition]] = payload.get("scopes")

        
        
        # Only set _solution if path params are provided (not None)
        if identity_id is not None or client_id is not None:
            self._solution = { 
                "identity_id": identity_id,
                "client_id": client_id,
            }
        
        
        self._context: Optional[AuthorizationContext] = None

    @property
    def _proxy(self) -> "AuthorizationContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: AuthorizationContext for this AuthorizationInstance
        """
        if self._context is None:
            self._context = AuthorizationContext(self._version, identity_id=self._solution['identity_id'], client_id=self._solution['client_id'],)
        return self._context
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.AuthorizationInstance {}>'.format(context)

class AuthorizationContext(InstanceContext):


    def __init__(self, version: Version, identity_id: str, client_id: str):
        """
        Initialize the AuthorizationContext

        :param version: Version that contains the resource
        :param identity_id: The external ID of the identity, as provided by the Twilio developer's existing user management system. 
        :param client_id: Account-unique identifier for the client. 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'identity_id': identity_id,
            'client_id': client_id,
        }
        self._uri = '/Identities/{identity_id}/Authorizations/{client_id}'.format(**self._solution)
        
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.AuthorizationContext {}>'.format(context)




class AuthorizationList(ListResource):
    
    class ConfigurationAuthorizationUpdateRequest(object):
        """
            :ivar rich_authorization_resources: RFC-9396 rich authorization resource type definitions for this account.
            :ivar scopes: Custom OAuth2 scope definitions for this account.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.rich_authorization_resources: Optional[List[AuthorizationList.RichAuthorizationResourceDefinition]] = payload.get("richAuthorizationResources")
            self.scopes: Optional[List[AuthorizationList.ScopeDefinition]] = payload.get("scopes")

        def to_dict(self):
            return {
                
                    "richAuthorizationResources": [rich_authorization_resources.to_dict() for rich_authorization_resources in self.rich_authorization_resources] if self.rich_authorization_resources is not None else None,
                    "scopes": [scopes.to_dict() for scopes in self.scopes] if self.scopes is not None else None,
            }

    class ErrorDetail(object):
        """
            :ivar detail: A human-readable explanation of the validation error for this specific field.
            :ivar pointer: A JSON Pointer (RFC 6901) to the location in the request where the error occurred.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.detail: Optional[str] = payload.get("detail")
            self.pointer: Optional[str] = payload.get("pointer")

        def to_dict(self):
            return {
                
                    "detail": self.detail,
                    "pointer": self.pointer,
            }

    class IdentityAuthorization(object):
        """
            :ivar client_id: The client ID for which the authorization was granted.
            :ivar client_name: The name of the client application.
            :ivar client_type: The type of the client application.
            :ivar created_at: Timestamp when the authorization was created.
            :ivar granted_scopes: List of scopes the identity granted to the client, with their descriptions.
            :ivar updated_at: Timestamp when the authorization was last updated.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.client_id: Optional[str] = payload.get("clientId")
            self.client_name: Optional[str] = payload.get("clientName")
            self.client_type: Optional[IdpClientType] = payload.get("clientType")
            self.created_at: Optional[datetime] = payload.get("createdAt")
            self.granted_scopes: Optional[List[IdentityGrantedScopeDefinition]] = payload.get("grantedScopes")
            self.updated_at: Optional[datetime] = payload.get("updatedAt")

        def to_dict(self):
            return {
                
                    "clientId": self.client_id,
                    "clientName": self.client_name,
                    "clientType": self.client_type.to_dict() if self.client_type is not None else None ,
                    "createdAt": self.created_at,
                    "grantedScopes": [granted_scopes.to_dict() for granted_scopes in self.granted_scopes] if self.granted_scopes is not None else None,
                    "updatedAt": self.updated_at,
            }

    class IdentityGrantedScopeDefinition(object):
        """
            :ivar description: A description of what the scope allows.
            :ivar name: The name of the scope.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.description: Optional[str] = payload.get("description")
            self.name: Optional[str] = payload.get("name")

        def to_dict(self):
            return {
                
                    "description": self.description,
                    "name": self.name,
            }

    class Pagination(object):
        """
            :ivar key: Identifier for the paginated resource type.
            :ivar next_token: Opaque cursor to retrieve the next page of results. Absent when there are no more results.
            :ivar previous_token: Opaque cursor to retrieve the previous page of results. Absent when on the first page.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.key: Optional[str] = payload.get("key")
            self.next_token: Optional[str] = payload.get("nextToken")
            self.previous_token: Optional[str] = payload.get("previousToken")

        def to_dict(self):
            return {
                
                    "key": self.key,
                    "nextToken": self.next_token,
                    "previousToken": self.previous_token,
            }

    class RichAuthorizationResourceDefinition(object):
        """
            :ivar approval_methods: Methods that can be used to approve requests with this resource.
            :ivar approval_ttl_seconds: TTL for tokens with this resource, in seconds.
            :ivar var_schema: A JSON Schema (draft 2020-12) object describing the fields in the authorization detail. Use \"properties\" to define fields and \"required\" to list mandatory ones. 
            :ivar template: Description template populated with values from the schema.
            :ivar type: Unique type identifier for this authorization resource.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.approval_methods: Optional[List[Enumstr]] = payload.get("approvalMethods")
            self.approval_ttl_seconds: Optional[int] = payload.get("approvalTtlSeconds")
            self.var_schema: Optional[Dict[str, object]] = payload.get("schema")
            self.template: Optional[str] = payload.get("template")
            self.type: Optional[str] = payload.get("type")

        def to_dict(self):
            return {
                
                    "approvalMethods": self.approval_methods,
                    "approvalTtlSeconds": self.approval_ttl_seconds,
                    "schema": self.var_schema,
                    "template": self.template,
                    "type": self.type,
            }

    class ScopeDefinition(object):
        """
            :ivar contains: A list of other scope names that this scope implicitly grants.
            :ivar description: A brief description of what permissions this scope grants.
            :ivar name: The scope identifier, used in OAuth2 scope parameters.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.contains: Optional[List[str]] = payload.get("contains")
            self.description: Optional[str] = payload.get("description")
            self.name: Optional[str] = payload.get("name")

        def to_dict(self):
            return {
                
                    "contains": self.contains,
                    "description": self.description,
                    "name": self.name,
            }


    def __init__(self, version: Version, identity_id: str):
        """
        Initialize the AuthorizationList

        :param version: Version that contains the resource
        :param identity_id: The external ID of the identity, as provided by the Twilio developer's existing user management system. 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'identity_id': identity_id,  }
        self._uri = '/Identities/{identity_id}/Authorizations'.format(**self._solution)
        
        
    
    def list(self, 
    ) -> List[AuthorizationInstance]:
        """
        Lists AuthorizationInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/problem+json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [AuthorizationInstance(self._version, item, identity_id=self._solution['identity_id']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[AuthorizationInstance]:
        """
        Asynchronously lists AuthorizationInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/problem+json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [AuthorizationInstance(self._version, item, identity_id=self._solution['identity_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AuthorizationInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AuthorizationInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> AuthorizationPage:
        """
        Retrieve a single page of AuthorizationInstance records from the API.
        Request is executed immediately
        
        :returns: Page of AuthorizationInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response = self._version.page(method='DELETE', uri=self._uri, params=data, headers=headers)
        return AuthorizationPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
    ) -> AuthorizationPage:
        """
        Asynchronously retrieve a single page of AuthorizationInstance records from the API.
        Request is executed immediately
        
        :returns: Page of AuthorizationInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        return AuthorizationPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = AuthorizationPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = AuthorizationPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AuthorizationPage:
        """
        Retrieve a specific page of AuthorizationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationInstance
        """
        response = self._version.domain.twilio.request(
            'DELETE',
            target_url
        )
        return AuthorizationPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AuthorizationPage:
        """
        Asynchronously retrieve a specific page of AuthorizationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationInstance
        """
        response = await self._version.domain.twilio.request_async(
            'DELETE',
            target_url
        )
        return AuthorizationPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[AuthorizationInstance]:
        """
        Lists AuthorizationInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [AuthorizationInstance(self._version, item, identity_id=self._solution['identity_id']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[AuthorizationInstance]:
        """
        Asynchronously lists AuthorizationInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [AuthorizationInstance(self._version, item, identity_id=self._solution['identity_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AuthorizationInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AuthorizationInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> AuthorizationPage:
        """
        Retrieve a single page of AuthorizationInstance records from the API.
        Request is executed immediately
        
        :returns: Page of AuthorizationInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return AuthorizationPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
    ) -> AuthorizationPage:
        """
        Asynchronously retrieve a single page of AuthorizationInstance records from the API.
        Request is executed immediately
        
        :returns: Page of AuthorizationInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return AuthorizationPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = AuthorizationPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = AuthorizationPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AuthorizationPage:
        """
        Retrieve a specific page of AuthorizationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return AuthorizationPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AuthorizationPage:
        """
        Asynchronously retrieve a specific page of AuthorizationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return AuthorizationPage(self._version, response, solution=self._solution)

    
    def list(self, 
        configuration_authorization_update_request: Union[ConfigurationAuthorizationUpdateRequest, object] = values.unset,
    ) -> List[AuthorizationInstance]:
        """
        Lists AuthorizationInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'ConfigurationAuthorizationUpdateRequest': configuration_authorization_update_request,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='PATCH', uri=self._uri, params=data, headers=headers)
        
        return [AuthorizationInstance(self._version, item, identity_id=self._solution['identity_id']) for item in payload[""]]
        

    async def list_async(self, 
        configuration_authorization_update_request: Union[ConfigurationAuthorizationUpdateRequest, object] = values.unset,
    ) -> List[AuthorizationInstance]:
        """
        Asynchronously lists AuthorizationInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'ConfigurationAuthorizationUpdateRequest': configuration_authorization_update_request,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='PATCH', uri=self._uri, params=data, headers=headers)
        
        return [AuthorizationInstance(self._version, item, identity_id=self._solution['identity_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        configuration_authorization_update_request: Union[ConfigurationAuthorizationUpdateRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AuthorizationInstance and returns headers from first page

        
        :param ConfigurationAuthorizationUpdateRequest configuration_authorization_update_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            configuration_authorization_update_request=configuration_authorization_update_request,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        configuration_authorization_update_request: Union[ConfigurationAuthorizationUpdateRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AuthorizationInstance and returns headers from first page

        
        :param ConfigurationAuthorizationUpdateRequest configuration_authorization_update_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            configuration_authorization_update_request=configuration_authorization_update_request,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      configuration_authorization_update_request: Union[ConfigurationAuthorizationUpdateRequest, object] = values.unset,
    ) -> AuthorizationPage:
        """
        Retrieve a single page of AuthorizationInstance records from the API.
        Request is executed immediately
        
        :param configuration_authorization_update_request: 
        :returns: Page of AuthorizationInstance
        """
        data = values.of({ 
            'ConfigurationAuthorizationUpdateRequest': configuration_authorization_update_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='PATCH', uri=self._uri, params=data, headers=headers)
        return AuthorizationPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      configuration_authorization_update_request: Union[ConfigurationAuthorizationUpdateRequest, object] = values.unset,
    ) -> AuthorizationPage:
        """
        Asynchronously retrieve a single page of AuthorizationInstance records from the API.
        Request is executed immediately
        
        :param configuration_authorization_update_request: 
        :returns: Page of AuthorizationInstance
        """
        data = values.of({ 
            'ConfigurationAuthorizationUpdateRequest': configuration_authorization_update_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='PATCH', uri=self._uri, params=data, headers=headers)
        return AuthorizationPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        configuration_authorization_update_request: Union[ConfigurationAuthorizationUpdateRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param configuration_authorization_update_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationPage, status code, and headers
        """
        data = values.of({ 
            'ConfigurationAuthorizationUpdateRequest': configuration_authorization_update_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='PATCH', uri=self._uri, params=data, headers=headers)
        page = AuthorizationPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        configuration_authorization_update_request: Union[ConfigurationAuthorizationUpdateRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param configuration_authorization_update_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationPage, status code, and headers
        """
        data = values.of({ 
            'ConfigurationAuthorizationUpdateRequest': configuration_authorization_update_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='PATCH', uri=self._uri, params=data, headers=headers)
        page = AuthorizationPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AuthorizationPage:
        """
        Retrieve a specific page of AuthorizationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationInstance
        """
        response = self._version.domain.twilio.request(
            'PATCH',
            target_url
        )
        return AuthorizationPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AuthorizationPage:
        """
        Asynchronously retrieve a specific page of AuthorizationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationInstance
        """
        response = await self._version.domain.twilio.request_async(
            'PATCH',
            target_url
        )
        return AuthorizationPage(self._version, response, solution=self._solution)

    
    def list(self, 
        
        page_token: Union[str, object] = values.unset,
    ) -> List[AuthorizationInstance]:
        """
        Lists AuthorizationInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'pageToken': page_token,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [AuthorizationInstance(self._version, item, identity_id=self._solution['identity_id']) for item in payload[""]]
        

    async def list_async(self, 
        
        page_token: Union[str, object] = values.unset,
    ) -> List[AuthorizationInstance]:
        """
        Asynchronously lists AuthorizationInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'pageToken': page_token,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [AuthorizationInstance(self._version, item, identity_id=self._solution['identity_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AuthorizationInstance and returns headers from first page

        
        :param str page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AuthorizationInstance and returns headers from first page

        
        :param str page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> AuthorizationPage:
        """
        Retrieve a single page of AuthorizationInstance records from the API.
        Request is executed immediately
        
        :param page_size: Maximum number of results to return per page.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :returns: Page of AuthorizationInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return AuthorizationPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> AuthorizationPage:
        """
        Asynchronously retrieve a single page of AuthorizationInstance records from the API.
        Request is executed immediately
        
        :param page_size: Maximum number of results to return per page.
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :returns: Page of AuthorizationInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return AuthorizationPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationPage, status code, and headers
        """
        data = values.of({ 
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = AuthorizationPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: Opaque pagination cursor returned by a previous list response. Pass the `nextToken` value to retrieve the next page, or the `previousToken` value to retrieve the previous page — both come back through this single slot. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationPage, status code, and headers
        """
        data = values.of({ 
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = AuthorizationPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AuthorizationPage:
        """
        Retrieve a specific page of AuthorizationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return AuthorizationPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AuthorizationPage:
        """
        Asynchronously retrieve a specific page of AuthorizationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return AuthorizationPage(self._version, response, solution=self._solution)



    def get(self, client_id: str) -> AuthorizationContext:
        """
        Constructs a AuthorizationContext
        
        :param client_id: Account-unique identifier for the client. 
        """
        return AuthorizationContext(self._version, identity_id=self._solution['identity_id'], client_id=client_id)

    def __call__(self, client_id: str) -> AuthorizationContext:
        """
        Constructs a AuthorizationContext
        
        :param client_id: Account-unique identifier for the client. 
        """
        return AuthorizationContext(self._version, identity_id=self._solution['identity_id'], client_id=client_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.AuthorizationList>'





