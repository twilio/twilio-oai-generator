r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Stytch Domain Unified API
    Customer Identity and Access Management API

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from __future__ import annotations

from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version






class PhoneNumberInstance(InstanceResource):



    """
    :ivar user_phone_number: 
    :ivar code: Twilio-specific error code. This numeric code uniquely identifies the error type within Twilio's error taxonomy and can be used for programmatic handling. 
    :ivar detail: A human-readable explanation specific to this occurrence of the problem. This field's value can change between different occurrences of the same problem type. 
    :ivar errors: Array of validation errors for HTTP 400/422 responses. Each error contains a human-readable detail message and a JSON Pointer indicating the location of the error in the request. 
    :ivar instance: A URI reference that identifies the specific occurrence of the problem. It may or may not yield further information if dereferenced. 
    :ivar status: The HTTP status code generated by the origin server for this occurrence of the problem. This MUST match the HTTP status code returned in the response. 
    :ivar title: A short, human-readable summary of the problem type. It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization. 
    :ivar type: A URI reference that identifies the problem type. This URI should resolve to human-readable documentation for the problem type. 
    """

    def __init__(self, version: Version, payload:ResponseResource, user_id: str, user_phone_number_id: Optional[str] = None):
        super().__init__(version)

        
        self.user_phone_number: Optional[UserPhoneNumber] = payload.get("userPhoneNumber")
        self.code: Optional[int] = deserialize.integer(payload.get("code"))
        self.detail: Optional[str] = payload.get("detail")
        self.errors: Optional[List[ErrorDetail]] = payload.get("errors")
        self.instance: Optional[str] = payload.get("instance")
        self.status: Optional[int] = deserialize.integer(payload.get("status"))
        self.title: Optional[str] = payload.get("title")
        self.type: Optional[str] = payload.get("type")

        
        
        # Only set _solution if path params are provided (not None)
        if user_id is not None or user_phone_number_id is not None:
            self._solution = { 
                "user_id": user_id,
                "user_phone_number_id": user_phone_number_id,
            }
        
        
        self._context: Optional[PhoneNumberContext] = None

    @property
    def _proxy(self) -> "PhoneNumberContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: PhoneNumberContext for this PhoneNumberInstance
        """
        if self._context is None:
            self._context = PhoneNumberContext(self._version, user_id=self._solution['user_id'], user_phone_number_id=self._solution['user_phone_number_id'],)
        return self._context
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.PhoneNumberInstance {}>'.format(context)

class PhoneNumberContext(InstanceContext):


    def __init__(self, version: Version, user_id: str, user_phone_number_id: str):
        """
        Initialize the PhoneNumberContext

        :param version: Version that contains the resource
        :param user_id: The ID of the user who owns this phone number.
        :param user_phone_number_id: The unique identifier of the user phone number.
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'user_id': user_id,
            'user_phone_number_id': user_phone_number_id,
        }
        self._uri = '/Auth/Users/{user_id}/PhoneNumbers/{user_phone_number_id}'.format(**self._solution)
        
    
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.PhoneNumberContext {}>'.format(context)




class PhoneNumberList(ListResource):
    
    class UserPhoneNumbersCreateRequest(object):
        """
            :ivar phone_number: The phone number to add in E.164 format. Must be unique within the account.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.phone_number: Optional[str] = payload.get("phoneNumber")

        def to_dict(self):
            return {
                
                    "phoneNumber": self.phone_number,
            }


    def __init__(self, version: Version, user_id: str):
        """
        Initialize the PhoneNumberList

        :param version: Version that contains the resource
        :param user_id: The ID of the user who owns this phone number.
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'user_id': user_id,  }
        self._uri = '/Auth/Users/{user_id}/PhoneNumbers'.format(**self._solution)
        
        
    
    def list(self, 
        hard: Union[bool, object] = values.unset,
    ) -> List[PhoneNumberInstance]:
        """
        Lists PhoneNumberInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/problem+json"
        
        data = values.of({ 
            'hard': hard,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [PhoneNumberInstance(self._version, item, user_id=self._solution['user_id']) for item in payload[""]]
        

    async def list_async(self, 
        hard: Union[bool, object] = values.unset,
    ) -> List[PhoneNumberInstance]:
        """
        Asynchronously lists PhoneNumberInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/problem+json"
        
        data = values.of({ 
            'hard': hard,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [PhoneNumberInstance(self._version, item, user_id=self._solution['user_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        hard: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists PhoneNumberInstance and returns headers from first page

        
        :param bool hard: When true, permanently deletes the phone number and frees it for reuse. When false (default), soft-deletes by setting status to deleted. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            hard=hard,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        hard: Union[bool, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists PhoneNumberInstance and returns headers from first page

        
        :param bool hard: When true, permanently deletes the phone number and frees it for reuse. When false (default), soft-deletes by setting status to deleted. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            hard=hard,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      hard: Union[bool, object] = values.unset,
    ) -> PhoneNumberPage:
        """
        Retrieve a single page of PhoneNumberInstance records from the API.
        Request is executed immediately
        
        :param hard: When true, permanently deletes the phone number and frees it for reuse. When false (default), soft-deletes by setting status to deleted. 
        :returns: Page of PhoneNumberInstance
        """
        data = values.of({ 
            'hard': serialize.boolean_to_string(hard),
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response = self._version.page(method='DELETE', uri=self._uri, params=data, headers=headers)
        return PhoneNumberPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      hard: Union[bool, object] = values.unset,
    ) -> PhoneNumberPage:
        """
        Asynchronously retrieve a single page of PhoneNumberInstance records from the API.
        Request is executed immediately
        
        :param hard: When true, permanently deletes the phone number and frees it for reuse. When false (default), soft-deletes by setting status to deleted. 
        :returns: Page of PhoneNumberInstance
        """
        data = values.of({ 
            'hard': serialize.boolean_to_string(hard),
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        return PhoneNumberPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        hard: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param hard: When true, permanently deletes the phone number and frees it for reuse. When false (default), soft-deletes by setting status to deleted. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PhoneNumberPage, status code, and headers
        """
        data = values.of({ 
            'hard': serialize.boolean_to_string(hard),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = PhoneNumberPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        hard: Union[bool, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param hard: When true, permanently deletes the phone number and frees it for reuse. When false (default), soft-deletes by setting status to deleted. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PhoneNumberPage, status code, and headers
        """
        data = values.of({ 
            'hard': serialize.boolean_to_string(hard),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = PhoneNumberPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> PhoneNumberPage:
        """
        Retrieve a specific page of PhoneNumberInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PhoneNumberInstance
        """
        response = self._version.domain.twilio.request(
            'DELETE',
            target_url
        )
        return PhoneNumberPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> PhoneNumberPage:
        """
        Asynchronously retrieve a specific page of PhoneNumberInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PhoneNumberInstance
        """
        response = await self._version.domain.twilio.request_async(
            'DELETE',
            target_url
        )
        return PhoneNumberPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[PhoneNumberInstance]:
        """
        Lists PhoneNumberInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [PhoneNumberInstance(self._version, item, user_id=self._solution['user_id']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[PhoneNumberInstance]:
        """
        Asynchronously lists PhoneNumberInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [PhoneNumberInstance(self._version, item, user_id=self._solution['user_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists PhoneNumberInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists PhoneNumberInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
    ) -> PhoneNumberPage:
        """
        Retrieve a single page of PhoneNumberInstance records from the API.
        Request is executed immediately
        
        :returns: Page of PhoneNumberInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return PhoneNumberPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
    ) -> PhoneNumberPage:
        """
        Asynchronously retrieve a single page of PhoneNumberInstance records from the API.
        Request is executed immediately
        
        :returns: Page of PhoneNumberInstance
        """
        data = values.of({ 
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return PhoneNumberPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PhoneNumberPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = PhoneNumberPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PhoneNumberPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = PhoneNumberPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> PhoneNumberPage:
        """
        Retrieve a specific page of PhoneNumberInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PhoneNumberInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return PhoneNumberPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> PhoneNumberPage:
        """
        Asynchronously retrieve a specific page of PhoneNumberInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PhoneNumberInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return PhoneNumberPage(self._version, response, solution=self._solution)

    
    def list(self, 
        user_phone_numbers_create_request: Union[UserPhoneNumbersCreateRequest, object] = values.unset,
    ) -> List[PhoneNumberInstance]:
        """
        Lists PhoneNumberInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'UserPhoneNumbersCreateRequest': user_phone_numbers_create_request,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [PhoneNumberInstance(self._version, item, user_id=self._solution['user_id']) for item in payload[""]]
        

    async def list_async(self, 
        user_phone_numbers_create_request: Union[UserPhoneNumbersCreateRequest, object] = values.unset,
    ) -> List[PhoneNumberInstance]:
        """
        Asynchronously lists PhoneNumberInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'UserPhoneNumbersCreateRequest': user_phone_numbers_create_request,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [PhoneNumberInstance(self._version, item, user_id=self._solution['user_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        user_phone_numbers_create_request: Union[UserPhoneNumbersCreateRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists PhoneNumberInstance and returns headers from first page

        
        :param UserPhoneNumbersCreateRequest user_phone_numbers_create_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            user_phone_numbers_create_request=user_phone_numbers_create_request,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        user_phone_numbers_create_request: Union[UserPhoneNumbersCreateRequest, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists PhoneNumberInstance and returns headers from first page

        
        :param UserPhoneNumbersCreateRequest user_phone_numbers_create_request: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            user_phone_numbers_create_request=user_phone_numbers_create_request,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      user_phone_numbers_create_request: Union[UserPhoneNumbersCreateRequest, object] = values.unset,
    ) -> PhoneNumberPage:
        """
        Retrieve a single page of PhoneNumberInstance records from the API.
        Request is executed immediately
        
        :param user_phone_numbers_create_request: 
        :returns: Page of PhoneNumberInstance
        """
        data = values.of({ 
            'UserPhoneNumbersCreateRequest': user_phone_numbers_create_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='POST', uri=self._uri, params=data, headers=headers)
        return PhoneNumberPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    async def page_async(self, 
      user_phone_numbers_create_request: Union[UserPhoneNumbersCreateRequest, object] = values.unset,
    ) -> PhoneNumberPage:
        """
        Asynchronously retrieve a single page of PhoneNumberInstance records from the API.
        Request is executed immediately
        
        :param user_phone_numbers_create_request: 
        :returns: Page of PhoneNumberInstance
        """
        data = values.of({ 
            'UserPhoneNumbersCreateRequest': user_phone_numbers_create_request,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response = await self._version.page_async(method='POST', uri=self._uri, params=data, headers=headers)
        return PhoneNumberPage(self._version, response, uri=self._uri, params=data, solution=self._solution)

    def page_with_http_info(self, 
        user_phone_numbers_create_request: Union[UserPhoneNumbersCreateRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param user_phone_numbers_create_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PhoneNumberPage, status code, and headers
        """
        data = values.of({ 
            'UserPhoneNumbersCreateRequest': user_phone_numbers_create_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        page = PhoneNumberPage(self._version, response, uri=self._uri, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        user_phone_numbers_create_request: Union[UserPhoneNumbersCreateRequest, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param user_phone_numbers_create_request: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PhoneNumberPage, status code, and headers
        """
        data = values.of({ 
            'UserPhoneNumbersCreateRequest': user_phone_numbers_create_request,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        headers["Accept"] = "application/problem+json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        page = PhoneNumberPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> PhoneNumberPage:
        """
        Retrieve a specific page of PhoneNumberInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PhoneNumberInstance
        """
        response = self._version.domain.twilio.request(
            'POST',
            target_url
        )
        return PhoneNumberPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> PhoneNumberPage:
        """
        Asynchronously retrieve a specific page of PhoneNumberInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PhoneNumberInstance
        """
        response = await self._version.domain.twilio.request_async(
            'POST',
            target_url
        )
        return PhoneNumberPage(self._version, response, solution=self._solution)



    def get(self, user_phone_number_id: str) -> PhoneNumberContext:
        """
        Constructs a PhoneNumberContext
        
        :param user_phone_number_id: The unique identifier of the user phone number.
        """
        return PhoneNumberContext(self._version, user_id=self._solution['user_id'], user_phone_number_id=user_phone_number_id)

    def __call__(self, user_phone_number_id: str) -> PhoneNumberContext:
        """
        Constructs a PhoneNumberContext
        
        :param user_phone_number_id: The unique identifier of the user phone number.
        """
        return PhoneNumberContext(self._version, user_id=self._solution['user_id'], user_phone_number_id=user_phone_number_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Identity.Users_manisingh_codehq_open_api_spec_identity_v1_openapi_yaml.PhoneNumberList>'





