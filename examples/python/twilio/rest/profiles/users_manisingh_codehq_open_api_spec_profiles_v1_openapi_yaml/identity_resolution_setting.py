r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio API definition for public-api profiles
    Powers Twilio public-api profiles

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class IdentityResolutionSettingInstance(InstanceResource):


    class IdentifierConfig(object):
        """
            :ivar id_type: Name of the identifier to use for identity resolution. Usual values are email, phone, user_id etc.
            :ivar trait_keys: List (optional) of traits to use as identifiers for identity resolution. This allows to promote a list of traits to be used as an identifiers for identity resolution. e.g. workPhone, homePhone traits can be promoted as one identifier as phone
            :ivar matching_algo: The algorithm to use for matching the identifier's value. Possible value are exact or fuzzy Exact will match the identifier exactly, while fuzzy (low precision) will allow for some variations.
            :ivar matching_threshold: The threshold as percentage to use for fuzzy matching. A value between 50 and 99. Required for fuzzy algo.
            :ivar limit: Maximum number of value to retain for this identifier for a profile.
            :ivar limit_policy: Policy to apply when the number of values  for the identifier exceeds the limit. The extra values will be removed using either fifo or lifo policy to make them compliant. fifo/lifo will be applied based on the event timestamp of the request that added the identifier.
            :ivar enforce_unique: True means only one profile will have the identifier, forcing profiles that share it to merge. False would allow multiple profiles to share the identifier and only merge if there is a matching MatchingRule.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id_type: Optional[str] = payload.get("idType")
            self.trait_keys: Optional[List[str]] = payload.get("traitKeys")
            self.matching_algo: Optional["IdentityResolutionSettingInstance.str"] = payload.get("matchingAlgo")
            self.matching_threshold: Optional[int] = payload.get("matchingThreshold")
            self.limit: Optional[int] = payload.get("limit")
            self.limit_policy: Optional["IdentityResolutionSettingInstance.str"] = payload.get("limitPolicy")
            self.enforce_unique: Optional[bool] = payload.get("enforceUnique")

        def to_dict(self):
            return {
                
                    "idType": self.id_type,
                    "traitKeys": self.trait_keys,
                    "matchingAlgo": self.matching_algo,
                    "matchingThreshold": self.matching_threshold,
                    "limit": self.limit,
                    "limitPolicy": self.limit_policy,
                    "enforceUnique": self.enforce_unique,
            }

    class IdentityResolutionSettings(object):
        """
            :ivar identifier_configs: List of identifier, traits to use as identifiers for identity resolution.
            :ivar matching_rules: List of rules to apply to match identifiers for new profile updates against existing profiles (if any). If no rule matches against existing profiles, a new profile will be created. If a rule matches to a single existing profile, the profile will updated with the new data. If a rule matches to multiple existing profiles, those existing profiles will be merged into a single unique profile updated with the new data.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.identifier_configs: Optional[List[IdentityResolutionSettingList.IdentifierConfig]] = payload.get("identifierConfigs")
            self.matching_rules: Optional[List[List[str]]] = payload.get("matchingRules")

        def to_dict(self):
            return {
                
                    "identifierConfigs": [identifier_configs.to_dict() for identifier_configs in self.identifier_configs] if self.identifier_configs is not None else None,
                    "matchingRules": self.matching_rules,
            }



    """
    :ivar identifier_configs: List of identifier, traits to use as identifiers for identity resolution.
    :ivar matching_rules: List of rules to apply to match identifiers for new profile updates against existing profiles (if any). If no rule matches against existing profiles, a new profile will be created. If a rule matches to a single existing profile, the profile will updated with the new data. If a rule matches to multiple existing profiles, those existing profiles will be merged into a single unique profile updated with the new data.
    """

    def __init__(self, version: Version, payload:Dict[str, Any], service_id: Optional[str] = None):
        super().__init__(version)

        
        self.identifier_configs: Optional[List[IdentityResolutionSettingList.str]] = payload.get("identifierConfigs")
        self.matching_rules: Optional[List[List[str]]] = payload.get("matchingRules")

        
        
        self._solution = { 
            "service_id": service_id or self.service_id,
        }
        
        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Profiles.Users_manisingh_codehq_open_api_spec_profiles_v1_openapi_yaml.IdentityResolutionSettingInstance {}>'.format(context)




class IdentityResolutionSettingList(ListResource):
    
    class IdentifierConfig(object):
        """
            :ivar id_type: Name of the identifier to use for identity resolution. Usual values are email, phone, user_id etc.
            :ivar trait_keys: List (optional) of traits to use as identifiers for identity resolution. This allows to promote a list of traits to be used as an identifiers for identity resolution. e.g. workPhone, homePhone traits can be promoted as one identifier as phone
            :ivar matching_algo: The algorithm to use for matching the identifier's value. Possible value are exact or fuzzy Exact will match the identifier exactly, while fuzzy (low precision) will allow for some variations.
            :ivar matching_threshold: The threshold as percentage to use for fuzzy matching. A value between 50 and 99. Required for fuzzy algo.
            :ivar limit: Maximum number of value to retain for this identifier for a profile.
            :ivar limit_policy: Policy to apply when the number of values  for the identifier exceeds the limit. The extra values will be removed using either fifo or lifo policy to make them compliant. fifo/lifo will be applied based on the event timestamp of the request that added the identifier.
            :ivar enforce_unique: True means only one profile will have the identifier, forcing profiles that share it to merge. False would allow multiple profiles to share the identifier and only merge if there is a matching MatchingRule.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.id_type: Optional[str] = payload.get("idType")
            self.trait_keys: Optional[List[str]] = payload.get("traitKeys")
            self.matching_algo: Optional["IdentityResolutionSettingInstance.str"] = payload.get("matchingAlgo")
            self.matching_threshold: Optional[int] = payload.get("matchingThreshold")
            self.limit: Optional[int] = payload.get("limit")
            self.limit_policy: Optional["IdentityResolutionSettingInstance.str"] = payload.get("limitPolicy")
            self.enforce_unique: Optional[bool] = payload.get("enforceUnique")

        def to_dict(self):
            return {
                
                    "idType": self.id_type,
                    "traitKeys": self.trait_keys,
                    "matchingAlgo": self.matching_algo,
                    "matchingThreshold": self.matching_threshold,
                    "limit": self.limit,
                    "limitPolicy": self.limit_policy,
                    "enforceUnique": self.enforce_unique,
            }

    class IdentityResolutionSettings(object):
        """
            :ivar identifier_configs: List of identifier, traits to use as identifiers for identity resolution.
            :ivar matching_rules: List of rules to apply to match identifiers for new profile updates against existing profiles (if any). If no rule matches against existing profiles, a new profile will be created. If a rule matches to a single existing profile, the profile will updated with the new data. If a rule matches to multiple existing profiles, those existing profiles will be merged into a single unique profile updated with the new data.
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.identifier_configs: Optional[List[IdentityResolutionSettingList.IdentifierConfig]] = payload.get("identifierConfigs")
            self.matching_rules: Optional[List[List[str]]] = payload.get("matchingRules")

        def to_dict(self):
            return {
                
                    "identifierConfigs": [identifier_configs.to_dict() for identifier_configs in self.identifier_configs] if self.identifier_configs is not None else None,
                    "matchingRules": self.matching_rules,
            }


    def __init__(self, version: Version, service_id: str):
        """
        Initialize the IdentityResolutionSettingList

        :param version: Version that contains the resource
        :param service_id: Profile Service ID
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'service_id': service_id,  }
        self._uri = '/ControlPlane/Services/{service_id}/IdentityResolutionSettings'.format(**self._solution)
        
        
    
    def list(self, 
        x_pre_auth_context: Union[str, object] = values.unset,
    ) -> List[IdentityResolutionSettingInstance]:
        """
        Lists IdentityResolutionSettingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'X-Pre-Auth-Context': x_pre_auth_context,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [IdentityResolutionSettingInstance(self._version, item, service_id=self._solution['service_id']) for item in payload[""]]
        

    async def list_async(self, 
        x_pre_auth_context: Union[str, object] = values.unset,
    ) -> List[IdentityResolutionSettingInstance]:
        """
        Asynchronously lists IdentityResolutionSettingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'X-Pre-Auth-Context': x_pre_auth_context,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [IdentityResolutionSettingInstance(self._version, item, service_id=self._solution['service_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        x_pre_auth_context: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists IdentityResolutionSettingInstance and returns headers from first page

        
        :param str x_pre_auth_context: Twilio Account
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            x_pre_auth_context=x_pre_auth_context,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        x_pre_auth_context: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists IdentityResolutionSettingInstance and returns headers from first page

        
        :param str x_pre_auth_context: Twilio Account
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            x_pre_auth_context=x_pre_auth_context,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      x_pre_auth_context: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> IdentityResolutionSettingPage:
        """
        Retrieve a single page of IdentityResolutionSettingInstance records from the API.
        Request is executed immediately
        
        :param x_pre_auth_context: Twilio Account
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of IdentityResolutionSettingInstance
        """
        data = values.of({ 
            'X-Pre-Auth-Context': x_pre_auth_context,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Pre-Auth-Context': x_pre_auth_context,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return IdentityResolutionSettingPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      x_pre_auth_context: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> IdentityResolutionSettingPage:
        """
        Asynchronously retrieve a single page of IdentityResolutionSettingInstance records from the API.
        Request is executed immediately
        
        :param x_pre_auth_context: Twilio Account
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of IdentityResolutionSettingInstance
        """
        data = values.of({ 
            'X-Pre-Auth-Context': x_pre_auth_context,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Pre-Auth-Context': x_pre_auth_context,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return IdentityResolutionSettingPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        x_pre_auth_context: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param x_pre_auth_context: Twilio Account
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityResolutionSettingPage, status code, and headers
        """
        data = values.of({ 
            'X-Pre-Auth-Context': x_pre_auth_context,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Pre-Auth-Context': x_pre_auth_context,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = IdentityResolutionSettingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        x_pre_auth_context: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param x_pre_auth_context: Twilio Account
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityResolutionSettingPage, status code, and headers
        """
        data = values.of({ 
            'X-Pre-Auth-Context': x_pre_auth_context,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Pre-Auth-Context': x_pre_auth_context,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = IdentityResolutionSettingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> IdentityResolutionSettingPage:
        """
        Retrieve a specific page of IdentityResolutionSettingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityResolutionSettingInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return IdentityResolutionSettingPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> IdentityResolutionSettingPage:
        """
        Asynchronously retrieve a specific page of IdentityResolutionSettingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityResolutionSettingInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return IdentityResolutionSettingPage(self._version, response, solution=self._solution)

    
    def list(self, 
        identity_resolution_settings: Union[IdentityResolutionSettings, object] = values.unset,
        x_pre_auth_context: Union[str, object] = values.unset,
    ) -> List[IdentityResolutionSettingInstance]:
        """
        Lists IdentityResolutionSettingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'IdentityResolutionSettings': identity_resolution_settings,
            'X-Pre-Auth-Context': x_pre_auth_context,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='PUT', uri=self._uri, params=data, headers=headers)
        
        return [IdentityResolutionSettingInstance(self._version, item, service_id=self._solution['service_id']) for item in payload[""]]
        

    async def list_async(self, 
        identity_resolution_settings: Union[IdentityResolutionSettings, object] = values.unset,
        x_pre_auth_context: Union[str, object] = values.unset,
    ) -> List[IdentityResolutionSettingInstance]:
        """
        Asynchronously lists IdentityResolutionSettingInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'IdentityResolutionSettings': identity_resolution_settings,
            'X-Pre-Auth-Context': x_pre_auth_context,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='PUT', uri=self._uri, params=data, headers=headers)
        
        return [IdentityResolutionSettingInstance(self._version, item, service_id=self._solution['service_id']) for item in payload[""]]
        


    def list_with_http_info(self, 
        identity_resolution_settings: Union[IdentityResolutionSettings, object] = values.unset,
        x_pre_auth_context: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists IdentityResolutionSettingInstance and returns headers from first page

        
        :param IdentityResolutionSettings identity_resolution_settings: 
        :param str x_pre_auth_context: Twilio Account
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            identity_resolution_settings=identity_resolution_settings,
            x_pre_auth_context=x_pre_auth_context,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        identity_resolution_settings: Union[IdentityResolutionSettings, object] = values.unset,
        x_pre_auth_context: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists IdentityResolutionSettingInstance and returns headers from first page

        
        :param IdentityResolutionSettings identity_resolution_settings: 
        :param str x_pre_auth_context: Twilio Account
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            identity_resolution_settings=identity_resolution_settings,
            x_pre_auth_context=x_pre_auth_context,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      identity_resolution_settings: Union[IdentityResolutionSettings, object] = values.unset,
      x_pre_auth_context: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> IdentityResolutionSettingPage:
        """
        Retrieve a single page of IdentityResolutionSettingInstance records from the API.
        Request is executed immediately
        
        :param identity_resolution_settings: 
        :param x_pre_auth_context: Twilio Account
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of IdentityResolutionSettingInstance
        """
        data = values.of({ 
            'IdentityResolutionSettings': identity_resolution_settings,
            'X-Pre-Auth-Context': x_pre_auth_context,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Pre-Auth-Context': x_pre_auth_context,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='PUT', uri=self._uri, params=data, headers=headers)
        return IdentityResolutionSettingPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      identity_resolution_settings: Union[IdentityResolutionSettings, object] = values.unset,
      x_pre_auth_context: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> IdentityResolutionSettingPage:
        """
        Asynchronously retrieve a single page of IdentityResolutionSettingInstance records from the API.
        Request is executed immediately
        
        :param identity_resolution_settings: 
        :param x_pre_auth_context: Twilio Account
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of IdentityResolutionSettingInstance
        """
        data = values.of({ 
            'IdentityResolutionSettings': identity_resolution_settings,
            'X-Pre-Auth-Context': x_pre_auth_context,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Pre-Auth-Context': x_pre_auth_context,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='PUT', uri=self._uri, params=data, headers=headers)
        return IdentityResolutionSettingPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        identity_resolution_settings: Union[IdentityResolutionSettings, object] = values.unset,
        x_pre_auth_context: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param identity_resolution_settings: 
        :param x_pre_auth_context: Twilio Account
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityResolutionSettingPage, status code, and headers
        """
        data = values.of({ 
            'IdentityResolutionSettings': identity_resolution_settings,
            'X-Pre-Auth-Context': x_pre_auth_context,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Pre-Auth-Context': x_pre_auth_context,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='PUT', uri=self._uri, params=data, headers=headers)
        page = IdentityResolutionSettingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        identity_resolution_settings: Union[IdentityResolutionSettings, object] = values.unset,
        x_pre_auth_context: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param identity_resolution_settings: 
        :param x_pre_auth_context: Twilio Account
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with IdentityResolutionSettingPage, status code, and headers
        """
        data = values.of({ 
            'IdentityResolutionSettings': identity_resolution_settings,
            'X-Pre-Auth-Context': x_pre_auth_context,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Pre-Auth-Context': x_pre_auth_context,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='PUT', uri=self._uri, params=data, headers=headers)
        page = IdentityResolutionSettingPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> IdentityResolutionSettingPage:
        """
        Retrieve a specific page of IdentityResolutionSettingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityResolutionSettingInstance
        """
        response = self._version.domain.twilio.request(
            'PUT',
            target_url
        )
        return IdentityResolutionSettingPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> IdentityResolutionSettingPage:
        """
        Asynchronously retrieve a specific page of IdentityResolutionSettingInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of IdentityResolutionSettingInstance
        """
        response = await self._version.domain.twilio.request_async(
            'PUT',
            target_url
        )
        return IdentityResolutionSettingPage(self._version, response, solution=self._solution)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Profiles.Users_manisingh_codehq_open_api_spec_profiles_v1_openapi_yaml.IdentityResolutionSettingList>'


