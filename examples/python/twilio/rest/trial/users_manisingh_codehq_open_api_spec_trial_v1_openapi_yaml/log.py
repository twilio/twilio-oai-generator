r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Sample API definition for trial experience in 1C
    Powers trial experience in 1C

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class LogInstance(InstanceResource):


    class TrialV1EmailLogsGetMessagesByFilterRequest(object):
        """
            :ivar query: **Allowed event fields and operators**  | Field                    | Operators            | |------------------------- |----------------------| | sg_message_id            | `=`                  | | subject                  | `=`                  | | to_email                 | `=`                  | | status                   | `IN`                 | | reason                   | `=`                  | | categories               | `IN`                 | | sg_message_id_created_at | `>`, `<`, `>=`, `<=` |  - multiple conditions can be combined with `AND` - nesting is not allowed - `IN` operator accepts a list of string values: `field IN ('value1', 'value2')` 
            :ivar limit: Number of messages to return (1–1000).
            :ivar subusers: For requests coming from an authenticated parent user principal, this changes the scope of the search to the provided subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility, in case we want to allow multiple subusers in the future. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.query: Optional[str] = payload.get("query")
            self.limit: Optional[int] = payload.get("limit")
            self.subusers: Optional[List[str]] = payload.get("subusers")

        def to_dict(self):
            return {
                
                    "query": self.query,
                    "limit": self.limit,
                    "subusers": self.subusers,
            }



    class TrialV1EmailLogsMessageStatus(object):
        PROCESSED = "processed"
        DELIVERED = "delivered"
        DEFERRED = "deferred"
        DROPPED = "dropped"
        BOUNCED = "bounced"
        BLOCKED = "blocked"

    """
    :ivar from_email: The 'From' email address used to deliver the message. This may not be a valid email address. 
    :ivar sg_message_id: A unique ID assigned to the message.
    :ivar subject: The email's subject line.
    :ivar to_email: The intended recipient's email address.
    :ivar status: 
    :ivar template_id: The ID associated with a Twilio SendGrid email template used to format the message.
    :ivar asm_group_id: The unsubscribe group associated with this email.
    :ivar teammate: Teammate's username.
    :ivar api_key_id: The ID of the API Key used to authenticate the sending request for the message.
    :ivar events: All events recorded for this message. Most recent events will be returned upto the maximum of 100 events.
    :ivar client_ip: The IP address of the user who sent the message.
    :ivar category: The categories associated with the message.
    :ivar custom_args: Custom key-value pairs associated with the message.
    :ivar outbound_ip: The IP address used to send to the remote Mail Transfer Agent.
    :ivar outbound_ip_type: The type of outbound IP address used to send the message.
    :ivar marketing_campaign_id: The ID of the marketing campaign associated with the message.
    :ivar marketing_campaign_name: The name of the marketing campaign associated with the message.
    :ivar marketing_campaign_split_id: The ID of the split associated with the marketing campaign.
    :ivar marketing_campaign_version: The version of the marketing campaign associated with the message.
    :ivar messages: List of messages matching the filter query. 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], sg_msg_id: Optional[str] = None):
        super().__init__(version)

        
        self.from_email: Optional[str] = payload.get("from_email")
        self.sg_message_id: Optional[str] = payload.get("sg_message_id")
        self.subject: Optional[str] = payload.get("subject")
        self.to_email: Optional[str] = payload.get("to_email")
        self.status: Optional["LogInstance.str"] = payload.get("status")
        self.template_id: Optional[str] = payload.get("template_id")
        self.asm_group_id: Optional[int] = deserialize.integer(payload.get("asm_group_id"))
        self.teammate: Optional[str] = payload.get("teammate")
        self.api_key_id: Optional[str] = payload.get("api_key_id")
        self.events: Optional[List[str]] = payload.get("events")
        self.client_ip: Optional[str] = payload.get("client_ip")
        self.category: Optional[List[str]] = payload.get("category")
        self.custom_args: Optional[Dict[str, str]] = payload.get("custom_args")
        self.outbound_ip: Optional[str] = payload.get("outbound_ip")
        self.outbound_ip_type: Optional[str] = payload.get("outbound_ip_type")
        self.marketing_campaign_id: Optional[int] = deserialize.integer(payload.get("marketing_campaign_id"))
        self.marketing_campaign_name: Optional[str] = payload.get("marketing_campaign_name")
        self.marketing_campaign_split_id: Optional[int] = deserialize.integer(payload.get("marketing_campaign_split_id"))
        self.marketing_campaign_version: Optional[str] = payload.get("marketing_campaign_version")
        self.messages: Optional[List[str]] = payload.get("messages")

        
        
        self._solution = { 
            "sg_msg_id": sg_msg_id or self.sg_msg_id,
        }
        
        
        self._context: Optional[LogContext] = None

    @property
    def _proxy(self) -> "LogContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: LogContext for this LogInstance
        """
        if self._context is None:
            self._context = LogContext(self._version, sg_msg_id=self._solution['sg_msg_id'],)
        return self._context
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Trial.Users_manisingh_codehq_open_api_spec_trial_v1_openapi_yaml.LogInstance {}>'.format(context)

class LogContext(InstanceContext):


    class TrialV1EmailLogsGetMessagesByFilterRequest(object):
        """
            :ivar query: **Allowed event fields and operators**  | Field                    | Operators            | |------------------------- |----------------------| | sg_message_id            | `=`                  | | subject                  | `=`                  | | to_email                 | `=`                  | | status                   | `IN`                 | | reason                   | `=`                  | | categories               | `IN`                 | | sg_message_id_created_at | `>`, `<`, `>=`, `<=` |  - multiple conditions can be combined with `AND` - nesting is not allowed - `IN` operator accepts a list of string values: `field IN ('value1', 'value2')` 
            :ivar limit: Number of messages to return (1–1000).
            :ivar subusers: For requests coming from an authenticated parent user principal, this changes the scope of the search to the provided subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility, in case we want to allow multiple subusers in the future. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.query: Optional[str] = payload.get("query")
            self.limit: Optional[int] = payload.get("limit")
            self.subusers: Optional[List[str]] = payload.get("subusers")

        def to_dict(self):
            return {
                
                    "query": self.query,
                    "limit": self.limit,
                    "subusers": self.subusers,
            }


    def __init__(self, version: Version, sg_msg_id: str):
        """
        Initialize the LogContext

        :param version: Version that contains the resource
        :param sg_msg_id: The ID of the message you are requesting details for.
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'sg_msg_id': sg_msg_id,
        }
        self._uri = '/Mail/Logs/{sg_msg_id}'.format(**self._solution)
        
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Trial.Users_manisingh_codehq_open_api_spec_trial_v1_openapi_yaml.LogContext {}>'.format(context)



class LogList(ListResource):
    
    class TrialV1EmailLogsGetMessagesByFilterRequest(object):
        """
            :ivar query: **Allowed event fields and operators**  | Field                    | Operators            | |------------------------- |----------------------| | sg_message_id            | `=`                  | | subject                  | `=`                  | | to_email                 | `=`                  | | status                   | `IN`                 | | reason                   | `=`                  | | categories               | `IN`                 | | sg_message_id_created_at | `>`, `<`, `>=`, `<=` |  - multiple conditions can be combined with `AND` - nesting is not allowed - `IN` operator accepts a list of string values: `field IN ('value1', 'value2')` 
            :ivar limit: Number of messages to return (1–1000).
            :ivar subusers: For requests coming from an authenticated parent user principal, this changes the scope of the search to the provided subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility, in case we want to allow multiple subusers in the future. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.query: Optional[str] = payload.get("query")
            self.limit: Optional[int] = payload.get("limit")
            self.subusers: Optional[List[str]] = payload.get("subusers")

        def to_dict(self):
            return {
                
                    "query": self.query,
                    "limit": self.limit,
                    "subusers": self.subusers,
            }


    def __init__(self, version: Version):
        """
        Initialize the LogList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/Mail/Logs'
        
        
    
    def list(self, 
        x_staging_access: Union[str, object] = values.unset,
        subuser: Union[str, object] = values.unset,
    ) -> List[LogInstance]:
        """
        Lists LogInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'X-Staging-Access': x_staging_access,
            'subuser': subuser,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [LogInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        x_staging_access: Union[str, object] = values.unset,
        subuser: Union[str, object] = values.unset,
    ) -> List[LogInstance]:
        """
        Asynchronously lists LogInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'X-Staging-Access': x_staging_access,
            'subuser': subuser,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [LogInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        x_staging_access: Union[str, object] = values.unset,
        subuser: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists LogInstance and returns headers from first page

        
        :param str x_staging_access: 
        :param str subuser: For requests coming from an authenticated parent user principal, this argument must be passed if the message belongs to a subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            x_staging_access=x_staging_access,
            subuser=subuser,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        x_staging_access: Union[str, object] = values.unset,
        subuser: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists LogInstance and returns headers from first page

        
        :param str x_staging_access: 
        :param str subuser: For requests coming from an authenticated parent user principal, this argument must be passed if the message belongs to a subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            x_staging_access=x_staging_access,
            subuser=subuser,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      x_staging_access: Union[str, object] = values.unset,
      subuser: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> LogPage:
        """
        Retrieve a single page of LogInstance records from the API.
        Request is executed immediately
        
        :param x_staging_access: 
        :param subuser: For requests coming from an authenticated parent user principal, this argument must be passed if the message belongs to a subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of LogInstance
        """
        data = values.of({ 
            'X-Staging-Access': x_staging_access,
            'subuser': subuser,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Staging-Access': x_staging_access,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return LogPage(self._version, response)

    async def page_async(self, 
      x_staging_access: Union[str, object] = values.unset,
      subuser: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> LogPage:
        """
        Asynchronously retrieve a single page of LogInstance records from the API.
        Request is executed immediately
        
        :param x_staging_access: 
        :param subuser: For requests coming from an authenticated parent user principal, this argument must be passed if the message belongs to a subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of LogInstance
        """
        data = values.of({ 
            'X-Staging-Access': x_staging_access,
            'subuser': subuser,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Staging-Access': x_staging_access,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return LogPage(self._version, response)

    def page_with_http_info(self, 
        x_staging_access: Union[str, object] = values.unset,
        subuser: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param x_staging_access: 
        :param subuser: For requests coming from an authenticated parent user principal, this argument must be passed if the message belongs to a subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with LogPage, status code, and headers
        """
        data = values.of({ 
            'X-Staging-Access': x_staging_access,
            'subuser': subuser,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Staging-Access': x_staging_access,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = LogPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        x_staging_access: Union[str, object] = values.unset,
        subuser: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param x_staging_access: 
        :param subuser: For requests coming from an authenticated parent user principal, this argument must be passed if the message belongs to a subuser. Only 1 item is allowed in this list, but we make it a list for forward compatibility. Even though all SG User IDs are numeric, we pass them as strings for forward compatibility, if user IDs ever change to non-numeric values. 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with LogPage, status code, and headers
        """
        data = values.of({ 
            'X-Staging-Access': x_staging_access,
            'subuser': subuser,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Staging-Access': x_staging_access,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = LogPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> LogPage:
        """
        Retrieve a specific page of LogInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of LogInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return LogPage(self._version, response)

    async def get_page_async(self, target_url: str) -> LogPage:
        """
        Asynchronously retrieve a specific page of LogInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of LogInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return LogPage(self._version, response)

    
    def list(self, 
        trial_v1_email_logs_get_messages_by_filter_request: Union[TrialV1EmailLogsGetMessagesByFilterRequest, object] = values.unset,
        x_staging_access: Union[str, object] = values.unset,
    ) -> List[LogInstance]:
        """
        Lists LogInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'TrialV1EmailLogsGetMessagesByFilterRequest': trial_v1_email_logs_get_messages_by_filter_request,
            'X-Staging-Access': x_staging_access,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [LogInstance(self._version, item) for item in payload[""]]
        

    async def list_async(self, 
        trial_v1_email_logs_get_messages_by_filter_request: Union[TrialV1EmailLogsGetMessagesByFilterRequest, object] = values.unset,
        x_staging_access: Union[str, object] = values.unset,
    ) -> List[LogInstance]:
        """
        Asynchronously lists LogInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'TrialV1EmailLogsGetMessagesByFilterRequest': trial_v1_email_logs_get_messages_by_filter_request,
            'X-Staging-Access': x_staging_access,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        
        return [LogInstance(self._version, item) for item in payload[""]]
        


    def list_with_http_info(self, 
        trial_v1_email_logs_get_messages_by_filter_request: Union[TrialV1EmailLogsGetMessagesByFilterRequest, object] = values.unset,
        x_staging_access: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists LogInstance and returns headers from first page

        
        :param TrialV1EmailLogsGetMessagesByFilterRequest trial_v1_email_logs_get_messages_by_filter_request: 
        :param str x_staging_access: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            trial_v1_email_logs_get_messages_by_filter_request=trial_v1_email_logs_get_messages_by_filter_request,
            x_staging_access=x_staging_access,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        trial_v1_email_logs_get_messages_by_filter_request: Union[TrialV1EmailLogsGetMessagesByFilterRequest, object] = values.unset,
        x_staging_access: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists LogInstance and returns headers from first page

        
        :param TrialV1EmailLogsGetMessagesByFilterRequest trial_v1_email_logs_get_messages_by_filter_request: 
        :param str x_staging_access: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            trial_v1_email_logs_get_messages_by_filter_request=trial_v1_email_logs_get_messages_by_filter_request,
            x_staging_access=x_staging_access,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      trial_v1_email_logs_get_messages_by_filter_request: Union[TrialV1EmailLogsGetMessagesByFilterRequest, object] = values.unset,
      x_staging_access: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> LogPage:
        """
        Retrieve a single page of LogInstance records from the API.
        Request is executed immediately
        
        :param trial_v1_email_logs_get_messages_by_filter_request: 
        :param x_staging_access: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of LogInstance
        """
        data = values.of({ 
            'TrialV1EmailLogsGetMessagesByFilterRequest': trial_v1_email_logs_get_messages_by_filter_request,
            'X-Staging-Access': x_staging_access,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Staging-Access': x_staging_access,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='POST', uri=self._uri, params=data, headers=headers)
        return LogPage(self._version, response)

    async def page_async(self, 
      trial_v1_email_logs_get_messages_by_filter_request: Union[TrialV1EmailLogsGetMessagesByFilterRequest, object] = values.unset,
      x_staging_access: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> LogPage:
        """
        Asynchronously retrieve a single page of LogInstance records from the API.
        Request is executed immediately
        
        :param trial_v1_email_logs_get_messages_by_filter_request: 
        :param x_staging_access: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of LogInstance
        """
        data = values.of({ 
            'TrialV1EmailLogsGetMessagesByFilterRequest': trial_v1_email_logs_get_messages_by_filter_request,
            'X-Staging-Access': x_staging_access,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Staging-Access': x_staging_access,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='POST', uri=self._uri, params=data, headers=headers)
        return LogPage(self._version, response)

    def page_with_http_info(self, 
        trial_v1_email_logs_get_messages_by_filter_request: Union[TrialV1EmailLogsGetMessagesByFilterRequest, object] = values.unset,
        x_staging_access: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param trial_v1_email_logs_get_messages_by_filter_request: 
        :param x_staging_access: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with LogPage, status code, and headers
        """
        data = values.of({ 
            'TrialV1EmailLogsGetMessagesByFilterRequest': trial_v1_email_logs_get_messages_by_filter_request,
            'X-Staging-Access': x_staging_access,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Staging-Access': x_staging_access,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='POST', uri=self._uri, params=data, headers=headers)
        page = LogPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        trial_v1_email_logs_get_messages_by_filter_request: Union[TrialV1EmailLogsGetMessagesByFilterRequest, object] = values.unset,
        x_staging_access: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param trial_v1_email_logs_get_messages_by_filter_request: 
        :param x_staging_access: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with LogPage, status code, and headers
        """
        data = values.of({ 
            'TrialV1EmailLogsGetMessagesByFilterRequest': trial_v1_email_logs_get_messages_by_filter_request,
            'X-Staging-Access': x_staging_access,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Staging-Access': x_staging_access,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='POST', uri=self._uri, params=data, headers=headers)
        page = LogPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> LogPage:
        """
        Retrieve a specific page of LogInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of LogInstance
        """
        response = self._version.domain.twilio.request(
            'POST',
            target_url
        )
        return LogPage(self._version, response)

    async def get_page_async(self, target_url: str) -> LogPage:
        """
        Asynchronously retrieve a specific page of LogInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of LogInstance
        """
        response = await self._version.domain.twilio.request_async(
            'POST',
            target_url
        )
        return LogPage(self._version, response)



    def get(self, sg_msg_id: str) -> LogContext:
        """
        Constructs a LogContext
        
        :param sg_msg_id: The ID of the message you are requesting details for.
        """
        return LogContext(self._version, sg_msg_id=sg_msg_id)

    def __call__(self, sg_msg_id: str) -> LogContext:
        """
        Constructs a LogContext
        
        :param sg_msg_id: The ID of the message you are requesting details for.
        """
        return LogContext(self._version, sg_msg_id=sg_msg_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Trial.Users_manisingh_codehq_open_api_spec_trial_v1_openapi_yaml.LogList>'


