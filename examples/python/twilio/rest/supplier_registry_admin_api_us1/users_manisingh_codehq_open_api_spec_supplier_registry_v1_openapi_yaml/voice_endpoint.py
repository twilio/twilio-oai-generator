r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Supplier Registry API
    API for managing supplier configurations (DLR mappings, and SMS settings, etc.) This API provides endpoints for CRUD operations on provider configurations. 

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class VoiceEndpointInstance(InstanceResource):



    """
    :ivar code: Twilio-specific error code
    :ivar message: Error message
    :ivar more_info: Link to Error Code References
    :ivar status: HTTP response status code
    """

    def __init__(self, version: Version, payload:Dict[str, Any], provider_sid: Optional[str] = None, provider_endpoint_sid: Optional[str] = None):
        super().__init__(version)

        
        self.code: Optional[int] = payload.get("code")
        self.message: Optional[str] = payload.get("message")
        self.more_info: Optional[str] = payload.get("more_info")
        self.status: Optional[int] = payload.get("status")

        
        
        self._solution = { 
            "provider_sid": provider_sid or self.provider_sid,
            "provider_endpoint_sid": provider_endpoint_sid or self.provider_endpoint_sid,
        }
        
        
        self._context: Optional[VoiceEndpointContext] = None

    @property
    def _proxy(self) -> "VoiceEndpointContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: VoiceEndpointContext for this VoiceEndpointInstance
        """
        if self._context is None:
            self._context = VoiceEndpointContext(self._version, provider_sid=self._solution['provider_sid'], provider_endpoint_sid=self._solution['provider_endpoint_sid'],)
        return self._context
    
    
    
    def delete(self, x_change_comment: str, if_match: str) -> bool:
        """
        Deletes the VoiceEndpointInstance
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete(x_change_comment=x_change_comment, if_match=if_match, )
    async def delete_async(self, x_change_comment: str, if_match: str) -> bool:
        """
        Asynchronous coroutine that deletes the VoiceEndpointInstance
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async(x_change_comment=x_change_comment, if_match=if_match, )

    def delete_with_http_info(self, x_change_comment: str, if_match: str) -> ApiResponse:
        """
        Deletes the VoiceEndpointInstance with HTTP info
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info(x_change_comment=x_change_comment, if_match=if_match, )

    async def delete_with_http_info_async(self, x_change_comment: str, if_match: str) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the VoiceEndpointInstance with HTTP info
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async(x_change_comment=x_change_comment, if_match=if_match, )
    
    
    
    
    
    
    def update(self, x_change_comment: str, if_match: str, body: object) -> "VoiceEndpointInstance":
        """
        Update the VoiceEndpointInstance
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 

        :returns: The updated VoiceEndpointInstance
        """
        return self._proxy.update(x_change_comment=x_change_comment, if_match=if_match, body=body, )

    async def update_async(self, x_change_comment: str, if_match: str, body: object) -> "VoiceEndpointInstance":
        """
        Asynchronous coroutine to update the VoiceEndpointInstance
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 

        :returns: The updated VoiceEndpointInstance
        """
        return await self._proxy.update_async(x_change_comment=x_change_comment, if_match=if_match, body=body, )

    def update_with_http_info(self, x_change_comment: str, if_match: str, body: object) -> ApiResponse:
        """
        Update the VoiceEndpointInstance with HTTP info
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(x_change_comment=x_change_comment, if_match=if_match, body=body, )

    async def update_with_http_info_async(self, x_change_comment: str, if_match: str, body: object) -> ApiResponse:
        """
        Asynchronous coroutine to update the VoiceEndpointInstance with HTTP info
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(x_change_comment=x_change_comment, if_match=if_match, body=body, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointInstance {}>'.format(context)

class VoiceEndpointContext(InstanceContext):


    def __init__(self, version: Version, provider_sid: str, provider_endpoint_sid: str):
        """
        Initialize the VoiceEndpointContext

        :param version: Version that contains the resource
        :param provider_sid: 
        :param provider_endpoint_sid: 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'provider_sid': provider_sid,
            'provider_endpoint_sid': provider_endpoint_sid,
        }
        self._uri = '/Providers/{provider_sid}/VoiceEndpoints/{provider_endpoint_sid}'.format(**self._solution)
        
    
    
    
    def _delete(self, x_change_comment: str, if_match: str) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        headers = values.of({'X-Change-Comment': x_change_comment, }{'If-Match': if_match, })
        

        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self, x_change_comment: str, if_match: str) -> bool:
        """
        Deletes the VoiceEndpointInstance

        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete(x_change_comment=x_change_comment, if_match=if_match)
        return success

    def delete_with_http_info(self, x_change_comment: str, if_match: str) -> ApiResponse:
        """
        Deletes the VoiceEndpointInstance and return response metadata

        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete(x_change_comment=x_change_comment, if_match=if_match)
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self, x_change_comment: str, if_match: str) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        headers = values.of({'X-Change-Comment': x_change_comment, }{'If-Match': if_match, })
        
        
        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self, x_change_comment: str, if_match: str) -> bool:
        """
        Asynchronous coroutine that deletes the VoiceEndpointInstance

        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async(x_change_comment=x_change_comment, if_match=if_match)
        return success

    async def delete_with_http_info_async(self, x_change_comment: str, if_match: str) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the VoiceEndpointInstance and return response metadata

        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async(x_change_comment=x_change_comment, if_match=if_match)
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    
    
    
    
    def _update(self, x_change_comment: str, if_match: str, body: object) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = body.to_dict()
        
        headers = values.of({})
        
        
        if not (x_change_comment is values.unset or (isinstance(x_change_comment, str) and not x_change_comment)):
            headers['X-Change-Comment'] = x_change_comment
        
        if not (if_match is values.unset or (isinstance(if_match, str) and not if_match)):
            headers['If-Match'] = if_match
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='PUT', uri=self._uri, data=data, headers=headers)

    def update(self, x_change_comment: str, if_match: str, body: object) -> VoiceEndpointInstance:
        """
        Update the VoiceEndpointInstance
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 

        :returns: The updated VoiceEndpointInstance
        """
        payload, _, _ = self._update(x_change_comment=x_change_comment, if_match=if_match, body=body)
        return VoiceEndpointInstance(
            self._version,
            payload,
            provider_sid=self._solution['provider_sid'],
            provider_endpoint_sid=self._solution['provider_endpoint_sid']
        )

    def update_with_http_info(self, x_change_comment: str, if_match: str, body: object) -> ApiResponse:
        """
        Update the VoiceEndpointInstance and return response metadata
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(x_change_comment=x_change_comment, if_match=if_match, body=body)
        instance = VoiceEndpointInstance(
            self._version,
            payload,
            provider_sid=self._solution['provider_sid'],
            provider_endpoint_sid=self._solution['provider_endpoint_sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, x_change_comment: str, if_match: str, body: object) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = body.to_dict()
        
        headers = values.of({})
        
        
        if not (x_change_comment is values.unset or (isinstance(x_change_comment, str) and not x_change_comment)):
            headers['X-Change-Comment'] = x_change_comment
        
        
        if not (if_match is values.unset or (isinstance(if_match, str) and not if_match)):
            headers['If-Match'] = if_match
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='PUT', uri=self._uri, data=data, headers=headers)

    async def update_async(self, x_change_comment: str, if_match: str, body: object) -> VoiceEndpointInstance:
        """
        Asynchronous coroutine to update the VoiceEndpointInstance
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 

        :returns: The updated VoiceEndpointInstance
        """
        payload, _, _ = await self._update_async(x_change_comment=x_change_comment, if_match=if_match, body=body)
        return VoiceEndpointInstance(
            self._version,
            payload,
            provider_sid=self._solution['provider_sid'],
            provider_endpoint_sid=self._solution['provider_endpoint_sid']
        )

    async def update_with_http_info_async(self, x_change_comment: str, if_match: str, body: object) -> ApiResponse:
        """
        Asynchronous coroutine to update the VoiceEndpointInstance and return response metadata
        
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(x_change_comment=x_change_comment, if_match=if_match, body=body)
        instance = VoiceEndpointInstance(
            self._version,
            payload,
            provider_sid=self._solution['provider_sid'],
            provider_endpoint_sid=self._solution['provider_endpoint_sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointContext {}>'.format(context)



class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"






class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"




class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"




class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"




class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"








class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"




class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"




class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"




class VoiceEndpointPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> VoiceEndpointInstance:
        """
        Build an instance of VoiceEndpointInstance

        :param payload: Payload response from the API
        """
        
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution["provider_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointPage>"





class VoiceEndpointList(ListResource):
    
    def __init__(self, version: Version, provider_sid: str):
        """
        Initialize the VoiceEndpointList

        :param version: Version that contains the resource
        :param provider_sid: 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'provider_sid': provider_sid,  }
        self._uri = '/Providers/{provider_sid}/VoiceEndpoints'.format(**self._solution)
        
        
    
    def list(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        body: Union[object, object] = values.unset,
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'body': body,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='PUT', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        body: Union[object, object] = values.unset,
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'body': body,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='PUT', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        body: Union[object, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param str provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param str x_change_comment: Reason for the change
        :param str if_match: Entity tag for optimistic concurrency control
        :param object body: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            provider_sid=provider_sid,
            x_change_comment=x_change_comment,
            if_match=if_match,
            body=body,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        body: Union[object, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param str provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param str x_change_comment: Reason for the change
        :param str if_match: Entity tag for optimistic concurrency control
        :param object body: 
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            provider_sid=provider_sid,
            x_change_comment=x_change_comment,
            if_match=if_match,
            body=body,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      provider_sid: Union[str, object] = values.unset,
      x_change_comment: Union[str, object] = values.unset,
      if_match: Union[str, object] = values.unset,
      body: Union[object, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'body': serialize.object(body),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Change-Comment': x_change_comment,
        'If-Match': if_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='PUT', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      provider_sid: Union[str, object] = values.unset,
      x_change_comment: Union[str, object] = values.unset,
      if_match: Union[str, object] = values.unset,
      body: Union[object, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'body': serialize.object(body),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Change-Comment': x_change_comment,
        'If-Match': if_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='PUT', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        body: Union[object, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'body': serialize.object(body),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Change-Comment': x_change_comment,
        'If-Match': if_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='PUT', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        body: Union[object, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'body': serialize.object(body),
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Change-Comment': x_change_comment,
        'If-Match': if_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='PUT', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'PUT',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'PUT',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    
    
    def list(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    
    def list(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
        })
        payload, _, _ = self._version.fetch_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        headers["Accept"] = "application/json"
        
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param str provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param str x_change_comment: Reason for the change
        :param str if_match: Entity tag for optimistic concurrency control
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            provider_sid=provider_sid,
            x_change_comment=x_change_comment,
            if_match=if_match,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param str provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param str x_change_comment: Reason for the change
        :param str if_match: Entity tag for optimistic concurrency control
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            provider_sid=provider_sid,
            x_change_comment=x_change_comment,
            if_match=if_match,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      provider_sid: Union[str, object] = values.unset,
      x_change_comment: Union[str, object] = values.unset,
      if_match: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Change-Comment': x_change_comment,
        'If-Match': if_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='DELETE', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      provider_sid: Union[str, object] = values.unset,
      x_change_comment: Union[str, object] = values.unset,
      if_match: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Change-Comment': x_change_comment,
        'If-Match': if_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Change-Comment': x_change_comment,
        'If-Match': if_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        provider_sid: Union[str, object] = values.unset,
        x_change_comment: Union[str, object] = values.unset,
        if_match: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param provider_sid: SID of the provider that owns this endpoint (required to locate the endpoint in storage)
        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'providerSid': provider_sid,
            'X-Change-Comment': x_change_comment,
            'If-Match': if_match,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'X-Change-Comment': x_change_comment,
        'If-Match': if_match,
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='DELETE', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'DELETE',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'DELETE',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    
    
    def _create(self, x_change_comment: str, if_match: str, body: object) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = body.to_dict()
        
        headers = values.of({
                'X-Change-Comment': x_change_comment,
                'If-Match': if_match,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, x_change_comment: str, if_match: str, body: object) -> VoiceEndpointInstance:
        """
        Create the VoiceEndpointInstance

        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 
        
        :returns: The created VoiceEndpointInstance
        """
        payload, _, _ = self._create(x_change_comment=x_change_comment, if_match=if_match, body=body)
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution['provider_sid'])

    def create_with_http_info(self, x_change_comment: str, if_match: str, body: object) -> ApiResponse:
        """
        Create the VoiceEndpointInstance and return response metadata

        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(x_change_comment=x_change_comment, if_match=if_match, body=body)
        instance = VoiceEndpointInstance(self._version, payload, provider_sid=self._solution['provider_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, x_change_comment: str, if_match: str, body: object) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = body.to_dict()
        
        headers = values.of({
                'X-Change-Comment': x_change_comment,
                'If-Match': if_match,
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, x_change_comment: str, if_match: str, body: object) -> VoiceEndpointInstance:
        """
        Asynchronously create the VoiceEndpointInstance

        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 
        
        :returns: The created VoiceEndpointInstance
        """
        payload, _, _ = await self._create_async(x_change_comment=x_change_comment, if_match=if_match, body=body)
        return VoiceEndpointInstance(self._version, payload, provider_sid=self._solution['provider_sid'])

    async def create_with_http_info_async(self, x_change_comment: str, if_match: str, body: object) -> ApiResponse:
        """
        Asynchronously create the VoiceEndpointInstance and return response metadata

        :param x_change_comment: Reason for the change
        :param if_match: Entity tag for optimistic concurrency control
        :param body: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(x_change_comment=x_change_comment, if_match=if_match, body=body)
        instance = VoiceEndpointInstance(self._version, payload, provider_sid=self._solution['provider_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[VoiceEndpointInstance]:
        """
        Streams VoiceEndpointInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[VoiceEndpointInstance]:
        """
        Asynchronously streams VoiceEndpointInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    
    def stream(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[VoiceEndpointInstance]:
        """
        Streams VoiceEndpointInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[VoiceEndpointInstance]:
        """
        Asynchronously streams VoiceEndpointInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[VoiceEndpointInstance]:
        """
        Asynchronously lists VoiceEndpointInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [VoiceEndpointInstance(self._version, item, provider_sid=self._solution['provider_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists VoiceEndpointInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a single page of VoiceEndpointInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of VoiceEndpointInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with VoiceEndpointPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = VoiceEndpointPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> VoiceEndpointPage:
        """
        Retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> VoiceEndpointPage:
        """
        Asynchronously retrieve a specific page of VoiceEndpointInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of VoiceEndpointInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return VoiceEndpointPage(self._version, response, solution=self._solution)



    def get(self, provider_sid: str, provider_endpoint_sid: str) -> VoiceEndpointContext:
        """
        Constructs a VoiceEndpointContext
        
        :param provider_sid: 
        :param provider_endpoint_sid: 
        """
        return VoiceEndpointContext(self._version, provider_sid=provider_sid, provider_endpoint_sid=provider_endpoint_sid)

    def __call__(self, provider_sid: str, provider_endpoint_sid: str) -> VoiceEndpointContext:
        """
        Constructs a VoiceEndpointContext
        
        :param provider_sid: 
        :param provider_endpoint_sid: 
        """
        return VoiceEndpointContext(self._version, provider_sid=provider_sid, provider_endpoint_sid=provider_endpoint_sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.SupplierRegistryAdminApiUs1.Users_manisingh_codehq_open_api_spec_supplier_registry_v1_openapi_yaml.VoiceEndpointList>'


