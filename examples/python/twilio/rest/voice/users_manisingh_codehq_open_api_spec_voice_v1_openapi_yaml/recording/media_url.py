r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Voice
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version









class MediaUrlInstance(InstanceResource):



    """
    :ivar media_url: 
    :ivar url: 
    :ivar recording_sid: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], recording_sid: str):
        super().__init__(version)

        
        self.media_url: Optional[str] = payload.get("media_url")
        self.url: Optional[str] = payload.get("url")
        self.recording_sid: Optional[str] = payload.get("recording_sid")

        
        
        self._solution = { 
            "recording_sid": recording_sid,
        }
        
        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Voice.Users_manisingh_codehq_open_api_spec_voice_v1_openapi_yaml.MediaUrlInstance {}>'.format(context)




class MediaUrlList(ListResource):
    
    def __init__(self, version: Version, recording_sid: str):
        """
        Initialize the MediaUrlList

        :param version: Version that contains the resource
        :param recording_sid: 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'recording_sid': recording_sid,  }
        self._uri = '/Recordings/{recording_sid}/MediaUrl'.format(**self._solution)
        
        
    
    def _fetch(self, expiration: Union[int, object]=values.unset) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        
        headers["Accept"] = "application/json"
        
        params = values.of({
                'Expiration': expiration,
                
            })
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers, params=params)

    def fetch(self, expiration: Union[int, object]=values.unset) -> MediaUrlInstance:
        """
        Fetch the MediaUrlInstance

        :param expiration: 
        :returns: The fetched MediaUrlInstance
        """
        payload, _, _ = self._fetch(expiration=expiration)
        return MediaUrlInstance(self._version, payload, recording_sid=self._solution['recording_sid'])

    def fetch_with_http_info(self, expiration: Union[int, object]=values.unset) -> ApiResponse:
        """
        Fetch the MediaUrlInstance and return response metadata

        :param expiration: 
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch(expiration=expiration)
        instance = MediaUrlInstance(self._version, payload, recording_sid=self._solution['recording_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self, expiration: Union[int, object]=values.unset) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        
        headers["Accept"] = "application/json"
        
        params = values.of({
                'Expiration': expiration,
                
            })
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers, params=params)

    async def fetch_async(self, expiration: Union[int, object]=values.unset) -> MediaUrlInstance:
        """
        Asynchronously fetch the MediaUrlInstance

        :param expiration: 
        :returns: The fetched MediaUrlInstance
        """
        payload, _, _ = await self._fetch_async(expiration=expiration)
        return MediaUrlInstance(self._version, payload, recording_sid=self._solution['recording_sid'])

    async def fetch_with_http_info_async(self, expiration: Union[int, object]=values.unset) -> ApiResponse:
        """
        Asynchronously fetch the MediaUrlInstance and return response metadata

        :param expiration: 
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async(expiration=expiration)
        instance = MediaUrlInstance(self._version, payload, recording_sid=self._solution['recording_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    def list(self, 
    ) -> List[MediaUrlInstance]:
        """
        Lists MediaUrlInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [MediaUrlInstance(self._version, item, recording_sid=self._solution['recording_sid']) for item in payload[""]]
        

    async def list_async(self, 
    ) -> List[MediaUrlInstance]:
        """
        Asynchronously lists MediaUrlInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [MediaUrlInstance(self._version, item, recording_sid=self._solution['recording_sid']) for item in payload[""]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists MediaUrlInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists MediaUrlInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> MediaUrlPage:
        """
        Retrieve a single page of MediaUrlInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of MediaUrlInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return MediaUrlPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> MediaUrlPage:
        """
        Asynchronously retrieve a single page of MediaUrlInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of MediaUrlInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return MediaUrlPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with MediaUrlPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = MediaUrlPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with MediaUrlPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = MediaUrlPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> MediaUrlPage:
        """
        Retrieve a specific page of MediaUrlInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of MediaUrlInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return MediaUrlPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> MediaUrlPage:
        """
        Asynchronously retrieve a specific page of MediaUrlInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of MediaUrlInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return MediaUrlPage(self._version, response, solution=self._solution)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Voice.Users_manisingh_codehq_open_api_spec_voice_v1_openapi_yaml.MediaUrlList>'


