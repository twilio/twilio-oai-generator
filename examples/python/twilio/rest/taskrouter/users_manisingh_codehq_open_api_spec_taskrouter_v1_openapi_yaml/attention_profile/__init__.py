r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Taskrouter
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page
from twilio.rest.taskrouter.users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.attention_profile.attention_profile_channel import AttentionProfileChannelList








class AttentionProfileInstance(InstanceResource):



    """
    :ivar account_sid: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workspace resource.
    :ivar workspace_sid: The SID of the Workspace that contains the Attention Profile.
    :ivar sid: A string that we created to uniquely identify this Attention Profile.
    :ivar friendly_name: The string that you assigned to describe the Attention Profile resource. For example `Sales Profile` or `Marketing Campaign Profile`.
    :ivar unique_name: The string that you assigned to uniquely identify the Attention Profile resource. Unique Names are non-editable after creation, and unique within the TaskRouter Workspace. For example `sales_profile` or `marketing_profile`.
    :ivar concurrency: The maximum concurrent tasks that can be assigned to a Worker using this Attention Profile.
    :ivar date_created: The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar date_updated: The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar url: The absolute URL of the Attention Profile resource.
    :ivar links: The URLs of related resources.
    """

    def __init__(self, version: Version, payload:Dict[str, Any], workspace_sid: Optional[str] = None, sid: Optional[str] = None):
        super().__init__(version)

        
        self.account_sid: Optional[str] = payload.get("account_sid")
        self.workspace_sid: Optional[str] = payload.get("workspace_sid")
        self.sid: Optional[str] = payload.get("sid")
        self.friendly_name: Optional[str] = payload.get("friendly_name")
        self.unique_name: Optional[str] = payload.get("unique_name")
        self.concurrency: Optional[int] = deserialize.integer(payload.get("concurrency"))
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_created"))
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_updated"))
        self.url: Optional[str] = payload.get("url")
        self.links: Optional[Dict[str, object]] = payload.get("links")

        
        
        self._solution = { 
            "workspace_sid": workspace_sid or self.workspace_sid,
            "sid": sid or self.sid,
        }
        
        
        self._context: Optional[AttentionProfileContext] = None

    @property
    def _proxy(self) -> "AttentionProfileContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: AttentionProfileContext for this AttentionProfileInstance
        """
        if self._context is None:
            self._context = AttentionProfileContext(self._version, workspace_sid=self._solution['workspace_sid'], sid=self._solution['sid'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the AttentionProfileInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the AttentionProfileInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the AttentionProfileInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the AttentionProfileInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    def fetch(self) -> "AttentionProfileInstance":
        """
        Fetch the AttentionProfileInstance
        

        :returns: The fetched AttentionProfileInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "AttentionProfileInstance":
        """
        Asynchronous coroutine to fetch the AttentionProfileInstance
        

        :returns: The fetched AttentionProfileInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the AttentionProfileInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the AttentionProfileInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()
    
    
    
    def update(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> "AttentionProfileInstance":
        """
        Update the AttentionProfileInstance
        
        :param friendly_name: A descriptive string that you define to describe the Attention Profile. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.

        :returns: The updated AttentionProfileInstance
        """
        return self._proxy.update(friendly_name=friendly_name, concurrency=concurrency, )

    async def update_async(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> "AttentionProfileInstance":
        """
        Asynchronous coroutine to update the AttentionProfileInstance
        
        :param friendly_name: A descriptive string that you define to describe the Attention Profile. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.

        :returns: The updated AttentionProfileInstance
        """
        return await self._proxy.update_async(friendly_name=friendly_name, concurrency=concurrency, )

    def update_with_http_info(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> ApiResponse:
        """
        Update the AttentionProfileInstance with HTTP info
        
        :param friendly_name: A descriptive string that you define to describe the Attention Profile. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(friendly_name=friendly_name, concurrency=concurrency, )

    async def update_with_http_info_async(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the AttentionProfileInstance with HTTP info
        
        :param friendly_name: A descriptive string that you define to describe the Attention Profile. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(friendly_name=friendly_name, concurrency=concurrency, )
    
    @property
    def channels(self) -> AttentionProfileChannelList:
        """
        Access the channels
        """
        return self._proxy.channels
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileInstance {}>'.format(context)

class AttentionProfileContext(InstanceContext):


    def __init__(self, version: Version, workspace_sid: str, sid: str):
        """
        Initialize the AttentionProfileContext

        :param version: Version that contains the resource
        :param workspace_sid: The SID of the Workspace that the Attention Profile belongs to.
        :param sid: The SID of the Attention Profile to update.
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'workspace_sid': workspace_sid,
            'sid': sid,
        }
        self._uri = '/Workspaces/{workspace_sid}/AttentionProfiles/{sid}'.format(**self._solution)
        
        self._channels: Optional[AttentionProfileChannelList] = None
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the AttentionProfileInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the AttentionProfileInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the AttentionProfileInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the AttentionProfileInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers)

    def fetch(self) -> AttentionProfileInstance:
        """
        Fetch the AttentionProfileInstance
        

        :returns: The fetched AttentionProfileInstance
        """
        payload, _, _ = self._fetch()
        return AttentionProfileInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            sid=self._solution['sid'],
            
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the AttentionProfileInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = AttentionProfileInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            sid=self._solution['sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers)

    async def fetch_async(self) -> AttentionProfileInstance:
        """
        Asynchronous coroutine to fetch the AttentionProfileInstance
        

        :returns: The fetched AttentionProfileInstance
        """
        payload, _, _ = await self._fetch_async()
        return AttentionProfileInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            sid=self._solution['sid'],
            
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the AttentionProfileInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = AttentionProfileInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            sid=self._solution['sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    
    def _update(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'FriendlyName': friendly_name,
            'Concurrency': concurrency,
        })
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def update(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> AttentionProfileInstance:
        """
        Update the AttentionProfileInstance
        
        :param friendly_name: A descriptive string that you define to describe the Attention Profile. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.

        :returns: The updated AttentionProfileInstance
        """
        payload, _, _ = self._update(friendly_name=friendly_name, concurrency=concurrency)
        return AttentionProfileInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            sid=self._solution['sid']
        )

    def update_with_http_info(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> ApiResponse:
        """
        Update the AttentionProfileInstance and return response metadata
        
        :param friendly_name: A descriptive string that you define to describe the Attention Profile. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(friendly_name=friendly_name, concurrency=concurrency)
        instance = AttentionProfileInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            sid=self._solution['sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'FriendlyName': friendly_name,
            'Concurrency': concurrency,
        })
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def update_async(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> AttentionProfileInstance:
        """
        Asynchronous coroutine to update the AttentionProfileInstance
        
        :param friendly_name: A descriptive string that you define to describe the Attention Profile. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.

        :returns: The updated AttentionProfileInstance
        """
        payload, _, _ = await self._update_async(friendly_name=friendly_name, concurrency=concurrency)
        return AttentionProfileInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            sid=self._solution['sid']
        )

    async def update_with_http_info_async(self, friendly_name: Union[str, object]=values.unset, concurrency: Union[int, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the AttentionProfileInstance and return response metadata
        
        :param friendly_name: A descriptive string that you define to describe the Attention Profile. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(friendly_name=friendly_name, concurrency=concurrency)
        instance = AttentionProfileInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            sid=self._solution['sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    @property
    def channels(self) -> AttentionProfileChannelList:
        """
        Access the channels
        """
        if self._channels is None:
            self._channels = AttentionProfileChannelList(
                self._version, 
                self._solution['workspace_sid'],
                self._solution['sid'],
            )
        return self._channels
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileContext {}>'.format(context)







class AttentionProfilePage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> AttentionProfileInstance:
        """
        Build an instance of AttentionProfileInstance

        :param payload: Payload response from the API
        """
        
        return AttentionProfileInstance(self._version, payload, workspace_sid=self._solution["workspace_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfilePage>"








class AttentionProfilePage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> AttentionProfileInstance:
        """
        Build an instance of AttentionProfileInstance

        :param payload: Payload response from the API
        """
        
        return AttentionProfileInstance(self._version, payload, workspace_sid=self._solution["workspace_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfilePage>"




class AttentionProfilePage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> AttentionProfileInstance:
        """
        Build an instance of AttentionProfileInstance

        :param payload: Payload response from the API
        """
        
        return AttentionProfileInstance(self._version, payload, workspace_sid=self._solution["workspace_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfilePage>"





class AttentionProfileList(ListResource):
    
    def __init__(self, version: Version, workspace_sid: str):
        """
        Initialize the AttentionProfileList

        :param version: Version that contains the resource
        :param workspace_sid: The SID of the Workspace that the Attention Profiles belong to.
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'workspace_sid': workspace_sid,  }
        self._uri = '/Workspaces/{workspace_sid}/AttentionProfiles'.format(**self._solution)
        
        
    
    
    
    def list(self, 
    ) -> List[AttentionProfileInstance]:
        """
        Lists AttentionProfileInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [AttentionProfileInstance(self._version, item, workspace_sid=self._solution['workspace_sid']) for item in payload["attention_profiles"]]
        

    async def list_async(self, 
    ) -> List[AttentionProfileInstance]:
        """
        Asynchronously lists AttentionProfileInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [AttentionProfileInstance(self._version, item, workspace_sid=self._solution['workspace_sid']) for item in payload["attention_profiles"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AttentionProfileInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AttentionProfileInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfilePage:
        """
        Retrieve a single page of AttentionProfileInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return AttentionProfilePage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfilePage:
        """
        Asynchronously retrieve a single page of AttentionProfileInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return AttentionProfilePage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfilePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = AttentionProfilePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfilePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = AttentionProfilePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AttentionProfilePage:
        """
        Retrieve a specific page of AttentionProfileInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return AttentionProfilePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AttentionProfilePage:
        """
        Asynchronously retrieve a specific page of AttentionProfileInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return AttentionProfilePage(self._version, response, solution=self._solution)

    
    
    def _create(self, friendly_name: str, unique_name: str, concurrency: int, channel_configurations: Union[List[object], object]=values.unset) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'FriendlyName': friendly_name,
            'UniqueName': unique_name,
            'Concurrency': concurrency,
            'ChannelConfigurations': serialize.map(channel_configurations, lambda e: serialize.object(e)),
        })
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, friendly_name: str, unique_name: str, concurrency: int, channel_configurations: Union[List[object], object]=values.unset) -> AttentionProfileInstance:
        """
        Create the AttentionProfileInstance

        :param friendly_name: A descriptive string that you create to describe the Attention Profile resource. It can be up to 64 characters long.
        :param unique_name: A descriptive string that you create to uniquely identify the Attention Profile resource in the Workspace. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.
        :param channel_configurations: An array of objects describing the Attention to be used for each Task Channel type. Each object should contain the following properties: `required_attention`, `task_channel_name` or `task_channel_sid`.
        
        :returns: The created AttentionProfileInstance
        """
        payload, _, _ = self._create(friendly_name=friendly_name, unique_name=unique_name, concurrency=concurrency, channel_configurations=channel_configurations)
        return AttentionProfileInstance(self._version, payload, workspace_sid=self._solution['workspace_sid'])

    def create_with_http_info(self, friendly_name: str, unique_name: str, concurrency: int, channel_configurations: Union[List[object], object]=values.unset) -> ApiResponse:
        """
        Create the AttentionProfileInstance and return response metadata

        :param friendly_name: A descriptive string that you create to describe the Attention Profile resource. It can be up to 64 characters long.
        :param unique_name: A descriptive string that you create to uniquely identify the Attention Profile resource in the Workspace. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.
        :param channel_configurations: An array of objects describing the Attention to be used for each Task Channel type. Each object should contain the following properties: `required_attention`, `task_channel_name` or `task_channel_sid`.
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(friendly_name=friendly_name, unique_name=unique_name, concurrency=concurrency, channel_configurations=channel_configurations)
        instance = AttentionProfileInstance(self._version, payload, workspace_sid=self._solution['workspace_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, friendly_name: str, unique_name: str, concurrency: int, channel_configurations: Union[List[object], object]=values.unset) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'FriendlyName': friendly_name,
            'UniqueName': unique_name,
            'Concurrency': concurrency,
            'ChannelConfigurations': serialize.map(channel_configurations, lambda e: serialize.object(e)),
        })
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, friendly_name: str, unique_name: str, concurrency: int, channel_configurations: Union[List[object], object]=values.unset) -> AttentionProfileInstance:
        """
        Asynchronously create the AttentionProfileInstance

        :param friendly_name: A descriptive string that you create to describe the Attention Profile resource. It can be up to 64 characters long.
        :param unique_name: A descriptive string that you create to uniquely identify the Attention Profile resource in the Workspace. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.
        :param channel_configurations: An array of objects describing the Attention to be used for each Task Channel type. Each object should contain the following properties: `required_attention`, `task_channel_name` or `task_channel_sid`.
        
        :returns: The created AttentionProfileInstance
        """
        payload, _, _ = await self._create_async(friendly_name=friendly_name, unique_name=unique_name, concurrency=concurrency, channel_configurations=channel_configurations)
        return AttentionProfileInstance(self._version, payload, workspace_sid=self._solution['workspace_sid'])

    async def create_with_http_info_async(self, friendly_name: str, unique_name: str, concurrency: int, channel_configurations: Union[List[object], object]=values.unset) -> ApiResponse:
        """
        Asynchronously create the AttentionProfileInstance and return response metadata

        :param friendly_name: A descriptive string that you create to describe the Attention Profile resource. It can be up to 64 characters long.
        :param unique_name: A descriptive string that you create to uniquely identify the Attention Profile resource in the Workspace. It can be up to 64 characters long.
        :param concurrency: The maximum concurrent tasks that a Worker associated with this profile can handle.
        :param channel_configurations: An array of objects describing the Attention to be used for each Task Channel type. Each object should contain the following properties: `required_attention`, `task_channel_name` or `task_channel_sid`.
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(friendly_name=friendly_name, unique_name=unique_name, concurrency=concurrency, channel_configurations=channel_configurations)
        instance = AttentionProfileInstance(self._version, payload, workspace_sid=self._solution['workspace_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[AttentionProfileInstance]:
        """
        Streams AttentionProfileInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[AttentionProfileInstance]:
        """
        Asynchronously streams AttentionProfileInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams AttentionProfileInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams AttentionProfileInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AttentionProfileInstance]:
        """
        Lists AttentionProfileInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AttentionProfileInstance]:
        """
        Asynchronously lists AttentionProfileInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AttentionProfileInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AttentionProfileInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfilePage:
        """
        Retrieve a single page of AttentionProfileInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return AttentionProfilePage(self._version, response, solution=self._solution)

    async def page_async(self, 
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfilePage:
        """
        Asynchronously retrieve a single page of AttentionProfileInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return AttentionProfilePage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfilePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = AttentionProfilePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfilePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = AttentionProfilePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AttentionProfilePage:
        """
        Retrieve a specific page of AttentionProfileInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return AttentionProfilePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AttentionProfilePage:
        """
        Asynchronously retrieve a specific page of AttentionProfileInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return AttentionProfilePage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[AttentionProfileInstance]:
        """
        Lists AttentionProfileInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [AttentionProfileInstance(self._version, item, workspace_sid=self._solution['workspace_sid']) for item in payload["attention_profiles"]]
        

    async def list_async(self, 
    ) -> List[AttentionProfileInstance]:
        """
        Asynchronously lists AttentionProfileInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [AttentionProfileInstance(self._version, item, workspace_sid=self._solution['workspace_sid']) for item in payload["attention_profiles"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AttentionProfileInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AttentionProfileInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfilePage:
        """
        Retrieve a single page of AttentionProfileInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return AttentionProfilePage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfilePage:
        """
        Asynchronously retrieve a single page of AttentionProfileInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return AttentionProfilePage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfilePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = AttentionProfilePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfilePage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = AttentionProfilePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AttentionProfilePage:
        """
        Retrieve a specific page of AttentionProfileInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return AttentionProfilePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AttentionProfilePage:
        """
        Asynchronously retrieve a specific page of AttentionProfileInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return AttentionProfilePage(self._version, response, solution=self._solution)





    def get(self, workspace_sid: str, sid: str) -> AttentionProfileContext:
        """
        Constructs a AttentionProfileContext
        
        :param workspace_sid: The SID of the Workspace that the Attention Profile belongs to.
        :param sid: The SID of the Attention Profile to update.
        """
        return AttentionProfileContext(self._version, workspace_sid=workspace_sid, sid=sid)

    def __call__(self, workspace_sid: str, sid: str) -> AttentionProfileContext:
        """
        Constructs a AttentionProfileContext
        
        :param workspace_sid: The SID of the Workspace that the Attention Profile belongs to.
        :param sid: The SID of the Attention Profile to update.
        """
        return AttentionProfileContext(self._version, workspace_sid=workspace_sid, sid=sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileList>'


