r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Taskrouter
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class AttentionProfileChannelInstance(InstanceResource):



    """
    :ivar account_sid: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Workspace resource.
    :ivar workspace_sid: The SID of the Workspace that contains the Attention Profile.
    :ivar sid: A string that we created to uniquely identify this Attention Profile Channel.
    :ivar attention_profile_sid: The SID of the Attention Profile that contains the Attention Profile Channel.
    :ivar required_attention: The Attention value required for a Task with this Task Channel type.
    :ivar task_channel_sid: The SID of the Task Channel.
    :ivar available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing
    :ivar date_created: The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar date_updated: The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar url: The absolute URL of the Attention Profile Channel resource.
    """

    def __init__(self, version: Version, payload:Dict[str, Any], workspace_sid: str, attention_profile_sid: str, sid: Optional[str] = None):
        super().__init__(version)

        
        self.account_sid: Optional[str] = payload.get("account_sid")
        self.workspace_sid: Optional[str] = payload.get("workspace_sid")
        self.sid: Optional[str] = payload.get("sid")
        self.attention_profile_sid: Optional[str] = payload.get("attention_profile_sid")
        self.required_attention: Optional[int] = deserialize.integer(payload.get("required_attention"))
        self.task_channel_sid: Optional[str] = payload.get("task_channel_sid")
        self.available: Optional[bool] = payload.get("available")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_created"))
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_updated"))
        self.url: Optional[str] = payload.get("url")

        
        
        self._solution = { 
            "workspace_sid": workspace_sid,
            "attention_profile_sid": attention_profile_sid,
            "sid": sid or self.sid,
        }
        
        
        self._context: Optional[AttentionProfileChannelContext] = None

    @property
    def _proxy(self) -> "AttentionProfileChannelContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: AttentionProfileChannelContext for this AttentionProfileChannelInstance
        """
        if self._context is None:
            self._context = AttentionProfileChannelContext(self._version, workspace_sid=self._solution['workspace_sid'], attention_profile_sid=self._solution['attention_profile_sid'], sid=self._solution['sid'],)
        return self._context
    
    
    
    def update(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> "AttentionProfileChannelInstance":
        """
        Update the AttentionProfileChannelInstance
        
        :param required_attention: The Attention value required for a Task with the Task Channel type.
        :param available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing. Set to `false` to prevent the Worker from receiving any new Tasks of this channel type.

        :returns: The updated AttentionProfileChannelInstance
        """
        return self._proxy.update(required_attention=required_attention, available=available, )

    async def update_async(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> "AttentionProfileChannelInstance":
        """
        Asynchronous coroutine to update the AttentionProfileChannelInstance
        
        :param required_attention: The Attention value required for a Task with the Task Channel type.
        :param available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing. Set to `false` to prevent the Worker from receiving any new Tasks of this channel type.

        :returns: The updated AttentionProfileChannelInstance
        """
        return await self._proxy.update_async(required_attention=required_attention, available=available, )

    def update_with_http_info(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> ApiResponse:
        """
        Update the AttentionProfileChannelInstance with HTTP info
        
        :param required_attention: The Attention value required for a Task with the Task Channel type.
        :param available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing. Set to `false` to prevent the Worker from receiving any new Tasks of this channel type.

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(required_attention=required_attention, available=available, )

    async def update_with_http_info_async(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the AttentionProfileChannelInstance with HTTP info
        
        :param required_attention: The Attention value required for a Task with the Task Channel type.
        :param available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing. Set to `false` to prevent the Worker from receiving any new Tasks of this channel type.

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(required_attention=required_attention, available=available, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileChannelInstance {}>'.format(context)

class AttentionProfileChannelContext(InstanceContext):


    def __init__(self, version: Version, workspace_sid: str, attention_profile_sid: str, sid: str):
        """
        Initialize the AttentionProfileChannelContext

        :param version: Version that contains the resource
        :param workspace_sid: The SID of the Workspace that the Attention Profile Channel belongs to.
        :param attention_profile_sid: The SID of the Attention Profile that the Attention Profile Channel belongs to.
        :param sid: The SID of the Attention Profile Channel to update.
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'workspace_sid': workspace_sid,
            'attention_profile_sid': attention_profile_sid,
            'sid': sid,
        }
        self._uri = '/Workspaces/{workspace_sid}/AttentionProfiles/{attention_profile_sid}/Channels/{sid}'.format(**self._solution)
        
    
    
    
    def _update(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'RequiredAttention': required_attention,
            'Available': serialize.boolean_to_string(available),
        })
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def update(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> AttentionProfileChannelInstance:
        """
        Update the AttentionProfileChannelInstance
        
        :param required_attention: The Attention value required for a Task with the Task Channel type.
        :param available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing. Set to `false` to prevent the Worker from receiving any new Tasks of this channel type.

        :returns: The updated AttentionProfileChannelInstance
        """
        payload, _, _ = self._update(required_attention=required_attention, available=available)
        return AttentionProfileChannelInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            attention_profile_sid=self._solution['attention_profile_sid'],
            sid=self._solution['sid']
        )

    def update_with_http_info(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> ApiResponse:
        """
        Update the AttentionProfileChannelInstance and return response metadata
        
        :param required_attention: The Attention value required for a Task with the Task Channel type.
        :param available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing. Set to `false` to prevent the Worker from receiving any new Tasks of this channel type.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(required_attention=required_attention, available=available)
        instance = AttentionProfileChannelInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            attention_profile_sid=self._solution['attention_profile_sid'],
            sid=self._solution['sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'RequiredAttention': required_attention,
            'Available': serialize.boolean_to_string(available),
        })
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def update_async(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> AttentionProfileChannelInstance:
        """
        Asynchronous coroutine to update the AttentionProfileChannelInstance
        
        :param required_attention: The Attention value required for a Task with the Task Channel type.
        :param available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing. Set to `false` to prevent the Worker from receiving any new Tasks of this channel type.

        :returns: The updated AttentionProfileChannelInstance
        """
        payload, _, _ = await self._update_async(required_attention=required_attention, available=available)
        return AttentionProfileChannelInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            attention_profile_sid=self._solution['attention_profile_sid'],
            sid=self._solution['sid']
        )

    async def update_with_http_info_async(self, required_attention: Union[int, object]=values.unset, available: Union[bool, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the AttentionProfileChannelInstance and return response metadata
        
        :param required_attention: The Attention value required for a Task with the Task Channel type.
        :param available: Whether the Worker should receive Tasks for the Attention Profile Channel with Attention Routing. Set to `false` to prevent the Worker from receiving any new Tasks of this channel type.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(required_attention=required_attention, available=available)
        instance = AttentionProfileChannelInstance(
            self._version,
            payload,
            workspace_sid=self._solution['workspace_sid'],
            attention_profile_sid=self._solution['attention_profile_sid'],
            sid=self._solution['sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileChannelContext {}>'.format(context)



class AttentionProfileChannelPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> AttentionProfileChannelInstance:
        """
        Build an instance of AttentionProfileChannelInstance

        :param payload: Payload response from the API
        """
        
        return AttentionProfileChannelInstance(self._version, payload, workspace_sid=self._solution["workspace_sid"], attention_profile_sid=self._solution["attention_profile_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileChannelPage>"






class AttentionProfileChannelPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> AttentionProfileChannelInstance:
        """
        Build an instance of AttentionProfileChannelInstance

        :param payload: Payload response from the API
        """
        
        return AttentionProfileChannelInstance(self._version, payload, workspace_sid=self._solution["workspace_sid"], attention_profile_sid=self._solution["attention_profile_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileChannelPage>"




class AttentionProfileChannelPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> AttentionProfileChannelInstance:
        """
        Build an instance of AttentionProfileChannelInstance

        :param payload: Payload response from the API
        """
        
        return AttentionProfileChannelInstance(self._version, payload, workspace_sid=self._solution["workspace_sid"], attention_profile_sid=self._solution["attention_profile_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileChannelPage>"





class AttentionProfileChannelList(ListResource):
    
    def __init__(self, version: Version, workspace_sid: str, attention_profile_sid: str):
        """
        Initialize the AttentionProfileChannelList

        :param version: Version that contains the resource
        :param workspace_sid: The SID of the Workspace that the Attention Profile Channels belong to.
        :param attention_profile_sid: The SID of the Attention Profile that the Attention Profile Channels belong to.
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'workspace_sid': workspace_sid, 'attention_profile_sid': attention_profile_sid,  }
        self._uri = '/Workspaces/{workspace_sid}/AttentionProfiles/{attention_profile_sid}/Channels'.format(**self._solution)
        
        
    
    def list(self, 
    ) -> List[AttentionProfileChannelInstance]:
        """
        Lists AttentionProfileChannelInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [AttentionProfileChannelInstance(self._version, item, workspace_sid=self._solution['workspace_sid'], attention_profile_sid=self._solution['attention_profile_sid']) for item in payload["channels"]]
        

    async def list_async(self, 
    ) -> List[AttentionProfileChannelInstance]:
        """
        Asynchronously lists AttentionProfileChannelInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [AttentionProfileChannelInstance(self._version, item, workspace_sid=self._solution['workspace_sid'], attention_profile_sid=self._solution['attention_profile_sid']) for item in payload["channels"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AttentionProfileChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AttentionProfileChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfileChannelPage:
        """
        Retrieve a single page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileChannelInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfileChannelPage:
        """
        Asynchronously retrieve a single page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileChannelInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfileChannelPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = AttentionProfileChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfileChannelPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = AttentionProfileChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AttentionProfileChannelPage:
        """
        Retrieve a specific page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileChannelInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AttentionProfileChannelPage:
        """
        Asynchronously retrieve a specific page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileChannelInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    
    
    def stream(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[AttentionProfileChannelInstance]:
        """
        Streams AttentionProfileChannelInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[AttentionProfileChannelInstance]:
        """
        Asynchronously streams AttentionProfileChannelInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams AttentionProfileChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams AttentionProfileChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AttentionProfileChannelInstance]:
        """
        Lists AttentionProfileChannelInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AttentionProfileChannelInstance]:
        """
        Asynchronously lists AttentionProfileChannelInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AttentionProfileChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AttentionProfileChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfileChannelPage:
        """
        Retrieve a single page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileChannelInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfileChannelPage:
        """
        Asynchronously retrieve a single page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileChannelInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfileChannelPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = AttentionProfileChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfileChannelPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = AttentionProfileChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AttentionProfileChannelPage:
        """
        Retrieve a specific page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileChannelInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AttentionProfileChannelPage:
        """
        Asynchronously retrieve a specific page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileChannelInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[AttentionProfileChannelInstance]:
        """
        Lists AttentionProfileChannelInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [AttentionProfileChannelInstance(self._version, item, workspace_sid=self._solution['workspace_sid'], attention_profile_sid=self._solution['attention_profile_sid']) for item in payload["channels"]]
        

    async def list_async(self, 
    ) -> List[AttentionProfileChannelInstance]:
        """
        Asynchronously lists AttentionProfileChannelInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [AttentionProfileChannelInstance(self._version, item, workspace_sid=self._solution['workspace_sid'], attention_profile_sid=self._solution['attention_profile_sid']) for item in payload["channels"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AttentionProfileChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AttentionProfileChannelInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfileChannelPage:
        """
        Retrieve a single page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileChannelInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AttentionProfileChannelPage:
        """
        Asynchronously retrieve a single page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AttentionProfileChannelInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfileChannelPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = AttentionProfileChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AttentionProfileChannelPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = AttentionProfileChannelPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AttentionProfileChannelPage:
        """
        Retrieve a specific page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileChannelInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> AttentionProfileChannelPage:
        """
        Asynchronously retrieve a specific page of AttentionProfileChannelInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AttentionProfileChannelInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return AttentionProfileChannelPage(self._version, response, solution=self._solution)



    def get(self, sid: str) -> AttentionProfileChannelContext:
        """
        Constructs a AttentionProfileChannelContext
        
        :param sid: The SID of the Attention Profile Channel to update.
        """
        return AttentionProfileChannelContext(self._version, workspace_sid=self._solution['workspace_sid'], attention_profile_sid=self._solution['attention_profile_sid'], sid=sid)

    def __call__(self, sid: str) -> AttentionProfileChannelContext:
        """
        Constructs a AttentionProfileChannelContext
        
        :param sid: The SID of the Attention Profile Channel to update.
        """
        return AttentionProfileChannelContext(self._version, workspace_sid=self._solution['workspace_sid'], attention_profile_sid=self._solution['attention_profile_sid'], sid=sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Taskrouter.Users_manisingh_codehq_open_api_spec_taskrouter_v1_openapi_yaml.AttentionProfileChannelList>'


