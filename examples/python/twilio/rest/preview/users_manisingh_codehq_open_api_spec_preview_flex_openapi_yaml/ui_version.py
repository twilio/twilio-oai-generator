r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Preview
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class UiVersionInstance(InstanceResource):



    """
    :ivar revision: Unique integer >0 describing the revision of the selected UI Version options.
    :ivar receives_canary_releases: Determines if the account should receive the latest version of Flex UI. This is useful for test accounts looking to try out the latest Flex UI versions
    :ivar date_created: The date that these UI Version options were created, given in ISO 8601 format.
    """

    def __init__(self, version: Version, payload:Dict[str, Any]):
        super().__init__(version)

        
        self.revision: Optional[int] = deserialize.integer(payload.get("revision"))
        self.receives_canary_releases: Optional[bool] = payload.get("receives_canary_releases")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_created"))

        
        
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        
        return '<Twilio.Preview.Users_manisingh_codehq_open_api_spec_preview_flex_openapi_yaml.UiVersionInstance>'






class UiVersionPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> UiVersionInstance:
        """
        Build an instance of UiVersionInstance

        :param payload: Payload response from the API
        """
        
        return UiVersionInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Preview.Users_manisingh_codehq_open_api_spec_preview_flex_openapi_yaml.UiVersionPage>"




class UiVersionPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> UiVersionInstance:
        """
        Build an instance of UiVersionInstance

        :param payload: Payload response from the API
        """
        
        return UiVersionInstance(self._version, payload)
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Preview.Users_manisingh_codehq_open_api_spec_preview_flex_openapi_yaml.UiVersionPage>"





class UiVersionList(ListResource):
    
    def __init__(self, version: Version):
        """
        Initialize the UiVersionList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        self._uri = '/UIVersionOptions'
        
        
    
    def _create(self, receives_canary_releases: bool) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'ReceivesCanaryReleases': serialize.boolean_to_string(receives_canary_releases),
        })
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, receives_canary_releases: bool) -> UiVersionInstance:
        """
        Create the UiVersionInstance

        :param receives_canary_releases: Determines if the account should receive the latest version of Flex UI. This is useful for test accounts looking to try out the latest Flex UI versions
        
        :returns: The created UiVersionInstance
        """
        payload, _, _ = self._create(receives_canary_releases=receives_canary_releases)
        return UiVersionInstance(self._version, payload)

    def create_with_http_info(self, receives_canary_releases: bool) -> ApiResponse:
        """
        Create the UiVersionInstance and return response metadata

        :param receives_canary_releases: Determines if the account should receive the latest version of Flex UI. This is useful for test accounts looking to try out the latest Flex UI versions
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(receives_canary_releases=receives_canary_releases)
        instance = UiVersionInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, receives_canary_releases: bool) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'ReceivesCanaryReleases': serialize.boolean_to_string(receives_canary_releases),
        })
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, receives_canary_releases: bool) -> UiVersionInstance:
        """
        Asynchronously create the UiVersionInstance

        :param receives_canary_releases: Determines if the account should receive the latest version of Flex UI. This is useful for test accounts looking to try out the latest Flex UI versions
        
        :returns: The created UiVersionInstance
        """
        payload, _, _ = await self._create_async(receives_canary_releases=receives_canary_releases)
        return UiVersionInstance(self._version, payload)

    async def create_with_http_info_async(self, receives_canary_releases: bool) -> ApiResponse:
        """
        Asynchronously create the UiVersionInstance and return response metadata

        :param receives_canary_releases: Determines if the account should receive the latest version of Flex UI. This is useful for test accounts looking to try out the latest Flex UI versions
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(receives_canary_releases=receives_canary_releases)
        instance = UiVersionInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        revision: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[UiVersionInstance]:
        """
        Streams UiVersionInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            revision=revision,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        revision: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[UiVersionInstance]:
        """
        Asynchronously streams UiVersionInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            revision=revision,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        revision: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams UiVersionInstance and returns headers from first page

        
        :param str revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            revision=revision,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        revision: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams UiVersionInstance and returns headers from first page

        
        :param str revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            revision=revision,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        revision: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[UiVersionInstance]:
        """
        Lists UiVersionInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            revision=revision,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        revision: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[UiVersionInstance]:
        """
        Asynchronously lists UiVersionInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            revision=revision,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        revision: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists UiVersionInstance and returns headers from first page

        
        :param str revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            revision=revision,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        revision: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists UiVersionInstance and returns headers from first page

        
        :param str revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            revision=revision,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      revision: Union[str, object] = values.unset,
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> UiVersionPage:
        """
        Retrieve a single page of UiVersionInstance records from the API.
        Request is executed immediately
        
        :param revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of UiVersionInstance
        """
        data = values.of({ 
            'Revision': revision,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return UiVersionPage(self._version, response)

    async def page_async(self, 
      revision: Union[str, object] = values.unset,
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> UiVersionPage:
        """
        Asynchronously retrieve a single page of UiVersionInstance records from the API.
        Request is executed immediately
        
        :param revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of UiVersionInstance
        """
        data = values.of({ 
            'Revision': revision,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return UiVersionPage(self._version, response)

    def page_with_http_info(self, 
        revision: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with UiVersionPage, status code, and headers
        """
        data = values.of({ 
            'Revision': revision,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = UiVersionPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        revision: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param revision: Parameter defining which revision to return. It can either be set to `latest` or `undefined`. If set to `latest`, the endpoint will only return the most up-to-date revision of the UI Version options.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with UiVersionPage, status code, and headers
        """
        data = values.of({ 
            'Revision': revision,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = UiVersionPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> UiVersionPage:
        """
        Retrieve a specific page of UiVersionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of UiVersionInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return UiVersionPage(self._version, response)

    async def get_page_async(self, target_url: str) -> UiVersionPage:
        """
        Asynchronously retrieve a specific page of UiVersionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of UiVersionInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return UiVersionPage(self._version, response)

    
    def list(self, 
    ) -> List[UiVersionInstance]:
        """
        Lists UiVersionInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [UiVersionInstance(self._version, item) for item in payload["revisions"]]
        

    async def list_async(self, 
    ) -> List[UiVersionInstance]:
        """
        Asynchronously lists UiVersionInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [UiVersionInstance(self._version, item) for item in payload["revisions"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists UiVersionInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists UiVersionInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> UiVersionPage:
        """
        Retrieve a single page of UiVersionInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of UiVersionInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return UiVersionPage(self._version, response)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> UiVersionPage:
        """
        Asynchronously retrieve a single page of UiVersionInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of UiVersionInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return UiVersionPage(self._version, response)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with UiVersionPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = UiVersionPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with UiVersionPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = UiVersionPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> UiVersionPage:
        """
        Retrieve a specific page of UiVersionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of UiVersionInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return UiVersionPage(self._version, response)

    async def get_page_async(self, target_url: str) -> UiVersionPage:
        """
        Asynchronously retrieve a specific page of UiVersionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of UiVersionInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return UiVersionPage(self._version, response)




    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Preview.Users_manisingh_codehq_open_api_spec_preview_flex_openapi_yaml.UiVersionList>'


