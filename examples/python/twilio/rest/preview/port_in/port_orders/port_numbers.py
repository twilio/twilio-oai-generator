r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Preview
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page








class PortNumbersInstance(InstanceResource):



    class Status(object):
        INITIALIZED = "initialized"
        PENDING_CARRIER_PROCESSING = "pending-carrier-processing"
        CARRIER_PROCESSING = "carrier-processing"
        ACCEPTED = "accepted"
        REJECTED = "rejected"
        PORTED_IN = "ported-in"
        PENDING_CANCELATION = "pending-cancelation"
        CANCELED = "canceled"
        FAILED = "failed"

    """
    :ivar sid: A 34 character string that uniquely identifies the PortInNumber.
    :ivar account_sid: A 34 character string that uniquely identifies the account.
    :ivar port_order_sid: A 34 character string that uniquely identifies the PortInOrder.
    :ivar phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
    :ivar phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
    :ivar status: 
    :ivar type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
    :ivar carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
    :ivar current_carrier: The current carrier of the phone number.
    :ivar current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
    :ivar confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
    :ivar partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
    :ivar rejection_codes: A list of rejection codes as integers that are referencable in [Twilio's error and warning dictionary](https://www.twilio.com/docs/api/errors).
    :ivar date_created: The date this resource was created, given as [GMT RFC 2822](http://www.ietf.org/rfc/rfc2822.txt) format.
    :ivar date_updated: The date that this resource was updated, given as [GMT RFC 2822](http://www.ietf.org/rfc/rfc2822.txt) format.
    :ivar url: The URL of this PortInNumber.
    """

    def __init__(self, version: Version, payload:Dict[str, Any], port_order_sid: str, sid: Optional[str] = None):
        super().__init__(version)

        
        self.sid: Optional[str] = payload.get("sid")
        self.account_sid: Optional[str] = payload.get("account_sid")
        self.port_order_sid: Optional[str] = payload.get("port_order_sid")
        self.phone_number: Optional[str] = payload.get("phone_number")
        self.phone_number_sid: Optional[str] = payload.get("phone_number_sid")
        self.status: Optional["PortNumbersInstance.Status"] = payload.get("status")
        self.type: Optional[str] = payload.get("type")
        self.carrier_account_number: Optional[str] = payload.get("carrier_account_number")
        self.current_carrier: Optional[str] = payload.get("current_carrier")
        self.current_resp_org: Optional[str] = payload.get("current_resp_org")
        self.confirmed_activation_date: Optional[datetime] = deserialize.iso8601_datetime(payload.get("confirmed_activation_date"))
        self.partner_reference_id: Optional[str] = payload.get("partner_reference_id")
        self.rejection_codes: Optional[List[str]] = payload.get("rejection_codes")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_created"))
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(payload.get("date_updated"))
        self.url: Optional[str] = payload.get("url")

        
        
        self._solution = { 
            "port_order_sid": port_order_sid,
            "sid": sid or self.sid,
        }
        
        
        self._context: Optional[PortNumbersContext] = None

    @property
    def _proxy(self) -> "PortNumbersContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: PortNumbersContext for this PortNumbersInstance
        """
        if self._context is None:
            self._context = PortNumbersContext(self._version, port_order_sid=self._solution['port_order_sid'], sid=self._solution['sid'],)
        return self._context
    
    
    def delete(self) -> bool:
        """
        Deletes the PortNumbersInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()
    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the PortNumbersInstance
        

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the PortNumbersInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the PortNumbersInstance with HTTP info
        

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()
    
    
    def fetch(self) -> "PortNumbersInstance":
        """
        Fetch the PortNumbersInstance
        

        :returns: The fetched PortNumbersInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "PortNumbersInstance":
        """
        Asynchronous coroutine to fetch the PortNumbersInstance
        

        :returns: The fetched PortNumbersInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the PortNumbersInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the PortNumbersInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()
    
    
    
    def update(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> "PortNumbersInstance":
        """
        Update the PortNumbersInstance
        
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param phone_number_pin: PIN number of the account (with your current provider/carrier) that is associated with the porting phone number in this port request. This is mandatory for Wireless numbers but optional for Landline and Toll Free numbers.

        :returns: The updated PortNumbersInstance
        """
        return self._proxy.update(carrier_account_number=carrier_account_number, phone_number_pin=phone_number_pin, )

    async def update_async(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> "PortNumbersInstance":
        """
        Asynchronous coroutine to update the PortNumbersInstance
        
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param phone_number_pin: PIN number of the account (with your current provider/carrier) that is associated with the porting phone number in this port request. This is mandatory for Wireless numbers but optional for Landline and Toll Free numbers.

        :returns: The updated PortNumbersInstance
        """
        return await self._proxy.update_async(carrier_account_number=carrier_account_number, phone_number_pin=phone_number_pin, )

    def update_with_http_info(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> ApiResponse:
        """
        Update the PortNumbersInstance with HTTP info
        
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param phone_number_pin: PIN number of the account (with your current provider/carrier) that is associated with the porting phone number in this port request. This is mandatory for Wireless numbers but optional for Landline and Toll Free numbers.

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(carrier_account_number=carrier_account_number, phone_number_pin=phone_number_pin, )

    async def update_with_http_info_async(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the PortNumbersInstance with HTTP info
        
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param phone_number_pin: PIN number of the account (with your current provider/carrier) that is associated with the porting phone number in this port request. This is mandatory for Wireless numbers but optional for Landline and Toll Free numbers.

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(carrier_account_number=carrier_account_number, phone_number_pin=phone_number_pin, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Preview.PortIn.PortNumbersInstance {}>'.format(context)

class PortNumbersContext(InstanceContext):


    def __init__(self, version: Version, port_order_sid: str, sid: str):
        """
        Initialize the PortNumbersContext

        :param version: Version that contains the resource
        :param port_order_sid: A 34 character string that uniquely identifies the PortOrder this resource belongs to.
        :param sid: A 34 character string that uniquely identifies this PortInNumber.
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'port_order_sid': port_order_sid,
            'sid': sid,
        }
        self._uri = '/Orders/{port_order_sid}/Numbers/{sid}'.format(**self._solution)
        
    
    
    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        
        headers = values.of({})
        
        
        
        return self._version.delete_with_response_info(method='DELETE', uri=self._uri, headers=headers)

    def delete(self) -> bool:
        """
        Deletes the PortNumbersInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the PortNumbersInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        
        headers = values.of({})
        
        
        
        return await self._version.delete_with_response_info_async(method='DELETE', uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the PortNumbersInstance

        
        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the PortNumbersInstance and return response metadata

        
        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)
    
    
    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers)

    def fetch(self) -> PortNumbersInstance:
        """
        Fetch the PortNumbersInstance
        

        :returns: The fetched PortNumbersInstance
        """
        payload, _, _ = self._fetch()
        return PortNumbersInstance(
            self._version,
            payload,
            port_order_sid=self._solution['port_order_sid'],
            sid=self._solution['sid'],
            
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the PortNumbersInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = PortNumbersInstance(
            self._version,
            payload,
            port_order_sid=self._solution['port_order_sid'],
            sid=self._solution['sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers)

    async def fetch_async(self) -> PortNumbersInstance:
        """
        Asynchronous coroutine to fetch the PortNumbersInstance
        

        :returns: The fetched PortNumbersInstance
        """
        payload, _, _ = await self._fetch_async()
        return PortNumbersInstance(
            self._version,
            payload,
            port_order_sid=self._solution['port_order_sid'],
            sid=self._solution['sid'],
            
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the PortNumbersInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = PortNumbersInstance(
            self._version,
            payload,
            port_order_sid=self._solution['port_order_sid'],
            sid=self._solution['sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    
    def _update(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'CarrierAccountNumber': carrier_account_number,
            'PhoneNumberPin': phone_number_pin,
        })
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def update(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> PortNumbersInstance:
        """
        Update the PortNumbersInstance
        
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param phone_number_pin: PIN number of the account (with your current provider/carrier) that is associated with the porting phone number in this port request. This is mandatory for Wireless numbers but optional for Landline and Toll Free numbers.

        :returns: The updated PortNumbersInstance
        """
        payload, _, _ = self._update(carrier_account_number=carrier_account_number, phone_number_pin=phone_number_pin)
        return PortNumbersInstance(
            self._version,
            payload,
            port_order_sid=self._solution['port_order_sid'],
            sid=self._solution['sid']
        )

    def update_with_http_info(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> ApiResponse:
        """
        Update the PortNumbersInstance and return response metadata
        
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param phone_number_pin: PIN number of the account (with your current provider/carrier) that is associated with the porting phone number in this port request. This is mandatory for Wireless numbers but optional for Landline and Toll Free numbers.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(carrier_account_number=carrier_account_number, phone_number_pin=phone_number_pin)
        instance = PortNumbersInstance(
            self._version,
            payload,
            port_order_sid=self._solution['port_order_sid'],
            sid=self._solution['sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        
        data = values.of({ 
            'CarrierAccountNumber': carrier_account_number,
            'PhoneNumberPin': phone_number_pin,
        })
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/x-www-form-urlencoded"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def update_async(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> PortNumbersInstance:
        """
        Asynchronous coroutine to update the PortNumbersInstance
        
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param phone_number_pin: PIN number of the account (with your current provider/carrier) that is associated with the porting phone number in this port request. This is mandatory for Wireless numbers but optional for Landline and Toll Free numbers.

        :returns: The updated PortNumbersInstance
        """
        payload, _, _ = await self._update_async(carrier_account_number=carrier_account_number, phone_number_pin=phone_number_pin)
        return PortNumbersInstance(
            self._version,
            payload,
            port_order_sid=self._solution['port_order_sid'],
            sid=self._solution['sid']
        )

    async def update_with_http_info_async(self, carrier_account_number: Union[str, object]=values.unset, phone_number_pin: Union[str, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the PortNumbersInstance and return response metadata
        
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param phone_number_pin: PIN number of the account (with your current provider/carrier) that is associated with the porting phone number in this port request. This is mandatory for Wireless numbers but optional for Landline and Toll Free numbers.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(carrier_account_number=carrier_account_number, phone_number_pin=phone_number_pin)
        instance = PortNumbersInstance(
            self._version,
            payload,
            port_order_sid=self._solution['port_order_sid'],
            sid=self._solution['sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Preview.PortIn.PortNumbersContext {}>'.format(context)







class PortNumbersPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> PortNumbersInstance:
        """
        Build an instance of PortNumbersInstance

        :param payload: Payload response from the API
        """
        
        return PortNumbersInstance(self._version, payload, port_order_sid=self._solution["port_order_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Preview.PortIn.PortNumbersPage>"






class PortNumbersPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> PortNumbersInstance:
        """
        Build an instance of PortNumbersInstance

        :param payload: Payload response from the API
        """
        
        return PortNumbersInstance(self._version, payload, port_order_sid=self._solution["port_order_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Preview.PortIn.PortNumbersPage>"




class PortNumbersPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> PortNumbersInstance:
        """
        Build an instance of PortNumbersInstance

        :param payload: Payload response from the API
        """
        
        return PortNumbersInstance(self._version, payload, port_order_sid=self._solution["port_order_sid"])
        

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Preview.PortIn.PortNumbersPage>"





class PortNumbersList(ListResource):
    
    def __init__(self, version: Version, port_order_sid: str):
        """
        Initialize the PortNumbersList

        :param version: Version that contains the resource
        :param port_order_sid: A 34 character string that uniquely identifies the PortInOrder.
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'port_order_sid': port_order_sid,  }
        self._uri = '/Orders/{port_order_sid}/Numbers'.format(**self._solution)
        
        
    
    
    
    def list(self, 
    ) -> List[PortNumbersInstance]:
        """
        Lists PortNumbersInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [PortNumbersInstance(self._version, item, port_order_sid=self._solution['port_order_sid']) for item in payload["items"]]
        

    async def list_async(self, 
    ) -> List[PortNumbersInstance]:
        """
        Asynchronously lists PortNumbersInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [PortNumbersInstance(self._version, item, port_order_sid=self._solution['port_order_sid']) for item in payload["items"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists PortNumbersInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists PortNumbersInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> PortNumbersPage:
        """
        Retrieve a single page of PortNumbersInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of PortNumbersInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return PortNumbersPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> PortNumbersPage:
        """
        Asynchronously retrieve a single page of PortNumbersInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of PortNumbersInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return PortNumbersPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PortNumbersPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = PortNumbersPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PortNumbersPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = PortNumbersPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> PortNumbersPage:
        """
        Retrieve a specific page of PortNumbersInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PortNumbersInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return PortNumbersPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> PortNumbersPage:
        """
        Asynchronously retrieve a specific page of PortNumbersInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PortNumbersInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return PortNumbersPage(self._version, response, solution=self._solution)

    
    
    def stream(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[PortNumbersInstance]:
        """
        Streams PortNumbersInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param str phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param &quot;PortNumbersInstance.Status&quot; status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param str type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param str carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param str current_carrier: The current carrier of the phone number.
        :param str current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param datetime confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param str partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            phone_number=phone_number,
            phone_number_sid=phone_number_sid,
            status=status,
            type=type,
            carrier_account_number=carrier_account_number,
            current_carrier=current_carrier,
            current_resp_org=current_resp_org,
            confirmed_activation_date=confirmed_activation_date,
            partner_reference_id=partner_reference_id,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[PortNumbersInstance]:
        """
        Asynchronously streams PortNumbersInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param str phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param &quot;PortNumbersInstance.Status&quot; status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param str type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param str carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param str current_carrier: The current carrier of the phone number.
        :param str current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param datetime confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param str partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            phone_number=phone_number,
            phone_number_sid=phone_number_sid,
            status=status,
            type=type,
            carrier_account_number=carrier_account_number,
            current_carrier=current_carrier,
            current_resp_org=current_resp_org,
            confirmed_activation_date=confirmed_activation_date,
            partner_reference_id=partner_reference_id,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams PortNumbersInstance and returns headers from first page

        
        :param str phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param str phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param &quot;PortNumbersInstance.Status&quot; status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param str type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param str carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param str current_carrier: The current carrier of the phone number.
        :param str current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param datetime confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param str partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            phone_number=phone_number,
            phone_number_sid=phone_number_sid,
            status=status,
            type=type,
            carrier_account_number=carrier_account_number,
            current_carrier=current_carrier,
            current_resp_org=current_resp_org,
            confirmed_activation_date=confirmed_activation_date,
            partner_reference_id=partner_reference_id,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams PortNumbersInstance and returns headers from first page

        
        :param str phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param str phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param &quot;PortNumbersInstance.Status&quot; status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param str type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param str carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param str current_carrier: The current carrier of the phone number.
        :param str current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param datetime confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param str partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            phone_number=phone_number,
            phone_number_sid=phone_number_sid,
            status=status,
            type=type,
            carrier_account_number=carrier_account_number,
            current_carrier=current_carrier,
            current_resp_org=current_resp_org,
            confirmed_activation_date=confirmed_activation_date,
            partner_reference_id=partner_reference_id,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[PortNumbersInstance]:
        """
        Lists PortNumbersInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param str phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param &quot;PortNumbersInstance.Status&quot; status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param str type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param str carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param str current_carrier: The current carrier of the phone number.
        :param str current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param datetime confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param str partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return list(self.stream(
            phone_number=phone_number,
            phone_number_sid=phone_number_sid,
            status=status,
            type=type,
            carrier_account_number=carrier_account_number,
            current_carrier=current_carrier,
            current_resp_org=current_resp_org,
            confirmed_activation_date=confirmed_activation_date,
            partner_reference_id=partner_reference_id,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[PortNumbersInstance]:
        """
        Asynchronously lists PortNumbersInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param str phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param &quot;PortNumbersInstance.Status&quot; status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param str type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param str carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param str current_carrier: The current carrier of the phone number.
        :param str current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param datetime confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param str partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)
        
        :returns: list that will contain up to limit results
        """

        return [record async for record in await self.stream_async(
            phone_number=phone_number,
            phone_number_sid=phone_number_sid,
            status=status,
            type=type,
            carrier_account_number=carrier_account_number,
            current_carrier=current_carrier,
            current_resp_org=current_resp_org,
            confirmed_activation_date=confirmed_activation_date,
            partner_reference_id=partner_reference_id,
            limit=limit,
            page_size=page_size,
        )]


    def list_with_http_info(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists PortNumbersInstance and returns headers from first page

        
        :param str phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param str phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param &quot;PortNumbersInstance.Status&quot; status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param str type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param str carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param str current_carrier: The current carrier of the phone number.
        :param str current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param datetime confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param str partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            phone_number=phone_number,
            phone_number_sid=phone_number_sid,
            status=status,
            type=type,
            carrier_account_number=carrier_account_number,
            current_carrier=current_carrier,
            current_resp_org=current_resp_org,
            confirmed_activation_date=confirmed_activation_date,
            partner_reference_id=partner_reference_id,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists PortNumbersInstance and returns headers from first page

        
        :param str phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param str phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param &quot;PortNumbersInstance.Status&quot; status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param str type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param str carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param str current_carrier: The current carrier of the phone number.
        :param str current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param datetime confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param str partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            phone_number=phone_number,
            phone_number_sid=phone_number_sid,
            status=status,
            type=type,
            carrier_account_number=carrier_account_number,
            current_carrier=current_carrier,
            current_resp_org=current_resp_org,
            confirmed_activation_date=confirmed_activation_date,
            partner_reference_id=partner_reference_id,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      phone_number: Union[str, object] = values.unset,
      phone_number_sid: Union[str, object] = values.unset,
      status: Union["PortNumbersInstance.Status", object] = values.unset,
      type: Union[str, object] = values.unset,
      carrier_account_number: Union[str, object] = values.unset,
      current_carrier: Union[str, object] = values.unset,
      current_resp_org: Union[str, object] = values.unset,
      confirmed_activation_date: Union[datetime, object] = values.unset,
      partner_reference_id: Union[str, object] = values.unset,
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> PortNumbersPage:
        """
        Retrieve a single page of PortNumbersInstance records from the API.
        Request is executed immediately
        
        :param phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param current_carrier: The current carrier of the phone number.
        :param current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of PortNumbersInstance
        """
        data = values.of({ 
            'PhoneNumber': phone_number,
            'PhoneNumberSid': phone_number_sid,
            'Status': status,
            'Type': type,
            'CarrierAccountNumber': carrier_account_number,
            'CurrentCarrier': current_carrier,
            'CurrentRespOrg': current_resp_org,
            'ConfirmedActivationDate': serialize.iso8601_datetime(confirmed_activation_date),
            'PartnerReferenceId': partner_reference_id,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return PortNumbersPage(self._version, response, solution=self._solution)

    async def page_async(self, 
      phone_number: Union[str, object] = values.unset,
      phone_number_sid: Union[str, object] = values.unset,
      status: Union["PortNumbersInstance.Status", object] = values.unset,
      type: Union[str, object] = values.unset,
      carrier_account_number: Union[str, object] = values.unset,
      current_carrier: Union[str, object] = values.unset,
      current_resp_org: Union[str, object] = values.unset,
      confirmed_activation_date: Union[datetime, object] = values.unset,
      partner_reference_id: Union[str, object] = values.unset,
      
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> PortNumbersPage:
        """
        Asynchronously retrieve a single page of PortNumbersInstance records from the API.
        Request is executed immediately
        
        :param phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param current_carrier: The current carrier of the phone number.
        :param current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of PortNumbersInstance
        """
        data = values.of({ 
            'PhoneNumber': phone_number,
            'PhoneNumberSid': phone_number_sid,
            'Status': status,
            'Type': type,
            'CarrierAccountNumber': carrier_account_number,
            'CurrentCarrier': current_carrier,
            'CurrentRespOrg': current_resp_org,
            'ConfirmedActivationDate': serialize.iso8601_datetime(confirmed_activation_date),
            'PartnerReferenceId': partner_reference_id,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return PortNumbersPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param current_carrier: The current carrier of the phone number.
        :param current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PortNumbersPage, status code, and headers
        """
        data = values.of({ 
            'PhoneNumber': phone_number,
            'PhoneNumberSid': phone_number_sid,
            'Status': status,
            'Type': type,
            'CarrierAccountNumber': carrier_account_number,
            'CurrentCarrier': current_carrier,
            'CurrentRespOrg': current_resp_org,
            'ConfirmedActivationDate': serialize.iso8601_datetime(confirmed_activation_date),
            'PartnerReferenceId': partner_reference_id,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = PortNumbersPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        phone_number: Union[str, object] = values.unset,
        phone_number_sid: Union[str, object] = values.unset,
        status: Union["PortNumbersInstance.Status", object] = values.unset,
        type: Union[str, object] = values.unset,
        carrier_account_number: Union[str, object] = values.unset,
        current_carrier: Union[str, object] = values.unset,
        current_resp_org: Union[str, object] = values.unset,
        confirmed_activation_date: Union[datetime, object] = values.unset,
        partner_reference_id: Union[str, object] = values.unset,
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param phone_number: Phone number to be ported into Twilio. This must be in [E.164](https://en.wikipedia.org/wiki/E.164) format, e.g., +16175551212
        :param phone_number_sid: A 34 character string that uniquely identifies the [IncomingPhoneNumber](https://www.twilio.com/docs/phone-numbers/api/incomingphonenumber-resource) resource that represents the phone number being ported into Twilio.
        :param status: The Status of this PortInNumber. One of `twilio-processing`, `initialized`, `pending-carrier-processing`, `carrier-processing`, `accepted`, `rejected`, `ported-in`, `pending-cancelation`, `canceled`, `failed`.
        :param type: Type of phone number that is porting into Twilio and can be `local`, `tollfree`, `mobile` depending on the country.
        :param carrier_account_number: External carrier account number of this phone number used for ownerhsip authorization.
        :param current_carrier: The current carrier of the phone number.
        :param current_resp_org: Current responsible organization of this phone number if it is a toll-free phone number.
        :param confirmed_activation_date: The confirmed Firm Order Commitment (FOC) date and time, given as ISO 8601 format – i.e., the date and time at which the number will port. This will be overridden if the earliest possible FOC date is on a later date.
        :param partner_reference_id: External ID of the port order for a phone number on the partnered provider sid.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PortNumbersPage, status code, and headers
        """
        data = values.of({ 
            'PhoneNumber': phone_number,
            'PhoneNumberSid': phone_number_sid,
            'Status': status,
            'Type': type,
            'CarrierAccountNumber': carrier_account_number,
            'CurrentCarrier': current_carrier,
            'CurrentRespOrg': current_resp_org,
            'ConfirmedActivationDate': serialize.iso8601_datetime(confirmed_activation_date),
            'PartnerReferenceId': partner_reference_id,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = PortNumbersPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> PortNumbersPage:
        """
        Retrieve a specific page of PortNumbersInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PortNumbersInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return PortNumbersPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> PortNumbersPage:
        """
        Asynchronously retrieve a specific page of PortNumbersInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PortNumbersInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return PortNumbersPage(self._version, response, solution=self._solution)

    
    def list(self, 
    ) -> List[PortNumbersInstance]:
        """
        Lists PortNumbersInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = self._version.fetch_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [PortNumbersInstance(self._version, item, port_order_sid=self._solution['port_order_sid']) for item in payload["items"]]
        

    async def list_async(self, 
    ) -> List[PortNumbersInstance]:
        """
        Asynchronously lists PortNumbersInstance records from the API as a list.
        
        :returns: list that will contain up to limit results
        """

        headers = values.of({'Content-Type': 'application/x-www-form-urlencoded'})
        
        data = values.of({ 
        })
        payload, _, _ = await self._version.fetch_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        
        return [PortNumbersInstance(self._version, item, port_order_sid=self._solution['port_order_sid']) for item in payload["items"]]
        


    def list_with_http_info(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists PortNumbersInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists PortNumbersInstance and returns headers from first page

        
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> PortNumbersPage:
        """
        Retrieve a single page of PortNumbersInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of PortNumbersInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = self._version.page(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return PortNumbersPage(self._version, response, solution=self._solution)

    async def page_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> PortNumbersPage:
        """
        Asynchronously retrieve a single page of PortNumbersInstance records from the API.
        Request is executed immediately
        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of PortNumbersInstance
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response = await self._version.page_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        return PortNumbersPage(self._version, response, solution=self._solution)

    def page_with_http_info(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PortNumbersPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = self._version.page_with_response_info(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = PortNumbersPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with PortNumbersPage, status code, and headers
        """
        data = values.of({ 
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='OPTIONS', uri=self._uri, params=data, headers=headers)
        page = PortNumbersPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> PortNumbersPage:
        """
        Retrieve a specific page of PortNumbersInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PortNumbersInstance
        """
        response = self._version.domain.twilio.request(
            'OPTIONS',
            target_url
        )
        return PortNumbersPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> PortNumbersPage:
        """
        Asynchronously retrieve a specific page of PortNumbersInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of PortNumbersInstance
        """
        response = await self._version.domain.twilio.request_async(
            'OPTIONS',
            target_url
        )
        return PortNumbersPage(self._version, response, solution=self._solution)



    def get(self, sid: str) -> PortNumbersContext:
        """
        Constructs a PortNumbersContext
        
        :param sid: A 34 character string that uniquely identifies this PortInNumber.
        """
        return PortNumbersContext(self._version, port_order_sid=self._solution['port_order_sid'], sid=sid)

    def __call__(self, sid: str) -> PortNumbersContext:
        """
        Constructs a PortNumbersContext
        
        :param sid: A 34 character string that uniquely identifies this PortInNumber.
        """
        return PortNumbersContext(self._version, port_order_sid=self._solution['port_order_sid'], sid=sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Preview.PortIn.PortNumbersList>'


