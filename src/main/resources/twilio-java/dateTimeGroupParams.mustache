        if ({{paramName}} != null) {
            request.addQueryParam("{{baseName}}", {{paramName}}.format(DateTimeFormatter.ofPattern(Request.QUERY_STRING_DATE_TIME_FORMAT)));
        }
        else if ({{paramName}}After != null || {{paramName}}Before != null) {
        {{#isDateTime}}
            request.addQueryDateTimeRange("{{baseName}}", {{paramName}}After, {{paramName}}Before);
        {{/isDateTime}}
        {{#isDate}}
            request.addQueryDateRange("{{baseName}}", {{paramName}}After, {{paramName}}Before);
        {{/isDate}}
        }